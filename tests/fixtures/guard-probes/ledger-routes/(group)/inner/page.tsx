/** Guard-probe fixture (ledger-covers-every-surface): a page inside a route
 *  group — the group segment must elide, so this is the route "/inner". */
export default function ProbeInnerPage() {
    return null
}
