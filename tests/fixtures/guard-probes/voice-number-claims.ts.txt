// Probe → number-format-guard and claims-guard. Copy to lib/services/probe-voice.ts.
// number-format MUST fail on `num`; claims MUST fail on `claim`.
export const PROBE = {
    num: { el: "Πάνω από 10,000 ασφαλιστήρια αναλύθηκαν.", en: "Over 10,000 policies analysed." },
    claim: { el: "Μας εμπιστεύονται 500+ ασφαλισμένοι με 98% ακρίβεια.", en: "500+ policyholders trust us at 98% accuracy." },
}
// Round 2: a timing promise with a different number — the guard must not stop at «1 λεπτό».
export const probeTiming = 'Ξεκινήστε σε λιγότερο από 2 λεπτά.'
