# PW-TRANSPARENCY-02 — progress ledger

One entry per goal. Each entry carries the before/after metrics the goal's acceptance criteria name; a metric not measured on fixtures before and after means the change did not happen.

| Goal | Status | Date | Metrics | Notes |
|---|---|---|---|---|
| 0 — Investigation | **done — approved 2026-09-05 (amendment 01 issued)** | 2026-09-05 | none by design (no code) | `STEP0-FINDINGS.md` at HEAD `32c95229`. Ten plan-changing findings in §0; stale spec premises in §H; unresolved facts in §I. C1 verdict: feasible without engine change, needs schema and three reconciliations (§G). |
| A1.1 — Terms name no metric | **done** | 2026-09-05 | named product metrics in Terms: EL 1 → **0**, EN 1 → **0**; Terms revision GR-GA-2026.07 → GR-GA-2026.09 (2026-09-05); consent stamp `LEGAL_CONTENT_VERSION` deliberately unchanged per the file's own convention | `lib/legal/legal-content.ts` §8 both locales. Guard `tests/unit/terms-no-named-metric.test.ts` proven red on the removed sentence. |
| A1.2 — checks-passed | **done** | 2026-09-05 | user-reaching sites of the prose-derived number: **4 → 0** — B2C progress source (`AnalysisCard.tsx`), run API `overall_success_pct` + step `success_pct`, portfolio `avgSuccessPct`, comparison `overallScoreChange` | Guard `tests/unit/checks-passed-not-rendered.test.ts` (universe app/components/lib from disk; 10 named producers; 3 probes). **Dead-for-now, stored and unrendered, to reconcile in B2:** `policy_analysis_runs.overall_success_pct`, `policy_analysis_steps.success_pct`, `resultJson.checklistScores[].checksPassed / successPct`. |
| A1.3 — fabricated-count guard | **done** | 2026-09-05 | baseline violations in the marketing universe: **2 → 0** (liability mock «Ελέγχθηκαν 2 ασφαλιστήρια»; AudienceTabs «100 αρχεία» against the enforced 10-file cap) + literal stat tiles **1 → 0**; universe > 120 files across 10 directories + `lib/help-content.ts` | Registry `lib/marketing/public-counts.ts` (7 counts, each derived from the enforced constant). Guard `tests/unit/no-fabricated-public-count.test.ts`: 3 defect classes, 4 probes, red before green. `lib/legal` excluded by design (BL-01). Copy freeze `tests/fixtures/greek-string-inventory.txt` regenerated for the three copy edits. |
| A1.4 — Terms §5 | **BLOCKED** | 2026-09-05 | — | `BLOCKED.md` BL-01: read the production `plans` free-tier row. |
| A2 | superseded by amendment 01 | | | Both documents already Greek-canonical with `/en` parity; entity, processors, retention, cookies, Art. 9 present (Goal 0 F20). |
| A3 | not in amendment 01 scope | | | |
| B0.1 — single writer | **done (dev)** | 2026-09-05 | modules that create gap rows: **2 → 1** (`lib/gaps/gap-instance-writer.ts`); importers of the legacy writer: **2 → 0** (protection refresh action, process-policy job); reactivate paths: **1 → 0**; readers without a live filter: **4 → 0** (agent policy page, `/api/v1/policies`, `/api/v1/policies/[id]`, `/api/v1/policies/[id]/gaps`) | Guards `gap-instance-single-writer` (3 probes), `gap-instance-writer` (fires → resolves → fires again is two rows; prior status kept), `gap-readers-exclude-superseded` (2 probes; 3 named history readers). Partial unique index re-scoped to CURRENT rows. |
| B0.2 — run provenance | **done (dev)** | 2026-09-05 | rows without a producing run: dev **0 / 10** after backfill (all 10 attributed to 3 runs); `analysis_run_id` and `line_of_business` NOT NULL; runs without terminal state: 0 among non-live runs (dev 9 completed); attempted-rule plan on every run row at creation (`planned`) and after evaluation (`evaluated`, with catalogue version `fingerprintGapDefinitions`) | Migration `20260905150000_gap_instance_run_provenance` applied on dev (`migrate deploy`, verified by query: columns, FK, indexes, 0 nulls). Prod pre-check: 1 row, 0 user-level, 0 orphans — applied at merge time with the code, never before it. |
| B0.3 — no stale findings as current | **done (dev)** | 2026-09-05 | surfaces rendering findings without their run date, among the four named: **4 → 0** (B2C policy detail + analysis card, B2B client policy view, B2B report from both routes); fixture success→forced failure asserted in both locales | `lib/gaps/findings-provenance.ts` (8 states incl. `unassessed`, pre-wiring B1.5) + `components/gaps/FindingsProvenanceLine.tsx` (`data-fact="gap.findingsProvenance"`, text always, icon aria-hidden). Test `findings-provenance` (matrix + surface guard). **Remaining:** portfolio roll-ups (dashboard attention list, protection page, agent dashboard counts) do not yet date their findings — scheduled with B1.5/B2 where those widgets are restructured. |
| B1.5 — unauthored-branch state | **done (dev)** | 2026-09-05 | fixture: 11 unauthored branches incl. `renters` (6 used in the roll-up fixture) · surfaces rendering an adequacy-implying state for them: B2C detail absence card **1 → 0** (`unassessed`, distinct copy), findings card **1 → 0**, findings provenance **0** (pre-wired), B2B client policy view **1 → 0** (same card), B2B report **0** (provenance block) · portfolio roll-ups counting unassessed as assessed: dashboard zero-tally **1 → 0** (now names assessed/excluded counts), hero facts gain «{count} χωρίς ορισμένους ελέγχους», agent client card gains the per-client unassessed count · roll-ups excluding without saying how many: **0** | `lib/gaps/assessment-coverage.ts` (static, exact-branch; the per-run truth is `attempted_rules`). Test `unauthored-branch-state` (both locales; textual distinctness from `never/failed/degraded/empty`; reassurance blocklist). Count keys `portfolio.unassessedCount`, `portfolio.assessedCount`, `client.unassessedPolicyCount`. **Not changed:** the protection lens tiles (`covered` there means «a live policy is held», a lifecycle fact, not a findings verdict); the agent customer list's opportunity chip (shows only when > 0). |
| B1.6 — Risk DNA dimension scores | **HALTED for decision** | 2026-09-05 | — | `HALTS.md` H-T01: the nine 0–100 dimension numbers are breadth-of-held-lines over a profile exposure list, coloured by threshold; returns 0 for an empty wallet, rises over never-analysed and unauthored-branch policies. Options A (measurement with visible denominators) and B (remove the number) presented with what each returns; not decided. |
| B1.7 — score render sites | **done (dev), APIs halted** | 2026-09-05 | protection-score renders on agent surfaces **3 → 0** (client card `/100` badge → open-findings count with subject; trend card removed; client overview `coverageScore%` → lines-held count) · B2C per-policy health number **1 → 0** (`calculatePolicyHealthScore` deleted with its card block and methodology link) · score-containment guard universe widened from B2C-only to every non-admin path, with the two relationship-health renders named and asserted | APIs `/api/v1/protection-score` and `/api/v1/customers/protection-scores`: no in-repo consumer; external consumer unknowable from the code → `BLOCKED.md` BL-02 / `HALTS.md` H-T02. Producers (`lib/services/gap-engine/protection-score.ts`, the cached `ProtectionScore` row, Risk DNA) untouched pending H-T01 and the standing score decision. |
| **B2** two lines, two denominators | 2026-09-05 | **Before:** no composition anywhere; the only "how much was checked" figure was the prose-derived «checks passed» A1.2 removed. **After:** `lib/gaps/composition.ts` classifies each attempted rule by question from its operator — coverage (is_false / all_false / value_drift / date_within_days: 20 rules) or recording (missing / all_missing: 9 rules) — and, for a non-firing coverage rule, reads the rule's declared inputs from `acordData` presentation-side: every input present and of the type the operator reads → covered; absent, mistyped, undeclared, or a date already past → indeterminate (reason kept per item). N₁ = A₁+B₁+C₁ and N₂ = A₂+B₂ are taken from the run's `attemptedRules.slugs` at the run's `catalogueVersion`; a different current version renders one sentence and no numbers; N₁ = 0 (unauthored) and no run render nothing (B1.5 speaks there). Rendered by `components/gaps/CoverageComposition.tsx` in exactly two places — the B2C findings card (`AnalysisCard`) and the B2B customer-policy view, which reuses it — below the B0.3 provenance line; never in email, push or the report (asserted). Denominator first on each line, indeterminate in the same weight whenever > 0, no percentage, no ring, no verdict adjective, a review-framing sentence. **Undeclared-input rules in the authored catalogue: none** (all 29 declare `field`/`fields`/`referenceField`; asserted). Guard `tests/unit/coverage-composition.test.tsx` (34 cases: sums hold on every authored branch under single-input ablation, a rule never reads covered while an input it reads is absent, type and window checks, unauthored/no-run/mismatch, both locales, mount sites). Suite 591 files / 6879 tests green. |
| **B3** requirement provenance (skeleton) | 2026-09-05 | **Before:** no provenance anywhere; severity was the only class a finding carried, and it ordered, coloured and chipped it. **After:** `lib/gaps/provenance.ts` maps all 29 authored slugs → `{ provenance: "under_review", citation: null, reviewedBy: null }`. The agent classified nothing; the four spec-named candidates are listed unfilled in `docs/transparency/PROVENANCE-CANDIDATES.md` with the exact human task. Rules enforced in code: only legislative/contractual may order first or carry emphasis (`mayCarryEmphasis`); `market` renders in its own group with review framing (`provenance.marketFraming`); `under_review` renders only inside a disclosed section (`data-provenance="under_review"` on the findings list, the disclosure line on the agent insights grid), is counted in NO summary (dashboard tile → `gap.underReviewOmitted` with a link and no number; agent client gap count → classified only; gaps API `by_provenance` + `under_review_omitted_from_counts`), and reaches no email (digest `newGaps` classified only), notification, or report (savings report omits them and says so). Ordering axis: `orderByProvenance` / `partitionByProvenance` in the findings list, the free-preview selection, the coverage-insights list, the gaps API. **Acceptance:** unmapped authored slug → `tests/unit/provenance-skeleton.test.ts` fails before the build (probe: invented slug reported); under_review in summary/notification/email/report: 0 (outbound guard enumerates lib/email, lib/notifications, lib/services/reports + the digest service from disk, probe pair committed); market copy deficiency blocklist: 0; provenance colour-only: 0 (label module carries no colour classes). Labels ≤ 24 chars EL/EN (320px proxy; a browser measurement is still owed). |
| **B1** record status + severity de-emphasis | 2026-09-05 | **Before:** status = lifecycle chip (Ενεργό/Λήγει/Έληξε); severity ordered the findings list and the free preview (`gapSeverityRank`), coloured dots on the dashboard tile, the attention list, the coverage-insights list and the digest, was a pill on the agent insights grid and the insight card, a tally of four chips on two surfaces, a `by_severity` block in the gaps API, and the ordering key of recommendations, opportunity scores and profile-gap dedupe. **After:** `lib/wallet/record-status.ts` resolves ΥΠΟ ΕΞΕΤΑΣΗ / ΧΡΕΙΑΖΕΤΑΙ ΣΤΟΙΧΕΙΑ (need named: consent, plan, tokens, document, permission, fields, technical) / ΠΡΟΣ ΕΠΙΒΕΒΑΙΩΣΗ / ΕΠΙΒΕΒΑΙΩΜΕΝΟ / ΑΝΕΝΕΡΓΟ from the ONE lifecycle call + the latest run; takes no finding count; `confirmed` needs `confirmedAt`, which every caller passes as `null` until C1 (asserted from source). Rendered as text in one neutral shape (`RecordStatusLabel`, `data-fact="record.status"`) on the policy head (B2C) and the agent findings card. Severity now influences nothing: `severity-tone.ts` deleted, the tally and its subject-scoped key replaced by `gap.provenanceCount`, all sorts/weights/dedupes neutralised (recommendation-generator comparator drops urgency; opportunity scoring uses one flat gap weight; profile-gap dedupe keeps the first rule; agent playbook uses one step template and names no severity; the decision engine's advisor task carries no severity word or priority — its routing floor stays, as workflow). **Acceptance** (`tests/unit/severity-never-orders.test.ts`, universe app/components/lib from disk, 4 probes): render sites where severity orders/colours/chips/emphasises: **0** outside the listed exemptions; statuses readable as reassurance across {motor, health, home} × {clean, partial, failed, unanalysed, expired}: **0** (`record-status.test.tsx`); status by colour only: **0** (one className for all five). **Exemptions, with reasons in the guard:** conditions severity (`policy-conditions.ts`, `PolicyConditionsCard`) — a different concept, **migration debt**; upload-error severity; the agent action queue's *urgency*, which is days-to-expiry and profile completeness, not severity; document-request urgency chosen by the agent; admin operational consoles; `PolicyBriefCard` (its own row tone; no consumer). **Not done here:** the 320/390/430 browser measurement of the Greek labels (proxy: ≤ 24 chars); the wallet list card and the B2B report header do not yet show the record status (the report shows the B0.3 provenance line). |
| **B4** dashboards become routers | 2026-09-05 | **Before → after, same harness, same fixtures (`docs/evidence/dashboard-b4/RESULT.md`, 12 B2C + 3 B2B captures).** B2C typical@320: counts without a door 6 → 0, primary actions 2 → 1, scroll 5010 → 5008px, sections 2 → 6, sub-44 0 → 0, dup facts 0 → 0, renewals in the first viewport false → true, life events above the findings false → true. heavy@320: counts without a door 9 → 0, primary 2 → 1, scroll 5755 → 5747px. B2B agent-book@320: sections 12 → 5, counts without a door 2 → 0, primary 1 → 1, sub-44 2 → 0, scroll 4287 → 4287px; @430 sections 12 → 5. **What changed:** every rendered count is a door — the hero's portfolio facts, the renewals header count, the plan progress, the open-recommendation count, the priorities card's five counts, the gap tile's assessed / unassessed / provenance counts, the agent KPI tiles (now `StatTile href + countKey`, eight registered `agent.*` keys) and the client card's two chips; the advisor row's inverse primary became a secondary so each surface has exactly one primary action; renewals and the life-event prompt moved above the generated findings; the KPI strip is one labelled section; the checklist's two 36px icon buttons are 44px; the drawer button's numeric badge is a presence dot (a count is a door, a button is not). New shared metric `countsWithoutNavigation` in `tests/measure/metrics.ts`; capture specs `dashboard-b4.spec.ts` / `agent-book-b4.spec.ts`; Playwright project `measure-agent`; unit guard `tests/unit/dashboard-counts-are-doors.test.tsx`. **Not done:** the `filter` parameter the KPI doors carry is not yet honoured by /customers (lands on the full list); the B2C section count is what the shared collector measures on this layout (2) and was already ≤6 by the Goal-2 restructuring; the empty B2C state is onboarding, measured as such. |
| **G** guards | 2026-09-05 | **Before:** the twenty-two classes lived in test files with no map from class to guard, so a missing class was invisible; no guard covered comparative pricing claims. **After:** `docs/transparency/GUARDS.md` maps every class to its guards (file or harness metric) and its probe (committed fixture or named in-test case); `tests/unit/guard-inventory.test.ts` parses the table and fails on a missing file, a present class without a probe, an every-commit class not under `tests/unit`, or CI dropping `tests/unit`. New: `no-comparative-pricing-claim` over the public routes, landing, marketing library, legal and both copy stores (probe pair; one existing line is a caveat against comparison and is marked). Status: 17 present, 2 partial (unconfirmed-value until C1; Greek 320px for the B1/B3 labels still by proxy), 3 not yet applicable (C1, C4, C5). The five every-commit classes the spec names are present, unit-level and probed. |
| B1 (taxonomy, severity de-emphasis) | not started | | | |
| B2 | not started | | | Denominator must be static (A3) and must separate the 9 «recorded» rules from the 20 coverage rules (A4). |
| B3 | not started | | | |
| B4 | not started | | | Dashboard baseline already measured: `docs/transformation/evidence/dashboard/BASELINE.md` (6 sections, 0 sub-44 at 320, scroll 4006px). |
| C1–C7 | gated | | | Each needs its own written approval. |
| G | not started | | | |

## Decisions taken under standing authority

| Date | Decision | Why | Reversal |
|---|---|---|---|
| 2026-09-05 | **`lib/gap-detection.ts` was edited once, to DELETE the unreachable legacy writer** (`DetectedGap`, `detectGapsForPolicy`, `detectGapsForUser`, `createGapInstances`) and, because the severity guard's freeze exemption ended with that edit, the two dead presentation maps it quarantined (`getSeverityColor`, `getSeverityLabel`, zero callers) — the guard's own instruction. `decideGapsForPolicy`, `evaluateGapLogic`, `evaluateSingleRule`, `evaluateAcordFieldCheck`, `hasEvaluableRule`, `ruleInputsFor` are byte-identical. | B0.2 makes `analysis_run_id` NOT NULL; the legacy `createGapInstances` could no longer compile and was the second writer B0.1 exists to remove. The standing constraint protects what fires; nothing that fires changed. | `git show 32c95229:lib/gap-detection.ts` restores the file; the single-writer guard would then fail, by design. |
| 2026-09-05 | **No rules-only run kind.** The protection refresh action now refreshes the profile engine only; `POST /api/v1/jobs/process-policy` creates and enqueues a real run (same contract shape: `processed`, plus `run_id` / `queued`; a gated run returns `processed: false, reason`). | A rules-only re-evaluation would need a second run kind that every "latest run" reader (14 sites, incl. the quota counter) must exclude — the larger blast radius. Findings change only when a run runs, which is what provenance means. C1 revisits this when a human correction must re-evaluate. | Re-add a rules-only executor behind the single writer. |
| 2026-09-05 | **Art. 15 export keeps superseded rows** (`compliance.service.ts` named as a history reader). | A person's superseded findings are still their data. | Filter it and update the guard's list. |
| 2026-09-05 | **`overall_success_pct` / `success_pct` dropped from the run API response** (A1.2) rather than kept for an unknown external consumer. | The number is fabricated; a consumer rendering it is the defect. Field removal from JSON does not break a client that ignores it. | Re-add the fields. |
| 2026-09-05 | **Copy freeze snapshot regenerated** for three intentional Greek copy edits (Terms §8, liability mock caption, AudienceTabs cap). | The freeze exists to make copy changes deliberate and reviewable; the diff is exactly those three lines. | Revert the fixture. |


## Decisions (B1/B3)

| Goal | Decision | Status |
|---|---|---|
| B1/B3 | Every outbound gap count excludes `under_review` — which today is every finding — so the weekly digest's «νέα ευρήματα» and the agent's per-client gap chip count 0 and the report carries no findings section until the human track classifies. Chosen over keeping the counts because the spec's rule is explicit and a count of unclassified findings is the unbacked claim. The dashboard tile says the findings exist and links to them; it states no number. | reversible: classify a slug and the counts return |
| B1 | `GAP_DETECTED` risk-event notifications are built from risk-graph transitions (`riskId`), not gap definitions; provenance applies to authored gap slugs, so they are unchanged. Logged for the human track to decide whether risk transitions need a class of their own. | open |
| B1 | `missingFields` for ΧΡΕΙΑΖΕΤΑΙ ΣΤΟΙΧΕΙΑ is wired in the resolver but every caller passes none: the extraction paths are technical (`vehicle.glassBreakage`) and no human-readable field dictionary exists until C1's checklist names them. B2's indeterminate count already states how many could not be read. | until C1 |
| B1 | The decision engine still creates an advisor task when a critical (or, by setting, high) gap opens. That is routing — who gets a task — not presentation; the task's title and priority no longer carry the severity. | kept |
| B4 | Counts whose set has no filtered page yet (hero facts → /wallet, KPI tiles → /customers?filter=…) link to the list that contains the set, with the filter carried in the URL for the page to honour later. Chosen over building filters into four list pages inside a presentation goal. | follow-up |
| B4 | The bottom-nav drawer BUTTON no longer shows the numeric unread badge (a count must be a door; a button is not). The count stays on the bell link and in the item's aria-label. | reversible |