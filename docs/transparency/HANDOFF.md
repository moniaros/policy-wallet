# PW-TRANSPARENCY-02 — handoff: the open human-track items

Written 2026-09-05 at the verification halt (V1–V4); refreshed after remediation R1–R4 and again after the close-out block F0–F7 (2026-09-06). Each item: the exact question, who can answer it,
what it blocks, and what the product does today in its absence. Nothing here is a code task; each
becomes one the moment it is answered.

| # | Question (exact) | Who answers | What it blocks | What the product does today |
|---|---|---|---|---|
| H1 | ~~Production free-tier plan row~~ **RESOLVED 2026-09-06 (F3a):** `ph-free` → `policies` 3, `aiAnalysisPerMonth` null (uncapped in number), `monthlyTokenBudget` 150000, `questionsPerDay` 0, `gapAnalysisPerDay` 0. Terms §5 now states exactly that, version GR-GA-2026.09.2. | — | Nothing. | Terms §5 matches production enforcement. |
| H2 | **`EXTRACTION_CITATIONS` in production — READ 2026-09-06 (F3b), not acted on.** The secret's value is unreadable by the agent (`vercel env ls` shows it as a hidden Secret; `vercel env pull` returns empty for sensitive vars), but its EFFECT is readable: 3 of the 6 analysed production policies carry `acord.extraction.sources` (written only when the flag is `1`, `extraction-enrichment.ts:178`), latest 2026-09-05 00:03Z. So the flag is effectively **ON** for recent runs. Open question for the owner: is that intended, and does C1's provenance scope build on it? | The owner (Vercel project settings). | C1 scope only. | Per-field citations are being produced and stored for new analyses; nothing renders them yet. |
| H3 | ~~B1.6 Risk DNA dimension numbers~~ **DECIDED 2026-09-06 (F2): removed** from every render site; the B3 disclosed treatment renders where they were. `HALTS.md` H-T01 closed, `DECISIONS.md` D-F2. | — | Nothing. | Nine dimensions render as lenses (name, under-review label, open risks, next action, confidence); no number, bar, arrow or points line anywhere. |
| H4 | ~~Do the two protection-score APIs receive external requests?~~ **RESOLVED 2026-09-06 (F4): both deleted.** Evidence: no in-repo consumer; `push_devices` = 0 in production; Sentry spans 30d: 0 requests to either path while 13 sibling `/api/v1/*` routes were sampled; Vercel's 30-day log NOT readable (billing gate, one-hour retention) — stated. `HALTS.md` H-T02 closed, D-F4. | — | Nothing. A caller surfacing later gets a two-file revert. | The paths 404. |
| H5 | **May policyholders correct values a rule reads** (e.g. the extracted `vehicle.glassBreakage`, an insured value), and does a corrected value re-run the rules? If yes, is the correction recorded with who and when, and does it mark the record as human-confirmed? | The owner (product + legal): a correction that changes a finding is the customer editing the basis of the product's statement about their cover. | Whether C1 (the review checklist as the primary object, manual entry a first-class path, ΕΠΙΒΕΒΑΙΩΜΕΝΟ reachable) exists at all, and C2's confirm-low-confidence flow. | No correction path: extracted values are read-only; `resolveRecordStatus` can never return `confirmed` (every caller passes `confirmedAt: null`, asserted by test). The status row says «Προς επιβεβαίωση» on every completed analysis and nothing lets a person confirm it. |
| H6 | **Provenance classification — PARTIAL 2026-09-06 (F5), citation-backed only.** 3 of 29 classified `legislative` with the law and article named (`insured_value_above_declared`, `insured_value_below_rebuild_cost` → Ν. 2496/1997 άρθρο 17; `missing_microchip_number` → Ν. 4830/2021 άρθρο 9 §1 β΄); 26 stay `under_review`; nothing `market` in this pass. **`docs/transparency/PROVENANCE-REVIEW.md` is pre-GA gated and BLOCKS GA:** legal must confirm the 3, decide 4 candidates (#4, #5, #9, #10, #29), and say whether any `none` row is market practice. | Legal / underwriting track. | **GA.** Until sign-off, the three citations render as the agent's reading, labelled so in code (`reviewedBy`). | Classified findings render their class WITH the citation at every site (card, coverage insights, agent insights, attention list, report, digest); everything else stays in the disclosed section, counted in no summary. |
| H7 | Three seeded test accounts in the **production** database (spec human-track item 7): delete and rotate before GA — which three, and who owns the rotation? | The owner. | GA readiness; nothing in this series. | Untouched by the series. |
| H8 | Deliverability of `info@`, `careers@`, `dpo@policywallet.gr` (Goal 0 §I): do they receive mail? | The owner / mailbox admin. | The trust and legal pages naming them. | The addresses are published; nothing in the code verifies a mailbox exists. |

| H9 | Two **app-shell overlaps** found by R4's layout-integrity metric (reported, not fixed): the navigation drawer's footer «Ρυθμίσεις» link spans under the language and theme toggles; the fixed bottom navigation overlays the content row at the fold. Who owns the shell, and is the drawer footer a defect to fix? | The owner / whoever owns `components/shell/AppShell.tsx`. | Nothing in this series; B4's own metric is scoped to page-flow elements. | Both behaviours are live; neither blocks a task. |
| H10 | **Legal sign-off of `docs/transparency/PROVENANCE-REVIEW.md` (F5).** Confirm the three `legislative` rows, decide the four candidates, and decide whether `none` rows become `market`. Each change lands in `lib/gaps/provenance.ts` with reviewer and date; the guard refuses a class without a citation. | Legal / underwriting. | **GA (blocking).** | The three citations render as the agent's citation-backed reading, marked pre-GA in `reviewedBy`. |

## What is NOT waiting on a human

- Track C (C1–C6) is gated per goal on separate written approval and is not started.
- B1.6 is halted (H3), not blocked on code.
- The `filter` parameter the B4 KPI doors carry (`/customers?filter=expiring|gaps|invited`) is a code follow-up: the customer list does not read it yet, so those doors land on the full list.
