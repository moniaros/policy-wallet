# PW-TRANSPARENCY-02 — handoff: the open human-track items

Written 2026-09-05 at the verification halt (V1–V4). Each item: the exact question, who can answer it,
what it blocks, and what the product does today in its absence. Nothing here is a code task; each
becomes one the moment it is answered.

| # | Question (exact) | Who answers | What it blocks | What the product does today |
|---|---|---|---|---|
| H1 | In the **production** `plans` table, for the B2C free tier (`plan_id` `ph-free`, or the row whose `tier_key` is `free`): what are `limits.policies` and `limits.aiAnalysisPerMonth` (null = unlimited)? Which of those two numbers do the Terms state? | The owner, reading the production row (Supabase dashboard or `SELECT plan_id, tier_key, limits FROM plans WHERE tier_key='free'`). | Terms §5 (`lib/legal/legal-content.ts`, BL-01): it still says «δωρεάν για ένα συμβόλαιο» / "free for one policy" while the code's canonical limit is 3. | The Terms understate the free tier by two policies; the paywall enforces the plan row; the marketing surface reads `PUBLIC_COUNTS.freePolicies` (3). A customer reading the contract and the pricing page sees two different numbers. |
| H2 | What is the value of `EXTRACTION_CITATIONS` in production (Vercel env)? Is per-field citation data (document + location) being produced and stored for extracted values? | The owner (Vercel project settings) or whoever set the flag. | C1 scoping: a review checklist with a per-row «where did this value come from» inspect affordance needs citations to exist; without them C1's row has a value and no source. | Extracted values render without a source; the B2 composition names inputs by path, not by location in the document. C1 is not started (gated). |
| H3 | **B1.6 Risk DNA dimension numbers** — keep them as measurements («X από Y κινδύνους απαντώνται από γραμμή που κατέχετε», no threshold colour, with the unanalysed / unauthored counts stated) or remove the per-dimension 0–100 and its colour, keeping the lens (named open risks, next action, confidence)? Options with what each returns for empty / never-analysed / all-expired / unauthored portfolios are in `HALTS.md` H-T01. | The owner (product) — the series presents, it does not decide. | B1.6. Nothing else in the series waits on it. | `components/risk-dna/RiskDnaPanel.tsx` still renders nine 0–100 numbers with threshold colours; an all-expired wallet reads as nine red bars and a never-analysed one as green — a verdict from unread policies (the exact shape B1.7 removed elsewhere). |
| H4 | Do `GET /api/v1/protection-score` and `GET /api/v1/customers/protection-scores` receive requests from any client other than the web app (a native app, an integration) in the last 30 days? | Whoever can read the Vercel request logs / API gateway for those two paths; the owner if a native client exists (the repo carries RevenueCat and `PushDevice`). | Deletion of the two routes and their inventory entries (BL-02 / H-T02). | Both routes still return `overallScore` / `tier` JSON on request; no in-repository consumer calls them; the score is otherwise gone from every rendered surface. |
| H5 | **May policyholders correct values a rule reads** (e.g. the extracted `vehicle.glassBreakage`, an insured value), and does a corrected value re-run the rules? If yes, is the correction recorded with who and when, and does it mark the record as human-confirmed? | The owner (product + legal): a correction that changes a finding is the customer editing the basis of the product's statement about their cover. | Whether C1 (the review checklist as the primary object, manual entry a first-class path, ΕΠΙΒΕΒΑΙΩΜΕΝΟ reachable) exists at all, and C2's confirm-low-confidence flow. | No correction path: extracted values are read-only; `resolveRecordStatus` can never return `confirmed` (every caller passes `confirmedAt: null`, asserted by test). The status row says «Προς επιβεβαίωση» on every completed analysis and nothing lets a person confirm it. |
| H6 | **Provenance classification of all 29 authored checks** — for each slug in `lib/gaps/provenance.ts`: `legislative`, `contractual` or `market`, with a citation (law / regulation article, or the contract clause type) and the name and date of who decided. The four spec-named candidates are listed unfilled in `PROVENANCE-CANDIDATES.md`; the other 25 have no candidate yet. | The underwriter and legal tracks. | Every summary count of findings, the digest's new-finding count, the agent's per-client finding chip and the report's findings section: by the B3 rule an `under_review` finding is counted in no summary and reaches no email, notification or report. **Urgency depends on V2's answer:** if the rule is applied literally (it is, for the counts that go through `excludeUnderReview`), those surfaces stay empty until slugs are classified; if a path leaks, classification is what makes the leak a legitimate count. | All 29 slugs render only inside the disclosed «Ευρήματα υπό αξιολόγηση» section of a findings list. The dashboard tile says findings exist and links to them without a number; the agent client chip counts classified findings (0); the weekly digest's «νέα ευρήματα» is 0; the report omits findings and says so. The recommendation path (attention list, hero count, /insights) still derives from these findings — see V2 in the verification report. |
| H7 | Three seeded test accounts in the **production** database (spec human-track item 7): delete and rotate before GA — which three, and who owns the rotation? | The owner. | GA readiness; nothing in this series. | Untouched by the series. |
| H8 | Deliverability of `info@`, `careers@`, `dpo@policywallet.gr` (Goal 0 §I): do they receive mail? | The owner / mailbox admin. | The trust and legal pages naming them. | The addresses are published; nothing in the code verifies a mailbox exists. |

## What is NOT waiting on a human

- Track C (C1–C6) is gated per goal on separate written approval and is not started.
- B1.6 is halted (H3), not blocked on code.
- The `filter` parameter the B4 KPI doors carry (`/customers?filter=expiring|gaps|invited`) is a code follow-up: the customer list does not read it yet, so those doors land on the full list.
