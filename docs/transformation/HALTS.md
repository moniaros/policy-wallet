# HALTS — PW-MOBILE-TRANSFORM-01

Questions only the human may answer (§12.1) and items blocked under §12.2.
A halt blocks the listed items, not the run, unless marked `blocks: RUN`.

**Open: 0.** The three §12.1 questions are raised when Phase 0 evidence exists to support them —
raising them without the evidence they require would waste the answer.

Expected, not yet raised:
- H-001 Should the protection score exist at all? — needs its rendered output for empty /
  unanalysed / all-expired / analysis-failed portfolios, which Phase 0 produces.
- H-002 Send-side notification policy — needs the outbound-copy inventory.
- H-003 Compliance boundary on renewal price intelligence — needs the Phase 2 specs.
