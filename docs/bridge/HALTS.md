# PW-BRIDGE-01 — halts

Written to and stopped on, per §11. Each entry names what stopped, why the loop may not decide it, and the exact answer that resumes it.

| Id | Date | What | Why it is a halt | Resumes on |
|---|---|---|---|---|
| H-B0 | 2026-09-06 | **Standing:** the agent→customer «add policy» flow (`addPolicyForCustomer`, `commitScannedPolicy`) is inventoried (INTERACTIONS I-10) and never built on. | Article 9 lawful-basis decision pending (§1). | The owner's decision, outside this series. |
| ~~H-B1~~ | 2026-09-06 | **RESUMED 2026-09-06 on the owner's «continue»** (D-B1 in `PROGRESS.md`: the two questions were decided under §5 as reversible decisions). Original entry: **L0 exit — approval to start the loop.** `PARITY.md`, `INTERACTIONS.md`, the two-sided harness (`tests/measure/bridge/`) and the four queues are complete; the harness caught a deliberately introduced divergence (`docs/evidence/bridge-l0/DIVERGENCE-DEMO.json`). | §2: «Halt for approval before the loop starts.» | The owner's written go, and any re-ordering of Queue A. Two questions the loop will otherwise decide itself under §5: (1) **the conversation number** — is the finding count both parties quote the `classified` count (agent side today) or the `disclosed` count with the under-review tail (customer home today)? §5 puts «gaps» last, which favours the count that never overstates: `classified`; (2) whether **actor attribution on notifications** may add a column (a migration — itself a halt) or must live in copy. |
| H-B2 | 2026-09-06 | **Agent-side disclosure of unshared policies (A-09, half).** The customer-side half is built: the wallet marks which policies the advisor can see and the advisor page says «N of M shared». The agent-side half would tell the advisor that the client holds policies not shared with them (existence, or a count). | §11: a change to privacy semantics — today the agent cannot infer an unshared policy exists; disclosing existence (even without identity) is new information about the customer's record that the customer did not share. | The owner's decision: (a) no agent-side disclosure; (b) existence only («ο πελάτης έχει και ασφαλιστήρια που δεν σας έχει κοινοποιήσει»); (c) a count. Until then the agent side stays as it is and PARITY C3 remains open on that side. |
