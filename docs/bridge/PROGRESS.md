# PW-BRIDGE-01 — progress

Appended after every item (§3). Before/after figures are measured; a figure without a source is not written here.

| Item | State | Date | Before → after (measured) | Notes |
|---|---|---|---|---|
| L0.1 Parity Matrix | done | 2026-09-06 | rows: **0 → 24** parity rows (A1–A7 identity/lifecycle, B1–B8 record/analysis, C1–C4 relationship, D the count families) **+ 5 residue** (E1–E5); rows that can differ by code: **9** (A1, A2, A4, A5, B1, B3, C2, C3 and the templates' A4); seeded claims verified: **5 / 5** (one resolved before the series, one corrected: the B2C edit form reaches 6 columns, not 7) | `docs/bridge/PARITY.md`. Built from code on `1f863b31`; every row names its files. |
| L0.2 Interaction Ledger | done | 2026-09-06 | rows: **0 → 23**; effects **invisible** to the affected party: **7** (I-04, I-05, I-06, I-07 old agent, I-08, I-17, I-22); **misleading**: 2; visible **without a declared actor**: 8 (the `notifications` table has no actor column); write-plus-trace **not transactional**: 3 | `docs/bridge/INTERACTIONS.md`. Stub verified: `lib/outbound/dispatch-guard.ts` refuses email/push outside production and throws in a test run; its enumeration guard `tests/unit/outbound-dispatch-stub.test.ts` walks every transport. |
| L0.3 Two-sided harness | done | 2026-09-06 | metric unit test: **4 / 4** green with the committed divergent pair (`tests/unit/bridge-fact-divergence.test.ts`); live demo: see the row below | `tests/measure/bridge/facts.ts` (pure metric), `two-sided.ts` (two sessions, nine fixture states, reversible seeding, prod refusal), `bridge-l0-demo.spec.ts`; Playwright project `bridge`. |
| L0.3 demo — deliberate divergence caught | **done — red as required** | 2026-09-06 | live pair `/wallet/<id>` ↔ `/customers/<uid>/policy/<id>`, healthy state, 390px, Greek: **4 fact pairs compared, 1 divergent** — `gap.findingsProvenance` customer «Ευρήματα από την ανάλυση της 20 Φεβρουαρίου 2026.» vs agent «…της 23 Φεβρουαρίου 2026.» after `finishedAt` was moved +3 days between the captures; restored afterwards. The same run also paired the shell's unread badge («9+» vs «3») — a viewer-scoped key, now excluded (decision below). | `docs/evidence/bridge-l0/DIVERGENCE-DEMO.json` |
| L0.3 sweep — both sides in every state | done (9 states × 2 pairs = 18 rows) | 2026-09-06 | **policy pair:** fact pairs 0–5 per state, **0 divergent in every state** (parity by construction on the shared `AnalysisCard`); count pairs 0–3, 0 divergent; **home pair (`/dashboard` ↔ `/customers/<uid>`): 0 fact pairs and 0 count pairs in every state** — the agent's customer profile carries no `data-fact`/`data-count`, so parity there is unmeasurable (A-08). One-sided facts surfaced two state contradictions: **unauthored branch** — agent renders the unauthored sentence, customer renders no analysis facts (A-16); **expired** — agent renders composition + provenance, customer renders none (A-18). **Revoked access:** the agent's page renders the not-found body with no customer data (absence holds), HTTP 200. **Failed run, pre-plan, stale catalogue:** identical on both sides (5/3/5 pairs). **Empty book / single-client book:** the policy pair unchanged (4 pairs, 0 divergent). No horizontal scroll on any capture; `lang=el` on all 36. | `docs/evidence/bridge-l0/RESULT.json`, per-state captures and screenshots under `docs/evidence/bridge-l0/<state>/` |
| A-08 — instrument the agent surfaces so parity is measurable | **done** | 2026-09-06 | policy pair, fact pairs compared: healthy **4 → 8**, unauthored **1 → 5**, expired **1 → 7** (0 divergent in each — the healthy fixture has a real number, a real premium and no renewal history, so the raw renders happen to agree); canonical `data-fact-value` on both sides for insurer, number, status, expiry, start, premium (parity on the value, not the label around it); agent list rows now carry `policy.insurer#id`, `policy.rowStatus#id`, `asset.identifier#id`; customer list rows `client.policyCount#uid`, `client.openGapCount#uid`, `client.nextRenewalDate#uid`; registry: +1 count key, +2 fact keys, +3 subject-scoped | **DIFF/CLASSIFY:** instrumentation, no fact moved. **Found while tagging:** the agent page computes its status label before resolving the account language → A-19 (locale contradiction). **Wallet pair measured 0 pairs** because the agent profile opens on its overview tab (local state, no URL) — the harness must click the policies tab; and the raw-number / premium-as-zero / raw-date contradictions need degraded IDENTITY fixtures (placeholder number, no premium, renewed end date) to be observed — three harness states added (placeholder_identity, no_premium, renewed_end_date) and the wallet pair opens the policies tab: **wallet pair 0 → 15–24 fact pairs**, which surfaced two new contradictions on every row (A-20 status resolver + casing, A-21 insurer name resolver). Registry guards green; tsc, lint clean. |
| A-05 — raw policy number on the agent policy page | **done** | 2026-09-06 | **OBSERVE (harness, placeholder_identity):** `policy.policyNumber` customer «» · agent «PENDING-1700000000099»; `policy.insurerName` customer «Αυτοκίνητο» (branch fallback) · agent «» | **CLASSIFY:** Contradiction (text). **ADJUDICATE:** the customer's page is right — a placeholder never renders; §5 q.1 (am I covered?) is not served by an internal sentinel. **ACT:** `displayPolicyNumber` + the customer's «Δεν διαβάστηκε από το έγγραφο» phrase on the breadcrumb and the sidebar; the insurer falls back to the same branch label. Guard `customer-policy-page-parity` (probe red on all four rules).  **VERIFY:** harness re-capture (placeholder_identity): `policy.policyNumber` customer «» · agent «» and `policy.insurerName` «Αυτοκίνητο» on both — **2 → 0 divergent**. |
| A-04 — raw end date on the agent page and in the templates | **done** (templates → C-01/C-02) | 2026-09-06 | **OBSERVE (renewed_end_date):** `policy.expiryDate` customer «2028-02-18» · agent «2027-02-18» — a recorded renewal moved the customer's end date a year, the agent read the column | **CLASSIFY:** Contradiction (date). **ADJUDICATE:** the lifecycle (latest renewal end → envelope → column) is the ONE call (CLAUDE.md); the column is its last fallback. **ACT:** one `resolvePolicyLifecycle` call feeds the countdown and the end date on the agent page.  **VERIFY:** harness re-capture (renewed_end_date): `policy.expiryDate` «2028-02-18» on both — **1 → 0 divergent**. |
| A-03 — unknown premium rendered as «0,00 €» | **done** | 2026-09-06 | **OBSERVE (no_premium):** the customer omits the tile; the agent showed «0,00 €» (one-sided by the metric — the customer renders nothing — so the contradiction is with the record, not with a pair) | **CLASSIFY:** Contradiction (value — zero is not unknown). **ACT:** the agent page says «Δεν διαβάστηκε από το έγγραφο» with an empty canonical value when no amount exists. **Residue:** the customer's silence is its own absence-as-reassurance question; the premium-exclusion note covers it on the dashboard, not on the policy page.  **VERIFY:** harness re-capture (no_premium): the agent page states the value could not be read; the pair stays one-sided (the customer omits the tile) — 0 divergent. |
| A-19 — status label resolved before the account language | **done** | 2026-09-06 | code: `getStatusLabel(status)` eight lines before `language` was resolved → Greek for every account | **CLASSIFY:** Contradiction (locale). **ACT:** the label is computed after the language, with it. The one-locale guards enumerate server pages but not defaulted parameters — noted for their next revision.  **VERIFY:** guard `customer-policy-page-parity` green (source asserts `getStatusLabel(status, language)`); Greek accounts render the same word as before. |
| A-20 — agent rows render the stored status with their own label table | **done** (with A-20b) | 2026-09-06 | **OBSERVE (wallet pair, every state):** `policy.rowStatus#<id>` customer «ΕΝΕΡΓΟ» · agent «Ενεργό» on every row (same word, two tables); on the placeholder-identity policy customer «ΑΠΑΙΤΕΙΤΑΙ ΕΝΕΡΓΕΙΑ» · agent «Ενεργό» (two resolvers: `policy-status-view` vs `effectivePolicyStatus`) | **CLASSIFY:** Contradiction (state) — the casing alone would be Cosmetic, the placeholder row is a different meaning, and §4 says a contradiction never becomes cosmetic by looking small. **ADJUDICATE:** the wallet's status pipeline is right (it knows the identity and extraction rules the bare lifecycle does not). **ACT:** the service DTO resolves `resolvePolicyStatusKey(p)`; `policyStatusLabel(key, t)` exported from the pipeline; the tab's label map deleted. Guard `agent-rows-use-wallet-resolvers` (probe red on three offences).  **VERIFY (harness, healthy + placeholder_identity):** wallet pair **10 → 1 divergent**; the survivor is the placeholder-identity row — customer «ΑΠΑΙΤΕΙΤΑΙ ΕΝΕΡΓΕΙΑ», agent «ΕΝΕΡΓΟ» — same table, same resolver, different INPUT: the wallet scrubs placeholder identity before resolving, the service passes the raw row. Fix at the resolver (A-20b): a placeholder identity counts as missing identity. |
| A-21 — one insurer, two names | **done** | 2026-09-06 | **OBSERVE (wallet pair):** `policy.insurer#<id>` customer «Εθνική Ασφαλιστική» · agent «ΕΘΝΙΚΗ» (registry display name vs raw column); placeholder insurer: customer «Αυτοκίνητο» · agent «» | **CLASSIFY:** Contradiction (text). **ADJUDICATE:** the registry name — it is what the customer reads and what the documents module writes. **ACT:** the service DTO resolves `resolveInsurerDisplay(p.insurerName).displayName`; the agent tab and page fall back to the branch label the customer uses.  **VERIFY:** `policy.insurer#<id>` pairs: «Εθνική Ασφαλιστική» on both sides; the placeholder insurer falls back to «Αυτοκίνητο» on both — **0 divergent**. |
| A-20b — a placeholder identity is missing identity, in the resolver | **done** | 2026-09-06 | **OBSERVE:** after A-20 the wallet pair kept ONE divergence — `policy.rowStatus` for the placeholder-identity policy: customer «ΑΠΑΙΤΕΙΤΑΙ ΕΝΕΡΓΕΙΑ», agent «ΕΝΕΡΓΟ» — same table, same resolver, different input (the wallet scrubs the sentinels before resolving; the service passes the raw row) | **CLASSIFY:** Contradiction (state). **ADJUDICATE:** the resolver, not the callers: the ONE status pipeline must give one answer for one record however it is handed over. **ACT:** `resolvePolicyLifecycle` treats a placeholder number or insurer as missing identity (predicates from the identity module — the literals stay there). Guard case in `policy-card-status.test.ts` using minted placeholders — **red against the stashed previous rule, green with it**.  **VERIFY (harness, placeholder_identity):** wallet pair **1 → 0 divergent** (24 pairs); policy pair 5 → 6 pairs, 0 divergent — `policy.status` now pairs too («ΑΠΑΙΤΕΙΤΑΙ ΕΝΕΡΓΕΙΑ» on both). |
| A-02 — the finding count on both sides | **done** | 2026-09-06 | **OBSERVE (code):** the customer's home already counts only CLASSIFIED findings in its headline (`CoverageGapsWidget`: «Only classified findings are counted») and renders under-review as a separate labelled line (`gap.underReviewOmitted`); the agent's «Κενά» is classified too — but **the agent has no under-review figure anywhere**, so a client whose app says «2 σημεία υπό αξιολόγηση» shows «0» in the book | **RECLASSIFIED:** not a Contradiction (L0's reading from code was wrong — B3 had already fixed the headline); an **Unjustified asymmetry**: a figure the customer reads about their own record that their advisor cannot see. **ADJUDICATE (D-B1):** classified is the conversation number on both sides; under-review is a separate labelled figure on both. **ACT:** the agent portal and the agent dashboard each make ONE disclosed read and split it by the accessor's provenance tag — classified stays the headline, under-review becomes `client.underReviewCount#uid` rendered «+N υπό αξιολόγηση» beside the count on the list and the card, with the customer's own wording. Guard rule 4 in `agent-rows-use-wallet-resolvers`: a classified-only read without its disclosed companion is an offence (probe extended to four).  **VERIFY (harness, book pair):** the agent's list renders `client.underReviewCount#<uid>` = «+10 υπό αξιολόγηση» for the seeded client (healthy) — a figure the agent had never seen; the classified «Κενά» headline unchanged. |
| A-06 — record status after the advisor confirms | **done** | 2026-09-06 | **OBSERVE (code, I-08):** `confirmPolicyReview` writes `extraction.reviewState = "confirmed"` + `confirmedAt`; both policy pages passed `confirmedAt: null` to `resolveRecordStatus`, so a record a person had confirmed still read ΠΡΟΣ ΕΠΙΒΕΒΑΙΩΣΗ on both sides while the «unverified» badge (which reads `reviewState`) had gone | **CLASSIFY:** Contradiction (state; intra-page and cross-lens). **ADJUDICATE:** §7: an agent-confirmed extraction is agent-declared and renders as such on the policyholder's side. **ACT:** `extractionConfirmation(acordData)` in the record-status module is the ONE reader of the envelope; both pages spread it into the resolver; the confirm writes `confirmedBy: "agent"` and the user id; the label renders «Επιβεβαίωση συμβούλου» / «Advisor-confirmed» (≤ 24 chars, neutral on both lenses). Guard: the null-only caller scan became an envelope-reader scan — **red with the two pages stashed, green with them**; five cases on the helper. Harness state `agent_confirmed`.  **VERIFY (harness, agent_confirmed):** `record.status` = «Επιβεβαίωση συμβούλου» on BOTH policy pages (8 pairs, 0 divergent); healthy still reads «Προς επιβεβαίωση» on both. |
| A-10 — the grant level on the customer's advisor page | **done** | 2026-09-06 | **OBSERVE (code, PARITY C2):** `/agent` selected the grants without `permissions`; the ledger row said only «shared by you»; the level was visible only inside the per-policy collaboration panel | **CLASSIFY:** Justified asymmetry without full disclosure (§4: keep, and disclose to the party who cannot see). **ACT:** the grant query carries `permissions`; the item normalises it through `normalizePermissions` (the access module's own reading of the CSV column); every row states the level as `data-fact="grant.level"` with the collaboration panel's words («Βλέπει το ασφαλιστήριο και τα έγγραφά του» / «Μπορεί να διορθώσει…» / «Διαχειρίζεται…»). Guard `justified-asymmetry-has-disclosure` — **red on all four rules before the repair**, probe carries the undisclosed shape.  **VERIFY (harness, advisor pair):** 9 grant rows on /agent each carry `grant.level` (read / write); no horizontal scroll at 390. |
| A-09 — a policy the customer never shared is invisible to the agent with no explanation (customer half) | **customer half done**; agent half **halted (H-B2)** | 2026-09-06 | **OBSERVE (code, PARITY C3):** the wallet card said nothing about sharing although the page already loaded `sharedWithAgents` per policy; `/agent` listed the shared policies without the denominator | **CLASSIFY:** Justified asymmetry (consent) — disclose on both sides. **ADJUDICATE:** the customer half is theirs to see and is built; the agent half (telling the advisor that unshared policies exist) changes privacy semantics → §11 halt H-B2, the owner decides among none / existence / count. **ACT:** the wallet card marks «Κοινοποιημένο στον σύμβουλο» (`policy.sharedWithAdvisor#id`); `/agent` says «Ο σύμβουλός σας βλέπει {N} από τα {M} ασφαλιστήριά σας» with `portfolio.sharedWithAdvisorCount` beside `portfolio.policyCount` — a number with its denominator.  **VERIFY, first capture:** the wallet marks 9 of 10 cards «Κοινοποιημένο στον σύμβουλο»; every grant row on /agent carries its level (13 rows: read/write) — and the summary read **«13 από τα 10»**: the ledger counted GRANTS, including grants over deleted fixture policies, one per grant. Repaired at once as ONE pure function (`lib/wallet/shared-policy-ledger.ts`: orphan grants dropped, one policy once at its highest level, numerator = distinct existing policies ≤ denominator; unit-tested). Re-capture: **«Ο σύμβουλός σας βλέπει 9 από τα 10 ασφαλιστήριά σας»**, 9 wallet cards marked, wallet pair 27 pairs / 0 divergent. |
| Queues populated | done | 2026-09-06 | **A 17 · B 23 · C 8 · D 6** (A-16, A-17 appended from the live captures) | `docs/bridge/QUEUES.md` |
| L0 exit | halted, then **resumed** on the owner's «continue» | 2026-09-06 | — | `docs/bridge/HALTS.md` H-B1 (closed); D-B1 below |
| A-01 — the credential hash loaded on eleven agent paths | **done** | 2026-09-06 | Prisma selects of `users.password` outside auth: **11 → 0** (`customers/[id]/page.tsx`, `agent/actions.ts` ×3, `bulk-import/route.ts`, `opportunities/page.tsx`, `activity/actions.ts`, `dashboard/agent/page.tsx`, `customer-resolution.service.ts`, `customer.service.ts` ×2, `agent-playbook.ts`); modules allowed to read the column: **1** (`lib/services/credential-signals.ts`, `IS NOT NULL` only); rule inputs carrying the value: **3 → 0** (`isPhantomCustomer`, `agentMaySeeCustomerIdentity`, `presentCustomerIdentity` take `hasPassword: boolean`; `deriveAiConsentState` likewise) | **OBSERVE:** the hash was selected to test it for null — the identity-twin rule and the AI-consent verdict need presence, never the value; the column is null for every real account (Supabase holds the credential), so the load was invisible. **DIFF:** none rendered; one path passed the row shape to a client component's props type. **CLASSIFY:** Leak (minimisation) — one side loads what it should not. **ADJUDICATE:** repair at the shared source: a presence query + boolean rule inputs. **ACT:** guard `password-column-never-loaded-outside-auth` (filesystem-enumerated universe, reasoned allowlist, probe flagged as both select and raw) — **demonstrated red on the real tree (8 files) before the repair, then green**; GUARDS.md row. **VERIFY:** guard green (4 cases incl. the schema-pinned column names — the first live run had rendered the agent dashboard's error boundary because the helper selected `id` while User.id maps to `user_id`; fixed and pinned); full unit suite 610 files green after mocking the presence module in five test files; warm request-level probe with the agent session: `/dashboard/agent`, `/customers`, `/customers/<uid>`, `/opportunities`, `/activity`, the customer policy page — **6/6 HTTP 200, 0 error boundaries, KPI strip present**, dev log clean. The agent journey spec: the client-directory case passes; the dashboard case fails on a 30 s `page.goto` timeout against a ~27 s cold Turbopack compile (pre-existing shape, not this change); two MEDIC-modal cases fail inside the opportunity modal, which A-01 did not touch — recorded, not repaired here. **Residue → A-01b:** two `include: { customer: … }` full-row loads (`customer.service.ts` profile, the tax-id backfill's `findUnique` without select) still bring every column; the guard's regex cannot see an include. |
| C-01 / C-02 — templates count from the lifecycle | done | 2026-09-07 | guard `expiry-countdown-single-source`: **red on 4 pinned files → green** (renewal.service:176, weekly-digest:205, document-insights:95, renewals/actions:318–319), residue 6 files / 8 hits exact; behavioural (`renewal-countdown-from-lifecycle`): a renewed policy (column 20 d out, history +1 y) is mailed **1 → 0** times and gets **no** cycle; an envelope five days later than the column is counted **20 → 25** d in both templates and its cycle keyed **2026-08-09 → 2026-08-14**; an open cycle keyed on a superseded period closes as completed instead of drifting to overdue; dev cron dry run (2026-09-07, `POST /api/v1/jobs/renewal-check`, dispatch guard blocked 9 outbound sends): the renewed shape (column +20 d, history +385 d, resolved column NULL) got **no** cycle and its stale pending row closed **pending → completed / renewed_same_insurer** inside the loop; the envelope-later shape got one cycle keyed **on the envelope date** with `daysBeforeExpiry` **25** (the column would have said 20); dev backfill **17 → 0** unresolved of 41; seeded rows restored, run artefacts deleted; production: after the deploy — SELECT pair + backfill + one cron trigger, recorded in the follow-up entry | One helper `expiryWindowWhere` in lib/policy-status (coverageEndDate first, raw column only while NULL — the resolver's own fallback order); the loop shadows `policy = { ...row, endDate: lifecycle.endDate }` so every pinned reminder/task literal stays and carries the resolved date; document-insights' third clock removed; VERIFY = guard + behavioural + dev cron SELECT pair + prod SELECT pair (D-B2: a template has no harness capture). |
| C-02 — production proof (follow-up) | done | 2026-09-07 | production `policy_renewals` after the deploy of #316 (`873eba68`, 12:32Z) and the three `coverage_end_date` repairs by SQL: open cycles keyed EARLIER than the resolved column **2 → 0**, keyed later **0**, unresolved columns **0 of 8** live policies, column = `end_date` on all 8; the three open rows (1 overdue, 2 pending) are keyed exactly on their policy's resolved date (SELECT 2026-09-07 18:40Z) | The repair of the column closed the divergence on its own — the two stale rows were keyed on the true date while the column carried the extractor's wrong year, so the sweep has nothing to close today; it acts the first time a recorded renewal moves a column later. The cron cannot be triggered by hand from here (`CRON_SECRET` differs → 401); the scheduled 05:00Z run of 2026-09-08 is the first to execute the new loop on production. |
| C-01b — the residue countdowns and the raw windows | done | 2026-09-07 | guard `expiry-countdown-single-source` RESIDUE **6 files / 8 hits → 1 file / 1 hit** (the NOT-debt monitoring site, kept so the universe is complete); new rule (c) raw expiry windows in a Prisma where: **2 sites (churn day-7 email, perk reminder) → 0**, probe red on 2 shapes; a FOURTH builder of `PortfolioPolicyFacts` found by the type check (`app/(protected)/wallet/[id]/page.tsx` — the same-subject overlap on the policy page compared the raw column); 445 affected tests green | The fact shapes now NAME the resolved date — `PortfolioPolicyFacts.coverageEndDate`, `ProtectingPolicy.coverageEndDate`, timeline `PolicyRow.coverageEndDate`, `classifyUrgencyTier(policies[].expiresAt)` — so a raw read cannot be spelled by accident, and every builder (gap-engine ×2, wallet page, risk-graph service, timeline service) resolves through `resolvePolicyLifecycle`; the comparison card sorts and counts from the lifecycle over the DTO it already holds; `expiryWindowWhere(from, to?)` gained an open upper bound for the in-force window. |
| A-01b — full-row user loads and bare User includes | done | 2026-09-07 | enumerated from the code (comments stripped): **19 full-row `db.user.find*`** → 11 narrowed to selects (wallet actions ×4, agent invite, share route, team invite, customer creator + its phantom create, /me ×3), 8 allowlisted by reason (5 auth-lifecycle, 2 session resolver, 1 audited admin record); **10 bare includes of a User relation** → 9 narrowed (customer profile, two opportunity/follow-up lists, agent portal card, home greeting, wallet share badges, two agent-approval mails), 1 was a comment; guard rules (c)(d)(e) green on the tree, probe red on 2 + 2 shapes; tsc 0, suite green | The backstop is structural: `lib/db.ts` constructs the client with `omit: { user: { password: true } }` (Prisma `omitApi`), so the hash cannot leave the database unless a query names it — the type check then enumerated every consumer typed on the bare `PrismaClient` or the full `User` (base service, events writer, the dashboard prop) and they now type on the exported `DbClient` / `Omit<User, "password">`. Nothing read the value (Supabase holds the credential; the one presence check is raw SQL). |
| Queue C-03/C-04/C-05/C-08 — the report says what the app says | done | 2026-09-09 | The two report routes had built their inputs INLINE, in duplicate — which is precisely how the report became a third surface that omits what the app states: the app grew a record-state line and a composition, and following meant editing the same block twice. One `lib/services/reports/report-context.ts` now builds provenance, pre-plan, staleness, composition and record status, and both routes read it. The generator renders the composition's two lines from the app's OWN copy keys (`composition.*`, the same bundle `components/gaps/CoverageComposition` uses) and the record-state sentence with its «describes the record» framing. C-05 verified: under-review rows are excluded and disclosed, never counted. C-08 closed by I-09. | Three existing guards pinned the CALL SITE rather than the invariant and went red on the refactor — re-pointed, never deleted: the staleness and provenance are now asserted where they are computed plus at both routes that carry them through. A fourth failure was CLAUDE.md's own recorded failure mode — the outbound-purity scan matched the component's NAME inside a comment explaining that the report reuses its copy keys; it strips comments now. |
| Queue B tiers 2 + 5 — the actor is named, and passive is a decision | done | 2026-09-09 | **Tier 2**: attribution is copy-level (the ledger sanctions it; `notification_events` has no actor column). I-16 said «Ο σύμβουλός σας» / «Ο πελάτης» — a ROLE, not an actor — and now names the person in both directions; I-10/I-11/I-12/I-14 named someone but via a raw `.name`, so all four route through `displayPersonName`. **Tier 5**: I-02 — all three branches of `applyInviteRedemption` were silent, and one of them MINTS THE ACCESS GRANT when the advisor had no account, so a policy share completed with neither side told; the two relationship branches now call one `announceRelationshipActivated` (sharePolicy's copy, deliberately — the same fact must not read differently because it arrived by invite) and the share branch tells both sides. I-15 and I-23 recorded as justified-passive with disclosure. Suite 624 files / 7270 tests | The tier-2 finding worth keeping: a notification that names a person via `x.name` is not attributed — it is attributed *until a fixture or placeholder token reaches it*, which is exactly what `displayPersonName` exists to stop. The guard asserts the identity module is used, not that a name appears. |
| Queue B tiers 3–4 — one truth per fact, and a specified half-failure | done | 2026-09-08 | **I-09**: the flag emitted `extraction_flagged` to the FLAGGING AGENT while the registry declared `owner`, the copy spoke to the owner and `requiredAction` was `confirm_extracted_values` — the person whose record was marked «flagged», and the only one who can resolve it, was the one not told. Split in two: the customer-facing event now goes to `policy.ownerUserId` naming the flagger; a new `extraction_flag_raised` carries the triage row, and the admin flag queue reads THAT (production held 0 rows of either, so the repoint cost no history). **I-03**: `accessGrant.create` and `customerRelationship.create` were separate awaits → one `$transaction`. | The half-state was not harmless: `computePolicyAccess` derives read/write/delete from an AccessGrant's level ALONE and never re-checks the relationship, so a grant that committed while the relationship failed left an advisor holding access that nothing explained and no termination path could reach — every one of them works from the relationship. Guard gains a tier-3/4 block whose universe is the ledger's own rows: each id is either checked or recorded as closed elsewhere WITH the reason. Per-emit recipient checking needed a brace matcher — a non-greedy regex for the transaction body stops at the first nested `})`. |
| Queue B tier 1 — an effect on someone's record is told to them | done | 2026-09-08 | invisible tier-1 interactions **7 → 0**: I-04 revoked share → `policy_share_revoked` to the advisor; I-05/I-06 relationship ended → `advisor_relationship_ended` to whichever side did NOT act; I-07 transfer → `customer_transferred` now also reaches the CUSTOMER (both agents were already told; the person whose policies a new human could see was not); I-08 confirm → `policy_details_confirmed` to the owner whose columns were overwritten; I-17 branded report → `branded_report_generated` on the owner's side, deduped per policy per day; I-22 → `policy_updated`/`policy_removed` reach grant-holding advisors via `getPolicyGranteeUserIds`. Registry: 4 new events, 2 recipient lists corrected. Suite 624 files / 7259 tests green | Every emit is best-effort AFTER the write it reports — the effect has already committed, and a failed notification must never report a successful termination as failed. The guard reads its universe from the Queue B tier-1 row itself, so the ledger and the wiring cannot drift; its extractor skips parameter lists (a default `edits = {}` was mistaken for the function body) and accepts a whole-file scope for the route wrapped in `withApiGuard`. Writing it caught three real gaps in the first run, one of them mine. |
| A-07 / A-11…A-18 — production smoke (agent side) | done | 2026-09-08 | agent session on `/customers/…/policy/cmt2ekeee…`: **10 facts, each rendered exactly once** — one insurer «Εθνική Ασφαλιστική» (A-07), `policy.insuredSubject` «ΙΚΖ3113» (A-13), `document.count` 1 (A-14), `gap.findingsProvenance` + `composition.prePlan` present (A-16/A-18), **0** score facts and **0** percentages (A-11/A-12); the customer's non-shared policy still 404s to this agent | Count VISIBLE nodes: `#gap-analysis` and `#documents` each appear twice in the DOM and ten `[data-fact]` nodes measure 0×0 because they sit in React's streaming buffers (`div#S:n[hidden]` at the end of `<body>`) — inert, not a duplicate-id defect. |
| A-11 — dead protection-score read on the agent side | done | 2026-09-07 | dashboard: one `protectionScore.findMany` per load + a wire field **→ 0** (nothing rendered it since B1.7); agent-portal.service: the same fetch and its owner-set query **→ 0**; containment guard rule (3 loaders) red on the probe | Void/cleanup. `ClientCardData.protectionScore` removed; the live-status guard's exemption for agent-portal ratcheted 2 → 1. |
| A-15 — `gap-readers-exclude-superseded` guard hole | done | 2026-09-07 | guard: a bare `status:` or `supersededAt` token satisfied it → now a LIVE set **and** `supersededAt: null` (or by id), brace-matched window (was 14 lines), split calls seen; red probe shapes **2 → 6**; readers it exposed and moved to the live set: **7** (customer.service `status:'open'`, the agent policy page `supersededAt` only — it listed resolved/dismissed rows as current — two API routes on `resolvedAt`, portfolio-gap-view, the wallet's two `_count`s, load-attention-areas) | The singular `gapInstance:` include is a by-reference read (the parent's FK) and stays outside the pattern, with the reason in the guard. New harness state `resolved_finding`. |
| A-07 — insurer rendered twice on the B2B page | done | 2026-09-07 | raw-envelope insurer + premium pair in the ACORD block **2 renders → 1** (the registry-resolved head); parity guard rule A-07 red on the probe | The extracted-vs-stored comparison belongs on the review screen. |
| A-12 — score vocabulary in `data-fact` keys | done | 2026-09-07 | `review.scoreAtOpen` («N%» on the home's review card) and `timeline.scoreDelta` («+3» on /account/history) **rendered → not rendered**, keys **registered → removed**; `riskDimension.*` verified text; `score-containment` IDS widened, red on the new probe | Columns stay stored. `life-timeline` builder unchanged. |
| A-13 — asset identity on the B2B policy page | done | 2026-09-07 | `policy.insuredSubject` on the agent page **absent → rendered** through `policyAssetIdentity` with the customer's words; first capture diverged («ΑΒΕ-4802» vs «E2E Policyholder») because the page read the stored line of business where the customer reads the envelope's — now the same `getCoverageType` order; re-capture: policy pair **7 fact pairs, 1 → 0 divergent** («ΑΒΕ-4802» on both sides), 1 count pair | Positive rule in the parity guard. |
| A-14 — documents parity | done | 2026-09-07 | agent page: `documents: true` → the customer's `orderBy uploadedAt desc` + six display fields; `document.count` **RESERVED → defined**, rendered as a door on both headings | Pairs when the customer's documents section is mounted; collapsed, the agent's count is one-sided depth (recorded, not forced). |
| A-16 / A-18 — customer renders no analysis facts on unauthored-branch / expired policies | done | 2026-09-07 | one `defaultOpen` predicate on the customer's review section; harness (policy pair): unauthored_branch `gap.findingsProvenance` **one-sided → paired**; expired **~7 → 11 fact pairs**, `composition.*` **one-sided → paired**, 0 count divergences; the one fact divergence the expired capture surfaced was the agent's premium («» vs «312.4 EUR» — a Prisma Decimal failing a typeof-number test), fixed with the customer's own derivation; re-capture: policy pair **11 fact pairs, 1 → 0 divergent** («312.4 EUR» on both sides), 4 count pairs; `document.count` one-sided on the agent (the customer's documents section was collapsed — recorded) | Rejected: gating the agent's card on coverage — removes information (B9/B11 «should not differ»). |
| A-17 — `gap.underReviewCount` only on the customer's card | done | 2026-09-07 | agent policy page: the customer's band sentence («Εντοπίστηκαν N … — εκ των οποίων M υπό αξιολόγηση») as one door with the under-review span keyed **absent → rendered** over the live rows; harness `resolved_finding`: policy pair **9 fact pairs, 4 count pairs incl. `gap.underReviewCount`, 0 divergent**; the resolved row lists on neither side | The customer's report LIST is not mounted on the agent side: the advisor's confirm action lives in the analysis card's own list. |

## Decisions taken under standing authority

| Date | Decision | Why | Reversal |
|---|---|---|---|
| 2026-09-06 | **D-B1 — «continue» after the L0 halt report is the go for the loop; the two open questions are decided under §5 until the owner says otherwise.** (1) The **conversation number** for findings is the `classified` count on both sides; under-review rows are a separate, labelled figure on both sides, never folded into the headline. (2) **Actor attribution** on notifications lives in the copy (a declared actor sentence per event) — no schema column, because a migration is itself a halt (§11). | §5 puts «gaps, suggestions» last and favours the number that never overstates; the CLAUDE.md B3 rule already keeps under-review out of summaries. A copy-level actor costs nothing and can be replaced by a column later. **The production catalogue alignment (PW-CONTENT-01 BL-C1) is NOT covered by this reading: a production write needs an explicit go.** | The owner answers either question differently; both are one-module changes (`gap-rows` scope at the render sites; the notification copy). |
| 2026-09-06 | **D-B3 — cross-side parity is asserted per policy, never at portfolio level.** Every book-level count on the agent's side (client policy count, open gaps, expiring) is scoped to the policies that agent may see (a grant or their own upload), so a portfolio-level pair would flag a JUSTIFIED asymmetry as a contradiction on every unshared policy. The harness's key-equivalence map is a mechanism with no portfolio entries; the measurable pairs are the policy page pair and the wallet ↔ client-policies pair on subject-scoped keys. The dashboard ↔ profile pair is kept and expected to pair nothing. | §4: a justified asymmetry is kept and DISCLOSED (A-09), not repaired into equality. | Add an entry to the map for any pair proven to count the same set. |
| 2026-09-06 | **D-B4 — the advisor's confirmation reaches the record status `confirmed`, declared as the advisor's.** PW-TRANSPARENCY-02 B1 had left `confirmed` unreachable «until C1»; C1 is the POLICYHOLDER's own review and stays unstarted. The advisor's existing review is a person confirming the record's data, and rendering it as «awaiting confirmation» after it happened is a contradiction on both lenses (PARITY B1, INTERACTIONS I-08). The actor is declared on the label; the notification to the owner (I-08's visibility) is a Queue B item. | §7 per-interaction requirement: «an agent-confirmed extraction is agent-declared and renders as such on the policyholder's side». | Revert `extractionConfirmation` to nulls and the guard to the null scan. |
| 2026-09-06 | **D-B2 — VERIFY cadence.** Per item: re-capture the affected pair(s) in the affected state(s); the full nine-state, two-pair sweep runs at the five-item report cadence and at loop exit. | A full sweep is ≈27 minutes; per-item it would cost more than the repairs. A leak of a never-rendered column has no visible capture at all — its VERIFY is the guard plus a 200 on the affected pages. | Run the full sweep per item if a repair ever moves a fact the per-item capture did not cover. |
| 2026-09-06 | The harness pairs facts by `data-fact` key **plus `data-fact-subject`**, and treats one-sided keys as depth, never divergence. | Two views may differ in depth; only a shared key with two values is a fact divergence. Counting one-sided keys would flood Queue A with justified depth. | Add a key-equivalence map (`client.openGapCount` ↔ `portfolio.attentionCount`) when the loop reaches A-08. |
| 2026-09-06 | **Viewer-scoped keys are never a pair.** `notification.unreadCount` renders the VIEWER's unread count on both shells; the first live run paired it («9+» vs «3») and called it a divergence. `VIEWER_SCOPED_KEYS` in `facts.ts` excludes it from both the pairs and the one-sided lists; the unit test carries the case. | A key that describes the person looking, not the object looked at, cannot contradict itself across two people. | Add a key to the set only with a sentence saying why it is about the viewer. |
| 2026-09-06 | The unauthored-branch fixture is provisioned as motor with no gap rows and its branch switched to `cyber` afterwards. | The matrix provisioner only knows motor and health payload shapes; adding a third shape for one state would widen a shared fixture for a harness-only need. | Teach `provisionMatrixFixtures` a branchless shape. |
| 2026-09-06 | The `empty_book` and `single_client_book` states flip the agent's OTHER relationships to `inactive` for the capture and restore them. | The E2E agent is the only agent; a second agent account would need provisioning in `global-setup`. Flipping is reversible and `inactive` is an ended state for visibility. | Provision `e2e-agent-empty@` in `tests/e2e-users.ts`. |
