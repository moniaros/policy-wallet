# ASSUMPTIONS — Grafí build

`A-nn | goal | assumption | why safe | what would change it`
A-01 | G0 | `PolicyWallet_Homepage_Premium.html` was named as attached but exists neither in the repo nor ~/Downloads | §6 spells out the target structure section-by-section, and §6 itself says "port its structure, not its raw CSS" — the structure list is the contract | The file appearing; then diff the build against it
A-02 | G2 | Brief says `pnpm tokens`; repo is npm (package-lock.json, no pnpm-lock) | Same script, different runner: `npm run tokens` | A repo-wide package-manager migration, which is not this brief's scope
A-03 | G10 | The standing owner decision held new guides behind the citation debt "repaired, or at minimum sized and scheduled". Debt is sized (G-07), one entry repaid (28 left), and this brief orders 6–8 guides. Read: proceed ONLY with guides whose every statutory claim carries a primary deep link at the SOURCES.md standard, so the debt cannot grow; the 28 stay scheduled | New guides at the higher standard shrink nothing but add nothing to the debt; the hold's purpose (don't dilute «Πηγές») is preserved | The owner restating the hold as absolute
A-04 | G8 | /solutions/partners remains blocked on Terms §3 + IDD opinion (both in flight). Build it behind noindex + excluded from sitemap, copy per the approved pass-2 draft | Building ≠ publishing; the blocker is on publication | The legal answers landing — flip to indexed
A-05 | G1 | "grep finds no duplicate literals" read as "no parallel registry of the class", not "the string never recurs" — SEO titles and card labels legitimately restate line names | The failure mode being guarded is drift between registries, and the new join test + existing guards catch exactly that; zero-recurrence would outlaw ordinary prose | A stricter owner reading
A-06 | G2 | Brief's sand-700 `#9A6B21` claimed "AA on sand-50" but measures 4.17:1 there and 4.36:1 on the page ground — both under 4.5. Darkened to `#8D621E` (4.82 / 5.03 / 5.39-on-white), the minimal same-hue shift clearing every declared pair with headroom | The brief's own §4.2 rule ("verify AA for every text-on-surface pair") outranks its example value; hue and role unchanged | A design veto preferring a different sand — any replacement must clear the same generator gate
A-07 | G2 | Dark-theme semantic values are my mapping from the brief's role hints (green-900 ground, green-300 brand text); the brief specifies light fully, dark only by roles | Every dark pair passes the same measured floors, and dark is a real second theme per §2 | Art-direction review of the dark palette
A-08 | G3 | Lockup SVGs use <text> with the display font stack rather than outlined paths (no font-outlining toolchain in-repo) | On-site the header sets the wordmark in HTML where the webfont loads; standalone SVG consumers fall back down the stack legibly | Adding an outlining step to scripts/brand-assets.mjs
AUTH-01 | A1 | Brief's `packages/grafi/` does not exist; the registered token layer on this branch is `tokens/*.json` → `app/grafi.css` + `src/design-system/` | Same system, same tokens, styleguide-proofed; creating a parallel package would fork the system the brief forbids forking | A monorepo package split
AUTH-02 | all | `docs/briefs/b2c-rebuild.md` exists on no branch; `docs/voice.md` only on feat/grafi-b2c. This brief treated as self-contained; §2.5 copy used verbatim | The brief carries its own copy and non-negotiables; voice.md's analyst rules match the §2.5 copy it prescribes | Either doc landing on NEW-UI with conflicting lines
AUTH-03 | A3 | Cloudflare reference screenshots were named as attached but not provided | §2.1 specifies the layout numerically (55/45, 400px column, breakpoints); the pattern is unambiguous | The screenshots appearing with details that differ
AUTH-04 | A2 | Existing phone-only (synthetic-email) accounts: keep sign-in's phone tab + resolveAuthEmailIdentifier; no data migration | Standing decision: zero real users — the cohort is owner test accounts; resolver is read-only and cheap | A real phone-only cohort appearing before cutover
AUTH-05 | A5 | Google OAuth needs a Google-Cloud client + Supabase provider enablement — dashboard operations only the owner can perform. Everything code-side ships ready; provider flips live via config with no code change [verify] | The registry pattern the brief itself orders makes credentials the only missing piece | Owner supplying credentials → set `status: live`, enable in Supabase, smoke the round trip
AUTH-06 | A2 | "CSRF on the email form" satisfied by Next Server Actions' built-in Origin/Host binding + SameSite session cookies; no bespoke token added | Next 16 enforces origin checks on Server Action POSTs; adding a parallel token would duplicate the framework's mechanism | A security review requiring double-submit tokens anyway
AUTH-07 | A2 | "No route can create an unverifiable account" read as: every new account has a real, deliverable email (synthetic identities retired); the HARD gate blocking unverified sessions stays behind the existing `auth.enforce_email_verification` flag | Verifiability is restored for 100% of new accounts; flipping enforcement is a product call affecting existing accounts and belongs to the owner | Owner turning the flag on after cutover
AUTH-08 | A4/A7 | Two brief copy lines adjusted to standing repo rules: «Δεν αξιολογούμε συμβόλαια» → «…ασφαλιστήρια» (policy-term guard: the B2C word is «ασφαλιστήριο», never «συμβόλαιο») and the Google button in prod stays hidden until the owner flips NEXT_PUBLIC_AUTH_GOOGLE=live (a rendered button with no Supabase provider behind it would error on every click) | The guards enforce decisions the owner already ratified; the brief's meaning is intact | The owner overruling either rule
