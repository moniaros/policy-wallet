# ASSUMPTIONS — Grafí build

`A-nn | goal | assumption | why safe | what would change it`
A-01 | G0 | `PolicyWallet_Homepage_Premium.html` was named as attached but exists neither in the repo nor ~/Downloads | §6 spells out the target structure section-by-section, and §6 itself says "port its structure, not its raw CSS" — the structure list is the contract | The file appearing; then diff the build against it
A-02 | G2 | Brief says `pnpm tokens`; repo is npm (package-lock.json, no pnpm-lock) | Same script, different runner: `npm run tokens` | A repo-wide package-manager migration, which is not this brief's scope
A-03 | G10 | The standing owner decision held new guides behind the citation debt "repaired, or at minimum sized and scheduled". Debt is sized (G-07), one entry repaid (28 left), and this brief orders 6–8 guides. Read: proceed ONLY with guides whose every statutory claim carries a primary deep link at the SOURCES.md standard, so the debt cannot grow; the 28 stay scheduled | New guides at the higher standard shrink nothing but add nothing to the debt; the hold's purpose (don't dilute «Πηγές») is preserved | The owner restating the hold as absolute
A-04 | G8 | /solutions/partners remains blocked on Terms §3 + IDD opinion (both in flight). Build it behind noindex + excluded from sitemap, copy per the approved pass-2 draft | Building ≠ publishing; the blocker is on publication | The legal answers landing — flip to indexed
A-05 | G1 | "grep finds no duplicate literals" read as "no parallel registry of the class", not "the string never recurs" — SEO titles and card labels legitimately restate line names | The failure mode being guarded is drift between registries, and the new join test + existing guards catch exactly that; zero-recurrence would outlaw ordinary prose | A stricter owner reading
A-06 | G2 | Brief's sand-700 `#9A6B21` claimed "AA on sand-50" but measures 4.17:1 there and 4.36:1 on the page ground — both under 4.5. Darkened to `#8D621E` (4.82 / 5.03 / 5.39-on-white), the minimal same-hue shift clearing every declared pair with headroom | The brief's own §4.2 rule ("verify AA for every text-on-surface pair") outranks its example value; hue and role unchanged | A design veto preferring a different sand — any replacement must clear the same generator gate
A-07 | G2 | Dark-theme semantic values are my mapping from the brief's role hints (green-900 ground, green-300 brand text); the brief specifies light fully, dark only by roles | Every dark pair passes the same measured floors, and dark is a real second theme per §2 | Art-direction review of the dark palette
A-08 | G3 | Lockup SVGs use <text> with the display font stack rather than outlined paths (no font-outlining toolchain in-repo) | On-site the header sets the wordmark in HTML where the webfont loads; standalone SVG consumers fall back down the stack legibly | Adding an outlining step to scripts/brand-assets.mjs

# ASSUMPTIONS — Grafí application tier (B2C rebuild, 2026-08-30)

`A-nn | goal | assumption | why safe | what would change it`
A-09 | G2 | Grafí stays at `tokens/` + `src/design-system/` (+ a `@grafi/*` tsconfig alias) instead of the brief's `packages/grafi/` | the marketing imports keep working; a later extraction to a package is a path change, not a rewrite; the repo has no workspaces | a monorepo/workspace decision
A-10 | G2 | App radii are purpose-named roles (`radius-control 12 / card 16 / sheet 20 / hero 26`) beside the marketing size-named steps (9/14/22) | marketing pixels untouched; one JSON source; the brief's app values honoured exactly | a design veto to unify the two scales
A-11 | G1/G11 | The ledger («Τι έκανα για εσάς φέτος») is a projection over `BusinessEvent`; no `ledger_event` table | an append-only, `subjectUserId`-indexed store with a working read-side (`lib/services/timeline`) already exists; adding a second one is the drift the repo keeps paying for | events proving NOT to be emitted on the paths the ledger needs — then a table, not a hand-typed list
A-12 | G1/G5 | A finding's source pointer = document (always) + the section/field read + rule provenance (`ruleId`, `ruleInputs`); page/snippet only when an `ExtractionSource` exists; never invented | OPEN-3b forbids relaxing anchors; `coverages[]`/`exclusions[]` carry no page pointers today; a document + field IS a location the user can open and check | enabling + extending `EXTRACTION_CITATIONS` to coverage lines (a schema change and re-analysis of every policy) — marked [verify] for the compliance reader
A-13 | G1/G7 | On Free/Plus the verdict states plainly «Κενά κάλυψης: δεν τα έλεγξα — περιλαμβάνεται στο Family» instead of rendering a silent all-clear or an invented gap | plan-gating truth (`plan-gating-parity`) untouched; transparent monetisation over hidden intent; absence of a check never reads as reassurance | the owner making gap detection free
A-14 | G12 | The per-document Article 9 gate renders `common.aiConsentBody` verbatim under one key, marked [verify] (user decision 2026-08-30) | it is the product's reviewed in-product wording; no externally reviewed text exists (legal queue item 3); swap is one key | the DPO wording landing (legal-review-queue item 5)
A-15 | G12 | LifeEventChips carry the ten events of §8.10 mapped onto the life-event registry (adding `job_change`, `relocation`, `other`); the brief's eleventh is unknown (user decision 2026-08-30) | the strategy document is not in the repo; the registry is the engine's only vocabulary | the strategy document landing
A-16 | G7 | `/` is claimed for signed-in policyholders by a `proxy.ts` rewrite to `/home`; the static marketing homepage stays for anonymous visitors (user decision 2026-08-30) | zero change to SEO/ISR for anonymous traffic; the app URL matches the brief | an SEO/analytics review asking for a distinct app origin
A-17 | all | All work on `feat/grafi-b2c`; a PR at G14; never a push to `NEW-UI` (user decision 2026-08-30) | `deploy.yml` ships `NEW-UI` to production on CI-green | the owner merging the PR
A-18 | G8 | The per-policy «healthScore» donut is removed (D-B2C-01) | brief §3/§10; reversible from git | the owner restoring a policy-level score
A-19 | G13 | `FF_APP_*` feature flags default `false` until the prod DDL for the new tables is verified; the Article 9 gate fails closed when its table is absent | the write paths cannot degrade; a flag flip is a row, not a deploy | prod DDL verified → flip in the PR
A-20 | G3/G14 | The Safari device pass (iPhone SE / 15 Pro / iPad) is not performed here; emulated equivalents are run and the device pass is recorded as owed | no device access from this session | the owner running the pass
A-21 | G2 | Commissioner and Inter load through `next/font/google` (Next self-hosts and subsets at build) rather than vendored files | same-origin serving, Greek subset, automatic size-adjusted fallbacks; no binary font files in the repo | an offline-build requirement
A-22 | G0 | The worktree's dev server runs on :3100, not the brief's :3000 — a parallel session's `next-server` (main checkout, started 19:01) holds :3000 and is not mine to stop | every rendered check asserts the server's cwd is the worktree before trusting a measurement; Playwright takes `BASE_URL=http://localhost:3100` | :3000 freeing up
| A-23 | G7 | Links from the new screens to routes that only exist from a later goal (`/see`, `/add`, `/me/household`, `/life-event/[type]`) point at the route that serves the flow **today** (`/protection`, `/wallet/add`, `/account/profile`, `/protection#life-events`) and flip in the goal that builds the successor. | The dead-link guard (`no-dead-internal-links`) proves every link resolves at every commit; the registry already carries `successor` per entry (D-B2C-07), so each flip is a one-line change with a guard behind it. | The goal that ships the successor route — G8 for `/see`, G11 for `/me/household`, G12 for `/add` and `/life-event/*`. |
| A-24 | G7 | On the home screen, «τώρα» findings that are identical in kind, rule and asset (three schedules for one plate) fold into one row; the folded ones count in «Υπάρχουν και N μικρότερα» and stay individually listed on `/see`. | Nothing is hidden — the count is exact and the full list is one tap away; the three slots would otherwise all say the same sentence. | If the owner prefers one row per policy on the home screen (then `foldIdenticalRows` becomes `capNow`). |
| A-25 | G8 | The legacy components the three cut-over routes used to mount stay on disk, unreferenced by any route, until a dedicated cleanup commit; the disclosure guard lists them explicitly as retired so the entry goes stale (fails) the moment one is deleted or becomes reachable again. | No route renders them, so no user can reach the template cards or the uppercase labels; the guard entry is a ratchet, not an exemption. | The cleanup commit (after G14), which deletes them and rewrites the guards that pin their class strings. |
| A-26 | G8 | A policy whose number matches another policy's (same insurer) is the same contract seen through two documents and renders as one row with «N έγγραφα»; the row keeps the later end date and the worse state. | `renewal-chain.ts` already treats a re-upload as the same policy; the merge is display-only — no rows are written or deleted. | If the owner wants duplicates listed separately (then the merge step in `loadPoliciesModel` is removed and «Ίσως πληρώνετε δύο φορές» on /money remains the only place they meet). |
| A-27 | G9 | Offer rows on `/money` link to `/benefits` (the existing page that redeems them); discarded-upload failures fold on a per-day dedupe key, per-policy failures on the policy id. | `/benefits` already renders `getOffersForUser` with redemption; a day window folds a burst of blurry retries without hiding a genuinely new failure the next day. | An owner decision to give offers a Grafí surface of their own (G14 polish), or tariff data arriving for paid-twice amounts. |
| A-28 | G10 | «Τα άλλα N ασφαλιστήρια» in the help flow is a toggle CHIP in the what-will-be-sent list (off by default), keeping the sheet to exactly one Switch — the consent itself, as the done-criterion demands. | The chip is part of the payload description, so toggling it visibly changes what the sheet says will be sent; the consent stays a single unambiguous act. | Owner preference for a second switch instead of a chip. |

