# Current fixes and marketing review — 2026-09-12

Baseline: `b9255578`. The restored `PRODUCT-TRUTH-f6bb47e.md` audits a substantially older tree. Its historical findings must not be presented as new findings against this checkout. This session's request authorises remediation; its obsolete approval checkpoints do not block this work.

## Broken / insecure

- **Exposed service-account artifact:** retained the already-staged removal and existing all-index/all-working-file private-material guard, CI wiring and five synthetic probe tests (the `prebuild` hook was dropped on 2026-09-20 before deploying: it fails closed without a git index, which the Vercel build does not have). Guard and probes pass. The key was never used. Removal cannot revoke it or remove historical copies. Revocation, IAM/log review and deployment remain unverified. See `S0-SECURITY-INCIDENT.md`.
- **Identifying health fixture:** replaced names, email and policy number with synthetic values; updated dependent tests including equivalent punctuation variants. Reserved `.invalid` email; explicitly a shape fixture, not a source-document accuracy benchmark. Historical copies remain.
- **Hidden translation degradation:** `batchTranslateToEnglish` swallowed provider errors and junk answers before the orchestrator's catch, so its incomplete-translation state stayed unset. A callback now records `translationFailed` and the `translation` missing artifact while retaining successful English results and Greek fallback. The existing AnalysisCard warning consumes the persisted `missingSections`. Two mocked tests cover complete failure and partial junk; no model call or message sent.
- **Homepage signup mismatch:** both locale routes read the existing registration gate. Paused hero, homepage header and final action offer the anonymous needs check and explain invitations; the hero no longer collects an email while signup is unavailable. Open-state email capture remains. Four rendered unit cases cover both states/locales. Homepage revalidation is 30 seconds, matching the flag cache interval; not an instant cache-invalidation claim. Other public-page headers retain their existing signup links and lead to the registration notice.
- **Unsupported closing verdict:** replaced “find out whether you are still protected” with a document-reading and agent-question action in both languages.

## Older findings checked against current code

| Historical class | Current evidence / remaining limit |
|---|---|
| Consent and storage single paths | Existing ingestion/consent services and their guard tests pass; deployment configuration is unverified here. |
| Resource/recipient authorization | Existing policy single-path, questionnaire/task, relationship and collaboration tests pass in full suite. No authenticated production journey performed. |
| AI deciding gaps / missing provenance / replacement history | Current deterministic decision service, one gap writer, superseded-row readers and evidence provenance tests pass; old audit's missing-service premise is obsolete. |
| Missing default model prices / 1,000x divisor / tracking throws | Current `token-utils.ts` declares per-million values and both reported Gemini models; fallback and atomic tracking tests pass. This does not verify current vendor prices, historical cost rows or margins. |
| Batch upload bypass | Current ingestion service and batch persistence tests pass; no real document uploaded here. |
| Sent status despite failed dispatch | Weekly digest now uses the notification orchestrator; dispatch and digest tests pass with mocks. No sends authorised or performed. |
| Translation failure visibility | Still present; fixed above. Real-document English quality and report language coverage are not certified. |
| Claimed absent methodology route | `/methodology` now renders, 200 with one h1 in local review. |
| Accuracy / benchmark / profitability | No labelled corpus, invoice or deployment credentials supplied. No numerical quality, launch-readiness or margin certification. |

## UI/UX improvements (separate from launch gates)

Removed repeated WhyNow, MarketNumbers and static growth-hook sections from the homepage composition; their reusable modules and other consumers remain. Kept the product demonstration, explanation and glossary, process, audiences, independence, live pricing, contact, FAQ and one closing action. Moved the closing action after FAQs; reduced hero whitespace and aligned desktop content at the top. Removed unused imports and one hardcoded surface color, updating both shrink-only debt registers. Reviewed the small Greek-copy inventory delta before refreshing it.

## Verification and limits

Node 20.20.2. Baseline full unit suite: 651 files / 7,540 tests passed. Final test/check totals are recorded in the evidence result. Private-material probes: 5/5. API inventory: 106/106 routes, zero mismatches. Lint, i18n, UTF-8, encoding and type check pass.

Browser plugin reported no connected browsers. Local headless Chrome/Playwright reviewed 11 public routes at 390 and 1280 px: all 22 captures 200, one h1, no horizontal overflow. Final homepage checks cover 320/390/430/1280 in both languages and actually click through to the English needs-check heading. Screenshots were visually inspected. This is not an accessibility certification, complete link crawl or measured production performance result. Evidence: `docs/evidence/marketing-fixes-2026-09-12/`.

There is no `.env` or `.env.local` in this checkout. Local browser uses dummy auth/Supabase configuration and `ALLOW_REGISTRATIONS=NO`, so plan/partner data comes from available fallbacks. `verify:migrations` fails P1012 for missing `DIRECT_URL`; dev and prod state cannot be verified. Build checks use explicit local placeholder values; no production secrets were guessed or reused. No migration, database write, production deployment, account creation, message dispatch or history rewrite performed.

## Decisions taken

Use current code and tests instead of reimplementing obsolete audit findings. Preserve the existing credential-containment work. Make reversible UI/copy and fixture changes, and preserve the prior oversized status document in `docs/archive/STATUS-before-marketing-fixes-2026-09-12.md`. No claim that every historical backlog item or external security action is closed.
