# B2C_PROGRESS — Grafí application-tier rebuild ledger

Run started 2026-08-30 on `feat/grafi-b2c` (worktree `/private/tmp/pw-cutover/grafi-app`, base `NEW-UI@abb1d46f`).
One row per goal, written at the start and the end of each. Goals served = the seven strategy goals
(1 Understand · 2 Protect · 3 Prioritise · 4 Monitor · 5 Adapt · 6 Household · 7 Act).
Plan of record: the approved execution plan (2026-08-30); assumptions in `docs/ASSUMPTIONS.md` A-09+.

| id | goal | status | artefacts | goals served (1–7) | assumptions |
|---|---|---|---|---|---|
| G0 | Audit | **DONE** | `docs/audit.md` § G0 B2C: 14 findings re-verified on :3100 (2 WRONG/STALE, 4 PARTLY, rest CONFIRMED — six severity vocabularies, badge 22, 27 uppercase labels on /protection, zero landmarks there); route table + 301 map; model inventory; before-screens in `docs/screens/before/` | 1 | A-09…A-22 |
| G1 | Single sources | **DONE** | `lib/app/{state,finding,tier,verdict,money,household,badge,dismissal,streams,lifeEvents,entitlements,prevention}.ts` (pure, 5 test files / 41 tests); the specificity gate (`toRenderableFinding`) and `findingHash`; migration `20260830200000_grafi_app_tier` (Finding · HouseholdPerson · AdviserShareAudit · DocumentAiConsent) generated by `prisma migrate diff`, applied + SQL-verified on dev (4 tables, 8 FKs, 12 indexes), prod attempted and classifier-denied — handed over (D-B2C-04); DSR export + erasure wired for all four (guard `erasure-covers-personal-data` green, eraser test extended); events catalogue +3 aggregates +5 planned events (`finding.shown`, `benefit.surfaced`, `question.answered`, `advisor.help_requested`, `household.person_added`); life-event registry +`job_change`, `relocation`, `other` (inert, `mark_known`); stream map covers all 78 registry events incl. the 8 generated `conv_*` mirrors. Duplicate status literals (`severity-tone.ts`, `InsightCard` map, `getStatusColor*` consumers) are retired WITH their consumers in G8 — deleting them now would break the legacy /protection surface still serving. Gate: 521 files / 5,941 tests, lint, i18n, utf8, api-auth green | 1,2,3,6,7 | A-11, A-12, A-13, A-15, A-19 |
| G2 | Tokens (app tier) | **DONE** | `tokens/primitives.json` +7 colours (green-400, slate-350/300, rust-800/700/300/200), `alpha` tier (hairline `rgba(16,49,43,.12)`, scrim, blur), `shadow` (raised/overlay — flat is default), `ease.spring`, purpose-named radii (control 12 / card 16 / sheet 20 / hero 26), app type steps with weight+tracking (title-lg/title/heading/row/app-body…), rhythm 17→16px base and 26→32px section at 768; `tokens/semantic.json` +8 roles (fg-faint, fg-disabled, border-hair, surface-overlay, surface-blur, action-danger/-hover, action-secondary-bg) + 11 checks → **46 measured pairs all ≥ floor**, generator refuses a contrast check on a composited role and a text role with no check; `scripts/build-tokens.mjs` emits `--shadow-g-*`, `ease-g-out/-spring`, weights, the media switch; `app/grafi-app.css` = 5 named choreographies with reduced-motion final states; Commissioner (600/700/800, latin+greek) beside the one Inter in `app/layout.tsx`, 7 stray `app/auth/*` font instances removed, `--font-display` theme key; **fonts transferred on a Greek page: 116.7 KB / 120 KB budget** (4 preloaded subsets); `cn()` learns rounded-g-/shadow-g-/ease-g-; DeviceFrame's raw rgb → `shadow-g-overlay`; **raw-hex lint** `grafi-app-no-raw-hex.test.ts` (hex/rgb/hsl/oklch, universe from disk, probe proven red — it caught its own `\b` gap on `_rgb(`); legacy ratchet extended to `components/ui` (21 keys / 32 occurrences enumerated, shrink-only); `/styleguide` renders every sheet light AND dark side by side (`docs/screens/styleguide/`, 5 widths, no h-overflow, 0 console errors). Gate green: 523 files / 5,957 tests | 1 | A-09, A-10, A-21 |
| G3 | Shell | **DONE** | `src/design-system/shell/{Shell,LargeTitleNav,TabBar,SideRail,Sidebar,Fab,BrandMark,Badge}` — one component tree, three chromes on the token breakpoints `tablet:` 768 / `desk:` 1100 (`--breakpoint-*` emitted by the generator): five tabs + FAB on the phone (translucent `.g-chrome` with `@supports` solid fallback and `prefers-reduced-transparency`, `env(safe-area-inset-bottom)`), 76px icon rail, 248px labelled sidebar with user card + add-policy; the large title collapses into the bar on scroll and a sub-screen shows a back control in place of the mark; `lib/app/navigation.ts` is the ONE registry (five tabs in §7 order, sidebar adds Ενημερώσεις + Σύμβουλος; each entry carries today's `href`, its `successor` route and `aliases`, so a tab never points at a route that does not exist — the dead-link guard resolves every href on disk); the Grafí logomark replaces the lucide Shield; `app/(protected)/layout.tsx` mounts the Grafí shell for policyholders only (agent/admin keep `AppShell`); `/money` route created (interim: the honest premium-footprint sentence, nothing else — G9 completes it) and registered in `proxy.ts`, `SURFACES.md`, `LEDGER.md`, `count-keys`; `app` catalogue namespace opened (`lib/i18n/translations/app/{el,en}.ts`, typed mirror) with `formatPlural` (ICU `=0/one/other`, Greek number formatting); P1-08's drawer-scrim / safe-area / sub-44px items are moot for the policyholder shell (no drawer; every target ≥ 44). Verified on :3100 at 375/393/768/1100/1440 × light/dark on /dashboard, /wallet, /money: the right chrome at every width, one `<main>`, no h-overflow, no chrome target under 44px (`docs/screens/shell/`); unit: `grafi-shell.test.tsx`, `app-navigation.test.ts`, `i18n-plural.test.ts`. Gate green: 526 files / 5,977 tests, lint, i18n, utf8, api-auth | 1,3 | A-20 |
| G4 | Primitives + layout | **DONE** | `src/design-system/primitives.tsx` gains Button `danger` (an action role, never a status) + `IconButton` (name required), `Badge` (state tones only), `Avatar`, `FilterChip` (`aria-pressed`), `SearchField` (16px floor, `type=search`); `switch.tsx` (iOS 51×31/27, `role=switch`, pending/disabled, off-track ≥3:1 via border-strong); `segmented-control.tsx` (radiogroup, roving tabindex, arrow keys); `sheet.tsx` (native `<dialog>`: top layer, focus trap, Escape, focus return; bottom sheet < 768 / centred modal from tablet; `::backdrop` on the overlay role; `sheet-present`); `tooltip.tsx` (hover + focus, `aria-describedby`, hidden below `(hover:hover) and (pointer:fine)`); `app-layout.tsx` — `AppSection` (owns the 26/32 gap, a `section[id]` landmark), `GroupHeader` (the one permitted uppercase), `GroupedList` (0.5px hairlines), `Row` (icon · text · trailing, ≥64px, the ROW is the link/button), `Grid`; toast = sonner restyled on the roles, top-centre; `/styleguide` gallery renders every primitive in every state in BOTH themes (`docs/screens/styleguide/`). Verified on :3100 at 375/393/768/1100/1440: no overflow, no target < 44px, sheet opens and Escape closes, 0 console errors. Tests: `grafi-primitives.test.tsx` (13). Gate green: 527 files / 5,990 tests, lint 0, type-check | 1 | |
| G5 | Product components | **DONE** | `src/design-system/app/*` (the marketing `product.tsx` shadows a `product/` directory — hence `app/`): **VerdictCard** (three-state ring of COUNTS `22/30`, mood chip, verdict + reassurance, three filter tiles, `quiet` variant, full text alternative, `g-ring-arc` draw), **ActionRow** (whole-row link: verb-first sentence · source · trailing days/chevron) + **TierHeader** + **KindChip**, **FindingCard** (props typed as `RenderableFinding` — a value only the specificity gate can produce; sentence/source/why-you arrive resolved from the catalogue; why-you absent ⇒ absent, never a template; three actions; dismiss = Sheet with the three reasons, confirm inert until one is chosen), **MoneyTriad** (null figure ⇒ «—» + the honest note), **CoverageMap** (glyph + label per cell, «δεν έχετε»), **HouseholdStrip**, **LifeEventChips**, **CoverageChecklist** (every line cites), **QuestionList**, **ConsentSheet** (chips · one sentence rendered verbatim from its key · one switch · confirm inert until on), **Ledger** (lines only for what happened; honest empty), **PlatformNote** (`role=note`, product voice — the disclosure guard now recognises it beside `AiDisclaimer`, probe-proven), **ExpiryRail** (`.pw-scroll-strip`, scroll-snap). Catalogue: `app.{note,finding,verdict,moneyLine,coverageMap,household,checklist,questions,ledger}` in both locales, every count an ICU plural. Instrumentation keys registered (`verdict.*`, `money.*`, `finding.source`, `consent.sentence`, `ledger.*`). `/styleguide` renders every one with «ΔΕΙΓΜΑ» data in both themes (`docs/screens/styleguide/styleguide-product-*.png`): 0 `%` on the page, no target < 44px, dismiss sheet verified. Tests: `grafi-product.test.tsx` (13). Gate green: 528 files / 6,004 tests, lint 0, type-check | 1,2,3,4,5,6,7 | A-12 |
| G6 | Voice system | **DONE** | `docs/voice.md` (who speaks, the six rules, the three words, the three tiers, the canonical lines verbatim, the banned list, the string architecture); `lib/i18n/voice-lint.ts` + `tests/unit/voice-banned-words.test.ts` = `npm run lint:voice` (in CI's lint job and in the unit suite): **STRICT** over `app.*` except `app.adviser.*` — one hit fails; **RATCHET** over the legacy dictionary (`tests/fixtures/voice-baseline.json`: 39 keys / 47 hits, all agent/collaboration/retiring surfaces; grows ⇒ fail, shrinks ⇒ must be delisted); 15 stems, accent- and case-insensitive with Unicode lookarounds (`\b` is ASCII-only — the probe caught it), each proven to fire, and the canonical lines proven legal («τι να αγοράσετε» is not «αγοράστε»); **ICU plural guard** — a bare `{count}` anywhere in `app.*` fails; the typed EN mirror + `translation-parity-full` cover key parity; **client locale seed**: `LanguageProvider.adoptLanguage` + `components/app/LanguageSeed` mounted by the protected layout for every role — server and client strings agree on first paint on every device (unit-tested; no API round-trip). Uppercase legacy lifecycle labels (`el.ts policyStatus`) retire WITH their consumers in G8. Gate green: 530 files / 6,013 tests, lint 0, type-check | 1 | |
| G7 | `/` Η προστασία σας | **DONE** | `app/(protected)/home/{HomeScreen,page,loading}.tsx` (7 `<section id>`: verdict · now · money · life · map · household · note) · `lib/app/{compose,gap-copy,home-model,lines}.ts` (findings composed from `GapInstance` rows through the gate; 29 authored sentences EL/EN; 16 lines from `productCategories`) · proxy: `/` rewritten to `/home` for policyholder sessions (cookies carried), `/dashboard`+`/home` 301 → `/`, `getPostLoginRedirectByRole` → `/` · `components/app/MomentOfTruthBeacon.tsx` (`moment_of_truth.shown` once per finding per session) · §11 events typed in `types/journey-events.ts` · tests: `app-compose`, `home-section-budget` (≤7 on the CI path), proxy wiring (rewrite + 301s), `tests/grafi-app-screens.spec.ts` (five widths × two themes, fold at 393×852, 44px, no %, no uppercase) · captures `docs/screens/home/home-{393,1440}-{light,dark}.png` · rendered: fold 393×852 verdict bottom 579 / first row 802; 1440 345/549; 0 page errors both themes | 1, 2, 3, 6 | A-22, A-23, A-24 |
| G8 | `/see`, `/policies`, `/policies/[id]` | **DONE** | `app/(protected)/see/{page,SeeScreen,actions,loading}` (three tiers, FindingCard for now/month, ActionRow for later, dismiss sheet → `Finding` row behind `app.findings`, memory line, expired labels, note defining «κενό» + ordering ≠ risk; `?state=` from the verdict tiles) · `app/(protected)/policies/{page,PoliciesScreen,loading}` (SearchField, three lenses, GroupHeader/GroupedList/Row with the asset on the row, StatusChip trailing, «N έγγραφα» merge by policy number, «Έληξαν · N» collapsed, one title) · `app/(protected)/policies/[id]/{page,PolicyDetailScreen,loading}` (hero with state chip · verdict · lede via summary-language · facts · «Το πλήρωσα / Ρωτάτε / Ζητάω βοήθεια»; CoverageChecklist citing document+page or «δεν αναφέρεται»/«δεν εντοπίστηκε»; findings; typed questions; PolicyQA; DocumentsCard/AddDocumentCard; manage; note — 8 sections, 2 AI entries) · `lib/app/{see-model,policies-model,policy-detail-model,render-copy,flags}.ts`, `loadFindingsContext` shared by `/`, `/see`, `/policies` · flags `app.findings`, `app.household` (category `app`, default false) · proxy 301s `/protection→/see`, `/wallet→/policies`, `/wallet/[id]→/policies/[id]` (+ page-level `redirect()` stubs); registry flipped · guards: `policy-detail-section-budget` re-pointed (≤8, ≤2 AI), `app-screens-budget` (≤5/≤4), proxy wiring, ledger/surfaces rows, count-keys, live-status exemptions ratcheted · rendered: 393/1440 × light/dark, 0 page errors, 0 %, 0 uppercase, no sub-44px targets; captures `docs/screens/{see,policies,policy}/` | 1, 2, 3, 4, 6 | A-23, A-25, A-26 |
| G9 | `/money`, `/updates` | **DONE** | `app/(protected)/money/{page,MoneyScreen,loading}` (triad with the «έως = largest single limit» sentence; paid-twice pairs named, never an invented amount; benefits from `acordData.perksAndBenefits[]` + live plan offers → `/benefits`; ΕΝΦΙΑ guide link only when a live home policy exists; «Πού πάνε τα N €» by line via `selectPremiumBearingPolicies` + `lib/app/lines.ts`; not-counted line for unreadable premiums; note: never «κόψτε»/«αλλάξτε») · `app/(protected)/updates/{page,UpdatesScreen,actions,loading}` + `lib/app/{money-model,updates-model}.ts` (two stream groups from `lib/app/streams.ts`, grouped on the stored dedupeKey, every row naming its policy; «Τα είδα όλα» stamps EVERY unread row of the stream; a failed reading is one entry — `policy_analysis_failed` now emits a dedupeKey at both sites — worded as the analyst's limitation with a re-upload) · shell badge = protection-stream unread only (`badgeCount`) for policyholders · proxy 301 `/notifications` → `/updates` (agents keep the legacy page) · guards: `analysis-failed-dedupes` (probe-shaped greps), budget tests ≤5/≤2, wiring, surfaces/ledger rows, count-keys | 1, 2, 4, 5, 6 | A-27 |
| G10 | `/adviser` + help flow | **DONE** | `app/(protected)/adviser/{page,AdviserScreen,actions,loading}` (the customer's OWN adviser card or the invite flow — never a directory; per-policy Switch rows «το βλέπει από {date}» from `AccessGrant.grantedAt`, every flip writing `AdviserShareAudit`; threads with status; disconnect rides `terminateRelationship` and audits the all-scope revocation) · `app/(protected)/adviser/help/[hash]/*` (the finding arrives with the person, re-derived server-side from its hash; chips say exactly what is sent — the finding, its source document, the profile field; «τα άλλα N» a toggle chip OFF by default; ONE consent switch gates «Στείλτε το»; on send → grants + `AdviserShareAudit{help_sent}` + one collaboration thread + `advisor.help_requested` published (catalog flipped planned→live) + journey `help.requested`/`help.consented`) · legacy `revokeShare` now audits and revalidates `/policies` + `/adviser` (G0 found it revalidating `/wallet` twice) · proxy 301 `/agent`→`/adviser` (settings/pricing stay agent-owned); registry flipped · guards: `adviser-share-audit` (six assertions on the write sites), budget ≤4, one-switch help test, wiring, ledger/surfaces rows | 1, 2, 3, 7 | A-28 |
| G11 | `/me` + household + ledger | **DONE** | The whole `/account` tree moved to `/me` in one `git mv` (relative imports intact; `lib/settings/sections.ts` stays the ONE registry, gaining `household` + `appearance`; `/account/*` 301s at the proxy with the query carried + a `[[...rest]]` stub for the dead-link guard; every in-tree link and the weekly-digest email rewritten) · `/me` index = Grafí «Εσείς»: `lib/app/ledger.ts` (a BusinessEvent projection — groupBy name, this year, replays excluded; lines only for what happened; honest empty sentence; plan price beside it, no commentary; `plan.viewed_ledger` once per session) + household summary + the settings list from the registry · `/me/household` (+`/[personId]`): people with the three-state verdict matched on the documents' insured names — a dependant with no policy is «για έλεγχο», never «κενό»; add = the one-minute task (name · relation · one Switch) publishing `household.person_added` (catalog planned→live) behind `app.household`; CRUD already wired to DSR export/erasure (G1) · `/me/appearance`: theme + language, one control each · SettingsShell keeps exactly one `<h1>` · guards: settings-ia re-pointed and extended (household/appearance need pages+copy in both langs), `app-ledger` (projection unit test), MeScreen budget ≤4, wiring `/account→/me`, surfaces/ledger rows, dead links | 1, 2, 5, 6, 7 | A-29 |
| G12 | `/add`, `/life-event/[type]`, `/welcome` | **DONE** | `/add`: the Article 9 gate STANDS BEFORE the dropzone — `common.aiConsentBody` VERBATIM (decision 3; DPO ask = legal-review-queue item 5) + plain words + one switch; no upload UI until on (fail closed); the existing AddPolicyClient runs behind it; `createPolicy` writes one revocable `DocumentAiConsent` row per created document; the orchestrator enforces per-document consent at BOTH provider sites behind `app.document_consent` (revoked ⇒ blocked `document_consent_revoked`; table unreadable while the flag is on ⇒ blocked — never fail-open; no row ⇒ the account-level consent that was the recorded basis, A-30); per-document withdrawal on `/me/privacy` (upsert — a pre-feature document gets a row born revoked) · `/life-event/[type]`: the ten §8.10 chips only (404 otherwise); two questions (when; amount only where the engine needs one via `magnitudePrompt`); `declareLifeEvent` decides; «Ξαναείδα την προστασία σας» = the before/after of the SAME composition every screen reads — findings that moved to «τώρα» and new ones, never a suggestion; `life_event.recorded` · `/welcome`: three screens (what I do / what I never do / first policy), first run only (0 policies AND `firstRunDone(profile.preferences)` false — where policyholder onboarding has always lived; A-16 upheld, no new User column); completing stamps the preferences and lands on `/add` · `/wallet/add` 301 → `/add`; FAB + chips flipped · guards: `document-consent-gate` (verbatim-before-dropzone, write site, both orchestrator sites, revocation reachable), welcome/life-event budget tests, wiring | 1, 2, 3, 5, 6 | A-30 |
| G13 | Logic + regulatory gates | pending | §9 unit tests; §10 gate tests with probes; flags; analytics events | — | A-19 |
| G14 | Hostile review + handover | pending | three reader passes; a11y/perf/contrast/product-evolution/handover docs; PR | — | A-17, A-20 |

## Decisions taken (standing authority — CLAUDE.md)

- D-B2C-01 (G0): the per-policy «healthScore» donut (`components/wallet/policy-detail/SummaryCard.tsx`) is removed with the rebuild of `/policies/[id]`. No owner halt covers it (H-001 = portfolio score, H-005 = «Πόσο καλά σας γνωρίζουμε»); the brief's §3/§10 «never a score» rule is the basis. Reversible: the component and `PolicyHealthScore` stay in git history.
- D-B2C-02 (G1): the ledger («Τι έκανα για εσάς φέτος») is a projection over `BusinessEvent`; no `ledger_event` table (A-11). The five new catalogue names ship `planned` with a note each and flip to `live` in the goal that wires the emitter — a planned event nothing publishes is stated as such (`business-events.test.ts` pins both halves).
- D-B2C-03 (G1): `advisor.help_sent` was renamed `advisor.help_requested` before it existed — the catalogue forbids names ending in a channel-shaped verb (`sent|delivered|queued|read`), and the fact is the customer's request, not a delivery.
- D-B2C-04 (G1): prod DDL for `20260830200000_grafi_app_tier` was ATTEMPTED per the DEV-then-PROD rule (dev applied + SQL-verified first) and **denied by the Claude Code permission classifier** (Supabase MCP `apply_migration` against `cquudefwfwrmvpftuhyl`). Not worked around. Production is unchanged; nothing shipped references the tables; the `FF_APP_*` flags default off until the DDL lands. The exact SQL + `_prisma_migrations` INSERT (sha256 `c6f4904cc605a985c43862bf935795d5805e0ed7bff06fe0b4c1851475622096`) and the verification query are in `docs/handover.md` § "B2C — production migration owed".
- D-B2C-05 (G2): `body`/`h1–h6`/`code` typography defaults in `app/globals.css` were UNLAYERED and therefore outranked every Tailwind utility — `font-display` on a heading silently rendered Inter. Moved into `@layer base` (the only correct layer for element defaults). Verified: marketing `h1`/body unchanged (Inter), the six guards that read `globals.css` green. Reversible by removing the layer wrapper.
- D-B2C-06 (G2): fonts are self-hosted by `next/font/google` at build (A-21). Measured, not assumed: 116.7 KB transferred for both faces on a Greek page. If a later route measures over 120 KB the first lever is dropping Commissioner 600 (the display face uses 700/800 on every screen spec).
- D-B2C-07 (G3): the navigation registry points each tab at the route that SERVES the screen today (`/dashboard`, `/protection`, `/wallet`, `/money`, `/account`) and records its `successor`; each rebuild goal flips one entry when its route lands. Alternative rejected: interim stub pages at the new URLs — a stub is a screen that says nothing, and the dead-link guard would have been satisfied by exactly the kind of placeholder the brief forbids.
- D-B2C-08 (G3): `/money` ships in G3 with ONE figure — the premium footprint the wallet already computes from `IN_FORCE_KEYS` — and a sentence saying the rest is not checked yet. No «έως», no «δύο φορές» until G9 computes them from the engine's own rules.
- D-B2C-09 (G3): the safe-area behaviour is verified as WIRED (padding/`calc()` on the tab bar, FAB and header read `env(safe-area-inset-*)`, and `viewport-fit=cover` is already exported by the root layout) — desktop Chrome reports 0 for every inset, so the on-device check on iPhone SE / 15 Pro / iPad stays owed (A-20) and is listed in the handover.
- D-B2C-10 (G4): the Sheet is a native `<dialog>` rather than a hand-rolled overlay — the browser owns the top layer, focus trap and Escape; the one cost is jsdom's missing `showModal`, which the test polyfills. The legacy `Modal` (framer-motion, `useDialog`) stays for the surfaces it still serves.
- D-B2C-11 (G4): a destructive action is `action-danger` (a restrained brick, `rust-700`/`rust-300`) — the three-state rule «no red, no traffic light» is about STATUS; a delete button is not a status and must not borrow the gap colour.
- D-B2C-12 (G5): the app-tier product components live in `src/design-system/app/` — `src/design-system/product.tsx` (marketing) shadows any `product/` directory under Node resolution, and renaming the marketing file would touch the tier this rebuild leaves alone.
- D-B2C-13 (G5): `PlatformNote` is a SECOND recognised disclosure component beside the legacy `AiDisclaimer` (guard regex + probe), not a wrapper around it — the legacy note's typography (11px slate) is one of the §2 findings; the product-voice note says the same three things (AI read it, it may be wrong, not insurance advice — ask your adviser) in the app's own type steps.
- D-B2C-14 (G6): the banned-word rule is enforced by a vitest guard (`npm run lint:voice`) rather than a standalone script — the dictionaries are TypeScript modules the unit runner imports natively, and the unit suite is already the CI step that blocks. Two regimes, not one: strict for the analyst's own namespace, a shrink-only ratchet for the legacy dictionary whose agent-side «πρόταση» strings are the intermediary's legitimate words until their namespaces retire.
- D-B2C-15 (G6): «switch to annual billing» is a billing toggle, not advice — the English `switch to` stem is scoped to the insurer/policy sense so the lint bans what the brief bans and nothing else.
- **D-B2C-16** — Home «τώρα» rows fold identical findings (A-24); `/see` keeps every one.
- **D-B2C-17** — The plural formatter accepts one level of nested `{var}` inside a plural branch (the verdict sentence needs `{active}` inside `{covered, plural, …}`); the freeze re-run for `lib/app/lines.ts` is deliberate — those labels are the marketing catalogue's, read, not re-authored.
- **D-B2C-18** — `PlatformNote`'s «Σημείωση» label is sentence case; the only uppercase permitted in the app tier is `GroupHeader` (single short words), and the rendered gate asserts zero uppercase Greek inside sections.
- **D-B2C-19** — The legacy finding and wallet components (`RecommendationCards`, `CoverageInsightsClient`, `InsightCard`, `PolicyWallet*`, `PolicyDetailsClientView` + `policy-detail/*`, `ProtectionSurface`) are no longer mounted by any route after G8 but stay on disk this goal (A-25); their deletion — with the guards that pin them (`typography-scale`, `gap-severity-display-single-source`, `design-token-debt`, the disclosure graph) rewritten in the same commit — is a dedicated cleanup after G14's readers, so the ladder is not blocked on a 4,000-line removal.
- **D-B2C-20** — Group headers are sentence case («Αυτοκίνητο · 12»), per the brief's own example; the app tier renders no uppercase Greek and the rendered gate asserts it.
- **D-B2C-21** — `/policies/[id]` reuses `PolicyQA`, `DocumentsCard`, `AddDocumentCard` and `DeletePolicyDialog` as-is inside Grafí sections (only the preview button grew to a 44px target); restyling their internals is G14-polish, listed in the handover.
- **D-B2C-22** — The life-event cause on recommendations does not render on `/policies/[id]` in G8/G9 (its findings come from the gate, not RecommendationInstance rows); it returns with G12's re-check delta. The `life-timeline` guard was re-pointed accordingly.
- **D-B2C-23** — The quick-start questions moved to `/see` (they feed `answeredFields`, which is what makes a finding «yours»); `/welcome` (G12) stays the three-screen introduction, not a questionnaire.
- **D-B2C-24** — `/updates` marks a whole dedupe group read at once, and «Τα είδα όλα» stamps the stream's entire unread set — the badge is always clearable (closes the badge-22 defect from G0).
- **D-B2C-25** — The help URL carries the finding HASH (percent-encoded — it contains «|» separators), and the server re-derives the finding from the same composition /see renders; the client can never send content of its own making into the adviser thread.
- **D-B2C-26** — `/account` moved to `/me` for EVERY signed-in role, not only policyholders: `lib/settings/sections.ts` is one registry and agents follow it (their `agency` entry still points into `/agent/settings`). The 301 carries the query so legacy `?tab=` deep links keep working through the `/me` index.
- **D-B2C-27** — The plan's claim that `User.onboardingCompletedAt` exists was wrong (that column is AgentProfile's); policyholder first-run state lives in `PolicyholderProfile.preferences` JSON and always has. `/welcome` reads and writes it there (`lib/app/first-run.ts`) — A-16 (no new User column) upheld.
- **D-B2C-28** — `/api/policies/extract` turned out to be called only by the retired BatchUploadModal (A-25); the LIVE upload path is `createPolicy` → orchestrator, so the per-document gate enforcement lives in the orchestrator's two provider sites and the write in `createPolicy`. The extract route keeps its account-level check and dies with the legacy cleanup.

