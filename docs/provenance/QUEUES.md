# PW-PROVENANCE-01 — the queue, populated at series open (2026-09-07)

Populated from `docs/planning/DATA_INVENTORY_AND_PROVENANCE.md` (D1–D3, W0–W5) and
`docs/compliance/DATA_PROTECTION_REVIEW_PACK.md` §14. Order is **W4 → W0 → W1 → W2 → W5 → W3**,
then the register items — value per unit of risk, with the self-contained work first and the work
that depends on everything else last. Every item runs the gate in `LOOP.md` §5; any item touching a
stored shape runs the blast-radius protocol in §4 first.

Depth at open: **W 11 · R 3 · halted 5**.

Status is folded into the class cell as a bolded verb + date, per house convention.

## Queue W — the waves

| # | Item | Source | Class / state |
|---|---|---|---|
| W4-01 | Machine-readable column tags on `prisma/schema.prisma` — purpose, lawful basis, Art. 9 flag, retention class, export scope, erasure treatment — and a generator that emits the Art. 30 record from the schema | Plan W4 · §14.2 | Register close (ROPA) — **ACT done 2026-09-07**: 57 models tagged, `docs/compliance/ROPA.md` generated, guard `ropa-tags-complete` red-on-real-tree then green, local gate green (620 files / 7116 tests / build). **VERIFY done 2026-09-11**: merged #320 → NEW-UI `568fee72` (17:08Z), CI green on a re-run (one `area-detail-questions` flake, a file the PR does not touch), deploy run 34627851127 success 17:31Z, anonymous production smoke green, no new Sentry group — `docs/evidence/provenance-w4-01/RESULT.md` |
| W4-02 | DPIA **input pack** generated from the same tags — per-column purpose/basis map, the Art. 9 inventory, the transfer table | Plan W4 · §14.2 | Register close (partial) — **ACT done 2026-09-11**: `docs/compliance/DPIA-INPUTS.md` generated (714 columns across 57 stores classed Art. 9 / subject key / identifier / ordinary, the two Art. 9 routes, a 10-row transfer table from the published subprocessor rows with each AI client's constructor options read from code), guard `generated-compliance-docs-current` red-on-real-tree then green — it also closes the W4-01 gap that nothing in CI ran `--check`. **VERIFY done 2026-09-11**: merged #334 → NEW-UI `7f570f51` (18:03Z), CI green first attempt, deploy run 34631981624 success 18:15Z, anonymous production smoke green, no new Sentry group — `docs/evidence/provenance-w4-02/RESULT.md`. The assessment itself is `H-P2`; this produces only its inputs |
| W0-01 | **The `acordData` read-site guard.** Derive the field universe from `AcordDataSchema`; enumerate every `acordData` access path in `app/`, `lib/`, `components/`; fail on any path absent from the schema. Probe fixture proven red first | D1/D2 · user requirement | **BLOCKING PREREQUISITE — ACT done 2026-09-11**: guard `acord-data-read-sites` enumerates 424 read sites (76 distinct paths, 66 files of 1230 scanned) against the 188 schema paths + a 7-root envelope with named writers; red on the real tree (10 findings), then green after 5 reader fixes (a wrong insurer key, three wrong currency keys, four dead fallbacks) and 2 envelope roots. **VERIFY done 2026-09-11**: merged #335 → NEW-UI `d98bf51f` (18:54Z), CI green (one flake re-run on the PR, none post-merge), deploy run 34637008142 success 19:10Z, anonymous production smoke green, no new Sentry group — `docs/evidence/provenance-w0-01/RESULT.md`. Not followed, stated: values passed under another parameter name; typed aliases from helpers (tsc's job). **`AcordData` may now be touched, under §4** |
| W0-02 | Carry the probe's **per-page** text through the gate verdict into `ValidatedAIDocument` (today `PdfProbeResult.text` is a single joined string and is discarded; `toValidatedAIDocument(verdict, bytes, mime)` rebuilds from raw bytes) | Plan W0 · D1 | Keystone — **ACT done 2026-09-11**: the probe keeps each sampled page's RAW text (`pages[i]` = page `i+1`) beside the folded classifier text; `validateDocumentWithLocalText` returns `{ verdict, localText }` with the old entry point unchanged; `ValidatedAIDocument` carries `localText` optionally and all three constructor sites pass it (bulk extract, agent scan, and the deep pipeline via `readLocalText` on the stamped path); the stamp and the activity row provably carry no text; guard «no provider spreads the document» red-on-real-tree then green. **VERIFY done 2026-09-11**: merged #337 → NEW-UI `cb583bce` (19:38Z), CI green first attempt, deploy run 34641015833 success 19:54Z, anonymous production smoke green, no new Sentry group — `docs/evidence/provenance-w0-02/RESULT.md`. Needs per-page retention because W1-02 needs page numbers |
| W0-03 | Text-first extraction contract: text-native PDFs send extracted text, scans still send the image (`imageOnly` already detects them). Own page cap and time budget, separate from the classifier's 12 pages / 8 s | Plan W0 · §7 | Keystone — **ACT done 2026-09-11**: flag `extraction.text_first` (`EXTRACTION_TEXT_FIRST=1`, env-only, default OFF — the citations rollout dialect); `lib/services/ai/extraction-input.ts` decides text-or-file in one place (text only for a text-native PDF with a text layer ≥ 400 chars; scans, photos and thin layers still send the file, with the reason logged), renders page-marked text fenced as data, cut on a page boundary at 200k chars, and reports `truncated` whenever the model did not get the whole document; the extraction's own probe cap and budget (60 pages / 20 s) on the deep pipeline; all three providers build their parts through `extractionContentParts`; guard «every provider decides text-or-file through extractionContentParts» red-on-real-tree (all three by hand) then green. Flag OFF: every request byte-identical. **VERIFY done 2026-09-11**: merged #338 → NEW-UI `1c8d98c2` (21:20Z), CI green, deploy run 34649883589 success 21:36Z, anonymous production smoke green, no new Sentry group — `docs/evidence/provenance-w0-03/RESULT.md`. **Owner's switch:** `EXTRACTION_TEXT_FIRST=1` in Vercel, Gemini-first; watch the `extraction input` log line. Shrinks the transmitted payload from every page to a bounded extract |
| W0-04 | **Declare the enrichment envelope.** `extraction`, `analysis`, `processingError`, `renewalHistory`, `renewalReview`, `policyholder`, `insured` are written beside `AcordDataSchema` by named writers and read through `as any`; `policyholder`/`insured` carry the customer's name, email, phone and ΑΦΜ on 7 of 8 production rows with no schema, no confidence and no minimisation rule. A `StoredAcordDataSchema` (additive: the schema plus the envelope) lets W0-01 validate beneath those roots too | W0-01 finding · D2 · W5 | Keystone (partial) — **ACT done 2026-09-12**: `lib/schemas/acord-envelope.ts` declares `StoredAcordDataSchema` = `AcordDataSchema.extend(envelope)`, every field from the writers' literals and the `jsonb_object_keys` sets on both databases (2026-09-11); envelope objects strict, `AcordDataSchema` untouched; the read-site guard's universe is now the stored schema (the ENVELOPE map retired — 7 roots, ~40 more paths checked); guard `stored-acord-envelope` (writer literals vs schema, enrichment output parses, production/dev fixtures parse) with probes; red-on-real-tree by removing `reviewState` (6 reads flagged) then green. One finding fixed: `extractStoredClarity` read `coverageGaps` and `acordData` that the compact clarity never stores. **VERIFY done 2026-09-11**: merged #339 → NEW-UI `ef731076` (21:54Z) — the PR had no CI run while it conflicted, the ship-workflow trap; CI green after the merge push; deploy run 34652505736 success 22:10Z, anonymous production smoke green, no new Sentry group — `docs/evidence/provenance-w0-04/RESULT.md`. Order: after W0-02, before W5-01 (which decides what of `insured` survives) |
| W1-01 | Extend `CITATION_FIELDS` from the nine identity/date/premium fields to **the fields the rules read** (coverage booleans, limits, sums insured); enable `EXTRACTION_CITATIONS` staged, Gemini-first | Plan W1 · D2 | Provenance — **ACT done 2026-09-12**: `lib/gaps/rule-read-fields.ts` derives the 34 `acordData` paths the 50 active authored rules read (the engine's own walk: `field` / `fields[]` / the drift `referenceField` / the `low_limit` defaults); `CITATION_FIELDS` = the 9 identity fields + those 34, keyed `acordData.<path>` in the prompt, the response schema and the sanitizer; guard `citation-fields-cover-rule-reads` (every active rule's paths ⊂ the list, universe = the catalogue) red-on-real-tree (one path dropped → its rules named) then green. Enabling stays the owner's env switch (`EXTRACTION_CITATIONS=1`, Gemini-first) — and production already carries `extraction.sources` on 5 of 8 rows (SELECT 2026-09-11), so the flag appears to be on there; with it on, the next extraction cites the coverage fields too. **VERIFY done 2026-09-11**: merged #340 → NEW-UI `b2449342` (22:09Z); the post-merge CI build died with SIGSEGV in a runner and skipped the deploy — re-run green 22:24Z; deploy run 34653889271 success 22:28Z, anonymous production smoke green, no new Sentry group — `docs/evidence/provenance-w1-01/RESULT.md`. Changes the extraction contract on the money path; stage it |
| W1-02 | **Verify** each citation against the locally-extracted text — a snippet absent from the document is a hallucination detected deterministically, at zero model cost. Same move as `summaryLanguage` (detected, never assumed) | Plan W1 · D2 | Provenance — **ACT done 2026-09-12**: `verifyExtractionSources` in `extraction-citations.ts` checks every citation against `localText.pages` (W0-02) — folded like the lexicon, then letters-and-digits only when punctuation differs — and marks `verified: true` (on the cited page, or `verifiedPage` elsewhere) or `verified: false`; refutes ONLY within reach (the cited page inside the pages read, or the whole document read), otherwise leaves the entry unverifiable; the enrichment takes the local text as a fourth argument and all three providers pass the document's; the stored schema's `sources` entry gains the two fields; provider guard widened («enriches without the document's localText») red-on-real-tree then green. **VERIFY done 2026-09-11**: merged #341 → NEW-UI `691f8f24` (22:34Z); CI run 34654545606 success 22:43Z first attempt; deploy run 34655235612 success 22:47Z (`dpl_Fb7VgitU2g7q1YTLhgtfmkyCyC4u`); anonymous production smoke green 22:50–22:51Z; no new Sentry group; no production row carries `verified` until the next text-native analysis — `docs/evidence/provenance-w1-02/RESULT.md` |
| W2-01 | Split document evidence into three states a rule can see: `policy_verified` (cited and snippet found) · `policy_asserted` (uncited or unverified) · `policy_silent` (field absent) | Plan W2 | Honesty — **ACT done 2026-09-12**: `lib/gaps/document-evidence.ts` — `policy_verified` (a value whose citation was found in the text, W1-02) · `policy_asserted` (a value with no citation, a refuted one, or one we could not check) · `policy_silent` (no value; an empty array counts); every gap row the ONE writer writes now carries one state per field its rule read plus the weakest, computed at `writeRuleDecidedGaps` against each definition's own logic (the W1-01 walker) and persisted additively as `ruleInputs._evidence` (no reader reads `ruleInputs` by key — enumerated); `lib/gap-detection.ts` stays byte-identical — it is owner-frozen (D-V1, pinned by hash) and a first cut inside it failed the pin, so the evidence moved to the writer (D-P29); the protection ladder is untouched, where an asserted read ranks on it is W3-01's question (D-P27); writer-level assertion red-on-real-tree (persistence dropped → red) then green; the catalogue-enumerating guard («a rule firing a gap declares its evidence floor») is W2-02's. **VERIFY done 2026-09-11**: merged #342 → NEW-UI `2d920c95` (22:52Z); CI run 34655826157 success 23:03Z first attempt; deploy run 34656525974 success 23:07Z (`dpl_3Wukph74boAr5xVXrWnU81EjbQnx`); anonymous production smoke green 23:10Z; no new Sentry group; no production row carries `_evidence` until the next analysis run — `docs/evidence/provenance-w2-01/RESULT.md` |
| W2-02 | Evidence floor on `GapDefinition`: a rule firing a **gap** on explicit `false` requires `policy_verified`; on `policy_asserted` it degrades to **review**; on `policy_silent` it uses the `missing` wording («δεν έχει καταγραφεί», never «δεν καλύπτεται»). Guard enumerating the catalogue from disk | Plan W2 | Honesty — **ACT done 2026-09-12**: `lib/gaps/evidence-floor.ts` — the floor is a property of the DEFINITION, derived from what its logic asks (a `missing`/`all_missing` rule fires on silence → `policy_silent`; every other rule asks the document → `policy_verified`; `evidenceFloor` on the catalogue overrides, with a reason beside it); the verdict for a stored row — `gap` / `review` / `not_recorded` — follows from the floor and the evidence its run recorded (W2-01), rows written before W2-01 counting as asserted (P-H3: no grandfathering); the ONE accessor tags every live row with the verdict and a `classified` read keeps only `gap` rows — a finding below its floor takes the treatment under-review provenance already gets, disclosed and labelled («Ευρήματα χωρίς επιβεβαίωση από το έγγραφο»), counted nowhere, sent nowhere; /protection renders the group, the agent card folds it into the under-review figure; guard `evidence-floor-declared` enumerates the catalogue (50 floors printed, silence rules never worded as absence of cover) and the accessor's filter red-on-real-tree then green. **VERIFY done 2026-09-11**: merged #343 → NEW-UI `b791fce8` (23:13Z); CI run 34657171454 success 23:24Z first attempt; deploy run 34657928624 success 23:28Z (`dpl_6effKuZHKoVgtF1NBWUn5EzRQBSe`); anonymous production smoke green 23:29Z; no new Sentry group. **The demotion itself — every live finding to the disclosed group — was NOT observed signed-in (BL-C2); asserted on fixtures, stated for the owner** — `docs/evidence/provenance-w2-02/RESULT.md` |
| W5-01 | `vehicle.namedDrivers` → count plus the flags cover actually turns on (named-driver restriction, age band). **Additive**: add the new fields, dual-read, deprecate the names in a comment | Plan W5 · D3 · §10.2 | Minimisation — **ACT done 2026-09-12**: both driver arrays (`vehicle.namedDrivers`, legacy `motor.namedDrivers`) now carry `ageBand`, `yearsLicensed`, `relationshipToPolicyholder` only; `vehicle.namedDriverCount` and `vehicle.namedDriverRestriction` added; the model is asked for NO names and NO licence numbers. Additive, as §4 mandates (D-P33): the two keys STAY in both item schemas described `DEPRECATED`, and `schemaPromptBlock` now drops every DEPRECATED-described field from the JSON-mode prompt block — nothing new is collected, a legacy row keeps its type (0 of 8 production and 0 of 14 dev rows carry either array anyway; 0 of 50 rules read it). Removing the keys themselves is the owner's call. Guard `named-drivers-minimised` with probes, red-on-real-tree by un-deprecating `name`. **VERIFY done 2026-09-11**: merged #344 → NEW-UI `43d1f62b` (23:31Z); CI run 34658378064 success 23:42Z first attempt; deploy run 34659023427 success 23:45Z (`dpl_JBtAfLtgi93tfm5eqYG6j4KgRJfo`); anonymous production smoke green 23:47Z; no new Sentry group — `docs/evidence/provenance-w5-01/RESULT.md`. The read-site guard could not see the one reader (`MotorCoverageDetails.tsx` reads through the domain type); type-check and two fixture tests found it, now rendering relationship and band |
| W5-02 | `lifeAndInvestment.beneficiaries` → count and relationship class. The only rule reading it, `no_beneficiaries_recorded`, uses `all_missing` — a count satisfies it exactly | Plan W5 · D3 | Minimisation — **ACT done 2026-09-12**: `lifeAndInvestment.beneficiaries` (a list of NAMES) stays declared, described `DEPRECATED`, so the prompt block never asks for it (W5-01's strip); `lifeAndInvestment.beneficiaryCount` and `beneficiaryRelationships` added. The rules are UNCHANGED (D-P36): `all_missing` over both paths still has the live top-level `beneficiaries[]`, and a count of 0 would read as «recorded». The top-level `beneficiaries[].name` is NOT deprecated — the life card renders designations «as written» and the review nudge lists them, a product promise the loop may not withdraw (D-P35, HANDOFF P-H5). Guard `deprecated-fields-never-asked` enumerates the deprecated set from the schema (5 paths, asserted exactly) and checks the prompt block the model receives; probe; red-on-real-tree by un-deprecating the list. 0 rows on either database carry the path |
| W3-01 | Needs-against-cover comparison: cover from a cited document fact (W1), need from a Layer 1 profile fact with its own evidence level. First pair `deathBenefit` vs income × `incomeDependency` × dependants. Publishable only when **both** sides carry evidence; the finding states the weaker | Plan W3 | Real-data proposals — **queued last**. Depends on W1 and W2 |

## Queue R — register items outside the data model

| # | Item | Source | Class / state |
|---|---|---|---|
| R-01 | Two-factor authentication / passkeys. `PasskeyCredential` and `WebAuthnChallenge` tables exist and `@simplewebauthn` is installed, but **no API routes implement them** and the settings UI deliberately offers nothing (its comment: an empty device list is worse than absence) | §14.4 | Feature — **queued**. Add routes + UI; keep the settings comment's honesty rule |
| R-02 | PITR re-erasure job. After any production restore, completed `deletion_requests` with `completed_at` after the restore point must be re-executed (idempotent). Today a manual runbook step. Joins the 16 existing crons via `withJobRun` | §14.6 | Automation — **ACT done 2026-09-12**: `/api/v1/jobs/pitr-re-erasure`, daily 06:50 UTC via `withJobRun` (the 17th cron), inventoried with the retention sweep's policy. The operator sets `PITR_RESTORE_POINT` (ISO-8601) after a restore; every run re-executes `eraseUserData` for each `deletion_requests` row `completed` after that instant, records the outcome in `job_runs.summary` (`executed` with request ids / `already_done` / `no_restore_point`), keeps going past a failure and fails the run so it is visible, and skips a restore point an earlier failure-free run handled (D-P43). DSR runbook step 6 now names the job. Guard `pitr-re-erasure` enumerates `DeletionRequestStatus` from the schema; probe; red-on-real-tree by filtering on `failed`. Not exercised against a real restore (BL-P2): no restore has happened, and the job's day-to-day state on production is `no_restore_point` |
| R-03 | `docs/operations/RUNBOOK_PERSONAL_DATA_BREACH.md` + a drill with evidence, following the existing `RUNBOOK_*.md` and `docs/operations/evidence/` conventions. Art. 33/34, HDPA (ΑΠΔΠΧ), 72 hours | §14.7 | Runbook — **queued**. The commitment is published; no procedure exists |

## Halted at open — recorded once, never retried

Pre-recorded in `HALTS.md`. Listed here so the loop does not re-derive them:

| Id | Item | Why the loop may not decide it |
|---|---|---|
| H-P1 | §14.1 core — AI providers not regionally pinned, no zero-retention setting | Needs a GCP project and Vertex AI (different SDK and auth model), or account-level ZDR terms. Not a code change |
| H-P2 | §14.2 the DPIA itself | A legal assessment. W4-02 generates its inputs; writing it is counsel's |
| H-P3 | §14.3 agent-attested consent | A legal opinion on Art. 9(2)(a). `lib/ai-consent.ts` flags it pending counsel in its own header |
| H-P4 | §14.5 single-operator DSR | A staffing change — approver ≠ executor needs a second admin operator |
| H-P5 | §14.8 counsel items | Terms §3 qualification, the IDD / ν. 4583/2018 opinion, Art. 9 wording |

**Rejected by construction, not queued:** lowering `maxDuration: 336`; any change that renames or
removes an `AcordData` field; disabling a test to reach green; writing production `plans` rows;
following `docs/operations/DEPLOYMENT_GUIDE.md`.
