# PW-PROVENANCE-01 — progress ledger

Appended after every item (`LOOP.md` §2). Before/after figures are measured; **a figure without a
source is not written here**. Every guard is named and asserted demonstrated red before green.

| Item | State | Date | Before → after (measured) | Notes |
|---|---|---|---|---|
| W4-01 — schema Art. 30 tags + generated ROPA + guard | **done — merged #320 → `568fee72`, deploy run 34627851127, smoked 2026-09-11** | 2026-09-07 | models holding personal data carrying a recorded purpose and lawful basis: **0 → 57** (universe: all 87 models in `prisma/schema.prisma`, personal-data detector shared with `erasure-covers-personal-data.test.ts`) · Art. 30 record: **absent → generated**, 1 model declaring Art. 9 columns, 0 untagged · unit test files **613 → 620**, tests **→ 7116 passed** · merged head `6855dd0d`: **633 files / 7354 tests** (one local red, the `area-detail-questions` flake — 46/46 three times alone; CI on NEW-UI flaked on the same file once and SKIPPED the deploy until the failed jobs were re-run) · production: deployment `dpl_EFfouiQowngaK8mBoZ4A63eJN79m` on `568fee72`, 8 public routes 200/el, 3 auth routes 307, Playwright 2/2, 0 new Sentry groups (`docs/evidence/provenance-w4-01/`) | Tag is one `///` line per MODEL, not per field: Art. 30(1)(c) asks for categories, and ~1300 field tags would be filled in mechanically — a tag written to satisfy a guard is worse than none. `unclear` is a VALID value for purpose and basis (`D-P4`), so the guard can never force an invented legal basis; the generator prints those as open questions. Guard `tests/unit/ropa-tags-complete.test.ts` (11 tests, 5 probe fixtures) **demonstrated red on the real tree** — removing `PolicyholderProfile`'s tag failed 2 assertions — then green. Generator `scripts/generate-ropa.ts` has a `--check` mode that fails when the committed record is stale. Gate: audit:api-auth · lint · lint:i18n-changed (SCAN_ALL) · lint:utf8 · lint:encoding · type-check · 620 files/7116 tests · build — all green. Output: `docs/compliance/ROPA.md`. **2026-09-11, after four days of base drift (#331/#332/#333 merged and `origin/NEW-UI` merged into this branch):** the guard still passes unchanged and the record is still current — the base added `CustomerRelationship.unsharedCountDisclosed`, a new COLUMN on an already-tagged model, and a model-level tag does not go stale on a column. That is the `D-P6` choice validated by an event rather than by argument. Gate re-run on the merged head: **633 files / 7354 tests**, type-check clean, build clean. One local-only stumble worth recording: type-check first reported 4 errors on `unsharedCountDisclosed` and `lib/db.ts`'s `omit` — a stale generated Prisma client from before the base moved, fixed by `npx prisma generate`, not a code defect. |
| W4-02 — the DPIA input pack, generated + the currency guard | **done — merged #334 → `7f570f51`, deploy run 34631981624, smoked 2026-09-11** | 2026-09-11 | generated compliance documents whose currency CI enforces: **0 → 2** (`docs/compliance/ROPA.md`, `DPIA-INPUTS.md`; before, `--check` existed and nothing ran it) · columns mapped to a purpose and basis: **0 → 714** (universe: every scalar and enum column of the 57 tagged stores in `prisma/schema.prisma`, relations excluded; 7 classed Art. 9 on 1 store) · transfer rows: **0 → 10** (universe: the subprocessor rows `/subprocessors` renders, read from `lib/legal/legal-content.ts`) · AI clients whose constructor options the pack states from code: **0 → 3** (universe: `lib/services/ai/*-ai.service.ts` minus the mock; all three `apiKey` only) · unit test files **633 → 634**, tests **7354 → 7364 passed** | The pack is DERIVED; the only authored facts are two closed maps (`PROCESSOR_FEEDS`, `PROVIDER_FILES`) that THROW on a processor or provider they do not know, so a new subprocessor cannot be silently absent. Rendering moved from `scripts/generate-ropa.ts` into `lib/compliance/ropa-report.ts` so the guard can call it; the script is a thin CLI. Guard `tests/unit/generated-compliance-docs-current.test.ts` (10 tests, 6 probes) **demonstrated red on the real tree** — run before regeneration: 3 of 10 failed (stale record header, missing pack, probe schema) — then green. Two gate reds fixed in-item: `count-copy-agreement` could not render a pluralised count cell (reworded, no exemption) and the Greek copy inventory froze the strings moved into `lib/` (regenerated per its header, same commit). Gate: audit:api-auth · lint · lint:i18n-changed (SCAN_ALL) · lint:utf8 (3186 files) · lint:encoding · type-check · verify:migrations · 634 files/7364 tests · build — green. Local journey: not applicable, no runtime surface. Production: deployment `dpl_6kpUFipHcXbjJz58r3C7ELWgAdZ6` on `7f570f51`, 8 public routes 200/el, 3 auth routes 307, Playwright 2/2, 0 new Sentry groups — `docs/evidence/provenance-w4-02/RESULT.md`. |
| W0-01 — the `acordData` read-site guard (+ the five reader fixes it forced) | **done — merged #335 → `d98bf51f`, deploy run 34637008142, smoked 2026-09-11** | 2026-09-11 | `acordData` read sites checkable against the schema: **0 → 424** (universe: 1230 `.ts`/`.tsx` files under `app/`, `lib/`, `components/`; 66 files read it through 76 distinct paths; schema universe 188 paths derived from `AcordDataSchema`) · reads of a path the schema does not declare: **10 → 0** (16 findings on the first correct scan, 10 distinct after dedupe; 5 fixed as readers, 2 given envelope roots with a named writer) · keys written beside the schema and documented with their writer: **5 → 7** (`policyholder`, `insured` added — on **7 of 8** production rows, **1 of 14** dev rows with extraction data, by SELECT 2026-09-11) · production rows carrying `policy.premium.currency` / `policy.insurer` / `coverageType` / `planType` / `coverageSummary`: **0 / 0 / 0 / 0 / 0** of 8 (dev: 11 / 1 / 0 / 0 / 0 of 14 — an older pipeline) | The guard derives BOTH universes: Zod 4 walked for the schema, the source tree for the readers (comments and strings blanked with newlines kept; direct chains through casts and `?.`, one-hop aliases whose value IS the data, destructuring). Envelope roots are a closed map naming the writer file, and the guard checks the file still writes the key. Stated holes: a value passed under another parameter name; a typed alias from a helper's return (tsc's job). **Demonstrated red on the real tree** — 10 findings across 5 files — then green. What it found and what was done: the analysis tab read `policy.insurer` (never written; schema `insurerName`), so **every production policy showed an empty insurer tile** → fixed through `displayInsurerName`; three readers of `policy.premium.currency` where the schema says `policy.currency` (0 prod rows carry the former) → fixed; four dead fallbacks (`coverageType` ×2, `planType`, `coverageSummary` — 0 rows on either database) → deleted; `policyholder`/`insured` written by `extraction-enrichment.ts` on 7 of 8 production rows with no schema → envelope roots + new queue item **W0-04**. Render check `policy-analysis-tabs-reads-schema-paths.test.tsx` proves the tile now shows the insurer and «420 EUR». Gate: audit:api-auth · lint · lint:i18n-changed (SCAN_ALL) · lint:utf8 (3190 files) · lint:encoding · type-check · verify:migrations · **634 → 636 files, 7364 → 7381 tests** · build — green. Local journey: the rendered outcome is asserted by the render check; no route changed. Production: deployment `dpl_9j5taQBoTGjstXmzemiTxa37FMx1` on `d98bf51f`, 8 public routes 200/el, 3 auth routes 307, Playwright 2/2, 0 new Sentry groups; the signed-in tiles not smoked (BL-C2) — `docs/evidence/provenance-w0-01/RESULT.md`. |
| W0-02 — the probe's per-page text carried beside the verdict into `ValidatedAIDocument` | **done — merged #337 → `cb583bce`, deploy run 34641015833, smoked 2026-09-11** | 2026-09-11 | constructor sites that hand the extraction a local per-page copy of the document's text: **0 → 3 of 3** (universe: every `toValidatedAIDocument(` call in `app/`, `lib/` — bulk extract, agent scan, deep pipeline — the same universe `document-gate-before-model` enumerates) · pages the probe retains per PDF: **0 → up to 12** (`PROBE_SAMPLE_PAGES`; raw text, `pages[i]` = page `i+1`) · text on the stamped verdict: **0 → 0** (12 keys, pinned; a page sentence asserted absent from the verdict JSON and the activity metadata) · provider files that spread or serialise the document whole: **0 → 0**, now guarded (universe: 2 directories enumerated from the filesystem) | Additive at every layer: `text` (folded, the classifier's input) is byte-identical; the old gate entry point keeps its signature and delegates to `runGate` with a sink; `localText` is optional on the brand and null for a scan, a photo or any verdict but `validated`. The deep pipeline's stamped path never ran the probe in-request, so `readLocalText` re-probes (~100 ms) rather than persisting text at ingest — nothing stored changes. Guard «no provider spreads the document» (block in `document-gate-before-model.test.ts`, inline probe) **demonstrated red on the real tree** — a `...document` injected into `openai-ai.service.ts` failed it — then green. `pagedTextPdf` fixture added for per-page assertions. Gate: audit:api-auth · lint · lint:i18n-changed (SCAN_ALL) · lint:utf8 (3193 files) · lint:encoding · type-check · verify:migrations · **636 files, 7381 → 7390 tests** · build — green. Local journey: the pipeline outcome (pages on the validated document, nothing on the stamp) is asserted at the gate's own level; no route or page changed. Production: deployment `dpl_4dZfvcsyeuRuh1Kyu5vbwPENnfi2` on `cb583bce`, 8 public routes 200/el, 3 auth routes 307, Playwright 2/2, 0 new Sentry groups — `docs/evidence/provenance-w0-02/RESULT.md`. |
| W0-03 — text-first extraction contract, flag-gated | **done — merged #338 → `1c8d98c2`, deploy run 34649883589, smoked 2026-09-11; flag OFF until the owner sets it** | 2026-09-11 | providers that decide text-or-file in one place: **0 → 3 of 3** (universe: `lib/services/ai/*-ai.service.ts` minus the mock, enumerated and pinned) · with `EXTRACTION_TEXT_FIRST=1`, bytes of the file leaving the boundary for a text-native PDF: **the whole file → 0** (payload tests: no file part; page-marked text instead; Gemini's gap call still attaches the file) · text sent: bounded at **200,000 chars**, cut on a page boundary, from up to **60 pages / 20 s** on the deep pipeline (the gate's 12-page sample on the upload paths) · with the flag off: requests **byte-identical** (payload tests unchanged and green) · flags in the registry: **9 → 10** · unit test files **636 → 637**, tests **7390 → 7408 passed** | Default OFF, env-only — the rollout dialect `extraction.citations` established, because this changes the extraction contract on the money path and the owner turns it on (Gemini-first) with a Vercel variable, no deploy. When ON the file is still sent for a photo, a scan (no local text) or a text layer under 400 non-space characters, and the reason is logged; `truncated` is reported both when the character cap cut pages and when the probe sampled fewer pages than the document has, so a partial read never looks whole. The page text is fenced as DATA (`<document_text>`, forged delimiters stripped) the way extracted values already are. Guard «every provider decides text-or-file through extractionContentParts» **demonstrated red on the real tree** — all three providers reported before wiring — then green; the file-name guard caught a bare `filename:` type annotation in the new module on the first gate run (fixed by deriving the type from the one builder that calls `providerDocumentFileName`). Gate: audit:api-auth · lint · lint:i18n-changed (SCAN_ALL) · lint:utf8 · lint:encoding · type-check · verify:migrations · unit suite · build — green. Local journey: not run against a live model — no provider key is used in tests; the request shape is asserted on Gemini and OpenAI with the SDK mocked. Production: deployment `dpl_7zcyyrPj9ZKeTP2Q7EiunAu2ReNs` on `1c8d98c2`, 8 public routes 200/el, 3 auth routes 307, Playwright 2/2, 0 new Sentry groups; `vercel env ls` shows no `EXTRACTION_*` name — the flag is unset or unreadable, either way off — `docs/evidence/provenance-w0-03/RESULT.md`. |
| W1-01 — citation fields cover the fields the rules read | **done — merged #340 → `b2449342`, deploy run 34653889271, smoked 2026-09-11** | 2026-09-12 | fields a rule reads that the extraction is asked to cite: **0 → 34 of 34** (universe: every path the 50 active authored definitions read, walked as the engine walks them — `field`, `fields[]`, the drift `referenceField`, the `low_limit` defaults) · citation fields in total: **9 → 43** (the nine identity fields first and unchanged) · rules whose every read path is citable: **0 → 50 of 50** · consumers that agree on the key form `acordData.<path>`: prompt section, response-schema description, sanitizer — **3 of 3** | The list is DERIVED from the catalogue (`lib/gaps/rule-read-fields.ts`, dependency-free so the prompt builder never imports the database client), so a rule authored tomorrow that reads a new field extends the citations by construction, and the guard fails if it ever does not. Keys are `acordData.<path>` — what the model already returns for `extractionSources`, unambiguous beside the identity keys, and any string is legal under the stored schema's `sources` record (W0-04). Enabling is unchanged: env-only `EXTRACTION_CITATIONS=1`, the owner's switch, Gemini-first — and production already carries `extraction.sources` on 5 of 8 rows (SELECT 2026-09-11), so the flag appears to be on there and the next extraction will cite the coverage fields too. Guard `citation-fields-cover-rule-reads` (7 tests, walker probes for every rule shape) **demonstrated red on the real tree** — one path filtered out → `no_roadside_assistance` and `moto_no_roadside_assistance` named — then green. The first gate run turned the read-site guard red on a PHANTOM read — the citations prompt nests a template literal inside an interpolation and the guard's blanker copied interpolation text verbatim — fixed in the guard (recursive blanking, probe added). Gate: audit:api-auth · lint · lint:i18n-changed (SCAN_ALL) · lint:utf8 (3197 files) · lint:encoding · type-check · verify:migrations · **637 → 638 files, 7408 → 7416 tests** · build — green. Local journey: no live model call (no key in tests); the prompt text and schema description are asserted, and the mock provider's flag-on fixture is unchanged. Production: deployment `dpl_DwMiMZrhkTsF5JETPJbAABE8QC8E` on `b2449342`, 8 public routes 200/el, 3 auth routes 307, Playwright 2/2, 0 new Sentry groups — `docs/evidence/provenance-w1-01/RESULT.md`. |
| W0-04 — the stored `acordData` schema declares the envelope | **done — merged #339 → `ef731076`, deploy run 34652505736, smoked 2026-09-11** | 2026-09-12 | envelope roots with a declared schema: **0 → 7** (`extraction`, `analysis`, `processingError`, `renewalHistory`, `renewalReview`, `policyholder`, `insured`; universe: every key written beside `AcordDataSchema` by the three writer files, and every key `jsonb_object_keys` returned under those roots on production and dev, 2026-09-11) · read sites beneath those roots now checked against a schema: **0 → 38** (15 distinct chains; before, the read-site guard waved every one through) · schema paths in the read-site universe: **188 → 237** · reads of a key the writer never stores: **2 → 0** (`extractStoredClarity` read `coverageGaps` and `acordData` from the compact clarity, which stores neither; both were always `undefined` — made explicit) · `AcordDataSchema` keys: **26 → 26** (untouched; the model contract has not grown) | Additive at every layer (§4): a NEW schema `StoredAcordDataSchema = AcordDataSchema.extend(envelope)`, the extraction schema untouched, the envelope objects STRICT so a writer that starts setting an undeclared key fails `stored-acord-envelope` — and fixtures in the exact key sets both databases hold (production's 11 extraction keys, dev's `confirmedBy`/`confirmedByUserId`, a legacy `renewalHistory` entry) prove the rows already there still parse: the schema cannot narrow past the data. The clarity summary's six members are declared present but typed `unknown` — the clarity model contract owns their shape and no read site reaches inside them. Guard `stored-acord-envelope` (11 tests, 3 probes): enrichment output parses; writer literals (brace-matched from source, `key:` or the ES6 shorthand) set only declared keys; production and dev fixtures parse. The widened read-site guard **demonstrated red on the real tree** — `reviewState` removed → 6 direct reads flagged — then green; a first attempt with `summaryLanguage` stayed green because its reader continues the chain on the next line after a cast, which the parser now follows (probe added). The alias tracker also gained an ambiguity rule: a name declared twice in a file is not followed (the orchestrator's `clarity` is both the stored summary and a live model result). Gate: audit:api-auth · lint · lint:i18n-changed (SCAN_ALL) · lint:utf8 (3194 files) · lint:encoding · type-check · verify:migrations · **636 → 637 files, 7390 → 7406 tests** · build — green. Local journey: not applicable — no route, page or provider behaviour changed. Production: deployment `dpl_GdyPSwPANpFYBpUFwim4zMJYMCUZ` on `ef731076`, 8 public routes 200/el, 3 auth routes 307, Playwright 2/2, 0 new Sentry groups — `docs/evidence/provenance-w0-04/RESULT.md`. |
| W1-02 — citations verified against the document's own text | **done — merged #341 → `691f8f24`, deploy run 34655235612, smoked 2026-09-11** | 2026-09-12 | stored citations that state whether the document contains their snippet: **0 → every citation on a text-native PDF extraction** (universe: every entry of a sanitised `extractionSources`, checked against every page the probe read — 12 on the upload paths, up to 60 on the deep pipeline) · providers that verify before storing: **0 → 3 of 3** (guard, red-on-real-tree) · what the verifier may refute: **only within reach** — the cited page inside the pages read, or the whole document read; a scan, a photo or a page beyond the sample is left unverifiable, never refuted | Two comparisons, both deterministic: the page and the snippet folded exactly as the lexicon folds them (`normalizeDocumentText` — tonos, case, final sigma, whitespace), then letters-and-digits only, because a model that quotes «420,00 €» as «420 00 €» has not hallucinated. `verified` is a third state beside true/false: absent means «could not check», and the distinction is what W2-01 needs (`policy_verified` / `policy_asserted` / `policy_silent`). The enrichment's fourth argument keeps the verifier out of the model modules; the providers pass the document's own `localText` (W0-02). Two optional fields on the stored schema's `sources` entry (W0-04's strict envelope now admits them; the enrichment's output still parses). Guard: the provider block of `document-gate-before-model` widened — «enriches without the document's localText» — **demonstrated red on the real tree** (OpenAI's call unwired → reported) then green; `extraction-citations-verified` covers every branch and the round trip. Gate: audit:api-auth · lint · lint:i18n-changed (SCAN_ALL) · lint:utf8 (3201 files) · lint:encoding · type-check · verify:migrations · **638 → 640 files, 7416 → 7441 tests** · build — green. Local journey: no live model call; the verifier is exercised on a two-page Greek fixture. Evidence `docs/evidence/provenance-w1-02/` on VERIFY. Stacked on W1-01 (#340). |
| W2-01 — document evidence in three states a rule can see | **done — merged #342 → `2d920c95`, deploy run 34656525974, smoked 2026-09-11** | 2026-09-12 | gap rows that record what the document was evidence of, per field read: **0 → every row written after this deploy** (universe: every row `writeRuleDecidedGaps` writes with the document in hand; persisted as `ruleInputs._evidence`, one state per path the W1-01 walker names plus `lowest`) · states: **1 → 3** (`policy_verified` split into verified / asserted / silent) · readers of `ruleInputs` that read it by key: **0** (enumerated — the orchestrator passes it through, the writer stores it; the additive key breaks nothing, §4) · protection-ladder levels: **6 → 6** (untouched) | The document side gets its own three-state vocabulary beside the person's ladder, dependency-free; where `policy_asserted` ranks against `user_reported` is deliberately not decided here (D-P27). Silence follows the engine's own `isAbsent` reading (`[]` counts) so a rule's «missing» and the evidence's «silent» never disagree. Computed at the ONE writer of gap rows, not in `decideGapsForPolicy`: a first cut inside `lib/gap-detection.ts` failed the owner-ratified frozen-file pin (D-V1), which says «have the change ratified, then re-pin» — so the engine stays byte-identical and the writer, which already sets a row's status and will apply W2-02's floor, computes the evidence against each definition's own logic in the same transaction (D-P29). Persistence rides inside `ruleInputs` under one reserved key rather than a new column (D-P28): additive, no migration, no reader affected. Writer-level assertion **demonstrated red on the real tree** (the `_evidence` line dropped → red) then green; probes for every state and for a prose-only definition. Gate: audit:api-auth · lint · lint:i18n-changed (SCAN_ALL) · lint:utf8 (3202 files) · lint:encoding · type-check · verify:migrations · **640 → 641 files, 7441 → 7452 tests** · build — green, the frozen-file pin included. Local journey: not applicable — no route or page changed; the next analysis run on production writes the first `_evidence`. Evidence `docs/evidence/provenance-w2-01/` on VERIFY. Stacked on W1-02. |
| W2-02 — the evidence floor on a definition; a finding below it is disclosed, never published | **done — merged #343 → `b791fce8`, deploy run 34657928624, smoked 2026-09-11 (signed-in demotion not observed)** | 2026-09-12 | active definitions with a resolved evidence floor: **0 → 50 of 50** (universe: `AUTHORED_GAP_DEFINITIONS`, `isActive`; derived by operator class, explicit `evidenceFloor` overrides — none declared today) · readers that publish a finding below its floor: **every classified read → 0** (universe: the 17 `readLiveGapRows`/`countLiveGapRows` call sites, all through the ONE accessor, whose `classified` scope now requires `evidence === 'gap'`) · surfaces that disclose a below-floor finding with its reason: **0 → 1** (/protection: the «Ευρήματα χωρίς επιβεβαίωση από το έγγραφο» group; the agent card folds the count into its under-review figure) · rows on production carrying `_evidence`: **0 today** → every live finding written before W2-01 reads as `policy_asserted` and demotes gap → review on this deploy — **the visible consequence the queue row named**, and the P-H3 stance (no grandfathering) applied | The floor is derived, not typed per row: a `missing`/`all_missing` rule fires ON silence by design and its wording already says «not recorded», so its floor is `policy_silent`; every other rule claims what the document SAYS and needs `policy_verified` (D-P30). A finding below its floor takes the treatment `under_review` provenance already gets — disclosed, labelled, counted nowhere — through the ONE accessor rather than a new gap status, so no reader had to change and none can forget (D-P31): `withSlug` now also selects `ruleInputs`, `tag` adds the verdict, `classified` filters on it. Rows written before W2-01 carry no `_evidence` and are treated as asserted (D-P32, the HANDOFF P-H3 stance): after this deploy every live production finding demotes to review until the next analysis run writes evidence for it — and with the citations flag on and W1-01/W1-02 live, that run can verify. Guard `evidence-floor-declared` (11 tests) **demonstrated red on the real tree** — the accessor's evidence filter dropped → the classified case failed — then green. Gate: audit:api-auth · lint · lint:i18n-changed (SCAN_ALL) · lint:utf8 (3204 files) · lint:encoding · type-check · verify:migrations · **641 → 642 files, 7452 → 7463 tests** · build — green (the surface-ledger test went red once on the new required prop; made optional with a default, the honest shape for callers that render no such group). Local journey: not run on `npm start` — the protection page's render is asserted by the surface ledger test (unchanged, still green with the new optional group) and the accessor's behaviour by the guard; a signed-in production look at /protection is the honest check, stated in the evidence. Evidence `docs/evidence/provenance-w2-02/` on VERIFY. Stacked on W2-01. |
| W5-01 — named drivers counted and characterised, never named | **done — merged #344 → `43d1f62b`, deploy run 34659023427, smoked 2026-09-11** | 2026-09-12 | third-party personal-data fields the model is ASKED for per named driver: **2 → 0** on each of the 2 driver arrays (`name`, `licenseNumber`; universe: the item keys of `vehicle.namedDrivers` and `motor.namedDrivers` read from the Zod definition minus those described DEPRECATED, and the rendered JSON-mode prompt block, which now drops DEPRECATED fields). Keys declared in the stored shape: unchanged (§4 additive: 2 kept, deprecated; 3 bands + 2 vehicle-level fields added). Stored rows affected: **0** — 0 of 8 production and 0 of 14 dev `acord_data` rows carry either array (SELECT 2026-09-11). Rules reading the array: **0 of 50** (`RULE_READ_FIELDS`). Readers: **1** typed card + 1 test fixture, both moved to relationship/band | Gate: 641/641 files, 7446/7446 tests, build green. Guard `named-drivers-minimised` demonstrated red before green (3 of 5 cases red with a `name` re-added; after the additive rework, 2 of 6 red with `name` un-deprecated). §4 reader count: 1 typed reader (the motor card) + 1 fixture, both moved. The read-site guard (W0-01) was blind to this reader because it reads `motor.namedDrivers` through the `AcordData` domain type, not a literal `acordData.` chain — recorded, not fixed here. The amount-completeness guard flagged the new head count as an unrendered number; exempted as «not a coverage amount» (D-P34) |
| W5-02 — the beneficiary name list deprecated; count and relationship class added | **done (local gate green)** | 2026-09-12 | schema fields the model is asked for that hold a beneficiary's NAME under `lifeAndInvestment`: **1 → 0** (universe: the prompt block's property paths, ~150+, walked from the wire JSON schema); deprecated paths declared in `AcordDataSchema`, enumerated by the guard: **4 → 5**, asserted exactly. Stored rows carrying `lifeAndInvestment.beneficiaries`: **0 of 8** production, **0 of 14** dev (SELECT 2026-09-11). Rules reading the path: unchanged (`all_missing` over `beneficiaries` + `lifeAndInvestment.beneficiaries`), `lib/gaps/authored-catalogue.ts` untouched, so the catalogue fingerprint cannot have moved. §4 reader count: 1 typed reader (`life_check_beneficiaries`), unaffected — additive | Gate green. Guard `deprecated-fields-never-asked` demonstrated red before green. The top-level `beneficiaries[].name` (rendered «as written» on the life card) is out of scope — a product promise, handed to the owner as P-H5 (D-P35) |
| R-01 — passkeys as a second factor (routes, step-up gate, settings) | **done (local gate green)** | 2026-09-12 | API routes implementing the passkey tables: **0 → 6** (universe: `app/api/auth/passkeys/**/route.ts`, all in `scripts/api-route-policy-inventory.json`, audit 0 findings); protected route patterns behind the second factor: **0 → 18 of 18** (universe: `ROUTE_OWNERSHIP` in `proxy.ts`, each driven through the real `proxy()` with and without the proof cookie); forgeries the step-up proof refuses: **5 of 5** (wrong user, expired, tampered expiry/nonce/signature, malformed shape, another secret). Production: **0** users with a passkey and the flag **unset** — nothing is enforced on this deploy | Gate green. Guard `proxy-step-up-gate` demonstrated red before green (the gate line disabled → 19 of 23 cases red: every pattern and the forgery case). Two premise guards updated, not skipped: `no-fake-biometric-auth-claim` now asserts the WebAuthn flows are EXACTLY the two second-factor surfaces and no login screen carries one; `settings-ia` asserts the passkeys block renders only behind the flag. A second factor over a Supabase session, not a Supabase MFA factor (D-P40); off by default with the flag as the break-glass (D-P41); the proof is a Web-Crypto HMAC cookie, not a session claim (D-P42). NOT exercised end to end with a real authenticator — the ceremony's cryptography is the library's, and this machine has no browser session (BL-C2); the owner's first enrolment is the live test, with the flag as the way back |
| R-02 — PITR re-erasure as a daily job | **done (local gate green)** | 2026-09-12 | post-restore re-erasure steps that are automated: **0 → 1** (universe: the DSR runbook's numbered controls, step 6, previously «manual, not alerted» per review pack §14.6); deletion-request statuses the job may re-execute: **1 of 7** (`completed`; universe: the `DeletionRequestStatus` enum read from `prisma/schema.prisma`, every other value asserted outside the query); crons recorded through `withJobRun`: **16 → 17** (universe: `vercel.json` `crons[]`). Production: `PITR_RESTORE_POINT` **unset** — every run will record `no_restore_point` until a restore happens | Gate green. Guard `pitr-re-erasure` demonstrated red before green (the status constant switched to `failed` → the universe case and the probe red). The restore point is an operator-set env var, not detected (D-P43). NOT exercised against a real restore — none has happened; the eraser's idempotence is the runbook's existing claim, re-used, not re-proved here |

## Decisions taken under standing authority

Agent choices, each with the recipe that reverses it. Owner-ratified decisions live in
`DECISIONS.md`, not here.

| Date | Decision | Why | Reversal |
|---|---|---|---|
| 2026-09-07 | **Queue order W4 → W0 → W1 → W2 → W5 → W3.** W4 first though it is not the largest finding. | W4 touches no runtime path, is self-contained, and closes a High register item (§14.2 Art. 30). W3 last because it depends on every wave before it. | Re-order `QUEUES.md`; no code depends on the order. |
| 2026-09-11 | **D-P6 — the Art. 30 tag stays at MODEL level, now confirmed against a real base change.** Recorded as a decision because the four-day drift tested it: a new column on a tagged model required no ledger or guard change. | Field-level tags would have forced a tag edit for `CustomerRelationship.unsharedCountDisclosed` and for every future column — ~1300 tags maintained by hand, which is how a record goes stale or gets filled in mechanically. Art. 30(1)(c) asks for categories, not a column census. | Move `@ropa` onto fields in `lib/compliance/ropa-tags.ts` and widen `modelsRequiringTags` to enumerate columns; the guard then fails until every column carries a tag. |
| 2026-09-07 | **D-P4 — `unclear` is a first-class value in the ROPA vocabulary** for `purpose` and `basis`, and passes the guard. | A guard that only goes green once all 57 stores claim a settled lawful basis manufactures confident answers to open legal questions and emits them into a document a regulator may read. Unknown is not absence. The generated record prints `unclear` rows under "Open questions" instead. | Remove `unclear` from `ROPA_BASES`/`ROPA_PURPOSES` in `lib/compliance/ropa-tags.ts`; the guard then forces every store to claim a basis. |
| 2026-09-07 | **D-P5 — the ROPA guard reuses the erasure guard's personal-data detector verbatim** (User `@relation` by shape, plus the seven plain subject columns) rather than defining its own. | Two guards disagreeing about what counts as personal data would be worse than either being wrong: a store could be erasure-covered but ROPA-invisible, or the reverse, and both would read green. | Give `modelsRequiringTags` its own detector in `lib/compliance/ropa-tags.ts`; the two guards then diverge by design and each needs its own exemption list. |
| 2026-09-07 | **W0-01 (the `acordData` read-site guard) is a blocking prerequisite** for every item touching `AcordData`, ahead of the keystone W0-02. | 198 files reference `acordData`, ~120 outside tests, all through `(x as any)?.field` against a Prisma `Json?` column — `tsc` catches nothing. Shipping a shape change before the guard is the documented "green gate, broken production" seam. | Drop the prerequisite in `QUEUES.md` if the guard proves unbuildable; then every W item carries a manual enumeration in its PROGRESS row instead. |
| 2026-09-11 | **D-P7 — the transfer table's «Stores under those purposes» column names coverage, not delivery.** Upstash and Sentry carry no purpose (`[]`) and a «How» cell states what actually reaches them (per-IP counters; scrubbed error events); the column header says the stores are the ones the purpose covers, not rows that reach the processor. | The first render listed seven security-purpose stores under Upstash, which reads as «session rows reach the rate limiter» — a claim the code does not support and the published list denies. A pack that over-claims a transfer is as wrong as one that omits it. | Set `purposes` for those two rows in `PROCESSOR_FEEDS` (`lib/compliance/ropa-report.ts`) and rename the column back; regenerate. |
| 2026-09-11 | **D-P8 — currency of the generated documents is enforced by a unit test, not by the script's `--check`.** `generate-ropa.ts --check` stays as the on-request form; `generated-compliance-docs-current.test.ts` runs the same comparison on every CI run. | W4-01's `--check` was wired to nothing: neither `ci.yml` nor any test invoked it, so the record could drift with a green gate. A guard that has to be remembered is not a guard. | Delete the test and add `npx tsx scripts/generate-ropa.ts --check` as a CI step instead; same comparison, one more workflow line to maintain. |
| 2026-09-11 | **D-P9 — the guard pins the §14.1 fact: every live AI client is constructed with `apiKey` and nothing else.** A client gaining a `baseURL`/`region` turns the case red on purpose. | The endpoint question (halt H-P1) is the register's top item; a pin means the day it changes, the pack, the halt and the review pack are updated in the same PR rather than discovered later. Red here is good news, and the test says so in its own comment. | Drop the last case in the probe block of `generated-compliance-docs-current.test.ts`; the pack still reports the options, it just stops insisting on them. |
| 2026-09-11 | **D-P10 — keys written beside the schema are an ENVELOPE with a named writer, not schema fields added in this item.** `extraction`, `analysis`, `processingError`, `renewalHistory`, `renewalReview`, `policyholder`, `insured` are read through `as any` and validated no further; the guard checks each writer still writes its key. | W0-01 is the prerequisite for touching `AcordData`; extending the schema inside the prerequisite would be the change it exists to protect. The envelope is honest about what is unchecked and files the schema work as W0-04. | Delete the ENVELOPE map in `tests/unit/acord-data-read-sites.test.ts` once W0-04 declares those roots; the guard then validates beneath them. |
| 2026-09-11 | **D-P11 — a reader of an undeclared path is fixed, deleted or exempted by ROW COUNTS on both databases, never by reading the code alone.** Fixed when the schema declares the intended field and production carries it (`insurerName`, `policy.currency`); deleted when no row on either database carries the key; envelope when a named writer writes it. | The guard says only «the schema does not declare this»; whether the data does is a question for the data. `policy.premium.currency` sits on 11 dev rows and 0 production rows — reading the code would have kept it as a live fallback. | Restore any deleted fallback from this commit; the guard will demand a reason for it. |
| 2026-09-11 | **D-P12 — the guard owns its stripper instead of importing the authorization guard's lexer.** | The shared lexer drops the newlines inside block comments, and this guard's whole output is `file:line`; importing a test file also re-registers its 20 cases in every importer (`admin-reads-are-audited` runs 33 tests for 13 of its own). | Import `stripCommentsAndStrings` from `policy-authorization-single-path.test.ts` and delete `blankCommentsAndStrings`; the line probe then fails. |
| 2026-09-11 | **D-P13 — `pages` carries RAW text; `text` stays folded.** The probe keeps both: the lexicon's folded join, unchanged, and each page's raw text with case and accents. | The folded form (lowercase, tonos stripped, final sigma folded) is right for matching a lexicon and wrong for everything W0-03/W1 need: a model extracting from folded Greek loses information, and a citation snippet can never match a folded page. Two consumers, two forms, one read. | Drop `pages` and derive per-page text from `text` — impossible without page boundaries, which is the point; or fold `pages` too and accept that W1's citation check compares folded strings. |
| 2026-09-11 | **D-P14 — the local text travels BESIDE the verdict, never on it, and only for `validated`.** `validateDocumentWithLocalText` returns `{ verdict, localText }`; the verdict's key set is pinned. | `DocumentValidationResult` is stamped into `validation_json` («No text, no filename») and logged; a field on it would persist page text on every document row and in the activity log, and would be a stored-shape change under §4. A refused document's text carried anywhere would be data kept about a file we refused. | Add `localText` to `DocumentValidationResult` and drop the sink; the stamp then stores text and the gate test's key pin fails, by design. |
| 2026-09-11 | **D-P15 — the deep pipeline re-probes on the stamped path instead of persisting the text at ingest.** `readLocalText(buffer, mime)` in `prepareDocument`, ~100 ms per run. | Persisting the pages at ingest would add a column or a JSON field holding document text — a stored shape, a retention question and a DSR-export question — to save 100 ms on a step that already fetches the bytes and base64s the whole file. Re-reading the bytes we already hold is free of all three. | Add a `local_text` column to `policy_documents` written at ingest and read in `prepareDocument`; then §4, a migration on both databases, ROPA tag update and erasure coverage for the new column. |
| 2026-09-11 | **D-P16 — text-first ships OFF, env-only (`EXTRACTION_TEXT_FIRST=1`), like citations.** The loop does not turn it on. | It changes what the extraction model receives on every analysis — the money path — and extraction quality on real Greek schedules has not been measured with text input. The owner flips one Vercel variable, Gemini-first, and can flip it back with no deploy. | Set `defaultValue: true` in `lib/flags/registry.ts` and make `extractionTextFirstEnabled` read the flag with the DB layer (drop `envOnly`); then the console can toggle it. |
| 2026-09-11 | **D-P17 — the file is still sent when text cannot stand in for it, with the reason logged**: not a PDF, no local text (a scan), or a text layer under 400 non-space characters. | A scan has no text; a thin layer (a header, a stray OCR line) is not the document, and sending it as if it were would make the model extract from nothing while the file sat unread. Each `reason` is a fact in the provider log, so the split between text and file runs is measurable before anyone widens it. | Lower or remove `EXTRACTION_MIN_TEXT_CHARS`; the `too_thin` branch then never fires. |
| 2026-09-11 | **D-P18 — the deep pipeline reads 60 pages / 20 s for extraction; the upload quick paths keep the gate's 12-page sample.** | The deep pipeline is where the whole file went and where the full document matters; it fetches the bytes anyway, so the wider probe is ~200 ms. The quick paths pre-fill a form from identity fields that live on the first pages and already had no page beyond what the gate read. Re-probing there would double the work of every upload for fields the sample already covers. | Call `readLocalText(bytes, mime, EXTRACTION_PROBE)` in `app/api/policies/extract/route.ts` and `app/(protected)/agent/actions.ts` instead of taking the gate's sample. |
| 2026-09-12 | **D-P22 — the citation list is derived from the catalogue, not authored beside it.** `RULE_READ_FIELDS` is computed from `AUTHORED_GAP_DEFINITIONS` at import; `CITATION_FIELDS` is identity + that. | Two lists that must agree and are kept by hand disagree — the register's D2 asymmetry is exactly that, one rule at a time. A derived list cannot drift, and the guard says so if the derivation ever misses a rule shape. | Replace `RULE_READ_FIELDS` with a literal array in `extraction-citations.ts`; the guard then does the agreeing for you, one red at a time. |
| 2026-09-12 | **D-P23 — a rule-read citation is keyed `acordData.<path>`, the identity ones stay bare.** | The model already returns `extractionSources` as a flat record; a bare `insuredValue` would be ambiguous across sections (`vehicle.insuredValue`, `property.insuredValue`) and a nested record would change the shape every reader (`policy-review.ts`, the policy page) already handles. The dotted key is a string like any other under the stored schema's `sources` record. | Nest instead: `extractionSources.acordData.<section>.<field>`; then `sanitizeExtractionSources`, the two readers and the W0-04 schema all change. |
| 2026-09-12 | **D-P24 — enabling stays the owner's switch; the item widens what the switch does.** The loop sets no Vercel variable. | `EXTRACTION_CITATIONS` is env-only by design (citations change the extraction contract on the money path), and the agent cannot read or set the production environment reliably (`vercel env ls` listed nothing; `env pull` redacts). The evidence that the flag is already on in production is the data: 5 of 8 rows carry `extraction.sources`. Widening the field set under an already-on flag is the staged rollout the queue asks for, Gemini-first because Gemini is the primary. | Gate the widened set behind its own variable (`EXTRACTION_CITATIONS_RULE_FIELDS`) if the first extractions after this deploy show the model citing the coverage fields badly; the identity nine would then keep citing while the rest waits. |
| 2026-09-12 | **D-P19 — the envelope objects are STRICT; the extraction schema stays as it was.** `z.strictObject` for every envelope root, `AcordDataSchema.extend` for the composition. | A loose envelope would declare nothing: any key would pass, and `policyholder`/`insured` reached 7 of 8 production rows precisely because nothing refused an undeclared key. Strict is what makes a writer's new key visible — and fixtures in both databases' real key sets prove it does not refuse a row that exists. | Replace `z.strictObject` with `z.object` in `lib/schemas/acord-envelope.ts`; the «undeclared key fails» probe then fails, by design. |
| 2026-09-12 | **D-P20 — the clarity summary's members are declared `unknown`, not modelled.** `generatedAt`, `plainLanguageSummary`, `coverageSnapshot`, `savingsOpportunities`, `checklistScores`, `priorityActions` are present-or-absent, nothing more. | Their shapes are the clarity model's output contract, owned by `extraction-schema.ts`/the clarity schema; copying them here would be a second source of truth for a shape this item does not change, and no read site reaches inside them today. Declare what is read; leave what is not. | Import the clarity response schema and use it for `AnalysisClaritySchema`'s members when a reader needs a path inside them. |
| 2026-09-12 | **D-P21 — legacy keys the databases hold are declared from the data, not dropped.** `confirmedBy`, `confirmedByUserId` (dev), and the older `renewalHistory` entry shape (`endDate`, `source`; dev) are in the schema with their provenance in a comment. | The schema describes rows that exist; a schema that fails on a row nobody can regenerate is a guard against the past. The SELECT is the evidence, the fixture is its record, and a future migration that removes the legacy shape removes the field with it. | Delete the legacy fields and the dev fixture rows that carry them once the rows are migrated or gone; the fixture test is where that shows up. |
| 2026-09-12 | **D-P25 — the verifier refutes only within its reach.** `verified: false` needs the cited page inside the pages read, or the whole document read; otherwise the entry is left without a verdict. | The upload paths read 12 pages and the deep pipeline 60; a snippet the model took from page 30 of a 40-page booklet is not a hallucination because we stopped at 12. A false refutation would demote a true finding to `policy_asserted` in W2 — the exact dishonesty the series exists to remove, in the other direction. | Drop the `readFarEnough` condition in `verifyExtractionSources`; every miss then reads as `false`, and the probe «cited beyond the pages read → untouched» fails. |
| 2026-09-12 | **D-P26 — two comparisons, folded then letters-only; no fuzzy matching.** | Folding removes the differences that are never evidence of anything (tonos, case, spacing); stripping punctuation removes the ones a model introduces when it re-types a figure. Anything looser — edit distance, token overlap — would start verifying quotes the document does not contain, which is the failure the item exists to detect. A miss under both comparisons is a miss. | Add a third comparison in `pageContains`; the probe «nowhere in a fully-read document is refuted» is where a too-loose one shows up. |
| 2026-09-12 | **D-P27 — the document's three states live beside the protection ladder, not on it.** `lib/protection/evidence.ts` is unchanged; `document-evidence.ts` is its own vocabulary. | The ladder ranks a person's statements against a policy's for the protection surfaces, and seven test files and the Greek label set pin it. Where an unverified model read ranks against «what the person said» is a judgement W3-01 (needs against cover) has to make with both sides in view — making it here, for a rule engine that does not need the rank, would be a claim smuggled in. | Add `policy_asserted` to `EVIDENCE_LEVELS` (between `user_reported` and `policy_verified`, or below), map the three states onto it, and give the level its Greek and English labels. |
| 2026-09-12 | **D-P28 — evidence is persisted inside `ruleInputs` under `_evidence`, not in a new column.** | A column is a migration on both databases, a ROPA tag check and an erasure-coverage check, to store a value nothing yet reads back; the JSON key is additive under §4, no reader reads `ruleInputs` by key (enumerated), and W2-02 uses the in-memory `evidence` at write time. The column is the right home once a surface renders the state. | Add `evidence_lowest`/`evidence` columns to `gap_instances` (migration dev → verify → prod → verify, `_prisma_migrations` stamped), write them in `gap-instance-writer.ts`, and stop nesting the key. |
| 2026-09-12 | **D-P29 — `lib/gap-detection.ts` stays byte-identical; the evidence is computed at the writer.** | The file is owner-frozen: `gap-severity-display-single-source` pins its sha256 under DECISIONS D-V1 («detection and severity logic untouched») and says in its own words never to re-pin without ratification. A W2-01 that edited the engine would either break the pin or re-pin it unratified — both are the loop deciding what §3 says it may not. `writeRuleDecidedGaps` is the ONE writer of gap rows, already sets `status`, and is where W2-02's floor will act; computing the evidence there against the definition's logic (one `findMany` by id inside the same transaction) keeps detection exactly where the owner froze it. | Once the owner ratifies an engine change (DECISIONS.md) and the pin is re-established, move `documentEvidenceForLogic` into `decideGapsForPolicy` and drop the writer's lookup. |
| 2026-09-12 | **D-P30 — the evidence floor is derived from the rule's operator class, with an explicit override on the definition.** | Fifty hand-typed floors would be filled in mechanically and drift from the logic they describe; the derivation states the only fact that matters — does this rule fire on what the document says, or on its silence? — and the guard prints every resolved floor so a reviewer reads them as declared. `evidenceFloor` exists for the day a rule needs something else, with a reason beside it. | Add `evidenceFloor` to each of the 50 definitions and make `evidenceFloorFor` refuse a definition without one; the guard then fails on omission instead of deriving. |
| 2026-09-12 | **D-P31 — a finding below its floor is «review» by TREATMENT, not by a new status.** The accessor's `classified` scope requires `evidence === 'gap'`; `disclosed` returns the row with its verdict; no `GapInstance.status` value was added. | A new status would have to be added to `LIVE_GAP_STATUSES`, `OPEN_GAP_STATUSES` and the 12 files that read them, and every reader would have to remember it; the accessor already exists so that no reader decides B0/B3 for itself, and this is the same decision on a third axis. The under-review disclosure machinery (heading, disclosure line, «not in this summary») is reused with its own copy. | Add `review` to the status vocabulary, write it at the writer when `lowest < floor`, and drop the accessor's evidence filter; then update the two status sets and their readers. |
| 2026-09-12 | **D-P32 — rows written before W2-01 are treated as asserted, not grandfathered.** Every live production finding demotes to review on this deploy until a run writes evidence for it. | HANDOFF P-H3 records the stance taken at the series' open: grandfathering is dishonest and was rejected. The loop applies it rather than re-opening it. The cost is visible and temporary: the next analysis run on each policy writes `_evidence`, and with citations on and W1-02 verifying, the confirmed findings return as gaps. | Treat a missing `_evidence` as `policy_verified` in `lowestEvidenceOf` — one line — and record it in DECISIONS.md as the owner overturning P-H3. |
| 2026-09-12 | **D-P33 — the name keys stay, DEPRECATED; the prompt block drops DEPRECATED fields.** A first cut removed `name`/`licenseNumber` from both item schemas outright, on the evidence that no row, no rule and one card used them. It was reverted the same night: LOOP.md §4 says «Additive only. Never rename, never remove, never narrow» and marks itself MANDATORY, and the queue row itself prescribed the additive route. | The protocol is the owner's, and it exists for exactly the seam this item sits on. Minimisation is achieved where it matters — what the model is ASKED — by a 20-line generic strip in `schemaPromptBlock` keyed on a `DEPRECATED` description prefix, reusable for W5-02. The stored shape keeps its keys; nothing is narrowed. | Owner: ratify removal of the two keys (0 stored rows carry them) and delete them plus the `DEPRECATED_NAME` descriptions; the guard's «keys still declared» assertion is the one line to drop. |
| 2026-09-12 | **D-P34 — the card renders relationship and band; the head count is exempted from the amount-completeness guard.** | `namedDriverCount` is a number, so `coverage-panel-completeness` demanded it be rendered or reasoned away. It is a head count, not a coverage amount, and the card already lists the drivers one per row — rendering «2» beside two rows is a duplicate fact (`data-fact` rule). | Render `vehicle.namedDriverCount` in the card and delete the `INTENTIONALLY_NOT_IN_PANEL` entry. |
| 2026-09-12 | **D-P35 — the top-level `beneficiaries[].name` is not deprecated.** W5-02 as queued names only `lifeAndInvestment.beneficiaries`, the string list D3 lists; the object array beside it is what the life card renders «as written», what the review nudge («Ελέγξτε αν οι δικαιούχοι είναι ενημερωμένοι») lists, and what `/product/life` promises. | Withdrawing a published product promise is not minimisation the loop may do on its own; D3's argument (no rule reads the name) is true of this field too, so the question is real and belongs to the owner. | One line: describe `beneficiaries[].name` DEPRECATED; the guard's expected set gains the path; the card and nudge then render relationship and share only. |
| 2026-09-12 | **D-P36 — the beneficiary rules keep their fields; the count is not wired into `all_missing`.** The queue row said «a count satisfies it exactly». | It does not, in this engine: `all_missing` treats a present number as recorded, so a model writing `beneficiaryCount: 0` would silence `no_beneficiaries_recorded` — the opposite of the intent. The rules still read the live top-level array, which a life schedule fills, so nothing is lost by leaving them. A count-aware check is «a rule plus an operator» (CLAUDE.md), not a field swap. | Add a `count_zero`-style operator to `lib/gap-detection.ts` (owner-frozen — ratify first), then add `lifeAndInvestment.beneficiaryCount` to the four `*_no_beneficiaries_recorded` rules and re-align both catalogues. |
| 2026-09-12 | **D-P40 — passkeys are a SECOND factor over the Supabase session, enforced in the proxy on a server-only claim.** Not a Supabase MFA factor: GoTrue's own MFA is TOTP, and a WebAuthn factor it does not verify cannot raise the session's assurance level. | The tables and the library were already chosen (§14.4); what was missing was where to enforce. The proxy already sees every request and the user's `app_metadata` on each `getUser()`, so the enrolment writes the claim there and the proxy needs no database round trip to know a passkey is required. | Replace with Supabase TOTP MFA (`auth.mfa.*`) and an `aal2` check in the proxy; delete `lib/auth/step-up.ts` and the claim. |
| 2026-09-12 | **D-P41 — off by default, env-only (`PASSKEYS_ENABLED`), no recovery codes; the flag is the break-glass.** | The owner is the only user and cannot be asked mid-run. An owner who enrols on production and loses the authenticator would be locked out of every protected page; unsetting the flag and redeploying restores access without touching data, and the claim stays in place for when it is set again. Recovery codes would be a second secret to store, render and erase for a case one env var already covers. | Add recovery codes (a hashed-codes table, a «use a code» arm on /auth/step-up) and default the flag on; the gate and the token do not change. |
| 2026-09-12 | **D-P42 — the proof is a 12-hour HMAC cookie bound to the Supabase user id, signed with Web Crypto.** | A Supabase session cannot carry a custom assurance level, and a database check per request in the proxy would put the pool in the path of every page. The cookie is the OAuth-intent shape the codebase already trusts, with the user id inside the signed payload so a cookie copied to another account fails, and Web Crypto so the same function verifies in the proxy and in the route that issues it. Deleting the cookie asks for the passkey again — it is not a bypass. | Shorten `STEP_UP_TTL_SECONDS`, or bind the token to the Supabase session id as well (`session.access_token` hash) so a sign-out invalidates it. |
| 2026-09-12 | **D-P43 — the restore point is told to the job (`PITR_RESTORE_POINT`), never detected; a point handled by a failure-free run is skipped on later days.** | Nothing in the database says «a restore happened» — a restored database looks like any other. Detecting one (a heartbeat row that goes backwards) would be a second mechanism to trust; the operator who ran the restore knows the instant and already follows the runbook. The skip keeps the variable harmless when it stays set; a NEW restore is a new value, and a run with failures never counts as handled, so the next day retries. | Replace the env var with a `restore_events` table written by the restore procedure; the job's `restorePointFromEnv` is the one seam. |
