# PW-VOICE-01 — HALTS

Wording that is known but blocked on a decision that is not the writer's. Each entry
names the question and what it needs. Nothing here is edited until the row is answered.
(PW-CONTENT-01's blocked items live in `BLOCKED.md`; this file is the voice series'.)

| ID | Blocked strings | The question | Needs |
|---|---|---|---|
| **H-V01** | 56 onboarding strings in singular («Νιώθεις», «Δοκίμασε ξανά», «τον σύμβουλό σου») | The series' V4 says πληθυντικός everywhere. The onboarding rebuild (Sept 2026) **decided singular on purpose** for that stage only («singular register onboarding-only»). Which decision governs? | **CLOSED 2026-09-15 (D-V10) — FLIPPED to the plural.** The Sept-2026 decision assumed a sealed one-time stage. It is not: onboarding is skippable, resumable and re-enterable from a dashboard banner, and the two registers already met on ONE screen — the upload screen renders the shared wallet errors («Το αρχείο που ανεβάσατε…») and the AI-consent modal beside «Όποιο έχεις πρόχειρο». 90 strings in `onboarding.protectionProfile.*` plus `derive-priorities.ts`'s `REASON_TEXT`, whose el strings are now **verbatim** `protection.attention.reasons`. The one survivor is «Δεν είμαι σίγουρος/η» — the user's own first-person answer, not an address. The `register-guard`'s onboarding allowlist is deleted; it now covers the whole corpus. |
| **H-V02** | `dashboard.home.recPriority{Critical,High,Medium,Low}`, `severityGroupLabel`, `insights.criticalGaps`, `insights.subtitle`, `onboarding…importance.high`, the two `lib/notifications/settings.ts` severity sentences | These render priority/severity as a fact. `CLAUDE.md`: not a verdict until an underwriter says so; `gap-severity-display-single-source` carries them as migration debt. Rewording cannot fix what the *policy* has not settled (§9.5). | Underwriter track; until then, render only via `describeSeverity()` with its caveat. **Round-2 verification:** all LIVE — `recPriority*` and `severityGroupLabel` render in `PolicyholderHome.tsx`, `RecommendationCards.tsx` and the public fixture screen `RealScreens.tsx`; `criticalGaps` in `ActionQueueCard.tsx`; `insights.subtitle` on the agent insights page. Blocked, not rewritten. |
| **H-V03** | ~15 strings that name «σκορ προστασίας» movements: `lib/services/risk-dna/*`, `lib/services/timeline/diff.ts`, `lib/services/risk-review/policy.ts`, `lib/notifications/settings.ts` (4), `settings.riskReview.scoreThen`, `LifeTimeline.tsx` «Σκορ», `AgentWidgets.tsx` «Σκορ Προστασίας» (public) | The score was removed from every render site (Aug 2026, F2). Are these strings **dead** (then: delete, backlog) or **live** on an agent/settings surface (then: §9.5 Blocked)? Also: `score-containment` is case-sensitive and does not match «Σκορ Προστασίας» — a guard hole, not a copy fix. | **Round-2 verification, per string.** LIVE: `AdvisorBookView.tsx:168` renders `howItImproves` («…θα μετακινούσε το σκορ προστασίας τους κατά περίπου N μονάδες», `lib/services/risk-dna/advisory-impact.ts`) on `/insights/book`; `HistorySection.tsx` renders `LifeTimeline` whose `score_change` entries (`lib/services/timeline/build.ts:277`, sentences in `timeline/diff.ts`) narrate score movement under the label «Σκορ». DEAD: `compute.ts` `ifActioned.statement` (typed in `RiskDnaPanel.tsx:49`, never rendered); `settings.riskReview.scoreThen` (`RiskReviewCard.tsx:100` comment: no longer renders); `risk-review/policy.ts` «Πτώση σκορ προστασίας» (no producer of `protection_score_drop`); `role-copy agentKpis.completeness` «Μέσος δείκτης προστασίας» and `noScores`. PUBLIC: `AgentWidgets.tsx` rendered «Σκορ Προστασίας 87/100» on `/solutions/agents` — removed in Round 2 (V8: a public claim the product no longer supports; the case-sensitive `score-containment` guard missed the capital «Σκορ»). → **H-V12** for the two live sites. **Round 3:** the public glossary entry `/lexiko/skor-prostasias` still described the score in the present tense (`howToCheck` «εμφανίζεται στην αρχική σας οθόνη», three FAQ answers) — corrected to the past tense and the facts in both languages (D-V08). |
| **H-V04** | «Δωρεάν Πράκτορας», «Πράκτορας Starter», «Πράκτορας Pro» (`lib/subscription-entitlements.ts`), «Το πλάνο πράκτορα», «Agent Starter» ×2 (public) | V1 bans «πράκτορας»; but these are **plan names** — catalogue rows and Stripe products (`CLAUDE.md` catalog coupling: names move WITH the catalogue, never ahead). | **CLOSED 2026-09-15 (D-V12) — the premise was false.** `AGENT_PRICING.label` has no consumer anywhere (only `monthlyEur` is read, by `plan-catalog.test.ts`), so those four Greek strings were never catalogue rows; and the public card title comes from **code** (`public-pricing-content.ts` → `PricingCard.tsx:69`), not the plan row, which contributes price and visibility only. Latin `Agent Starter/Pro/Agency` stay as branded names. The `label` field is deleted, and with it the guard exemption it forced. **A live hit it had been hiding:** `/agent/pricing` renders «Πλάνα **Πρακτόρων**» — the guard's `/πράκτορ/` never matched it because Greek moves the stress in the genitive plural (πράκτορ-ας → πρακτόρ-ων). Now «Πλάνα συμβούλων», and the guard matches both accentuations with an accent-blind lookahead (so «ΠΡΑΚΤΟΡΕΙΟ» in caps stays exempt). |
| **H-V05** | 15 strings with Latin `wallet` («στο wallet σας»), plus the «φάκελος»/«πορτοφόλι» split | One product noun. The nav says «Ο φάκελός μου», the guides say «ψηφιακό πορτοφόλι ασφαλίσεων». | **CLOSED 2026-09-15 (D-V11) — «πορτοφόλι».** The halt's premise was wrong: «Ο φάκελός μου» was never a nav label, only a landing screenshot tab (`GrafiHero.tsx:42`), now «Το πορτοφόλι μου». Decisive evidence for the noun: the positioning names «ο φάκελος στο συρτάρι» as the thing PolicyWallet replaces. Nine Latin `wallet` strings and four product-as-«φάκελος» strings moved; «φάκελος» keeps its four real senses (LEXICON #11b). «wallet pass» was not the product noun at all — an Apple/Google Wallet pass whose own siblings already say «πάσο», so `generateFailed` now matches them and metric 6 reaches **0**. Two `wallet` strings remain in the cookie banner: consent copy, **blocked by §4.3**. |
| **H-V06** | The calibration anchor's own second paragraph as shipped in partner-connection copy | §9.3 — how far «οι απαντήσεις που σας δίνει πατάνε στα δικά σας συμβόλαια» may go before it reads as the platform brokering advice. (Note the anchor itself says «συμβόλαια»; LEXICON #1 makes it «ασφαλιστήρια».) | DPO + underwriter track. |
| **H-V07** | «14 ημέρες δωρεάν δοκιμή» (`UpgradeModal.tsx`) | No trial is documented in the plan catalogue. Removal is the default (§4.2), but the modal's argument leans on it (§9.4). | **CLOSED 2026-09-15 — the trial is REAL, so the line stays.** `plans.trial_days` → `lib/billing.ts:90` → `subscription_data.trial_period_days` → the webhook's `trialing → active`. Verified against production: `ph-pro` = 14, every other plan 0. The modal renders the line only when the figure is non-zero, so it cannot go stale. C9 is sourced. The defect was elsewhere: the deprecated `/api/stripe/checkout` hardcoded 14 days for `plus` as well (#359). |
| **H-V08** | *(none)* | §9.1 free-tier «2 vs 3»: **not reproduced.** The `/product` FAQ says «έως 3»; the six «2/δύο ασφαλιστήρια» hits are the comparison tool and glossary sentences. `cta-reassurance-single-source` pins the figure to `DEFAULT_ENTITLEMENT_LIMITS`. | No decision needed — recorded so it is not re-raised. |
| **H-V09** | one string outside `el` copy: `support@policywallet.com` | §9.2 contact identity: the Greek corpus carries only `dpo@policywallet.gr` (4) and `info@policywallet.gr` (1); no placeholder phone (the signin «+30 69X XXX XXXX» is an input-format hint). Outside the corpus, a single `support@policywallet.com` sits beside `mailto:support@policywallet.gr`. | **CLOSED 2026-09-15 (D-V13) — no code change was needed.** The only live support address is already `support@policywallet.gr` (`lib/push/web-push.ts:75`); the single `.com` occurrence is a comment in `lib/email/templates/agent-emails.ts:4` that records the fix («not addresses this company controls»). DNS: both domains accept mail (Zoho `.gr`, Microsoft 365 `.com`), so unifying blind would have been a guess. |
| **H-V10** | `protection.life.lead` «…τι ελέγχουμε και τι σας προτείνουμε» | Not blocked — a clean V9 rewrite («…και τι σας δείχνουμε»). Listed so the Round-1 adjudication starts from it. | none |
| **H-V11** | `dashboard.allClear` «Όλα καθαρά!» (11 chars) | An all-clear with no denominator (§4.5), but no honest form fits a 12-character budget — «Καμία ενέργεια.» is 15, and even that asserts absence. Either the slot can take «Κανένα εύρημα στα ελεγμένα.» (needs the render check at 320 — §9.6) or the label goes and the card shows only the counted facts beside it. | **CLOSED 2026-09-15 — the premise was wrong: the key is dead.** Nothing renders `t.dashboard.allClear` (only eight `t.dashboard.*` keys have consumers, and neither this nor its neighbour `noPriorities` is among them); the `corpus.json` `sites` entries were substring collisions on the leaf name `allClear`. Both keys deleted in both languages — the `en` twin still carried the Round-1 praise («Great work staying on top of everything!»). The honest all-clear already ships with its denominator: `protection.gaps.allClear` renders «Ελέγξαμε {assessed} ασφαλιστήρια…» through `GapList.tsx:152`. |
| **H-V12** | `AdvisorBookView.tsx:168` (`howItImproves`), `HistorySection.tsx` → `LifeTimeline` `score_change` entries (`timeline/build.ts:277`, `timeline/diff.ts`) | Two render sites F2 (Aug 2026) missed still narrate the protection score with a number. No wording fixes a sentence whose subject is the score; the `en` side says the same thing, so an `el`-only rewrite would change meaning (§4.6, §8). This is a product defect (a claim the glossary says the product no longer makes), not a tone problem. | **Done under «approved continue» (2026-09-13, follow-up PR):** `howItImproves` deleted end-to-end (advisory-impact → book page → AdvisorBookView, test updated); the timeline keeps its assessment entries but no longer states the score's movement («Επανελέγξαμε την προστασία σας μετά από …: N κίνδυνοι άνοιξαν»), its label is «Αξιολογήσεις», no delta leaves the server and no icon/colour follows a sign; dead strings deleted (`compute.ts` projection sentence, `riskReview.scoreThen`, `agentKpis.completeness`/`noScores`). Kept: `risk-review/policy.ts` «Πτώση σκορ προστασίας» — a trigger the documented review matrix names and its tests enumerate; no producer, never rendered. `score-containment` now matches the label case-insensitively and has a literal-score net («Σκορ Προστασίας» … `87/100`) with a probe proven red. **Residual CLOSED 2026-09-15:** the book's tile «Ανακτήσιμες μονάδες» printed a sum of score points; it is now the counted «Ανοιχτές περιοχές» (B-V14). Why two guards missed it: the identifier `recoverablePoints` was unknown to `IDS`, and the value reached the screen as a **prop on a multi-line element**, a shape `JSX_RENDER` cannot match. Both closed, with a probe in the real shape. |
| **H-V13** | `role-copy customerList.actionAllGood` «Όλα καλά» (8 chars), derived in `lib/services/agent-portal.service.ts:116` | The agent customer list labels a customer «Όλα καλά» when no rule fired — but the derivation never checks whether the customer's policies were analysed (§4.5: absence rendered as reassurance). The honest label («Καμία εκκρεμότητα», 17) does not fit the 15 % budget of an 8-character chip. | **(a) done under «approved continue» (2026-09-13, follow-up PR):** `deriveRecommendedAction` returns a new `run_analysis` («Εκκρεμεί ανάλυση» / "Analysis pending") when active policies exist and none carries `lastAnalyzedAt` — after a renewal inside the window, before gaps; «Όλα καλά» is now reachable only for a book something has read. **(b) CLOSED 2026-09-15 — the budget premise was wrong.** The chip (`CustomerList.tsx:166`) is `inline-flex … whitespace-nowrap rounded-full px-2` with no `max-w` and no truncation, and its siblings already ship at 25 («Επικοινωνία επανασύνδεσης») and 29 («Προσθήκη πρώτου ασφαλιστηρίου») characters. «Όλα καλά» → **«Καμία εκκρεμότητα»** (17) / "Nothing pending", both languages — the same honesty class as D-V08, and (a)'s `run_analysis` already shipped an `en` twin. Caveat recorded: past ~35 characters the chip overflows its card rather than clipping. |
