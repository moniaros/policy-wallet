# PW-VOICE-01 — backlog at series close (2026-09-13)

Hard stop after Round 3 (§5). Everything below is a finding the series could not apply without a decision or a code change that is not copy work. Severity is the §3 class; the reason names what blocks it. Ledgers: `PROGRESS.md` (rounds), `HALTS.md` (the questions), `DECISIONS.md` (settled wording).

| # | Item | Severity | Why it is here | Needs |
|---|---|---|---|---|
| B-V01 | H-V01 — 56 onboarding strings in the singular, plus the 7 in `derive-priorities.ts` that render in the onboarding summary | Major (V4) | The Sept-2026 onboarding rebuild chose the singular on purpose for that stage; the series' V4 says plural everywhere. Test-pinned. | **DONE 2026-09-15 (D-V10)** — flipped. Guard allowlist deleted. |
| B-V02 | H-V04 — «Δωρεάν Πράκτορας», «Πράκτορας Starter/Pro», «Το πλάνο πράκτορα», «Agent Starter» ×2 | Major (V1) | Plan names are catalogue rows and Stripe products; names move WITH the catalogue (CLAUDE.md catalog coupling). | **DONE 2026-09-15 (D-V12)** — the strings were unread constants, not catalogue rows; deleted. Latin tier names stay branded. Fixed the live «Πλάνα Πρακτόρων» the guard could not see, and closed the accent hole that hid it. |
| B-V03 | H-V05 — the product noun: «wallet» ×15, «wallet pass» (the last English-in-el line, ceiling 1), «φάκελος» vs «πορτοφόλι» | Major (V1/V7) | One noun for the product. Proposed «πορτοφόλι» with «φάκελος» for a single policy's documents. | **DONE 2026-09-15 (D-V11)** — «πορτοφόλι». Metric 6 reaches 0; the two remaining `wallet` strings are consent copy (§4.3), recorded not missed. |
| B-V04 | H-V07 — «14 ημέρες δωρεάν δοκιμή» (`UpgradeModal.tsx`) | Blocking (V8) | No trial in the plan catalogue; the modal's argument leans on it. | **DONE 2026-09-15** — the trial is real and correctly gated; C9 sourced against the production row (`ph-pro` = 14, all others 0). The real defect was the deprecated checkout route hardcoding 14 for `plus` (#359). |
| B-V05 | H-V11 — `dashboard.allClear` «Όλα καθαρά!» (11 chars) | Blocking (§4.5) | No honest all-clear fits a 12-character slot. | **DONE 2026-09-15** — the key was dead; deleted in both languages. No slot existed to measure. |
| B-V06 | H-V12 — live score renders F2 missed: `AdvisorBookView.tsx:168` (`howItImproves`), the settings-history timeline's `score_change` entries; dead strings `compute.ts` projection, `riskReview.scoreThen`, «Πτώση σκορ προστασίας», `agentKpis.completeness`/`noScores` | Blocking (CLAUDE.md invariant) | A sentence whose subject is the score cannot be reworded into honesty; the `en` twin says the same. Code deletion. | **Done 2026-09-13 (follow-up PR, «approved continue»)** — renders deleted / rewritten without the score, dead strings removed, guard case-insensitive with a literal-score net and probe. `protection_score_drop` kept (documented trigger matrix; never rendered). |
| B-V07 | H-V13 — agent customer-list chip «Όλα καλά» (`agent-portal.service.ts:116`) | Blocking (§4.5) | Derived without checking whether the customer's policies were analysed; the honest label does not fit an 8-character chip. | **DONE 2026-09-15** — (a) `run_analysis` «Εκκρεμεί ανάλυση» shipped 2026-09-13; (b) the chip has no width constraint and siblings run to 29 chars, so «Καμία εκκρεμότητα» (17) fits with no component change. |
| B-V08 | H-V02 — 15 severity/priority strings (`recPriority*`, `severityGroupLabel`, `insights.subtitle`, …), all verified live | Blocking (§4.4) | Severity is not a verdict until an underwriter says so; rewording cannot settle what the policy has not. | Underwriter track; until then `describeSeverity()` + caveat. |
| B-V09 | H-V06 — the partner-connection copy's advice boundary («οι απαντήσεις που σας δίνει πατάνε στα δικά σας ασφαλιστήρια») | Blocked (§9.3) | How far it may go before it reads as the platform brokering advice. | DPO + underwriter track. |
| B-V10 | H-V09 — one `support@policywallet.com` beside `mailto:support@policywallet.gr` | Minor (V7) | Contact identity is the owner's call. | **DONE 2026-09-15 (D-V13)** — no change needed; the code already uses `.gr` and the `.com` string is a comment recording that fix. |
| B-V11 | `en` copy — the English twins of every `el` rewrite in Rounds 1–3, except the V8 truth fixes applied to both languages (D-V08) | out of scope (§8) | `en` is its own series. | The en series. |
| B-V12 | In-app fixture matrix — the `all-expired` state for the Round-2/3 in-app strings (`tests/measure/dashboard-voice-matrix.spec.ts`) | Minor (§5 evidence) | The Round-3 run verified empty/typical/heavy with every ledger's strings (45 renders, 0 clipped); `all-expired` failed at its fixture write five times on the local-dev session pooler held by a parallel session. Its Round-1-string evidence is in `round2-matrix.json`. | **Addressed 2026-09-15** — `withDb` now retries a saturated pooler (4 attempts, 2/4/8 s backoff, matching only «Timed out fetching a new connection» / «Can't reach database server» / «max clients reached»). `acquirePoolerLock` could never have fixed this: it serialises OUR processes, and the holder was a parallel session's `next dev` on the same 15-client pooler. Re-run to confirm: `BASE_URL=http://localhost:3200 npx playwright test tests/measure/dashboard-voice-matrix.spec.ts --project=measure-dash --no-deps`. |
| B-V13 | The Step-0 screen definitions are case-sensitive for the score vocabulary | guard hole | It hid «Σκορ Προστασίας» ×9 until Round 3 added a case-insensitive pass. | **Done 2026-09-13** — `INV.score` in `voice-01-findings.json` is case-insensitive. |
| B-V14 | `AdvisorBookView` factor tile «Ανακτήσιμες μονάδες» (`factors.recoverable` = recoverable score points) — seen on production `/insights/book` after #355 | Blocking (CLAUDE.md invariant) | Score points by another name: the tile prints the number the deleted `howItImproves` sentence used to narrate. Neither render net catches it (no score label, no `N/100`). | **DONE 2026-09-15** — the tile is now «Ανοιχτές περιοχές» / "Open areas", a COUNT summed from each household's `openDimensions` (computed in `book.ts` since Sept and previously thrown away); no tone, because a count is not a verdict. The three dead `factors` props stop crossing to the client. Guard: `score-containment`'s IDS gains `recoverablePoints`/`recoverable`, **and a new JSX_PROP net** — the original leak was a PROP on a multi-line element, which `JSX_RENDER` structurally could not see. Probe `score-points-tile.tsx.txt` reproduces that exact shape, and the net was proven red against the real file, not only the probe. |
| B-V15 | `REASON_TEXT` (`lib/services/protection-profile/derive-priorities.ts`) duplicates `protection.attention.reasons` — since D-V10 the el strings are identical character for character | Minor (duplication) | Two layers, not two texts: one is a `Bilingual` a server service composes, the other a per-locale i18n bundle the client reads. Merging means wiring the service to the bundle, a structural change §0 keeps out of a wording series. | Read the bundle in the service (or generate one from the other) so the next register decision cannot move one and miss the other. |
| B-V16 | `tests/unit/protection-profile-summary.test.tsx` no longer guards that the onboarding card withholds `explanation.why` / `explanation.unknown` | Minor (test coverage) | Neither was ever separate copy: `why` IS `copy.reasons[...]` (`attention-areas.ts:413`) and `unknown` names the same factors the card lists. They read as different strings only while the registers differed, so D-V10 makes them identical by construction. `next` — the coaching step, genuinely separate — is still guarded. | Nothing, unless the surfaces are meant to diverge again; recorded so the narrowing is not mistaken for a gap. |

