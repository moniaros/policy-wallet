# PW-VOICE-01 — backlog at series close (2026-09-13)

Hard stop after Round 3 (§5). Everything below is a finding the series could not apply without a decision or a code change that is not copy work. Severity is the §3 class; the reason names what blocks it. Ledgers: `PROGRESS.md` (rounds), `HALTS.md` (the questions), `DECISIONS.md` (settled wording).

| # | Item | Severity | Why it is here | Needs |
|---|---|---|---|---|
| B-V01 | H-V01 — 56 onboarding strings in the singular, plus the 7 in `derive-priorities.ts` that render in the onboarding summary | Major (V4) | The Sept-2026 onboarding rebuild chose the singular on purpose for that stage; the series' V4 says plural everywhere. Test-pinned. | Owner: keep the exception (then `register-guard` allowlists `onboarding.*` for good) or flip it. |
| B-V02 | H-V04 — «Δωρεάν Πράκτορας», «Πράκτορας Starter/Pro», «Το πλάνο πράκτορα», «Agent Starter» ×2 | Major (V1) | Plan names are catalogue rows and Stripe products; names move WITH the catalogue (CLAUDE.md catalog coupling). | Owner: the Greek tier names, applied to catalogue + Stripe + copy in one change. |
| B-V03 | H-V05 — the product noun: «wallet» ×15, «wallet pass» (the last English-in-el line, ceiling 1), «φάκελος» vs «πορτοφόλι» | Major (V1/V7) | One noun for the product. Proposed «πορτοφόλι» with «φάκελος» for a single policy's documents. | Owner: the noun. |
| B-V04 | H-V07 — «14 ημέρες δωρεάν δοκιμή» (`UpgradeModal.tsx`) | Blocking (V8) | No trial in the plan catalogue; the modal's argument leans on it. | Owner: does a Stripe trial exist? Yes → CLAIMS C9 gets a source; no → the line goes. |
| B-V05 | H-V11 — `dashboard.allClear` «Όλα καθαρά!» (11 chars) | Blocking (§4.5) | No honest all-clear fits a 12-character slot. | Render the card with «Κανένα εύρημα στα ελεγμένα.» at 320 or drop the label for the counted facts — a component decision. |
| B-V06 | H-V12 — live score renders F2 missed: `AdvisorBookView.tsx:168` (`howItImproves`), the settings-history timeline's `score_change` entries; dead strings `compute.ts` projection, `riskReview.scoreThen`, «Πτώση σκορ προστασίας», `agentKpis.completeness`/`noScores` | Blocking (CLAUDE.md invariant) | A sentence whose subject is the score cannot be reworded into honesty; the `en` twin says the same. Code deletion. | Owner / score track: delete the two renders and the dead strings; make `score-containment` match «Σκορ» case-insensitively (it missed the public 87/100 for weeks). |
| B-V07 | H-V13 — agent customer-list chip «Όλα καλά» (`agent-portal.service.ts:116`) | Blocking (§4.5) | Derived without checking whether the customer's policies were analysed; the honest label does not fit an 8-character chip. | Code: an «unanalysed» branch; then the chip's slot. |
| B-V08 | H-V02 — 15 severity/priority strings (`recPriority*`, `severityGroupLabel`, `insights.subtitle`, …), all verified live | Blocking (§4.4) | Severity is not a verdict until an underwriter says so; rewording cannot settle what the policy has not. | Underwriter track; until then `describeSeverity()` + caveat. |
| B-V09 | H-V06 — the partner-connection copy's advice boundary («οι απαντήσεις που σας δίνει πατάνε στα δικά σας ασφαλιστήρια») | Blocked (§9.3) | How far it may go before it reads as the platform brokering advice. | DPO + underwriter track. |
| B-V10 | H-V09 — one `support@policywallet.com` beside `mailto:support@policywallet.gr` | Minor (V7) | Contact identity is the owner's call. | Owner confirms `.gr`; one-line fix. |
| B-V11 | `en` copy — the English twins of every `el` rewrite in Rounds 1–3, except the V8 truth fixes applied to both languages (D-V08) | out of scope (§8) | `en` is its own series. | The en series. |
| B-V12 | In-app fixture matrix — the `all-expired` state for the Round-2/3 in-app strings (`tests/measure/dashboard-voice-matrix.spec.ts`) | Minor (§5 evidence) | The Round-3 run verified empty/typical/heavy with every ledger's strings (45 renders, 0 clipped); `all-expired` failed at its fixture write five times on the local-dev session pooler held by a parallel session. Its Round-1-string evidence is in `round2-matrix.json`. | Re-run when the pooler is free: `BASE_URL=http://localhost:3200 npx playwright test tests/measure/dashboard-voice-matrix.spec.ts --project=measure-dash`. |
| B-V13 | The Step-0 screen definitions are case-sensitive for the score vocabulary | guard hole | It hid «Σκορ Προστασίας» ×9 until Round 3 added a case-insensitive pass. | Fold the `(?i)` pass into the next audit's definitions. |
