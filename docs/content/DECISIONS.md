# PW-CONTENT-01 — decisions of record

| Id | Date | Decision | Evidence / constraint as restated |
|---|---|---|---|
| D-C1 | 2026-09-06 | **One resolved locale per request.** `resolveUserLanguage` is the only normaliser of the stored preference; every authenticated layout seeds `LanguageProvider` with it; a seeded provider never lets localStorage win; every formatter and the html stamp share one tag table (`resolveLocale`). | Step 0 C6–C8: two sources that never met, 61 hand-rolled normalisations, en-US vs en-GB. Guards demonstrated red on committed probes. |
| D-C2 | 2026-09-06 | **Classification stays citation-backed; `market` is now permitted only on a named public source.** 6 legislative, 2 market, 0 contractual, 21 under review with search records. Every classified requirement renders its citation at every render site; the review document blocks GA until legal and underwriting sign it. | Goal 2 rules as written; Step 0 E12. A product page counts as a named public source for now, flagged medium confidence for the legal track to confirm or replace. |
| D-C3 | 2026-09-06 | **The relationship index is removed from every render site** (the "weighted or judged" path of Goal 3). | F13: coefficients 40/20/20/20, thresholds 7/30/90/180 days, colour bands with verdict labels; «Καλή» for an all-expired customer and for an all-failed one. A wrongly removed number costs a UI element; a wrongly kept one is the defect the programme exists to eliminate. |
| D-C4 | 2026-09-06 | **Catalogue growth: the composition is always computed against the run's recorded plan and never withheld; a newer catalogue is one dated sentence offering re-analysis.** Three states, three sentences. | Step 0 D9/D10 recommendation, taken as written. The withheld state (`catalogue_mismatch`) deleted an honest dated measurement because something unrelated changed. |
| D-C7 | 2026-09-06 | **/status is a documented equivalent, not a monitor.** It states where the service runs, where its providers publish status, and where incidents are recorded; it publishes no uptime figure because none is measured (the daily synthetic check persists nothing a page could read). | Step 0 G15 and the synthetic-launch-check route. A percentage nobody measures is the fabricated count the programme removed. |
| D-C8 | 2026-09-06 | **Dev's active `gap_definitions` follow the branch that is shipping, not the branch furthest ahead.** CI's catalogue check verifies the repository catalogue against the dev database, so dev was moved back from the rules branch's 50 active rows to the shipping PR's 29 by `align:gap-catalogue --apply` from the `feat/content-01` checkout — 21 rows DEACTIVATED, none deleted; both sides then print production's fingerprint `2df9d0fd4b581caa` (dev: 29 active / 24 inactive). Re-aligning to 50 is step one of BL-C1 and is an idempotent upsert on slug, so ids and any `gap_instances` survive. | PR #308's «Verify gap catalogue matches the database» step failed with «nobody wrote these» for the 21 new slugs (run 34036080588). The align script's deactivate-never-delete contract (CLAUDE.md, «Gap definitions are reference data») is exactly the reversible move; the alternative — merging the rules PR to make CI green — would have deployed 21 rules against a production table that does not carry them. |

