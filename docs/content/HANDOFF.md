# PW-CONTENT-01 — handoff: the open human-track items

Inherits every open row of `docs/transparency/HANDOFF.md` (H2, H5, H7 (stale — see H16), H8, H9, H10). New in this series:

| # | Question (exact) | Who answers | What it blocks | What the product does today |
|---|---|---|---|---|
| C-H1 | **Production catalogue alignment — go / no-go.** (PR #309, draft, rebased on NEW-UI `3c287313`; dev currently on the 29-row set per D-C8.) 21 new rules (renters ×8, home contents ×3, personal accident ×2, roadside ×2, pension ×3, income protection ×1, group life ×2) exist in the repository and in dev; production's `gap_definitions` still holds the 29-rule set. Approve the one upsert command (BLOCKED.md BL-C1). | The owner. | Merging and deploying Goals 5–6. | Production analyses plan from the 29-rule set; renters is not yet a write branch in production. |
| C-H2 | **Legal sign-off of `docs/transparency/PROVENANCE-REVIEW.md` (now 50 rows: 6 legislative, 2 market, 42 under review).** Confirm the six legislative citations and the two market rows (a product page as a named public source, medium confidence), and decide the candidates. | Legal / underwriting. | **GA (blocking, H10).** | Classified findings render their class WITH the citation at every site; everything else stays disclosed and uncounted. |
| C-H3 | **Step 0 H16 corrected HANDOFF H7:** the three seeded `example.com` accounts do not exist in production. Is there any production account the owner considers a test account to rotate before GA (the `moniaros.*` / `amoniarou*` addresses)? | The owner. | GA readiness only. | Nothing to delete was found; Goal 8.1 had no target. |
| C-H4 | **Extraction requests E1–E5** (`docs/content/DEFERRED-RULES.md`): should the extraction schema gain the tenant fields (declared inventory value, tenant/landlord liability, water damage and glass flags, single-article limit, temporary accommodation)? Each unlocks a filed rule. | The owner (schema + prompt change, model cost). | The deferred renters rules. | The eight renters rules read only what the extractor fills today. |
| C-H5 | **Composition «not applicable» outcome** for conditional rules (Step 0 A3): a presentation-side change with its own acceptance, needed before any rule gated on an asset attribute can be authored. | The owner (scope for a future series). | The conditional contents rules. | Conditional rules are not authored. |
| C-H6 | **H8 answered: the mailboxes cannot receive mail.** `policywallet.gr` publishes **no MX record** (checked 2026-09-06: `dig MX` empty; TXT holds Brevo sender codes and site verifications; DMARC `p=none` reporting to Brevo). So `info@`, `careers@` and `dpo@policywallet.gr` — printed 13, 1 and 10 times in the product and legal pages — bounce. The DPO address is the Art. 37 GDPR contact in the privacy policy. Set up inbound mail (an MX to Google Workspace / Zoho / the Brevo inbox) or change the printed addresses to ones that exist. | The owner (DNS + a mailbox provider). | **GA (blocking):** a privacy policy naming an unreachable DPO is a defect, not a preference. | Outbound transactional mail goes through Brevo; inbound to the printed addresses is impossible. |
| C-H7 | **H9 assessed, not fixed.** (1) The drawer footer's «Ρυθμίσεις» link box overlapping the language/theme toggles needs a drawer-OPEN capture to attribute and fix; the R4 metric measured page flow only, and the drawer is a modal at z-50 above the z-40 bottom nav, so the fix is not provably one line. (2) The fixed bottom nav overlaying the content row at the fold is by design: the shell reserves the bar's height (`pw-bottom-nav-reserve`) and the bar is `inert` while the drawer is open. | Whoever owns `components/shell/AppShell.tsx`. | Nothing. | Both behaviours live; neither blocks a task. |

