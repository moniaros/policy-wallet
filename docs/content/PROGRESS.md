# PW-CONTENT-01 — progress ledger

One row per goal, with the before/after metrics the goal's acceptance names. Step 0: `STEP0-FINDINGS.md` (approved 2026-09-06). Decisions of record: `DECISIONS.md`. Deferred rules and extraction requests: `DEFERRED-RULES.md`.

| Goal | Status | Date | Metrics (before → after) | Notes |
|---|---|---|---|---|
| 0 — Investigation | done, approved | 2026-09-06 | none by design | `STEP0-FINDINGS.md` at NEW-UI `caaf4f3f` |
| 1 — One locale source | done | 2026-09-06 | hand-rolled normalisations of the stored preference: **61 → 0** (one resolver, `lib/i18n/resolve-language.ts`); client components carrying an English fallback: **2 → 0**; inline `el-GR/en-GB` ternaries outside the tag table: **~40 → 0**; authenticated layouts seeding the client provider from the server: **0 → 2** (protected, onboarding); html `data-locale` for English: **en-US → en-GB** (matches every formatter); Step 0's 11 server+client surfaces: all under a seeded layout, asserted | Guards: one-locale-per-request, no-component-locale-fallback, formatted-value-locale-matches-copy — each with a committed probe. The seeded provider never reads localStorage over the server value, follows the server after a toggle, and mirrors the value for the public tree. **The English sweep found a second seam the unit guards had not:** the root layout's unseeded provider and the protected layout's seeded provider both stamped `<html lang>`, and React runs the parent's effect last, so an English preference rendered under `lang="el"`. A seeded provider now claims the stamp and the unseeded one yields; the guard renders the nested pair. Sweep evidence: `docs/evidence/content-g1/` (both audiences, both locales, 320/390/430, trust pages included). |

| 2 — Classify the remaining slugs | done (proposal; legal sign-off pending, blocks GA) | 2026-09-06 | classified **3 → 8** (legislative 3 → 6: + `missing_enfia_components` on Ν. 4223/2013 άρθρο 3 παρ. 7Ζ as amended by ν. 5162/2024 άρθρο 10, + `missing_accident_declaration_phone` and its motorbike sibling on Π.Δ. 237/1986 άρθρο 9 παρ. 1; market 0 → 2: `missing_hospital_class`, `no_direct_billing` on Εθνική Ασφαλιστική's published «Ασφάλεια Υγείας» page); under review **26 → 21**, each with what was searched; classified without a citation: **0**; classified on inference: **0**; contractual: 0 (no named contract class could be tied to a rule that fires regardless of that contract). Rendered numbers before/after: `docs/evidence/content-g2/RESULT.md` (captured with the final tree). | `PROVENANCE-REVIEW.md` rewritten (29 rows, search records, sign-off list). The insights pill split: class in the pill, citation wrapped beneath — the nowrap citation pushed the agent page 201px wide at 320. Both slug forms asserted for EVERY classified slug. |
| 3 — Relationship index | done: **removed** | 2026-09-06 | render sites of the 0–100 index: **6 → 0** (customer profile card, client-detail overview, client card dot, customer list column + mobile pill, agent dashboard rows, landing mock); verdict adjectives in its subtree: **3 → 0** («Ενεργή σχέση»/«Χρειάζεται παρακολούθηση»/«Χωρίς πρόσφατη επαφή» gone with the copy); the `intelligence.healthScore` field on the agent portal — which carried the customers' PROTECTION score — gone with it | Evidence F13: inputs were counts but the index was weighted (40/20/20/20), thresholded (7/30/90/180 d) and banded into verdict colours; it read «Καλή» (100) for a customer whose every policy had expired and for one whose every analysis had failed. Guard `relationship-index-removed` (module absent, no render, copy gone; probe). D-C3. |
| 4 — Catalogue growth semantics | done | 2026-09-06 | surfaces computing a denominator against a version the run did not attempt: **0** (guard); compositions withheld on a version difference: **every pre-growth run → 0** — the lines render against the run's plan with one dated sentence («Η ανάλυση έγινε στις {date}. Έχουν προστεθεί έλεγχοι από τότε — μια νέα ανάλυση θα τους περιλάβει.»); the three states asserted pairwise distinct in both languages (pre-plan / unauthored / stale) and by state attribute; the stale line reaches both report routes | `catalogue_mismatch` kind deleted; `Composition.stale` flag; `describeCatalogueStaleness` for the report routes; re-analysis stays the user's action. Guards `denominator-matches-run-catalogue-version` (probe: a denominator read from the current catalogue) and `three-states-distinct`. D-C4. |

| 5 — Rules for renters and contents | done in code; **production alignment awaits the owner's go** | 2026-09-06 | renters: unauthored-branch state on a renters fixture: **rendered → not rendered** (`renters` is now a write branch: taxonomy, `WRITE_BRANCH_IDS`, the gate's home family, the prompt's section rule); rules authored: **renters 8, home +3 = 11** (40 total, 9 branches); composition on a typical renters contents policy: N₁ **0 → 8**, A₁+B₁+C₁ = N₁ asserted; rules reading unpopulated fields: **0** (guard, schema-derived inventory per branch); rules without a static input list: **0** (guard); rules without both a firing and a quiet fixture: **0** (guard); `lib/gap-detection.ts`: byte-identical, freeze pin green | `contents` is not a branch — the contents questions are authored on home and renters; conditional variants and five field requests filed in `DEFERRED-RULES.md`. Dev catalogue aligned and verified (40 active, fingerprint `2e45676e003562ce`); production stays on the 29-rule fingerprint until `npm run align:gap-catalogue -- --apply` runs against it — a production write this series forbids without approval — so the rules PR merges only with that alignment. |
| 5–6 — production catalogue alignment (BL-C1) | executed, then PARKED on the deploy blocker (BL-C3) | 2026-09-08 | production `gap_definitions` **29 → 50 active** (0 inactive, 14 branches); dev likewise 29 → 50; the repo's fingerprint `15fa2758cebeaecc` matches dev, and prod and dev return the identical Postgres-side digest `8045aa4e46935b149e2864f9a20c502a` over the active set **Production is back at 29 active**: GitHub Actions stopped starting jobs on a billing failure right after the merge, so `43615847` could not deploy, and 50 active rows under 29-rule code would have made every new run stale with model-titled findings. The 21 rows were DEACTIVATED (not deleted) and the active 29 verified byte-identical to their pre-alignment state (`c66ba5a69febc06973dffcfe56d97879`). One UPDATE re-activates them after the deploy. Archive first (`docs/archive/2026-09-08T1600Z_…sql`, md5 `60dc1550889125794da79be70070a6e8` equal to Postgres's own); only the 21 NEW rows were written — the 29 existing rows were proven byte-identical to the repository (21 added / 0 changed / 0 removed, and per-slug text digests equal), so their ids, versions and timestamps are untouched and every `gap_instances` row survives. The runbook's expected fingerprint `d6f515a1d400f9d5` was stale and matched nothing; measured, the 29-set is `2df9d0fd4b581caa` and the 50-set `15fa2758cebeaecc`. |
| 7 — Public trust pages | done | 2026-09-06 | pages: `/methodology`, `/changelog`, `/status` + `/en` pairs (**0 → 6 routes**); capability claims on /methodology not verifiable in the codebase: **0** (each paragraph's citation is in the content module's comments; the guard checks the schema has no verdict field and the provenance map exists); coverage numbers as literals: **0** — six catalogue-derived entries added to `PUBLIC_COUNTS` and a guard refuses any rendered digit outside the registry in either language; changelog entries without a date and PR number: **0**; uptime figures on /status: **0** (none is measured); SEO titles ≤ 60 and descriptions 140–160 in both languages (existing guard) | Reuses the trust-page machinery (page registry → sitemap + hreflang, proxy allowlist, JSON-LD breadcrumbs), not the legal-document one: these are marketing/trust prose, not versioned contracts. Lighthouse SEO is measured after deploy (Goal 9). |
| 8 — Production hygiene | done as far as the agent can (8.1 moot; 8.2 agent-side + anonymous smoke; policyholder smoke BL-C2) | 2026-09-06 | seeded `example.com` accounts in production: **0 before, 0 after** (Step 0 H16 — nothing to remove, no production write); trust pages live in both languages: **0 → 6** (all 200, self-canonical, in the sitemap); `<html lang>`/`data-locale` on the six: post-hydration correct on the EN page (el in the server HTML of /en/* is the documented root-layout limitation, pre-existing); score vocabulary on public pages: **0**; Lighthouse SEO on /methodology, /changelog, /status: **100 / 100 / 100** (accessibility 100 on all three); agent-side production checks: /insights, /dashboard/agent, /customers — 0 score/index vocabulary hits, no health column, lang/locale = the account preference; new Sentry groups in the window after deploy: **0** in the first 15 minutes (weak evidence: 10 % sampling) | Production catalogue alignment NOT performed (BL-C1, needs the owner's go). Dev re-aligned to the shipping set (D-C8). No email or push dispatched. |
| 9 — Report | done | 2026-09-06 | the nine-part report delivered in-session; `STATUS.md` phase block, `HANDOFF.md` C-H1…C-H7, `BLOCKED.md` BL-C1 (amended) / BL-C2, memory updated | Next series: PW-BRIDGE-01 L0 (inventory only, halts for approval). C1–C7 not started. |

| 6 — Next branches | done in code (same gate as Goal 5) | 2026-09-06 | branches authored: **personal_accident 2, roadside 2, pension 3, income_protection 1, group_life 2 = 10 rules, 5 branches** (50 rules, 14 branches); unauthored-branch state on fixtures for each: **rendered → not rendered**; rules reading unpopulated fields / without static inputs / without both fixtures: **0 / 0 / 0** (guards). **Stopping condition: the prevalent branches with extractable fields are covered.** Remaining unauthored write branches, with the reason: cyber, liability, legal_expenses, business, professional_liability, employer_liability, money, fidelity, other — no branch section in the extraction schema (shared fields only, no defensible branch check); boat, boat_hull, boat_tpl, marine_hull, marine_cargo, marine_crew, transports — a section exists (marineVessel / transit) but these are not prevalent Greek personal lines; fine_art, group_pension — a section exists, niche or B2B. | Ordered by Greek personal-lines prevalence and by whether the prompt fills a branch section (`BRANCH_SECTIONS`), never by production policy count. |

## Decisions taken under standing authority

| Date | Decision | Why | Reversal |
|---|---|---|---|
| 2026-09-06 | **Greek is the resolver's only fallback.** Any stored value that is not exactly `en` resolves to `el`. | 22 sites fell back to English, 39 to Greek; the product is Greek-canonical and the column's default is `el`. | Change one line in `resolveUserLanguage`. |
| 2026-09-06 | **Anonymous contexts keep a literal default** (cookie-consent locale, public form emails). | There is no user whose preference could be resolved; the guard exempts them by name with the reason. | Route them through a visitor-locale helper later. |
| 2026-09-06 | **Group-health siblings stay under review** although the individual-health rows are `market`. | The named source is an individual-programme page; inferring it onto group schemes is the inference the goal forbids. | Add a group-scheme source and flip the two rows. |
| 2026-09-06 | **A retired rule in a stale run's plan is reported (`unclassified`), not counted.** | Its question cannot be classified without its definition; counting it would invent a category. The stale sentence already tells the reader the catalogue moved. | Keep the retired definition inactive-but-present so it classifies. |
| 2026-09-06 | **`renters` becomes a write branch** rather than authoring conditional home rules. | The taxonomy already modelled it (child of home, label «Ενοικιαστή»); it is the only way "renters no longer renders the unauthored state" is measurable; a flag flip is reversible; the conditional alternative mis-composes (Step 0 A3). | Set `writeEnabled: false` and remove it from `WRITE_BRANCH_IDS`. |
| 2026-09-06 | **Goals 5–6 ship on their own PR, gated on the production catalogue alignment.** | The engine plans and evaluates from the database's active definitions while the composition fingerprints the repository catalogue: deploying new rules without aligning production would make every new production run render the stale line, permanently, until the alignment. The alignment is a production write, forbidden here without explicit approval. | Approve and run the one command; then merge. |
| 2026-09-06 | **Dev's active catalogue was moved back from 50 to 29 rows (deactivated, never deleted) so the shipping PR's CI could pass.** CI's `verify:gap-catalogue` step checks the repository catalogue against the dev database; the rules branch had aligned dev to 50 ahead of the merge order, so PR #308 (29 rules) went red with «nobody wrote these» for the 21 new slugs. `align:gap-catalogue --apply` from the `feat/content-01` checkout deactivated them; both sides then print `2df9d0fd4b581caa`, the fingerprint production carries. Dev: 29 active / 24 inactive. | The merge order is #308 first, rules second; the database must follow the branch that is shipping, and deactivation is the reversible move the align script was designed for. | Merge the rules PR and re-run the alignment (upsert on slug re-activates the 21 rows; ids and any `gap_instances` survive). This is now step one of BL-C1's sequence, on dev before prod. |

---

# PW-VOICE-01 — progress ledger

Loop protocol, max 3 rounds. Step 0: `VOICE-01-STEP0.md` (report for approval, 2026-09-12). Lexicon: `LEXICON.md`. Claims: `CLAIMS.md`. Halts: `HALTS.md`. Corpus: `CORPUS.md` + `corpus.json`. Screen: `voice-01-findings.json`.

| Round | Status | Date | Findings (Blocking / Major / Minor / Blocked) | Strings changed | Guards added (proven red first) | Clean? |
|---|---|---|---|---|---|---|
| 0 — inventory & calibration | report delivered, approved 2026-09-13 («approved continue», PR #351 → `91321c60`) | 2026-09-12 | 7 / ~90 / ~14 / 7 halts (H-V01–H-V07); two brief halts not reproduced (H-V08, H-V09) | 0 | none yet (§7 guards are Round-1 work) | n/a |
| 1 — full audit, apply, re-measure, re-read | **shipped** — PR #352 → NEW-UI `25dac377`, deploy run 34724777005 success 2026-09-12 23:15Z; prod-smoked anonymously (`/solutions/agents` «Έτοιμες υπενθυμίσεις», `/pricing` «Υπενθυμίσεις & εξαγωγή αναφοράς», `/trust` «από τον λογαριασμό σας· την εκτελεί άνθρωπος», `/product` «Γεια σας, Νίκο» — each present, each old form absent) | 2026-09-13 | 7 / 64 / 25 / 8 halts standing (H-V01–H-V07 unanswered, H-V11 new); of the 7 Blocking, 6 applied and 1 halted (H-V11) | **136** (130 rewrites paired with their baseline + 6 low-similarity replacements reviewed in `corpus-additions.json`); public 19 · in-product 107 · outbound 10 | `tests/unit/voice-guards.test.ts` — lexicon, advice-verb, register, locale-purity, number-format, claims, length-budget, new-string (8 arms; 4 probes each proven red on its own arm and no other) | **no** — metric 6 still 40; H-V02/H-V03 render-site verification and the in-app fixture matrix are Round-2 work |
| 2 — full audit, apply, re-measure, re-read | **shipped** — PR #353 → NEW-UI `8cda0e21` (deploy and prod smoke recorded below) | 2026-09-13 | 3 / 0 / 3 / 2 new halts (H-V12 live score renders, H-V13 the agent «Όλα καλά» chip); 146 screen hits read, 140 dismissed with a reason (third-person aorists, `{policy}` placeholders, «συνεργάτης» in its business senses, glossed English terms, the glossary explaining the removed score) | **14** (13 rewrites + 1 replacement); public 9 · in-product 5 | none new; claims-guard widened to any «σε λιγότερο από N λεπτά/δευτερόλεπτα» (probe line added, proven red); `tests/measure/dashboard-voice-matrix.spec.ts` = the §5 fixture matrix, reusable per round | **no** — two new Blocked entries (H-V12, H-V13); metric 6 at 29 |
| 3 — full audit, apply, re-measure, re-read (FINAL) | **applied**, PR to NEW-UI (`feat/voice-01-round3`) | 2026-09-13 | 1 / 8 / 0 / 0 new halts; the Blocking one is the public glossary describing the removed score in the present tense (found only by a case-insensitive screen, B-V13) | **15** (all public) | none new; `locale-purity-guard` gains the gloss convention (D-V09), nesting-aware `${…}` blanking, and drops the staff console, the mock provider and the alias table from its universe | **no** — findings were produced and applied; hard stop after Round 3 (§5). Metric 6: 177 → **1** (the halted «wallet pass»). Backlog: `BACKLOG.md` (13 items). |

## Round 1 — detail (2026-09-13)

**Dispositions.** Every Blocking finding from Step 0 was read at its render site before being called one. Applied: the three all-clears that named no denominator (`portfolioInsights.allGood`, `wallet.notices.allClear`, `status.allClear30Days` → «Στα ασφαλιστήρια που ελέγξαμε…», «Στα ελεγμένα: …»), the praise on an empty priorities list, the «σε λιγότερο από 1 λεπτό» claim (no `CLAIMS.md` row → removed), `protection.life.lead` (V9, «…τι σας δείχνουμε»), and the six `deterministic-savings` sentences, each now attributed to the ασφαλιστής with its invented figure removed. Halted: `dashboard.allClear` (H-V11 — no honest form fits 12 characters). Major: V1 46 strings («συμβόλαιο» in-app and outbound, «πράκτορας», «συνεργάτης», the «ασφαλιστικό αποτύπωμα» coinage ×2), V4 18 strings outside onboarding, V7 34 anglicisms. Minor: V3/V5 25 («έξυπνες», «ξεκλειδώστε», «με ένα κλικ», «premium», «προηγμένη»). (A string can carry two tests; the classes overlap.) `derive-priorities.ts` was rewritten plural and **reverted the same day** — it renders in the onboarding summary and is test-pinned singular (D-V04 corrected, H-V01 annotated).

**Measurement (§6), baseline → Round 1.** Corpus 9,926 strings (`corpus.json`, unchanged universe). Strings changed 136; mean Δ −0.3 ch (public +0.8, in-product −0.5, outbound −0.6); max Δ +19 ch (+6 %, guides B2 «ο νόμος απαιτεί άμεση ασφάλιση»); pairs over the 15 % budget: 3, each a V1 lock on a bare label and listed with a reason in `budget-exemptions.json` (D-V01). English-in-el (metric 6): the Step-0 screen counted 350; the definition was corrected in `CORPUS.md` (plain literals only, placeholders and addresses stripped, Greek-majority literal, `admin` and model-facing files out) — under it the baseline was **177 → 40**; the guard pins 40 as a ceiling that can only go down. Claims without a row: 0 (claims-guard green, outbound templates included). Advice verbs in platform voice: 0. Register drift outside onboarding: 0. Truncation: the fourteen public routes render at 320/390/430 with **zero horizontal overflow** and nothing past the right edge at 320; the harness's other hits are by design (the skip link and sr-only labels off-canvas, `.pw-scroll-strip` cells, `line-clamp` excerpts on guide/glossary cards) and are reported with their boxes in the session log, not as findings. **The in-app fixture matrix (longest insurer, longest asset, empty, all-expired, failed run) did not run this round** — Round 2.

**Guards (§7), proven red first.** One file, one universe (`app/ components/ lib/`, comments stripped with line numbers preserved, legal/consent/admin/model-facing files out). Probes: `tests/fixtures/guard-probes/voice-{lexicon,advice,register,number-claims}.ts.txt` — copied to `lib/services/probe-voice.ts`, each turns exactly its own arm red (the number-claims probe turns two, by design). Three false positives were found and fixed while proving: JS `\b` is ASCII-only, so Greek word boundaries use `(?<!\p{L})…(?!\p{L})`; the Latin-run class admitted `'`, so «AI'» counted as a word; block comments were deleted with their newlines, so every reported line number after one was wrong. The length budget pairs each rewrite with its baseline by trigram similarity (the inventory is sorted by text, so ordinal ids shift on every edit); a replacement too different to pair is a NEW string and fails until it is listed in `corpus-additions.json` — six are.

**Adversarial re-read.** All 130 pairs read once as a list. Four corrections: `upgradeHint` had gained «Με Plus ή Pro:» — a plan-feature claim the original never made — now «Με αναβάθμιση:»; the agents page read «όποτε θέλει … όποτε θέλει»; «Γρήγορο ανέβασμα» drifted from the noun the app uses (LEXICON #21 added: noun «μεταφόρτωση», verb «ανεβάστε»); «πληρωμένων προνομίων» → «Τι προσφέρουν τα πληρωμένα πλάνα».

**Halts.** Raised: H-V11. Unanswered and untouched: H-V01 (onboarding singular — now also covers `derive-priorities.ts`), H-V04 (Greek plan names), H-V05 («wallet» ×15 kept), H-V07 (14-day trial line kept). Not reproduced, recorded: H-V08, H-V09.

**Round-2 backlog.** (1) metric 6's remaining 40: loanwords with a Greek spelling («σέρβις», «πάρκινγκ», «στιγμιότυπα οθόνης»), glossed English terms in the guides, `wallet pass`, «Slug (Εσωτερικό)», the mock provider's strings; (2) H-V02/H-V03 render-site verification per string; (3) the in-app fixture matrix at 320/390/430; (4) H-V11's dashboard card render; (5) the one `support@policywallet.com` string once H-V09 is confirmed.

## Round 2 — detail (2026-09-13)

**Audit.** The Step-0 screen definitions (`voice-01-findings.json` → `definitions`, reused verbatim) were run over the Round-2 corpus (`corpus.round2.json` = the Step-0 corpus with the Round-1 ledger applied; admin, legal and model-facing files out). 146 hits outside the onboarding halt. Read one by one: `V4.singular` 27 → 0 findings (every hit is a third-person aorist — «άνοιξε», «ανέβασε», «έλεγξε» — or a `derive-priorities` string under H-V01); `V1.politiki_policy` 29 → 0 (all `{policy}` / `${policy.…}` placeholders); `V1.synergatis` 18 → 0 (D-V06: perk partners, collaborators, freelancers, contractors); `V1.apotypoma` 2 → 0 («δακτυλικό αποτύπωμα» = fingerprint); `V3.superlative` 6 → 0 (`premium` as a key or in `en`; «η μοναδική στιγμή» = "the only moment"); `V3.empty_qualifier` 13 → 1 (`HERO_SUBHEAD`: the calibration anchor's own counter-example, «διαβασμένα και εξηγημένα στα ελληνικά»); `V5.calque` 2 → 1 («για την πιο γρήγορη εμπειρία»); `V9.advice` 2 → 0 (both negations); `INV.reassurance` 5 → 0 applied, 1 halted (H-V13); `INV.score` 24 → 1 Blocking applied + 2 halted (H-V12), the rest dead strings or the glossary explaining the removal; `INV.severity_fact` 15 → H-V02 (verified live, Blocked). The screen is case-sensitive and missed the public «Σκορ Προστασίας 87/100» — found by grep, the same hole as `score-containment`. The fixture matrix then surfaced a V8 the screen had no definition for: «σε λιγότερο από 30 δευτερόλεπτα» on the wallet empty state and «σε λιγότερο από 2 λεπτά» on account creation — the guard knew only «1 λεπτό».

**Applied (14).** Blocking: the public score row → «Ευρήματα με πηγή» (D-V07); the two timing promises removed. Minor: `HERO_SUBHEAD` → «…τα διαβάζουμε και σας τα εξηγούμε σε απλά ελληνικά» (V2+V3, +13 %); «— είναι ο πιο γρήγορος τρόπος» (V5). Metric 6: «σέρβις» ×5, «στιγμιότυπα οθόνης» ×2, «μικροτσίπ», «ετήσιος προληπτικός έλεγχος» (hero slide), «Διακομιστές στην ΕΕ», «Χρήση tokens AI», «Λείπει ο κωδικός ή το email». Every rewrite within budget (max +13 %); one low-similarity replacement reviewed in `corpus-additions.json`.

**Measurement (§6).** Strings changed 14 (public 9 · in-product 5); mean Δ public +2.8 ch, in-product −6.6 ch; max Δ +13 ch. English-in-el 40 → **29** (ceiling pinned at 29; the 29 are glossed English terms in the guides, brand and standard names not yet allowlisted, `wallet pass` under H-V05, «Slug (Εσωτερικό)», the mock provider). Claims without a row 0; advice verbs in platform voice 0; register drift outside onboarding 0. Public render: `/` and `/solutions/agents` at 320/390/430 on this tree — zero horizontal overflow, nothing past the right edge, the new hero subhead unclipped. **In-app fixture matrix (§5), `tests/measure/dashboard-voice-matrix.spec.ts`, evidence `docs/evidence/voice-01/round2-matrix.json`:** one complete run — 4 portfolio states (empty · typical with a failed run · heavy with the longest real insurer names and a failed run · all-expired) × 320/390/430 × `/dashboard` `/wallet` `/protection` `/recommendations` = 60 page renders (a fifth route, `/settings`, was a harness mistake — the page is `/account`; corrected in the spec); 14 distinct rewritten strings found on the pages, **0 clipped, 0 past the right edge, max horizontal overflow 0 px**. The run is also what surfaced the «σε λιγότερο από 30 δευτερόλεπτα» claim on the empty wallet. Three re-runs to cover the five Round-2 in-app rewrites failed before rendering: the local-dev session pooler is held by a parallel session's `next dev` tonight, and the fixture writes hit `Timed out fetching a new connection` / `Can't reach database server … :6543` at provisioning, so **the Round-2 in-app rewrites are not matrix-verified** — Round 3 re-runs the spec (it now reads every round's ledger).

**Guards.** No new arm. `claims-guard` widened (probe proven red on the new line). `hero-slides-rotation-regression` baselines re-captured for the one changed slide line (four files, one line each); `design-token-debt` decremented for the removed `#a7f3d0` span.

**Adversarial re-read.** The 14 pairs read as a list; the hero subhead checked at 320 in the browser. Nothing reopened.

**Halts.** Raised: H-V12, H-V13. Verified and recorded under H-V02 (live) and H-V03 (per string: two live, four dead, one public — removed). Standing, untouched: H-V01, H-V04, H-V05, H-V07, H-V11.

**Round-3 backlog (final round).** (1) metric 6's 29 — allowlist the brand/standard names, decide the glossed-term convention for the guides; (2) the dead score strings and `settings.riskReview.scoreThen` need a code deletion, filed under H-V12; (3) H-V11's dashboard card render; (4) close the series: `BACKLOG.md`, `STATUS.md`, `CLAUDE.md`.

## Round 3 — detail (2026-09-13, final)

**Audit.** The Step-0 definitions re-run over the corpus with Rounds 1–2 applied, plus a case-insensitive score pass. Outside the onboarding halt the hits are the Round-2 set already dismissed with a reason (V4 27 third-person aorists, V1 29 placeholders, «συνεργάτης» 18 in its business senses, V3 12 «στα ελληνικά έγγραφα/ύδατα», …) — and 9 the case-sensitive screen had hidden: the glossary's «Σκορ Προστασίας» entry and two guide links to it. The entry's definition says the score was removed in Aug 2026; its `howToCheck` said «εμφανίζεται στην αρχική σας οθόνη» and its three FAQ answers spoke in the present tense. **Blocking V8, public**, applied in both languages (D-V08): past tense, and `howToCheck` now names what the home screen shows instead. The guide links («Σκορ Προστασίας» → `/lexiko/skor-prostasias`) are the entry's name and stay.

**Applied (15, all public).** The glossary entry (8 fields). The last loanwords: «διαφημιστική κατάρτιση προφίλ» (profiling), «δάνειο ή χρηματοδοτική μίσθωση» (leasing), «χώρος στάθμευσης σκαφών» (parking), «ηλεκτρονικό κατάστημα» (e-shop), «λίστα συσκευασίας» (packing list — the gloss was trimmed to stay under 15 %), «έναν δίσκο στο cloud», «τα σέρβις», «προληπτικό έλεγχο» (check-up). Every rewrite within budget (max +11 %).

**Measurement (§6).** Strings changed 15; mean Δ −0.1 ch; max Δ +16 ch (+5 %). English-in-el (metric 6) 29 → **1**: the guard now strips glosses (D-V09), blanks nested `${…}`, allowlists brand/standard tokens (cloud, phishing, cyberbullying, Level, eu-west-, XXXX) and excludes the staff console's `admin.*` keys, `lib/notifications/settings.ts` (admin-only importers), the mock AI provider and the insurer alias table; the one remaining line is «wallet pass» under H-V05. Claims without a row 0; advice verbs in platform voice 0; register drift outside onboarding 0. Public render: covered by the Round-1/2 harness; the glossary page is a text column. **In-app fixture matrix, Round-3 run (`docs/evidence/voice-01/round3-matrix.json`):** empty · typical (failed run) · heavy (longest real insurer names, failed run) × 320/390/430 × `/dashboard` `/wallet` `/protection` `/recommendations` `/account` = 45 renders against the patterns of all three ledgers (122); 14 distinct rewritten strings on the pages — now including the Round-2 in-app lines («Ανεβάστε ένα PDF και η AI εξάγει…», «Κρυπτογράφηση AES-256 · Διακομιστές στην ΕΕ · GDPR») — **0 clipped, 0 past the right edge, max horizontal overflow 0 px**. The `all-expired` state failed at its fixture write again (`Can't reach database server … :6543` on the shared local-dev pooler); its Round-1-string evidence stands in the Round-2 file. The spec now dates its evidence file so a run cannot overwrite an earlier round's measurement.

**Guards.** No new arm. All four probes re-proven red on their own arm after the universe change. Series total: 8 arms in `tests/unit/voice-guards.test.ts`, one probe file per arm group, the matrix spec.

**Adversarial re-read.** The 15 pairs read as a list. Nothing reopened.

**Halts.** None raised. Standing: H-V01, H-V04, H-V05, H-V07, H-V11, H-V12, H-V13 (→ `BACKLOG.md` B-V01…B-V08).

## Series close (2026-09-13)

Three rounds, none clean — each full audit pass produced findings, and the hard stop applies. Across the series: **165 strings rewritten** (Round 1: 136, Round 2: 14, Round 3: 15; public 43 · in-product 112 · outbound 10), every one within 15 % of its baseline but three listed bare-label locks (D-V01); **no claim added, one public score claim and three timing claims removed, no legal/consent string touched, no `en` copy except the D-V08 truth fixes**. English-in-el 177 → 1. Eight guards and a fixture-matrix spec run on every commit. Thirteen items wait on the owner or on code (`BACKLOG.md`). The rule the series leaves behind, now in `CLAUDE.md`: **the product reads and explains; the licensed partner advises. Copy that blurs the two is a regulatory defect, not a tone problem.**
