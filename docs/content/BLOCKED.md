# PW-CONTENT-01 — blocked items

Each entry names the exact human task that unblocks it.

| Id | Item | Blocked on | Exact human task | Filed |
|---|---|---|---|---|
| BL-C1 | **Goals 5–6 are MERGED (`43615847`) and the catalogue is written to production — but PARKED at 29 active.** The owner's go was given and executed on 2026-09-08: archive first, then the 21 new rows written and verified. The deploy then could not run (BL-C3), so the 21 rows were deactivated again to keep production's database matching its deployed code; the 29 active rules are byte-identical to their pre-alignment state. | **BL-C3 only.** Nothing about the catalogue itself is outstanding. | After Actions can run: deploy `43615847`, then `UPDATE gap_definitions SET is_active = true WHERE created_at::date = '2026-09-08'` and check the active-set digest is `8045aa4e46935b149e2864f9a20c502a`. Steps and rollback: [PROD-ALIGNMENT.md](PROD-ALIGNMENT.md). | 2026-09-06, executed-and-parked 2026-09-08 |
| BL-C3 | **GitHub Actions will not start jobs: «recent account payments have failed or your spending limit needs to be increased».** CI on NEW-UI cannot finish, so `deploy.yml` never fires, and a manual `workflow_dispatch` of the deploy fails identically. Nothing has deployed since `55051255` (2026-09-07 20:16Z). | The GitHub account's **Billing & plans**. Not fixable from here. | Owner: settle the payment / raise the spending limit, then re-run CI on NEW-UI (or dispatch «Deploy to Vercel» from NEW-UI) and finish BL-C1 step 2. | 2026-09-08 |
| BL-C2 | **B2C policyholder production smoke (Goal 8.2).** | The owner's policyholder session in the Chrome profile the extension drives; the agent does not sign in with credentials. | Sign in to www.policywallet.gr as the policyholder in that Chrome profile, then say so; the smoke runs the wallet page, /protection and /dashboard. | 2026-09-06 |
