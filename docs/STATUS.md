# STATUS

## Current phase
**Spec v2 roadmap complete — 2026-09-24** (Phases 0–7, #364–#373, all live; readiness re-scored ≈ 83 %, residue in [planning/V2_SPEC_ROADMAP_STATUS.md](planning/V2_SPEC_ROADMAP_STATUS.md) §3). **B2B first release live — 2026-09-22.** [PR #363](https://github.com/moniaros/policy-wallet/pull/363), production code `4673c1f5`, [release evidence](evidence/agent-workspace/release-2026-09-22.json). Full accepted plan remains in progress; [scope and backlog](audits/agent-workspace-implementation-2026-09.md).

## Done
- **Prevention ↔ benefits (2026-09-24, `feat/prevention-benefits`, NOT deployed):** blue Benefit Reminder per health policy, worded by evidence («αναφέρει» only with a verified citation, else «χρειάζεται επιβεβαίωση»; silence «δεν καταγράφηκε»), next steps the evidence supports (terms, coordination centre as recorded, advisor who can already see the policy; never booking), the person's own intent + a date they pick driving a daily reminder scan (the 15 Jan blast is retired); a daily general habit nudge (in-app, opt-in push); consented, minimised health-snapshot sharing with one advisor, logged views, revoked with the relationship. Age/sex screening calendar and risk-derived «ρωτήστε τον γιατρό» list RETIRED. Security: `createThread` now refuses a policy the advisor cannot see. Migration `20260924120000_prevention_benefits` on DEV only. Journey spec `tests/measure/wellness-prevention.spec.ts` green at 320/390/430.
- **Spec v2 Phase 7 (2026-09-24, `feat/spec-v2-phase7`):** private policy notes (`policy_notes`, migrated dev then prod, checksum-matched; erased + exported), compare-with-previous card from the two most recently read documents of the chain, Pro-only printable portfolio report (`/api/v1/me/portfolio-report`), printable/shareable digital card (`/wallet/[id]/card`), and post-checkout activation polling. The spec v2 roadmap is complete; what was deliberately not built is listed per phase in the roadmap doc.
- **Spec v2 Phase 6 (2026-09-23, `feat/spec-v2-phase6`):** passkey sign-in on the sign-in page (identifier-first; assertion → `generateLink(magiclink)` → `verifyOtp` on the SSR client, no custom JWT; hidden while `PASSKEYS_ENABLED` is off in prod), 30-day session cookie on all three Supabase clients, `/offline` + `/api/v1/me/offline-card` (the document-stated emergency numbers and end dates, cached network-first by the push worker, age stated), GDPR trust line on signup, two-step account deletion. Owner: turn on `PASSKEYS_ENABLED` after a dev soak; align the Supabase refresh-token lifetime with 30 days.
- **Spec v2 Phase 5 (2026-09-23, `feat/spec-v2-phase5`):** family sharing on `WalletMembership` (owner's accepted, email-bound invite; not a CustomerRelationship). One new arm in `computePolicyAccess`: an active member reads/writes/analyses every policy not `privateToOwner`, never deletes. `/account/family` (invite on paid tiers, section visible to all, remove/leave both-sided and announced), per-policy private toggle on the edit form (owner only), family rows on the member's `/wallet`, policy-scoped notifications mirrored to members. Migration `20260923170000_family_wallet` on dev and prod, checksum-matched, RLS on; erased both directions, exported, ROPA/DPIA regenerated. Dashboard counts stay own-only by design.
- **Spec v2 Phase 4 (2026-09-23, `feat/spec-v2-phase4`):** `/wellness` — annual check-up tracker per health policy (status the person sets; «book» is a tap-to-call on the coordination centre the policy records), a nine-question self-assessment scored by a fixed table (`lib/wellness/scoring.ts`, never a model) behind explicit consent recorded on the row, and a preventive calendar keyed off the assessment's age band and sex (CLAIMS C16). Two Art. 9 stores (`health_benefit_usages`, `health_risk_assessments`, migrated dev then prod, checksum-matched): erased, exported, read by no admin surface. Dashboard check-up nudge only when a policy's reading STATES the benefit; `benefit_reminder` yearly job (15 Jan). ROPA/DPIA regenerated.
- **Spec v2 Phase 3 (2026-09-23, `feat/spec-v2-phase3`):** renewal ladder gains the day-3 rung (free tier stays at 30 by the 2026-07 decision); `green_card_expiry` calendar reminder 30 days out inside the daily renewal-check (one per policy per expiry date); `enfia_season` yearly prompt (1 Nov cron, one per owner, a prompt to check — never a verdict); push opt-in offered on the onboarding advisor screen, and on iPhone it says to install to the home screen first instead of spending the one-shot prompt. Grace-period alert is the existing `renewal_overdue` (critical, transactional) — no duplicate event.
- **Spec v2 Phase 2 (2026-09-23, `feat/spec-v2-phase2`):** `professionalLiability` extraction block (profession, claims-made, retroactive date, per-claim/aggregate limits, ERP, defence costs) filled for `liability` / `professional_liability`; five authored rules on document-stated inputs only — `motor_insured_value_below_market` (value_drift below), `retroactive_date_not_recorded`, `liability_limit_not_recorded`, `marine_navigation_limits_not_recorded`, `marine_lay_up_terms_not_recorded` — with trace cases, content-map titles and `under_review` provenance. Catalogue 55 active on dev at fingerprint `33a0731577205d72`; the five prod rows were inserted PARKED, activated after #366 deployed (3569d502), and prod re-fingerprinted from JSON rows: `33a0731577205d72` = dev = repo. NOT authored, by the document-stated rule: construction-cost index, deductible-vs-savings, specialty settlement and breed vet-cost benchmarks, and the tender check (needs a new array operator).
- **Spec v2 Phase 1 (2026-09-23, `feat/spec-v2-phase1`):** upload wizard (type tiles → file/camera → stepped reading state → extracted-fields check with edit → analyse/share CTAs); branch quick-fact chips on the wallet card from one resolver (`lib/wallet/quick-facts.ts`, never from silence); wallet grouped by category with sort (renewal/insurer/type), insurer filter and an owner-typed nickname (`policies.nickname`, migrated dev then prod, checksum-matched). NOT done, by recorded decisions: coverage tabs stay stacked (CoverageTabView rationale), no dashboard upload FAB / AI-insight card (§11 gated-duplicate measurement), no savings badge (the figure mixes model estimates with rule results and no surface publishes it as a number yet), no support chat (no backend).
- **Spec v2 Phase 0 (2026-09-23, `fix/spec-v2-phase0`):** RevenueCat rows keyed per subscriber; proxy role gate reads `app_metadata.roles` and honours the active-role cookie; share re-use/revive instead of duplicate or orphan grants; one invite-redemption core (`lib/invites/redeem-invite.ts`) so typed codes respect direction; analysis polling backs off and stops at 5 min; QStash failure callback marks abandoned runs failed; advisor-added documents notify the owner and are badged. Dev and prod checked: 0 duplicate grants, 0 RevenueCat rows, 0 grants on ended relationships.
- Action-first dashboard, consistent portfolio scopes/counts, honest review states, active coverage, localized templates, serial multi-file intake and versioned extraction reuse.
- Private suggestions/feedback/revisions, exact-content approval and idempotent in-app delivery; ended-relationship access controls; unsaved feedback recovery. Communication workspace enabled; independent second-provider verification off.
- Identical additive migrations verified dev then prod: 20 columns, RLS, 2 FKs, exact checksums. Production deployment READY on www/app/apex aliases.
- 7,608 unit tests / 664 files; 51 agent/responsive cases without retry; build, types, lint, API inventory (108), i18n/UTF-8, private-material, migrations and catalogue checks passed locally. Hosted production build passed.
- Live dashboard and policy workspace verified. Synthetic private draft saved, archived before deletion and absence checked after reload. No production message sent.

## In progress
Durable batches/document chains, page/OCR artifacts, incremental assessments, targeted verification/economics, proposal integration and full 19-PDF accuracy/cost benchmark. Dates remain unpopulated in document chains; legacy English and nested agent-copy issues remain.

## Blocked
GitHub Actions cannot start (billing/spending limit); Vercel Git integration targets an inaccessible team. Release used verified local checks and authenticated CLI against the correct project. Sentry source-map auth token missing.

## Top risks ranked
0. **Prevention branch awaits merge:** prod migration `20260924120000_prevention_benefits` is applied only at deploy time; `EXTRACTION_CITATIONS` decides whether any benefit reads «αναφέρει» — without it every card says «χρειάζεται επιβεβαίωση».
1. **Spec v2 residue** ([planning/V2_SPEC_ROADMAP_STATUS.md](planning/V2_SPEC_ROADMAP_STATUS.md) §3, ≈83 % ready): `PASSKEYS_ENABLED` still off in prod, benchmark rules wait for dated reference data; still open: `AccessGrant` has no DB-level uniqueness (app-level dedupe only), `PolicyStatus` lives in three divergent lists, and Task 0.7 (GitHub Actions billing, Vercel Git team, Sentry token, 4 critical advisories) is owner-only.
1. **Security debt:** unchanged dependencies report 35 advisories, including 4 critical; triage and patch promptly.
2. **Full-plan release gate:** OCR/verification accuracy/costs unbenchmarked; six-field agreement is not measured accuracy. Independent verification stays off.
3. **Functional/UX debt:** durable processing, document-chain completion and all-channel approval integration unfinished; legacy English remains.

## Next 3 actions
1. Triage dependency advisories and repair CI/Vercel Git/Sentry configuration (spec-v2 Task 0.7); then the spec-v2 «not delivered» residue (reference-data rules, pdf-lib with a Greek font, PASSKEYS_ENABLED soak).
2. Implement durable intake with explicit document targeting/history and derived-artifact lifecycle.
3. Complete page-referenced 19-PDF ground truth and accuracy/cost evaluation before broader AI rollout.

Before further commits/pushes: `audit:api-auth`, `lint`, `type-check`, `verify:migrations`, `lint:i18n-changed`, `lint:utf8`, unit/build and relevant journeys.
