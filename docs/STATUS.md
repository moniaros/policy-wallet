# PolicyWallet — Project Status

_Living dashboard — not a log. Updated at the end of each session with meaningful work. Keep it under one screen._

**Last updated:** 2026-07-25 — insurance-correctness audit on `claude/ui-foundation-audit-gtm05i` (**NOT merged, NOT deployed**). Latest: single-select choice buttons (onboarding goal / questionnaire answers / proposal decline) lacked `aria-pressed` — added across 3 surfaces, guarded (RiskProfileWizard sound via ChipToggle). Prior: onboarding step-3 "ready" contradiction fixed; client Zod-message class CLOSED; password-recovery Zod localised. Prior: Pet card SOUND (branch family complete); Life/Home cards SOUND; policyholder home dashboard SOUND. **Convergence note:** the customer-facing insurance-content surfaces are auditing consistently SOUND; genuinely-unaudited areas left are largely environment-gated (mobile-viewport/E2E need Playwright) or intentionally out of insurance-correctness scope (admin console, English by design). Older detail in [status-archive-2026-07.md](status-archive-2026-07.md).

## Current phase

**Production is live** at policywallet.gr. `NEW-UI` is at `65183b7`; the last deploy this doc recorded was `97f908b`, so **`65183b7` may not be deployed — verify before assuming prod matches `NEW-UI`.**

Everything since sits on **`claude/ui-foundation-audit-gtm05i`** — a continuous insurance-correctness audit, 287 commits, unmerged and undeployed pending review. All guardrails + 2257 unit tests (269 files) + prod build green at every commit; Playwright not yet re-run against the branch.

⚠️ **Branch-name correction.** `fix/coverage-insights-verdict` is a **stale pointer** at `65183b7` (= `NEW-UI`); commit messages on this branch that name it are misattributed — all work is on `claude/ui-foundation-audit-gtm05i`. Delete the stale pointer.

## Done — this branch (newest first, grouped)

- **Single-select choice buttons conveyed their selected state only visually — no `aria-pressed` (a11y class, 3 surfaces).** Choice buttons that carry the user's actual answer/selection showed the chosen option only with a filled/`border-primary` style, so a screen-reader user couldn't tell which was selected: the onboarding **goal** picker ("what matters most to you?" — the first screen a new user sees), the **questionnaire** boolean + select answers, and the **proposal decline-reason** chips. Added `aria-pressed={isSelected}` to all three (the codebase already uses `aria-pressed` in ~9 components, so these were inconsistent omissions). `RiskProfileWizard` verified SOUND — it uses `ChipToggle`, a real `sr-only` checkbox that announces its state natively (a better pattern than aria-pressed). Guarded (mutation-tested) across the three files.
- **Onboarding step-3 header claimed "Your first analysis is ready" while the analysis was still queued/running (contradiction on the first-run screen).** The analysis step's header subtitle said "Your first analysis is ready" **unconditionally**, but analysis queues (QStash is the active prod route), so the body simultaneously said "Analysis will complete in a few minutes" (queued) or "AI is analyzing your policy…" (in progress) — the header contradicting the body on the very first experience a new user has. Made the subtitle state-aware: "Preparing your analysis…" while loading, "Your first analysis is ready." only when `status === "completed"` (or no upload), else a neutral "Here's the current status below." Guarded (mutation-tested). **Also verified (class CLOSED):** only three client forms use `zodResolver` — SignupForm (sound, key+map pattern), reset-password and forgot-password (both localised this branch); no other client form renders hardcoded Zod messages, so that class is fully closed.
- **Password-recovery pages showed hardcoded ENGLISH Zod validation errors to Greek users.** `reset-password` and `forgot-password` render Zod messages verbatim (`errors.*.message`), and the schemas hardcoded English: "Use at least 8 characters", "Passwords do not match", "Please provide a valid email address". So a Greek user resetting their password saw English validation errors on the recovery flow. `lint:i18n-changed` misses these — Zod `.min(n, "…")` / `.email("…")` message args aren't JSX/toast literals (a fresh variant of the blind spot). Fixed both via a **lang-aware schema factory** (`buildResetSchema`/`buildForgotSchema`) built with `useMemo` from localised copy; `SignupForm` was already sound (it uses the key-plus-`getZodError`-map pattern). Guarded (mutation-tested). **Two of the repo's own guardrails caught my fixes mid-flight** — the React-Compiler eslint rule rejected a `useMemo` whose deps didn't match its `t`-closure, and my earlier full-tree i18n guard flagged the `el ? '…' : '…'` ternary I reached for; resolved both with a module-level bilingual const indexed by `lang`. **Also verified SOUND:** the CollaborationTimeline advisor↔client composer — after sending a private (agent-only) note it resets `isPrivateMessage` to false, so the next customer-intended message can't silently go out as private (the real failure mode, handled).
- **Policy-detail branch guidance cards (`BranchGuideCard` / `BranchActionsCard`) audited SOUND; image-alt a11y class verified clean.** `BranchGuideCard` is excellent educational content ("what you should understand about this line") with the correct proportionate framing yet again: a common gap the engine **actually detected for this user** gets an amber "detected" chip (data-backed, personalised — comment: "the gap engine actually flagged this concept for this user's portfolio"), while merely-educational gaps stay neutral grey — amber (not alarming red) for a real detected concern. Accessible (`aria-expanded`/`aria-label`, keyboard, collapsed-by-default), i18n-clean (strings resolved by the caller). `BranchActionsCard` resolves **answered** actions from real policy data (the opposite of the fabricated "Coverage Highlights" removed earlier), gates paid CTAs honestly (lock chip → upgrade, never hides the feature), never gates self-assigned tasks, `aria-hidden` icons. **Also verified (class clean):** a tree-wide sweep for `<img>`/`next/image` without `alt` across customer components found none — every image carries alt text (the two flagged were a filename-alt and code comments).
- **Pet coverage card — audited SOUND; completes the branch-card family, and VALIDATES the red-vs-neutral discipline applied to the gap/exclusion fixes.** `PetCoverageDetails` shows **leishmaniasis** cover (a sandfly-borne dog disease endemic to Greece — genuinely local insurance knowledge) in **red when NOT covered** (an important, locally-relevant gap, like earthquake for homes), but **direct-vet-payment unavailable** in **neutral grey** (a minor missing feature); annual-limit usage bar reddens only past 80% used (running low is a real concern). So the app already distinguishes *important missing cover → red* from *minor missing feature → neutral* — the exact principle by which the AnalysisCard gaps and CoverageTabView exclusions were neutralised (those over-alarmed uniformly; these signal proportionately). The five branch cards (Motor / Health / Life / Home / Pet) are now all audited: sophisticated, locally-aware, compliance-careful, with correct proportionate risk-communication and the raw-money class closed across all of them.
- **Branch coverage-detail cards (Life / Home) — audited SOUND (life-underwriting + compliance lens); no change.** `LifeCoverageDetails` is exemplary IBIP presentation: death benefit leads (the figure that IS a term policy), and every required investment-linked disclosure is present and correct — a **past-performance** note on the fund's YTD growth ("not a reliable indicator of future results"), a **tax caveat** on tax-free-at-maturity ("this is not tax advice; depends on the law in force at maturity"), a **unit-linked risk** note, a **surrender warning**, and beneficiary/surrender glossary hints. `HomeCoverageDetails` shows missing catastrophe perils (fire/earthquake/flood) in red — verified as *defensible* risk signalling, NOT the over-alarming pattern fixed for gaps/exclusions: these are three high-importance perils, and "no earthquake cover" in seismic Greece genuinely warrants a flag (consistent with the questionnaire treating earthquake as a key concern). Distinct from uniform-red-for-all-severity-gaps. With Motor (documented Green-Card date fix) and Health (money-format fix this branch), the branch-card family is collectively strong and compliance-aware; the raw-money class across all five is closed (shared formatter).
- **Policyholder home dashboard — audited SOUND (compliance + underwriting lens); verified, not assumed; no change.** The core customer screen (`PolicyholderHome` → `StatTiles` → `ScoreMethodology`). Protection-score presentation is textbook: `—` not `0` for no score ("no score" ≠ "scored zero"), an amber **provisional** label when it's the fallback formula (I checked it's actually rendered, not a passed-but-dead prop — the failure mode I found on the team role icon), per-state sublines (its own label, never a verdict under a nonexistent score). `ScoreMethodology` is a native `<details>` (keyboard-accessible, works pre-hydration, 44px target, focus ring) rendering the real methodology + an explicit **limits** line ("does NOT assess premiums, insurers or wording quality") + a **not-advice** line ("an informational indicator… not personalised insurance advice. For a recommendation… speak to a licensed insurance intermediary" — IDD-appropriate). Premium is the audited footprint (currency-safe + excluded-note), colours on the app ramp, decorative SVG `aria-hidden`. Heavily-audited already (documented prior fixes); confirmed the compliance-critical score card surfaces every prop it's given.
- **AI-consent flow audited SOUND (compliance); one microcopy fix on the standalone approval page.** The `AiConsentModal` is exemplary GDPR Art. 9 capture: body explicitly names health data + purpose + recipient + "withdraw any time"; server record is the enforcement anchor (never proceeds optimistically — `onConsented` fires only after `POST /api/v1/consents` succeeds); versioned consent (`policyVersion`) + audit `source`; accessible (`aria-labelledby`/`describedby`); explicit Accept/"Not now" with no dark pattern or pre-checked box; fail-closed. The customer approval page reuses the same modal. **Fixed:** the standalone approval page (reached via the advisor's request link, then redirects to the dashboard) toasted `aiConsentTitle` ("Consent to AI analysis" — a heading) as its success message, leaving a customer who just granted a significant privacy consent unsure it worked. Added a dedicated `aiConsentSaved` ("Your consent has been recorded.", el «Η συγκατάθεσή σας καταχωρήθηκε.») and toast it instead. The in-flow consenters correctly need no toast (analysis starting is the feedback). Guarded (mutation-tested).
- **AnalysisCard rendered EVERY gap in alarming red, ignoring severity — contradicting the app's documented neutral-gap principle.** The live gap display on the wallet analysis tab and the agent-policy page (`AnalysisCard`) showed every detected gap in a red container with a red-500 AlertTriangle and red title/text, regardless of the gap's `severity` — so a low-severity finding looked as critical as an uninsured compulsory line, over-alarming the customer and losing the priority signal. The canonical `GapCard` is explicitly *"neutral by design — severity values are unvalidated"* (documented in its source); `AnalysisCard` was the outlier (same red-vs-neutral inconsistency as the earlier exclusions fix — an alarming presentation contradicting a documented neutral principle on the same product). Neutralised the gap card to the GapCard surface (neutral container/icon/title/text; the recommendation keeps its amber highlight). Guarded (mutation-tested), scoped to the gap-render region since the file legitimately uses red for failed-analysis states. (tsc/build caught a JSX-sibling slip from the doc comment before the fix landed — moved it above `return`.)
- **Hover-only row actions were invisible to keyboard users (a11y class, 3 screens).** Several list rows reveal their action buttons only on `group-hover` (`opacity-0 group-hover:opacity-100`), with no keyboard reveal — so the buttons stay focusable while `opacity-0`, and a keyboard user tabs onto INVISIBLE controls (WCAG 2.4.7 focus-visible); touch users get no hover at all. Enumerated the pattern tree-wide: the genuinely-interactive offenders were the **opportunities** row actions (update/view), the **customers** row actions (call/email), and the **questionnaire** template actions (edit) — decorative hover overlays/chevrons/hints on fully-focusable row Links were correctly excluded. Added `group-focus-within:opacity-100` to all three so the actions reveal on keyboard focus too. Guarded (mutation-tested) across the three files. A coherent cross-screen a11y class fixed in one pass.
- **Team screen: member role shown only as an unlabelled icon (a11y + dead `roleLabel`).** On the agency team management screen (`TeamClient`), each member's role — owner / manager / member, a **permission** indicator — was rendered as a bare lucide icon (Crown/Shield/User) with no text and no accessible name: a screen reader announced nothing and a sighted user had to guess which icon meant which role. A `roleLabel` helper existed for exactly this but was **dead code**. Wired `roleLabel` into `roleIcon` as the icon's accessible name (`role="img"` + `aria-label`) and a hover `title`; the inner SVG is now `aria-hidden`. Guarded (mutation-tested). (Rest of the member row is sound — status badge and role/won-value are localised; `fmt` is locale-correct.)
- **Raw-enum class verified CLOSED in user-facing data; one demo-money localisation on the landing mock.** Ran a full-tree scan for enum fields rendered directly in JSX (`>{x.status}<`, `: x.enum}` fallbacks, etc.). The hits were false positives (column-header labels like `columns.type`/`t.status`) or admin (intentionally English) — no remaining raw-enum leak in real user-facing data (the recent collaboration-timeline and client-overview fixes were the last live ones). The one genuine residual: `AgentWidgets` (the marketing landing's agent-dashboard mock) localised its row labels via `t(...)` but hardcoded demo premiums as `"€450/yr"` (English money + English "/yr"), so a Greek visitor saw "Αυτοκίνητο … €450/yr" — Greek and English mixed in one row on a first-impression credibility surface. Made the demo premiums bilingual (el: "450 €/έτος"). Guarded (mutation-tested). Minor, but on the landing that sells the product to agents.
- **Client-overview conversion pill leaked the raw `high/medium/low` enum to Greek agents (score-null fallback).** The agent's client-overview "Identified Gaps" pill shows the opportunity conversion score, falling back to the likelihood when there's no numeric score. `conversionLikelihood` and `conversionScore` are BOTH independently optional (`components/agent/types.ts`), so the fallback is reachable — and it rendered the raw English `"high"/"medium"/"low"` enum. The sibling `OpportunitiesClient` already localises it via `t.agentPages.opportunities.likelihood[...]` («Υψηλή»/«Μεσαία»/«Χαμηλή»); the overview tab now uses the same. Same raw-enum class. Guarded (mutation-tested). **Rest of `ClientOverviewTab` / `ClientDetailView` audited SOUND** — the relationship "health score" is exemplary: it carries the disclaimer *"Combines cover, account activation and recent contact. It is not an assessment of insurance adequacy."* and its captions are relationship language ("Actively managed" / "No recent contact"), not the coverage-verdict framing a prior fix removed; severity colours match the app ramp; conversion-likelihood high=green is correct (high conversion is good for the agent, unlike task priority); `branchLabel` uses the taxonomy; tablist has real a11y semantics.
- **Agent's customer-policy page showed FABRICATED "Coverage Highlights" — an unverified 24/7-assistance claim with a green check on every policy.** The agent→customer→policy detail page (`customers/[id]/policy/[policyId]/page.tsx`) rendered a "Coverage Highlights" block with the SAME two static items, green-checkmarked, for **every** policy regardless of its actual cover: "Standard Coverage · Full protection based on policy specifications" (meaningless filler) and "Direct Support · **24/7 assistance via insurer**" (a fabricated coverage claim — not extracted from the policy, and many policies have no 24/7 assistance). Presenting invented cover as a checkmarked fact to the advisor is a serious trust/correctness defect (a claims manager / underwriter would reject it on sight) and squarely on the panel's remove list — "misleading wording", "prototype-like elements", "weak trust signals". Removed the block; the page's REAL coverage — the policy summary, the AI-extracted **structured coverages** (`acordData.coverages`), and the gap analysis — is all shown below, so nothing real is lost. Guarded (mutation-tested): the page must not render the generic `standardCoverage`/`directSupport`/`coverageHighlights` filler. (Found while enumerating the money-render classes tree-wide — the raw premium render on this page was locale-correct; the fabricated highlights beside it were the real defect.)
- **Closed the raw `€{n}` money class tree-wide + pinned it.** After the same raw-euro-prefix render recurred (billing history, health card, recommendations), ran a full-tree scan for `€{` and found the whole remaining population was just **2** sites — the plan prices in `UpgradeModal` (`€{plusPrice}` / `€{starterPrice}`), which hand-rolled the euro instead of using the canonical `formatEur` helper that `PricingComparison` and the pricing view-model already use (and which also normalises non-integer prices to 2 decimals — a latent edge the hand-rolled version skipped). Routed both through `formatEur`. **New durable guard** (`no-raw-euro-money-interpolation.test.ts`, mutation-tested): walks every `.tsx` in `components/`+`app/` and fails on ANY raw `€{…}` — money must go through `formatCurrency` (insurance amounts, localised suffix) or `formatEur` (subscription prices, €-prefix convention). Converts the recurring one-per-surface find into a single permanent tree-wide gate; the tree is now clean. (The €-prefix on subscription prices is a deliberate, consistent SaaS-pricing convention — distinct from insurance amounts — so `formatEur` was the right target, not `formatCurrency`.)
- **AI recommendation cards rendered money as raw English `€{n}` on a Greek-default app.** `RecommendationCards` (the coverage-insights recommendations — an advice-adjacent, compliance-careful surface) showed the indicative premium range (`€{low}–€{high}`) and the order-of-magnitude cost (`~€{cost}`) as raw euro-prefixed numbers with no thousands grouping — while el-GR places € as a suffix ("1.500 €") and every other amount is localised via `formatCurrency`. So the Greek default audience saw "€1500" where the rest of the UI shows "1.500 €". Routed both through `formatCurrency` (0 decimals — these are indicative estimates). Same raw-money class already closed in billing history / health card. Guarded (mutation-tested): no raw `€{…}` interpolation; the range + cost go through `formatCurrency`. **Rest of the recommendation surface SOUND** — `AiDisclaimer` embedded (compliance), matched product framed as "One option on the market" / "Typical premium range" (indicative, not a buy-this recommendation), the order-of-magnitude cost carries an honest "your actual premium depends on your details" caveat, and the priority labels are all tiers (a prior fix removed the "Nice to have" value judgement). (Noted, not fixed: severity palettes differ across components — `CoverageGapsWidget` rose/amber/sky/grey vs `RecommendationCards` red/orange/amber/slate — a design-system harmonisation call, too broad to change unilaterally.)
- **Health coverage card mixed money formats — some amounts with cents, some without.** `HealthCoverageDetails` formatted the annual limit / room & board / out-of-pocket through the shared `fmt` (`formatCurrency`, 0 decimals) but the outpatient limit and per-claim deductible through a **raw `toLocaleString({style:"currency"})`** (2 decimals, hardcoded EUR) — so a "1.500,00 €" outpatient limit sat beside a "15.000 €" annual limit on the same card, and two amounts bypassed the shared formatter. Routed both through `fmt` (0 decimals, like the card's other amounts and Motor's card; Greek health figures are round). Consistency + component-consistency fix; also removes two raw EUR-hardcoded formatters. Guarded (mutation-tested): no money on the card may use a raw `toLocaleString(style:"currency")`; outpatient + deductible go through the shared helper. **Sibling cards verified internally consistent** — Home=2 decimals throughout, Motor=0, Life has explicit `fmt`/`fmt0` for its two cases; Health was the only mixed one.
- **Exclusions framed as alarming RED in the coverage tab, contradicting the app's own documented neutral-exclusions principle.** The policy-detail page renders exclusions in TWO places: the Coverage-breakdown "Not Covered" tab (`CoverageTabView`) and the dedicated Exclusions section (`ExclusionsCard`) — on the same page. `ExclusionsCard`'s documented principle is that "standard exclusions are facts of the contract, not alarms" (neutral grey styling). `CoverageTabView` contradicted it, framing the same exclusions in **alarming red** (red bg/border/text + a red ✗ XCircle) — so standard, universal exclusions (war, wear-and-tear, pre-existing conditions) looked like dangers, and the same content was styled two opposite ways on one screen. An underwriter/claims-director would flag red-flagging normal contract terms as alarmist and misleading. Neutralised the "Not Covered" rows to match the documented principle: grey container + `MinusCircle` ("excluded"), not a red ✗. Covered rows keep their green check ("you have this") — a clean, non-alarming binary. Guarded (mutation-tested): the coverage tab's exclusion rows must use no red palette and the neutral MinusCircle. **Noted (owner-gated):** exclusions still appear in both sections (behind a non-default tab in the coverage breakdown); consolidating to the single richer ExclusionsCard is a product-structure call.
- **Task list coloured HIGH priority GREEN (success) while MEDIUM was amber — inverted risk signal.** The policyholder task list (`TasksClient.tsx`) coloured high-priority tasks with the brand green (`primary-soft`/mint — a success/positive colour) and medium ones amber, so the **most urgent** task looked reassuring while a medium one looked alarming — the opposite of the intended urgency signal, and inconsistent with the app's own severity ramp (`CoverageGapsWidget`: critical=rose, high=amber, medium=sky, low=grey — so amber meant "high" everywhere else but "medium" here). Re-mapped to that ramp: high=amber (the app's high-urgency colour), medium=sky, low=grey, and the priority-summary high count from green→amber. A risk-communication + visual-consistency fix (the label said "high" while the colour said "good"). Guarded (mutation-tested): high-priority must use amber, never the success/brand-green palette, in both the pill and the summary count. **Rest of the task list SOUND** — localised type/priority labels + filters + sort, mojibake-free, proper empty state ("Everything is perfect").
- **Policy detail — exclusions/hero/documents newly opened, audited SOUND (claims + architect lens); a plausible crash path chased to ground and disproved.** The **ExclusionsCard** is exemplary insurance content: exclusions rendered neutrally as "facts of the contract" (not alarms), an inline glossary hint on «Εξαίρεση» (customer education), notable conditions with typed icons + "action required" chips, fine print risk-sorted (critical→warning→info), an incompleteness disclaimer when content is shown — and, critically, an **empty state that refuses to falsely reassure**: "We could not identify any exclusions… that does not mean there are none. Every policy has exclusions — check your wording or ask your insurer." That is exactly the claims-manager-safe framing (a policyholder must never read "no exclusions detected" as "covered for everything"). Hero premium is currency-aware with correct decimals; document dates use a client-side viewer-zone `<time>` (an upload event, not a contractual date); no raw enums/LOB/hardcoded literals. **Due-diligence finding (no defect):** `PolicyHero` and `document-insights` pass a currency straight into `toLocaleString({style:"currency"})`, which throws on an invalid code — and the first source is the AI-extracted `acordData.policy.premium.currency`. Traced it: the AI output is parsed through `AcordDataSchema` (`anthropic-ai.service.ts`), which has **no** `premium.currency` field, so Zod strips it → the read is always `undefined` → the value is always the EUR-controlled DB column or the `"EUR"` fallback. The crash is **not reachable**; the `acordData…currency` read is harmless vestigial code. Verified rather than assumed — no change warranted.
- **Notifications inbox — each row was a `role="button"` that ignored the Space key (a11y).** In the policyholder's notification list (`NotificationsClient.tsx`) every row is `role="button"` (activating marks it read), but the keydown handler fired on **Enter only** — so a keyboard/AT user pressing Space (the natural button key, per the WAI-ARIA button pattern) got nothing, and Space scrolled the page instead. Added Space activation with `preventDefault` (to stop the scroll). Isolated instance — a tree-wide sweep found no other customer `role="button"` with an Enter-only handler. Guarded (mutation-tested): the row's keydown must handle Enter||Space and preventDefault. **Rest of the notifications surface audited SOUND** — fully bilingual (preference catalog, channel labels, empty state, all via a `tr(el,en)` helper), mojibake-safe content rendering, correct read/unread state, and client-side timestamps that correctly render in the viewer's local zone (an event time, not a contractual policy date). (The `title`-vs-`subject` field naming is a harmless server→client rename, verified end-to-end; content localisation remains the separate owner-gated item.)
- **Policy comparison (`PolicyComparison.tsx`) — newly opened, audited SOUND (underwriting lens); no change.** The side-by-side compare is genuinely insurance-literate: highlight direction is underwriting-correct (LOWEST premium, HIGHEST for cover figures — annual limit / sum insured / death benefit); the total-annual-cost card refuses to sum mixed currencies (shows "mixed currency" rather than a meaningless total — same defect the wallet footprint had); premium comparison preserves cents (so €1,104.87 and €1,105.20 don't both render "1.105 €" with one flagged cheapest); expiry uses Athens calendar days (`calendarDaysUntil`); branch-specific rows compare the figures that actually matter per line (motor tier, health annual-limit + hospital class, home sum-insured + earthquake, life death-benefit) via branch-family matching; and it carries an explicit underwriting caveat that "lowest premium" ≠ best value (lower premium usually buys less cover / higher excess). Dialog is accessible (aria-modal, focus trap, labelled close). **One latent note, deliberately NOT fixed:** the "Lowest Premium" insight card sorts by raw `premiumAmount` and defaults the € symbol without the mixed-currency guard its sibling total-cost card has — but this is latent-only (the Greek market is 100% EUR; no caller has a non-EUR premium, the standing conclusion across this audit), so a defensive guard here would protect nobody today. Recording it honestly rather than manufacturing a change.
- **Full-tree i18n cleanup pass — closed the whole remaining hardcoded-literal backlog + pinned it tree-wide.** After the same leak class (English past `lint:i18n-changed`) recurred on ~4 surfaces, stopped playing whack-a-mole: ran the project's OWN checker in full-tree mode (`SCAN_ALL=1`, a mode its authors built for a cleanup pass) to enumerate the entire population. Only **2** remained — the offline/online toasts in `OfflineProvider` ("You are back online" / "You are offline. Showing cached data.") shown in English to Greek users on any connectivity change. Localised them. **Architectural catch:** `OfflineProvider` mounts in the ROOT layout, so it runs on public/marketing routes where the `t` dictionary (TranslationsProvider) is NOT mounted — my first fix (`t.common.*`) broke the prod build's prerender of public pages. Corrected to the root-safe pattern: `language` from the root LanguageProvider + an inline bilingual const (per `docs/design/I18N_CONSUMER_MAP.md`). **New durable guard** (`i18n-no-hardcoded-full-tree.test.ts`, mutation-tested): runs `check-i18n-hardcoded.js --all` and requires ZERO issues, so a hardcoded toast/ternary literal ANYWHERE — not just in the diff — now fails a unit test. This converts the recurring one-per-surface class into a single permanent gate; the codebase's own rules confirm it's otherwise clean.
- **Account settings security audit-trail had a hardcoded English "Alert" badge.** `components/account/Settings.tsx` flagged failed security events (e.g. a failed login) with a bare `Alert` literal while every other label used `t.settings.*` — so a Greek user reviewing their own security log saw "Alert" in English on a trust-critical surface. Now `t.settings.securityAlertBadge` (el «Προσοχή», the app's established caution word). Guarded (mutation-tested): the badge must render the key, both languages must define it, and the Greek value must be actual Greek. (`lint:i18n-changed` scans only CHANGED `.tsx`, so a literal in an untouched file ships unflagged — the recurring blind spot; a duplicate-key `securityAlert` already existed as a section title, so the badge key is `securityAlertBadge`.) **Rest of the settings surface audited SOUND** — registry-driven notification preferences (no invented keys), GDPR Art. 15/20 export, account deletion via a review queue with honest "nothing deleted yet" copy, ConfirmDialog on the destructive action, focus-visible rings, aria-checked switches.
- **Collaboration timeline leaked THREE raw English enums to users (raw-enum class, again).** `CollaborationTimeline.tsx` — the advisor↔client thread view — rendered `{thread.status}`, `{thread.priority}` and the action-status `<select>` options verbatim, so a Greek advisor read "waiting_policyholder", "high" and "in_progress" (raw machine codes) on the collaboration screen. Same class already fixed for the AgentInbox badge, opportunity labels and the activity feed — the timeline was the remaining offender. Extracted a shared `lib/collaboration/status-labels.ts` (thread status ×5 incl. the two `waiting_*`, priority ×3, action status ×4 — all bilingual) as the single source of truth, routed both the timeline and AgentInbox through it (AgentInbox's local map deleted; the prior agent-inbox guard updated to read the shared map), and localised all three leaks. Guarded (mutation-tested): behavioural checks that every status/priority/action label is non-empty el+en and never the raw enum (with unknown-value fallback), plus a source scan that the timeline no longer renders raw `{thread.status}`/`{thread.priority}` or a raw enum `<option>` label. (`lint:i18n-changed` misses these — they're `{expr}` interpolations and `<option>` text, not flagged literals.)
- **Needs-analysis questionnaire collected health data with NO data-handling notice (transparency/GDPR gap).** The system questionnaire templates are excellent, professional Greek-market intake instruments (motor coverage tiers, 1985 seismic code, ΕΟΠΥΥ, Α/Β θέση room class, ΕΚΑΑ/EHIC, «Ισόβια» whole-life — an underwriter would approve). But the health and life templates collect GDPR **special-category data** (pre-existing conditions, medication, smoking, "heart disease, cancer, diabetes"), and the policyholder's `QuestionnaireForm` rendered the questions and a submit button with **no notice at all** of who receives this or how it's handled — inconsistent with the app's strong transparency discipline everywhere else (explicit AI-health consent, provenance disclaimers, the new proposal disclaimer). A compliance officer would flag collecting medical history on a regulated platform with zero privacy/purpose framing. Added a bilingual data-handling note above the submit action ("Your answers — including any health details — are shared with your insurance advisor to assess your needs and are handled under our **Privacy Policy**", linking to `/privacy`). Guarded (mutation-tested): the form must render the note + link to `/privacy`, and both languages must state health-data + shared-with-advisor. (The Greek sentence-case guard caught my Title-Cased link label mid-iteration — fixed to «Πολιτική απορρήτου μας».) **Question CONTENT audited SOUND** — the intake instruments are genuinely insurance-literate; no technical errors found.
- **Public marketing surface (15 product pages + landing + pricing + perks + company) — newly opened, audited SOUND; one owner-gated watch-item.** Scanned for the compliance-risk language a Compliance Officer flags first (savings %, superlatives, guarantees, absolutes) and for the "are you an insurer?" misrepresentation risk. Findings: (a) the product pages are consistently framed as an **analysis tool for your existing policy** ("track your vehicle's value, identify gaps, automate renewals"; CTA "Get Started Free"), never as selling insurance — the misrepresentation risk is well-handled; (b) the content is genuinely insurance-literate (boat: own-damage optional, towing/salvage not always included; pension: guaranteed vs investment component; business: multi-risk sections rarely all bought at once); (c) `PRODUCT_STATS` are defensible — "100%" is about **data control** (backed by the GDPR export/delete infra), "guaranteed component"/"100% network coverage initiates" are accurate insurance concepts; (d) the public **/pricing** page is catalog-driven (`getPlanCatalog()`→`buildPublicPricingContent()`, ISR-revalidated on admin saves) so it can't drift from billing; (e) **/perks** is honesty-guarded — it `notFound()`s until the first partner is activated and is `robots: noindex`, so nothing is promoted today. No bounded current defect → no change manufactured. **Owner-gated watch-item recorded** (see Blocked): the company-page claim "we…promote no products" is accurate now but will need revisiting the moment partner-perks activate.
- **Document-request taxonomy: "Bank Statement" was mistranslated in Greek to an ambiguous "latest bill".** The advisor→client document-request dropdown (`DOCUMENT_TYPE_TAXONOMY`) paired `en: "Bank Statement"` with `el: "Τελευταίος Λογαριασμός"` — but «Τελευταίος Λογαριασμός» ("latest bill/account") is what a Greek reader associates with a **utility bill** («λογαριασμός ΔΕΗ»), not a record of bank transactions. So the two languages requested different documents: the advisor asks for a bank statement, the Greek client sees "latest bill" and could upload a utility bill (or vice-versa) — real KYC/underwriting friction and a wrong-document risk. Fixed to «Αντίγραφο Κίνησης Λογαριασμού» (the precise banking term for a statement of account movements). Isolated (single source of truth, no other occurrence). Guarded (mutation-tested): the `bank_statement` Greek label must denote «Κίνηση» and must not be the ambiguous old string. **Rest of the document-request flow audited SOUND** — the other nine doc types are correctly termed (ΑΜΚΑ, Φορολογική Δήλωση, Ιατρικό Πιστοποιητικό, Άδεια Κυκλοφορίας, Τίτλος Ιδιοκτησίας…), bilingual, urgency framing + camera capture + secure `acceptAttribute`, error surfacing, uploaded/expired states. (`lint:i18n-changed` can't see a wrong-but-bilingual label.)
- **Advisor→client PROPOSAL had no "indicative, not a binding contract" disclaimer (compliance gap).** The proposal card (`ProposalCard.tsx`) shows the client a firm annual premium and an **"Accept"** button, with only the agent's EAEE licence number + "last updated" in the footer — nothing telling the client this is an *indicative proposal* rather than concluded cover. A client could read "Accept" as binding cover and believe they're insured before the insurer issues the policy → an uninsured-loss dispute, and a misrepresentation a compliance officer flags under IDD (Law 4583/2018). The app already applies exactly this "the insurer's terms prevail" discipline to AI outputs (`aiAdviceDisclaimer`), claims (`claimsDisclaimer`) and exclusions (`exclusionsDisclaimer`) — the proposal, a more consequential advisor-originated recommendation, had none. Added a bilingual `proposalDisclaimer` ("indicative proposal, not a binding contract; cover/final premium/terms are governed by the insurer's policy documentation; accepting lets your advisor proceed with the application") rendered above the Accept/Decline CTAs in the client view **and** the agent's preview, for every status. Closes an inconsistency with the app's own pattern, not a new requirement. Guarded (mutation-tested): pins that the disclaimer is rendered to the client AND that both languages carry substantive not-binding / insurer-governs content — so it can't be silently dropped or hollowed out. (Rest of the proposal surface audited SOUND: LOB via `normalizeBranch` fallback, bilingual status/type labels, honest decline+counter-offer flow, `formatCurrencyFull` EUR latent-only as before — the create form has no currency picker.)
- **Advisor client-profile fresh-eyes pass — one English word leaked into Greek copy on the busiest agent screen.** The customer-profile action menu (`CustomerProfileClient.tsx`) showed `createTask: { el: "Δημιουργία Task" }` — the English word "Task" mid-phrase while every sibling item was proper Greek («Αίτημα εγγράφου», «Δημιουργία πρότασης», «Προσθήκη ασφαλιστηρίου», «Ερωτηματολόγιο»). To a Greek advisor that reads as unfinished, prototype-grade copy. The app translates "task" as «εργασία» everywhere else (dashboard `pendingTasks` = «Εκκρεμείς εργασίες», the existing `createTaskBtn` key = «Δημιουργία εργασίας») — so it was an isolated leak with an established correct term; now «Δημιουργία εργασίας». `lint:i18n-changed` can't see it (it flags monolingual strings, not an English word on the GREEK side of a valid {el,en} pair). Guarded: a mutation-tested check walks the agent/customer/collaboration client surfaces and fails on any `el:` string containing English "Task". **The rest of the advisor client-profile surface audited SOUND** — fully bilingual copy, shared Modal/ConfirmDialog with focus traps (replaced hand-rolled overlays + a native confirm on the relationship-severing action), error surfacing on doc-request/proposal sends, loading skeletons, empty states, a "no data deleted" reassurance on customer removal. (Deliberately NOT touched: `AgentPlanGate` keeps tier names "Starter"/"Pro"/"Agency" English while translating "Free"→«Δωρεάν» — a defensible brand-vs-descriptor split, a branding call; and the QuestionnaireSender WhatsApp nudge's «Εκκρεμότητες» vs the actual section label is an unconfirmed wayfinding nuance not worth guessing at.)
- **Manual renewal reminder emailed the customer the WRONG expiry date (Athens-midnight off-by-one).** The agent's batch "send renewal reminders" action (`renewals/actions.ts`) formatted the policy expiry with a bare `endDate.toLocaleDateString(...)` — no timezone — so it rendered in the runtime zone (UTC on Vercel) and showed the PREVIOUS day for any policy ending at Athens midnight. The **automated** `policy_expiring` reminder for the very same policy (`renewal.service.ts`) had already documented and fixed exactly this by routing through `formatDate` (which pins `timeZone: APP_TIME_ZONE`) — so the two reminders for one policy disagreed by a day, and both could disagree with the wallet. In a renewal reminder the expiry date is THE consequential fact; a wrong one is a direct trust hit (Claims Director / Senior Agent lens). Now uses the shared Athens-zone `formatDate`, making the manual and automated reminders identical in both zone and format. Guarded: a mutation-tested check pins BOTH renewal-reminder composers against bare `endDate.toLocaleDateString` and asserts each routes dates through `formatDate`. **Notable:** this defect lived in the notification area I'd previously deferred as "owner-gated localisation" — probing it surfaced a genuine correctness bug, not the localisation-strategy question. (The two customer renewal reminders were already language-localised; `savings-report`'s bare `toLocaleDateString` formats only the "Generated on {today}" stamp, not a policy date — immaterial, left alone.)
- **Agent portfolio-analytics (`/insights`) — newly opened, audited SOUND (CIO/underwriting lens); no change.** The last Stop-hook-named "unopened" surface. Metrics are internally consistent and honestly framed: `totalPremium`/`avgPremiumPerCustomer`/`avgPoliciesPerCustomer` share a numerator and denominator that both span all relationships, and premium is filtered to in-force via `isPremiumBearing` (not the untrustworthy `Policy.status` column); the renewal timeline uses Athens calendar days via `resolvePolicyLifecycle`, consistent with the wallet/renewals (same documented same-day-expiry fix); the opportunity "funnel" bars show real mutually-exclusive current-status counts (normalised to the max, actual values labelled) and "Conversion rate = won/total" is labelled as such. Currency EUR-assumption is latent-only (Greek market; no non-EUR caller) — same conclusion as the `formatCurrencyFull` audit. **One cosmetic nit, not a defect (left as-is):** `page.tsx` passes `language={dbUser.preferredLanguage || 'en'}` but `InsightsClient` explicitly ignores that prop ("kept for prop signature compatibility") and derives `lang` from `useLanguage()` — so the `|| 'en'` is doubly dead (schema default is `el` *and* the prop is unconsumed). Removing it is pure dead-code cleanup with zero customer benefit; not worth a commit.
- **Two defect CLASSES traced to ground and pinned so they can't recur:**
    - *branchFamilyId child-branch matching* — a motorbike/truck/renters/income-protection policy treated as the wrong (or a missing) branch. Fixed across gap engine, protection score, taxonomy, coverage panels, and cross-sell (which was pitching a motorbike owner motor insurance). Swept every raw `lineOfBusiness` comparison; no live instance left. Emitter-side guard pins the coverage matrix to parent branches.
    - *extracted-but-unrendered coverage amounts* — a schema field + i18n label added, panel row forgotten. Six instances fixed (health annual limit / room&board / out-of-pocket; life **death benefit**; motor excess + market value; home rebuild cost; pet annual limit read the legacy alias only). A completeness guard now reads the Zod schema and fails if any amount field is unrendered.
- **Gap/score recompute wired on every policy-data mutation** — the derived gaps + protection score are now rebuilt (for the OWNER, not the editor) after: extraction-review confirm, direct edit, delete, admin edit/delete, merge, batch upload, and onboarding basic extraction. Before, a correction, delete, merge or bulk upload left the customer's gaps and score computed from stale data — most starkly, a new free user's FIRST policy computed no score at all.
- **Renewal-path correctness** — a policy uploaded near expiry emailed the customer five days running (backlog drained one milestone/day); a policy stored `expiring_soon`/`action_needed`/`incomplete` got NO reminder at all because three queries filtered `status: "active"` (now one shared `NON_LIVE_POLICY_STATUSES`); the churn win-back under-counted the same way.
- **Free-tier gap paywall hid the most critical gap** — the €3 lock boundary fell on coverage-area/alphabetical order, not severity, so an uninsured compulsory line could be the one behind the paywall while trivia showed free. Now severity-ranked on both surfaces.
- **Billing: a cancelled subscriber was shown "Billing renews on —".** On cancel, Stripe sets `cancel_at_period_end`, the DB status stays `active` until period end, and `account/actions.ts` nulls `next_billing_date` when `!autoRenew`. But the billing card rendered `{renewsOn} {formatDate(next_billing_date)}` unconditionally — so a just-cancelled user read "Billing renews on —" (the opposite of reality, and alarming). Now branches: renewing → "Billing renews on {date}"; cancelled → "Access ends on {current_period_end}" (new `t.billing.endsOn`, el+en). A fresh re-audit (step 5) then caught that Billing renders for FREE users too — whose placeholder sub has a 30-day period-end — so the ends-branch would imply the free plan expires; gated the whole renewal/end line on `currentPlan.price > 0` (free shows plan name only). Guarded. **Also verified SOUND:** the cancel-confirmation copy is compliance-aware — it states access continues to period end AND surfaces the EU **14-day right of withdrawal with pro-rata refund** (Terms §5). (Pricing↔entitlement audited SOUND: free/Starter get organizer+basic-extraction, Plus gates all deep AI — claims match gates. **Noted gap:** no in-app `past_due`/payment-failed banner — a failed payment shows only as a failed invoice row; adding a banner vs relying on Stripe dunning is a product call.)
- **Savings/branded report was ~90% hardcoded English (client-facing document).** The report generator (both the policyholder savings-report route and the **agent branded-report** route) has a `language` param, but only dates/"Prepared by"/disclaimer followed it — every heading/label was English, so a Greek advisor handed a Greek client a branded report reading "Coverage Snapshot / Covered / Not Covered / Exclusions / Premium / Period" in English. It also leaked the raw `lineOfBusiness` code (Type field) and raw severity enum (gap badge), and hardcoded `€{toFixed(2)}`. Localised the whole document (L helper), resolved Type via `normalizeBranch(...).label`, severity via a new `gapSeverityLabel`, amounts via `formatCurrency`, and `<html lang>` now follows the language. Guarded (no bare-English heading/meta-label; Type/severity not raw). A .ts template, so `lint:i18n-changed` never saw it. **Rest of the email/document class then swept and confirmed SOUND**: all customer emails (churn, engagement, invite, DSR, contact-confirmation) are properly bilingual; `mail-templates`/`form-emails` bilingual; `formatCurrencyFull`'s EUR-hardcoding is latent-only (no caller has a non-EUR currency). **One owner-gated note:** the agent approval/rejection/welcome emails are marked "English-only" but the agent's `preferredLanguage` IS available (`agentProfile.user`) — a Greek advisor gets their approval email in English; overriding that documented choice is a comms decision.
- **Component language props defaulted to English on a Greek-default app.** `PolicyWalletLogo`, `RiskProfileWizard`, `TokenUsageCard` defaulted `language = "en"` — latent (all callers pass it) but a future caller omitting the prop would render English for the primary Greek audience. Flipped to `"el"`; guard sweeps customer components for `language = "en"` (caught the Logo one, single-quoted, that a first grep missed). **Verified NOT a live bug:** `User.preferredLanguage` is `@default("el")` non-null, so the server-side `|| "en"` fallbacks and the analysis orchestrator's `= "en"` default never fire — a Greek user's AI analysis is never English (ruled out a potentially large defect). The RiskProfileWizard itself is sound (appropriate needs-analysis + Art. 9 fields, bilingual).
- **Upgrade/cancel money errors were hardcoded English (systematic sweep close-out).** A systematic sweep of `toast.error(result.error)`/`setError` sites confirmed the wallet flows SOUND (all route through `mapWalletErrorToMessage`, a localiser with a localised `byContext` fallback) — but the account money actions leaked: `upgradeSubscription`/`cancelSubscription` returned hardcoded English ("You are already on this plan.", "Failed to initialize payment", "Failed to cancel…the billing portal.") shown verbatim on the upgrade/account pages, so a Greek user hit English at checkout/cancel. Localised via `acctErr` using `dbUser.preferredLanguage`; extended the auth-errors guard to account/actions.ts. **This closes the `.ts` server-action error class across all customer surfaces** — auth/verify/onboarding (fixed), wallet/collaboration (sound), account (fixed); remaining raw displays are the admin console (English, `i18n-hardcoded-ignore`) and the owner-gated notification content.
- **Onboarding (post-verify funnel step) — audited SOUND; one raw-exception leak fixed.** Invite-code errors are mapped to localised copy client-side; the policy-limit error is localised (`getUpgradeMessage`); the analysis step renders localised copy keyed on status (its English error strings are internal, never shown). **Fixed:** `uploadOnboardingPolicy`'s catch returned `error.message`, which flow.tsx toasts — so an unexpected upload exception surfaced a raw (English, sometimes technical) error to a Greek user on their first policy upload; now returns no message so the localised "upload failed" fallback shows.
- **Email-verification errors were hardcoded English (post-signup funnel).** `verifyEmailToken` returned 5 English errors shown verbatim on the verify-email page — a Greek user whose link expired read "Verification link has expired. Please request a new one." in English right after signup. Threaded `language` from the page (it has `useLanguage`) and localised all five via `vErr`; extended the auth-errors guard to scan `verify-email/actions.ts` too. (Verify-email page itself is otherwise sound — bilingual, loading/success/error states.)
- **Auth error messages were hardcoded English at the signup entry point.** `app/auth/actions.ts` returns error strings the client shows VERBATIM (`setServerError(result.error)`), so a Greek user hit "User already exists" / "Invalid Greek mobile number" / "Registration failed" in English on the first screen. `lint:i18n-changed` only checks `.tsx`, so the server-action English shipped unflagged. Localised all 14 user-facing errors via an `authErr(language, el, en)` helper: `registerUser`/`resetPasswordForEmail`/`resendVerificationEmail` (language already available), and `resetPasswordWithToken` (threaded `language` into its payload from the reset client). Every REACHABLE auth error is now localised — the one remaining bare string is in `updateUserPassword`, which is **dead code** (zero callers repo-wide). Guarded (no bare `error: "English"`; each carries a Greek translation). (Same-pass sweep: `placeholder=`/`title=`/`alt=` attributes clean — only format examples like "POL-123456".)
- **Accessibility: hardcoded English aria-labels on a Greek app.** Seven customer-facing icon-button aria-labels were hardcoded English (notification bell "Notifications"; getting-started checklists "Expand"/"Collapse"/"Dismiss" ×2; NPS "Close"; PWA install prompt) — a Greek screen-reader user heard English. `lint:i18n-changed` doesn't check aria-labels, so they shipped clean. Localised each via its existing mechanism (local `t(el,en)` helper or `useLanguage` keys `t.nav.notifications`/`t.common.close`); a new guard sweeps for bare-English or English-only-braced aria-labels. (Also confirmed sound: no `<img>`/`<Image>` without alt; core Greek terminology consistent — no «πολιτική» mistranslation, the «ασφαλιστήριο»/«συμβόλαιο» mix is valid Greek.)
- **Route resilience gaps — missing loading + error boundaries on policyholder pages.** `coverage-insights` was the only data-heavy route with no `loading.tsx` (blank flash on slow load; multi-await server render), and BOTH the main policyholder `dashboard` and `coverage-insights` lacked a route-level `error.tsx` — so a query failure fell through to the app-wide boundary, tearing down the whole protected shell instead of a contained retry-in-place. The dashboard is the highest-traffic policyholder screen, the worst place to lack one. Added the loading skeleton (a11y-correct `DashboardSkeleton`) and both error boundaries (shared `RouteError`); one guard now asserts every data route ships BOTH a loading.tsx and an error.tsx. (Money/date/error content otherwise re-swept SOUND: `RouteError` is bilingual + Sentry-reported; dashboard/renewals money is currency-aware; sole caveat — `formatCurrencyFull` hardcodes EUR — latent only for the rare non-EUR agent policy.)
- **Billing history: English-format money + ignored invoice currency.** The invoice amount hard-rolled `€{amount.toFixed(2)}` — English format for Greek-default users ("€12.99" not "12,99 €") and it ignored the invoice's own `currency` field (stamped "€" on any currency), while the same row already localised the date and labelled the status. Now `formatCurrency(amount, language, { currency: invoice.currency, decimals: 2 })` — the explicit `decimals: 2` avoids a cents-dropping trap (the helper defaults to 0 decimals → would round €12.99 to "13 €"). Guarded (render test: localised, cents preserved, non-EUR renders its own symbol). Broader currency sweep otherwise clean — no wrong-currency bug elsewhere. **Error boundaries audited SOUND** (shared `RouteError`: bilingual, Sentry-reported, recoverable; 20 routes + global + not-found).
- **Raw extracted dates in the batch-upload review.** The bulk-upload review card formatted the premium but rendered the extracted start/end dates raw (a polished "€1.234,00" beside a raw "2024-06-15T00:00:00.000Z"). Now routed through `formatPolicyDate` (the policy-detail helper; ISO/DD-MM-YYYY/Greek-month aware, "-" when missing); guarded. (Sweep also confirmed `permissions` renders as readable words `view`/`edit`, and other date renders are locale-aware or intentionally ISO+UTC for admin ops — no other date defect.)
- **Raw status-enum leaks — activity feed + agent inbox fixed.** (1) The activity feed built a bilingual message but interpolated the raw English `Opportunity.status` into BOTH languages, so a Greek advisor read «…ενημερώθηκε σε **won**» — new `opportunityStatusLabel` localises it (open/contacted/won/lost/dismissed). (2) The agent inbox rendered the raw thread status ("waiting_policyholder"), and two of the five statuses had **no label anywhere** (i18n only covered open/resolved/closed) — added a complete `THREAD_STATUS_LABELS` map + a guard that every *styled* status must also be *labelled*. Both guarded. Landing-widget status render was a **false positive** (already-localised demo data). **Remaining (noted):** admin policies/DSR status tables render raw — internal English console, lower priority.
- **Raw branch-code leak — swept across ALL user-facing surfaces (new defect CLASS closed).** `lineOfBusiness` is a raw taxonomy code (`income_protection`), and it was rendered verbatim to users in three surface families, all now resolved via `normalizeBranch(...).label` and each pinned by a guard: **email** (renewal title/task + weekly-digest table), **in-app notifications** (policy-added → customer, policy-shared → agent), and **the UI** (renewals table, questionnaire subtitle, batch-upload + proposal fallbacks, policy-comparison line, admin user/policies tables). Three guards (`no-raw-lob-in-email`, `-notifications`, `-ui`) — the UI one classifies by the char before `{` so `value=`/`key=` bindings stay codes, and excludes translation/label objects. Form-control values and React keys correctly remain codes.
- **In-app notification text (newly opened) — raw branch codes fixed; English-only localisation gap flagged.** Notifications render `{event.message}` verbatim (no localisation layer), so two leaked the raw `lineOfBusiness` code — the agent "policy added" notice to the **customer** ("added a income_protection policy…") and the "policy shared" notice to the **agent** ("shared their group_life policy…"). Both resolved via `normalizeBranch(...).label`; guarded (class scoped to `message:`/`title:` fields so AI prompts / dedup keys / DB filters stay untouched). **Broader finding, owner-gated (see Blocked):** these messages are hardcoded **English** and shown verbatim to Greek-default customers — inconsistent across ~18 notification call sites (some use `t.`, most don't).
- **Outbound email content (newly opened) — raw branch codes leaked into customer/agent mail.** Three surfaces interpolated the raw `lineOfBusiness` taxonomy code straight into user-facing text: the agent renewal-alert email title and task title ("Renewal alert: Maria's **income_protection** policy") and the customer weekly-digest renewals table ("group_life"). An underscored machine code in a professional insurance email reads as auto-generated. Swept the class through `normalizeBranch(...).label` (localised for the bilingual digest); pinned by a behavioural guard on the title helper + a source guard sweeping `lib/email/**` and the renewal composer for any `${…lineOfBusiness}` leak. (The renewal EMAIL body/title day-counts + Athens timezone were already sound.)
- **Add-policy first-run + document provenance (newly opened) — provenance sound; empty-state CTA fixed.** Data provenance is exemplary: self-serve users' AI-extracted data is honestly labelled ("read from your document by AI and have not been verified — check them against your policy before relying on them") and the code deliberately gives the fact, not a "confirm" action the reader shouldn't take against an unchecked document. **Fixed:** the zero-policy empty state sells uploading ("Upload a PDF… the AI extracts…") but its single CTA was wired to `onAddManually` while the passed `onUploadDocument` was silently dropped — now opens the upload flow it advertises; guarded. **Still owner-gated** (see Blocked): the "Add details manually" menu item forces a document upload with no manual path.
- **Agent portfolio dashboard + i18n completeness — audited, sound; parity pinned.** The agent dashboard is exceptionally mature (deduped book revenue, real per-line commission not premium/12, suppressed vanity growth %, `MAX_PLAUSIBLE_ANNUAL_PREMIUM` guards, lifecycle-date renewals — all documented prior fixes). i18n audited whole-tree: el and en are in perfect parity (2220 keys each, zero drift) and no untranslated English prose leaks into Greek values. Added a full-tree el/en parity guard (the prior one covered only 6 wallet namespaces) so a one-sided key add can't ship `undefined` to default-language Greek users; mutation-tested. Preventive — no live defect found.
- **Public content graph (glossary / guides / product) — audited, sound; pinned.** The /lexiko glossary (12 terms, answer-first, "D7 honesty" editorial rules) and /guides articles are the AEO/SEO surface and cross-link each other + /product heavily (24 internal links). Verified every /lexiko, /guides, /product cross-link resolves and every in-product glossary hint (`POLICY_HINT_SLUGS`) points at a real term — no dead links. Added a durable link-integrity guard so it can't rot as content is added/removed (mutation-tested with an injected dead link); fixed a stale "14 terms" comment (real count 12). No live defect found — preventive infrastructure on a confirmed-sound surface.
- **Claims + AI-advice-disclaimer surfaces (newly opened) — assessed mature; one contradiction fixed.** The not-regulated-advice disclaimer system is robust and travels with its components: `AiDisclaimer` + "not insurance advice" are embedded in `RecommendationCards` and `RecommendedActionsWidget`, and applied on the wallet, insights, opportunities, customer and coverage surfaces; the score methodology carries the not-personalised-advice line; `GapCard` is neutral (severity suppressed, "recommendation" framing). Claims guidance content is genuinely strong (duty-to-mitigate, "don't repair before documenting", notification windows, no fabricated claims number). **Fixed:** the generic claims **step 4** asserted "Deadlines detected in your policy:" with a dangling colon — contradicting the dedicated deadlines panel's "No specific deadlines were detected" state on the same post-loss screen. Reworded to generic guidance (the real deadlines stay in their data-backed panel); guarded.
- **Billing surface (newly opened) — assessed mature; one consistency defect fixed.** Verified sound and left unchanged: prices come from the admin catalog and equal what Stripe charges (monthly + annual overridden from live facts in `PricingComparison`; the copy literals are only fallbacks); prices are VAT-inclusive and charged as-is (a prior ×1.24 double-charge is fixed and commented); the 14-day trial is per-plan and the trial badge uses `planHasTrial`, matching checkout (`lib/billing.ts`); downgrade/cancel run through the Stripe billing portal, so the FAQ claim holds. **Fixed:** the pricing comparison flagged the *Starter* tier "Popular" — but the "Popular"/"Best value" badge belongs to PolicyWallet **Plus** (the flagship AI tier every other surface recommends), and because Starter has no badge the emphasis ribbon literally rendered the word "Upgrade". Moved the highlight onto Plus; guarded.
- **Signup surface (newly opened) — a false security promise removed** — the sign-up form showed a "Use FaceID after first signup" trust chip on every device (FaceID is Apple-only) although the product has no biometric login: no WebAuthn/passkey flow exists, and the sign-in page's fake "Biometric / PIN" unlock had already been removed for the same reason. Its twin survived because sign-in was fixed and sign-up was missed. Removed; a cross-file guard now asserts no auth screen renders a FaceID/biometric-login promise while no client WebAuthn flow exists. (Rest of the signup form assessed and found acceptable — the "cancel anytime / 90 seconds" reassurance reads as account-level; email-optional + synthetic-email recovery is a deliberate phone-auth tradeoff.)
- **Agent-verification surface (newly opened) — two insurance-trust defects:**
    - *Approved intermediaries never showed as Verified* — the admin review writes `verificationStatus: "approved"` (and the admin console reads it correctly), but every customer/agent-facing reader compared against `"verified"`, a value **nothing writes**. So an admin could approve an advisor, send the approval email, and the "Verified Agent" trust mark still never appeared on the public `AgentCard` a policyholder sees, the agent's own portal, or their settings card (which also leaked the raw untranslated status). New `lib/agent/verification.ts` (`isAgentVerified`/`isAgentRejected`) is the single source of truth; all readers routed through it; guard extracts the literal the admin approve path writes and asserts the readers accept it.
    - *The onboarding licence step promised verification it does not perform* — titled "Verify your license", it told advisors the upload "unlocks Verified Agent status and premium features"; in reality the file is submitted for review and stays `pending` until an admin approves it. It also ignored the upload result and advanced even when the submit failed, silently dropping a compliance document. Now honest copy (submit → review → badge after approval) and a surfaced error that blocks the silent advance.
- **A GATED analysis run was reported as a failed one** — a `blocked` run (deep AI analysis is a Plus feature, or the owner has not granted AI-processing consent; persisted with `status: "blocked"` + a `blockedReason`) folded into the coverage-absence "failed" state on reload, telling the reader the analysis broke and to re-analyse or upload a clearer copy — wrong three ways (it did not fail, retrying reproduces the block, the document is fine). `blocked` now has its own state, split on `blockedReason` (consent → grant consent; else → upgrade to Plus), pointing at the real unlock. The whole state+copy decision is extracted into pure `resolveCoverageAbsence` / `resolveCoverageAbsenceCopy` and pinned by a mutation-tested guard.
- **Also:** the "call your insurer" claims button never rendered on any policy (read a schema-less field); beneficiaries listed as people the policy covers; the dashboard premium total added foreign currencies and hid excluded policies; the root 404 was English-only; notable conditions rendered in raw order (a claim deadline below a no-claims bonus). Plus the earlier gap-engine, GDPR-export, Q&A-prompt, upload-format and outbound-email work — see archive.
- **A near-miss worth keeping:** I misread `premiumAmount` (it is the TERM total, not per-period) and nearly shipped a 12× "annualiser"; caught by the lateral sweep, reverted, and pinned with a contract test.

## In progress

Continuous audit, priority order **insurance correctness → customer trust → professional credibility → clarity → consistency**. The recurring root causes that were generating findings on every opened surface (untrustworthy `Policy.status`, raw child-branch matching, schema/label/render drift) are now closed or pinned. Highest-severity correctness defects on the surfaces opened so far are behind us; remaining work on those is more modest polish.

**The audit is deep into diminishing returns, but not dry** — probing the deferred notification area surfaced a genuine renewal-date correctness bug (above), a reminder that "owner-gated localisation" and "no remaining correctness defects" are not the same claim. The customer-facing surfaces the panel named as high-value — wallet, dashboard, coverage-insights, onboarding, billing, claims, agent dashboard, opportunities, signup/auth, verification, savings/branded report, emails, notifications, and portfolio-analytics — have all been opened; each genuine bounded defect found was fixed+guarded, the rest recorded honestly as sound. What remains is largely **owner-gated** (see Blocked): notification-content *localisation* (systemic, ~44 heterogeneous sites — the localisation-strategy call, distinct from the correctness bug just fixed), agent-email language, the manual-add workflow, a `past_due` banner — plus the single highest-value action, **getting this branch reviewed/merged/deployed.** Remaining solo passes should hold the same bar: fix genuine, bounded, customer-facing defects (like today's), not manufacture motion.

## Blocked / user-gated

- **Merge + deploy of `claude/ui-foundation-audit-gtm05i`** — needs owner authorisation. 226 commits is a large review surface; consider splitting by theme.
- **Company-page "promote no products" claim vs. partner-perks program (owner-gated watch-item).** `/company` states the business model is "deliberately simple: subscriptions… we take no commissions from insurers, sell no data, and **promote no products** — so the analysis you see always has your interest as its only criterion." This is **accurate today** (the partner catalog is empty, `/perks` 404s until a vendor is activated, and pricing is subscription-only). But the partner-perks program is built to promote third-party partner services once activated in `/admin/partners`. The moment the first partner goes live, "promote no products" and the "your interest as its only criterion" framing need revisiting — either scope the claim to the *analysis* (perks don't influence the coverage engine) or soften it. Not a current defect; flagging so it isn't missed at launch. Owner's call on wording + monetisation framing.
- **Claims "call your insurer" contact — audited SOUND (claims-director lens).** `resolveClaimsContact(acordData, coverageType)` is branch-aware — motor → accident-declaration line, home → technical-assistance, health → coordination-centre — extracted from the policy, with the honest "we didn't find a number; it's on your schedule" fallback (never fabricated) and covered by the AI-provenance disclaimer. Reliable-by-design for a claims moment.
- **Notification-content localisation scope confirmed LARGE (owner-gated).** A sweep found ~44 hardcoded-English `message:` sites, but heterogeneous: Zod validation (client validates first with localised codes; server prose is a rarely-hit backstop), success-returns many clients ignore (they show their own localised copy), API responses (not UI), and genuine notification content. Correct localisation needs per-site display-path analysis + a recipient-language strategy — a coherent chunk best done deliberately, not piecemeal.
- **In-app notifications are hardcoded English, shown verbatim (localisation gap, owner-gated).** `notificationEvent.message`/`title` are rendered as-is by NotificationCard/Bell/Client — no translation layer — yet most of the ~18 `notificationEvent.create`/`sendNotification` call sites hardcode English (a few use `t.`). Greek-default policyholders receive English notices ("New Policy Added", renewal/opportunity alerts). Fix needs an audience-aware strategy: resolve the recipient's `preferredLanguage` at creation and store the localised string, OR store a key+params and localise on render. Bounded raw-code leaks already fixed; the systemic localisation is the owner's call (sizeable, and agent-vs-customer audiences may warrant different handling).
- **`marketingSlug` is a dead taxonomy field (minor tech-debt, owner-gated).** Every rich/basic branch in `lib/insurance/taxonomy.ts` carries `marketingSlug` (e.g. home→`property`, motor→`motor`) — the documented "slug under /product when a marketing page exists" — but it has **zero consumers** repo-wide. Either it should be wired (a branch page's "learn more about X insurance" → `/product/{marketingSlug}`, all of which resolve) or removed as dead scaffolding. Not touched — wire-vs-remove is a product call. (Audit otherwise found the branch taxonomy ↔ content ↔ product graph sound: `contentTier` is deliberately decoupled from bundle existence, every rich branch has a bundle, unauthored branches degrade to a graceful generic fallback.)
- **"Add details manually" forces a document upload (workflow finding, owner-gated).** The wallet's add-menu offers «Χειροκίνητη προσθήκη» / "Add details manually" (pen icon) as distinct from "Upload document" — but both route to `AddPolicyClient`, which hard-requires a file (`selectedFiles.length === 0 → uploadDocumentRequired`, `app/(protected)/wallet/add` never reads the `?method=upload` param). No document-less manual-entry path exists anywhere, so "Add details manually" cannot add anything manually; it's a misleading duplicate of "Upload document" (the empty-state CTA also advertises "Upload a PDF…" while wired to the same handler). **Not fixed — the resolution is a product call:** (a) make manual entry real (the form already has insurer/policy#/LOB/date fields + `__PENDING_EXTRACTION__` placeholders; only line 84's file-required check blocks it — but a document-less policy's downstream blast radius on extraction/gaps needs sign-off), or (b) remove/relabel the redundant menu item. Flagged as *broken/misleading*, not a preference.
- **Owner decisions, deferred:** Plus at €7.99 vs €9.99 (`lib/subscription-copy.ts` and `tests/money-path.spec.ts:251` still assert €7.99); whether `FREE_LIFETIME_QUESTIONS` stays 0; whether extraction review stays agent-only (self-serve users' `reviewState` never leaves `unconfirmed`); whether `duplicate_coverage_detection` should be gated.
- **"Verified" agent terminology drift (low, not guessed):** the same status is surfaced as «Πιστοποιημένος» (certified — regulatorily loaded), «Επαληθευμένος» and «Επιβεβαιωμένος» across `AgentCard`, agent settings and onboarding. Correctness fix (readers agreeing with the writer) is done; picking ONE customer-facing term — and whether "certified" is defensible vs the platform's actual document-review process — is a product/compliance wording call, left to the owner rather than standardised unilaterally.
- **Brevo IP allowlist (POLICYWALLET-3):** Brevo rejects sends with 401 "unrecognised IP" — Vercel egress IPs rotate. Disable authorised-IPs at https://app.brevo.com/security/authorised_ips. Not fixable in code.
- **Housekeeping:** rotate Brevo API key, both Supabase DB passwords and the `sk_test` key (all transited chat); Supabase outstanding-invoices banner; old dev project `lzqvtvjggylcujenlelh` idle — keep or pause.

## Top risks (ranked)

1. **High — 226 unmerged commits.** The longer this branch runs, the larger the divergence from `NEW-UI` and the harder the review. Prod has none of these insurance-correctness fixes. This is now the single highest-value action, above any further finding.
2. **Medium — Playwright not re-run on this branch.** E2E is not in CI. Use the IPv4 pooler (`aws-1-eu-west-3.pooler.supabase.com:5432`, `connection_limit=2`) and `--workers=3`; the dev `db.*` host is IPv6-only and this machine has no global IPv6.
3. **Medium — governance HOLD:** legal/DPO/product + UAT + SRE-restore sign-offs pending; `ENFORCE_EMAIL_VERIFICATION=1` still off.
4. **Medium — scale:** QStash queue active in prod since 22 Jul; still needs a real-upload round-trip proof and a k6 run. Pooler `pool_size 15` is the next ceiling.
5. **Low — TEST-mode billing:** live keys, live webhook + secret and a catalog re-run are required before charging real cards.
6. **Low — `Policy.endDate` / `Policy.status` are untrustworthy** (no enum, ~7 values, never recomputed). Symptoms patched query-by-query this session (premium totals, active counts, three renewal queries → `NON_LIVE_POLICY_STATUSES`). **Durable fix, recommended to the owner: a `Policy.status` enum + a lifecycle that maintains it, and backfill `endDate` from the extracted envelope.** Until then the shared status filter is the guard.
7. **Low — recommendation↔product matching** keys a rec's raw lob to a catalog product's lob; whether to normalise to branch family depends on the catalog's (unestablished, empty) lob convention. Left for an owner decision, not guessed.

## Next 3 actions

1. **Get this branch reviewed and merged.** Propose splitting into themed PRs (gap engine / compliance & GDPR / policy presentation / uploads & documents) so the diff is reviewable, then deploy and re-verify against prod data.
2. **Run Playwright against the branch** — `npx playwright test --project=chromium --project=agent-chromium` plus `RUN_UX_AUDIT=1`, using the pooler connection above. The coverage panels changed shape for five branches; the policy page is the highest-traffic authenticated screen.
3. **Continue the audit** on surfaces not yet opened. Covered since the last STATUS and found SOUND (guards added where missing): team/tenant permissions matrix (no escalation, tenant-isolated), notification-preference UI (registry-driven), risk-profile wizard (Art. 9 notice + bounds), agent commission (child-branch-aware), claims guidance content. Still unopened: billing beyond cancellation, customer-acquisition/signup, full mobile-viewport responsiveness, portfolio analytics. Newly-opened surfaces now yield mostly polish or confirm-sound — the high-severity correctness defects are behind us and the three recurring root causes (Policy.status, child-branch matching, schema/label/render drift) are guarded.
