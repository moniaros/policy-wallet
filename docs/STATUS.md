# PolicyWallet — Project Status

_Living dashboard — not a log. Updated at the end of each session with meaningful work. Keep it under one screen._
**Last updated:** 2026-07-23 (**SHARED FORMS/DATA-DISPLAY CONSOLIDATION COMPLETE — buttons, controls, chips, skeletons and alerts now go through the design system; two cascade bugs caught by rendering, not grep. Still on `claude/ui-foundation-audit-gtm05i`, NOT merged, NOT deployed.** Previously: **UI FOUNDATION — AUDIT-MAP PROGRAM COMPLETE except two decision-blocked items on `claude/ui-foundation-audit-gtm05i` (NOT merged, NOT deployed; all guardrails + 1334 unit + 17 Playwright responsive checks + prod build green)** — every executable cluster is done; two of the three items I'd called decision-blocked turned out executable on a closer look and are **done** (`ca961fe`): the customer list already HAD a card grid behind a desktop-only toggle (now the mobile presentation, no design invention needed), and EmptyState's hexes were an exact match for the `--pw-*` variables all along — my earlier objection compared them against the wrong token (`--card`, not `--pw-surface-dark`), verified byte-identical by dark-mode screenshot. The five remaining agent tables also got a mobile presentation: `.pw-stacked-table` turns rows into labelled cards below `lg` (labels from each table's own `<th>` via `data-label`), so **every column survives** and the "which columns survive" decision I'd claimed was needed evaporates. **The landing RSC refactor is now DONE too** (`aa358a3`) — owner initially deferred it, then chose the full version: locale is a prop threaded from each route instead of a React context, taking the 15 product `PageClient`s from **15 client → 0** and landing components from 9 → 7 client. Verified by rendering all 15 products + 7 other public pages in BOTH locales before/after and diffing visible text: **44/44 byte-identical** — which caught a real regression the refactor had introduced (a blanket `language`→`locale` rename had rewritten "in plain language" to "in plain locale" in user-facing copy on three pages). ⚠️ **Perf benefit remains unmeasured** — this build emits no per-route First Load JS table, so the ~448 KB claim still has no before/after; measure before investing further. Branch stays unmerged for review. Late finds this pass: **74 `<label>`s were associated with no control** (they looked right, announced as unlabelled); the wallet's **add-policy button was buried ~45px under the mobile bottom nav** at 375px; `TrustStrip` had **zero `dark:` variants** so the trust chip rendered white-on-white in dark mode. **7 of the map's rows were wrong** and are corrected in its §0. Prev: **UI FOUNDATION — SHARED LAYERS COMPLETE on `claude/ui-foundation-audit-gtm05i` (NOT merged, NOT deployed; all guardrails + 1327 unit + prod build green per commit)** — worked the merged `docs/ui-responsive-audit-map.md` end to end, re-verifying every claim rather than trusting it (**5 of its rows were wrong** — corrected in its §0). Highlights: the mobile wallet **could not delete, batch-upload, consent, compare or upgrade** (a fork returned before those modals — and my own earlier breakpoint change had widened it from phones to tablets); the sign-in page shipped a **fake "Biometric" unlock** gated on localStorage keys nothing writes; **all 4 native `confirm()`s** (incl. account deletion) are now branded dialogs; the shell's language toggle and role switcher **called props the layout never passed**; 36 `overflow-x-auto` tables were **not keyboard-scrollable**; 3 of 4 pollers ran while the tab was hidden; every form validated by **toast only** (5 `aria-invalid` app-wide). New shared primitives: `ConfirmDialog`, `AdminDialog`, `PageContainer`, `TableShell`, `components/ui/form/*`, `hooks/usePolling`, `lib/i18n/format`. **+100 unit tests** (1227→1327). **Next:** adoption breadth (form kit across the 17× agent-modal recipe; table card fallbacks — a product decision), the palette/hex sweep, and the landing RSC refactor. Prev: **UI FOUNDATION — WALLET UN-FORK + SHELL COMPLETION SHIPPED on `claude/ui-foundation-audit-gtm05i` (NOT merged, NOT deployed; all guardrails + 1292 unit + prod build green per commit)** — a second, independently-produced audit (`docs/ui-responsive-audit-map.md`, merged onto this branch) caught a **critical defect my batches missed**: the wallet rendered a completely different tree below the breakpoint, and that early `return` sat **before** the delete/batch-upload/AI-consent/compare/upgrade modals — so on a phone the product's core screen could not do any of them. Verifying it also exposed that **my own Batch-2 breakpoint change (768→1024) had widened the defect from phones to tablets**. Fixed in `6fa6074`, and the fix needed a second layer the map didn't identify: `PolicyCard` accepted `onShare`/`onViewDocuments`/`onRunAnalysis`/`onDelete` and **silently ignored all four**, so un-forking alone would have mounted the modals with nothing able to open them. Then `399892e` finished the shell: the mobile language toggle and role switcher both called props the layout never passes (the switcher toasted "Viewing as X" for a switch that never happened), the mobile unread badge was keyed to an id no nav item has, `safe-area-inset-bottom` was applied but **defined in no stylesheet**, the drawer had no focus trap, nav was built from `roles[0]` (stranding dual-role users), and `/home`+`/dashboard` were one screen on two URLs. Map reconciled — it has two factually wrong rows, corrected in its new §0. **Next:** audit-map Clusters D–H (form kit, ResponsiveTable, ~21 focus-trap-less overlays + 4 native `confirm()`s, route-state files, polling). Prev: **UI FOUNDATION AUDIT — BATCHES 0–2 SHIPPED on `claude/ui-foundation-audit-gtm05i` (NOT merged, NOT deployed; all guardrails + 1227 unit + prod build green per batch; Playwright still owed locally)** — audited the shared component layer + app shell (the app surfaces never got the Brand-Elevation pass the marketing site did; prior grade C+). 12 broken/a11y-defective findings, 10 fixed: shared `Modal` was edge-to-edge <512px on every modal in the app, the GDPR consent dialog had no accessible name, the blocking `ProcessingHUD` was silent to AT **and** English-only to Greek users, admin ran a **double shell** (two disagreeing sidebars + a nested `<main>`) while four admin sections were unreachable from the sidebar admins actually see, admins got an empty 76px bottom bar, phones painted the desktop wallet then swapped after mount (and 768–1023px got mobile chrome around a desktop layout), the mobile profile screen's four actions all 404'd, and the app had no skip link or `<main>` target at all. Doc: `docs/audits/ui-foundation-audit-2026-07.md`. Batches 3–5 (form/table primitives, consolidation, page adoption) are the active backlog. Prev: **COVERAGE-INSIGHTS VERDICT SYNC — MERGED + DEPLOYED (`97f908b`, prod `policy-wallet-b1rv7osgp` target=production, aliased www.policywallet.gr; smoke: public 200 / `/coverage-insights` 307→signin; no migration; all guardrails + unit + build green)** — the page showed contradictory verdicts («χρειάζεται ενίσχυση» next to «Δεν εντοπίστηκαν κενά»); reworked into two consistently-sourced concepts (policy-gaps gated on `lastAnalyzedAt` drives the headline; profile protection-score stays in its card, badge relabeled "coverage types missing") with a defined value for every state. See Done. Prev: **ADVISOR-INVITE-BY-EMAIL — MERGED + DEPLOYED (`0f830ae`, prod `policy-wallet-3kl2lq1ji` target=production, aliased www.policywallet.gr; smoke: public 200 / `/invite/*` reachable anon / `/onboarding`+`/agent` 307→signin / `?role=agent`→agent signup 200; no migration; all guardrails + 1227 unit + build green)** — policyholders now invite their advisor by EMAIL (primary), invite-code demoted to secondary; advisor accepts → registers as agent (auto-connect) or logs in (connect). See Done. Prev: **B2C UI/UX CONSISTENCY PASS — COMPLETE across the whole operational surface: `/account` (6) + `/wallet` (9) + checkout `/upgrade` (4) + support `/help`+article (2) + history `/notifications` (1) — MERGED to NEW-UI + DEPLOYED to prod (`dpl_7Csu2WXHP1Aaq` / `ecfa79f`, aliased www.policywallet.gr; smoke: public 200 / `/upgrade`+`/help`+`/wallet` 307→signin); ~20 verified per-batch commits, all guardrails + 1218 unit + prod build green — see Done.** Prev: **TRACK A — ADMIN SECTION MADE OPERATIONAL: 6 PRIORITIES MERGED + DEPLOYED (PR #220, prod `dpl_CTAez58Cur` / `b381239`, aliased www.policywallet.gr, smoke-verified `/admin/*`→307)** — admin went from a config/compliance console to an operations console: P1 admin-grant JWT sync (`changeUserRole` now also writes Supabase `user_metadata.role` → UI promotion works, effective on re-login) + built the dead role modal; P2 billing ops (refund/cancel/credit — Stripe still TEST-mode in prod); P3 token grant; P4 `/admin/policies` (requeue stuck-`analyzing` via QStash + consent gate, cascade delete, edit, merge via extracted `mergePolicyRecords`); P5 `/admin/users/[id]` support view; P6 persistent admin sidebar + `/admin/activity` + removed fabricated token-dashboard figures. +30 unit tests (1218 total); all guardrails + build green; impersonation deferred. Prev: **B2C+B2B FULL AUDIT — 8 FINDINGS FIXED, PR #212 (awaiting merge auth)** — reviewed wallet/home/upgrade/onboarding + agent sections + all unresolved prod Sentry. Fixed: 2 HIGH collab-route outages (proposals + document-requests 500'd on a never-populated `body`; POLICYWALLET-C), cross-tenant taxId write, 3 arbitrary-email/consent vectors (createThread/addAction/sendQuestionnaire), gap vocab (POLICYWALLET-7), 1 naive role check. b2c wallet surface verified fully ownership-scoped. Ops follow-up: Brevo IP allowlist (POLICYWALLET-3). Doc: `docs/audits/b2c-b2b-full-review-2026-07.md`. Prev: **SENTRY POLICYWALLET-E FIXED + DEPLOYED** — #211, POLICY_LIMIT_REACHED thrown from the createPolicy server action could never reach the client's upgrade-modal mapping in prod (message redaction) → structured return + client branch; see Done. Prev: **AUDIT REMEDIATION SHIPPED — ALL SIX BATCHES MERGED + DEPLOYED** (D #208, E #209, F #210 landed and live at policywallet.gr, deploy verified: READY + alias 200 + reaper smoke w/ CRON_SECRET + clean logs) — A (#203/#204, deployed), self-heal (#205), B privacy (#206), C email honesty (#207) all MERGED + DEPLOYED + prod-verified; CRON_SECRET set + reaper smoke-tested live. **Shipped batches:** D (#208 AI-call hygiene: abortSignal+maxRetries:0 on all 16 call sites incl. batch-translator, ONE transient classifier w/ anchored codes + Retry-After backoff, honest createPolicy status from VALID docs + createMany), E (#209 route hardening: gapId ownership, canWrite gap mutations via DI-clean computePolicyAccess, masked team totals, token-purchase limits+Zod, per-user rate buckets, proposal-status Zod, error-echo hygiene, projections, oracle-free deduped share invites, /api/doc+/api-docs admin-gated in prod, hasAnyRole everywhere, validated changeUserRole), F (#210 perf safe subset: home 7-query Promise.all, React cache() auth helpers [cross-request safety verified against React/Next internals], per-gap acordData stripped from coverage-insights props; wallet/dashboard acordData strips deferred — premium-footprint resolves lifecycle from acordData). Every batch adversarially reviewed; all confirmed findings fixed pre-merge (incl. two stale transient-classifier copies, share-response oracle, team-totals derivation leak). Prev: **INTENT & SUSTAINABILITY AUDIT + BATCHES A–C** — full-codebase audit (#202, `docs/audits/intent-sustainability-audit-2026-07.md`): 1 Critical, 8 High, 13 Medium, 8 perf. **Batch A MERGED + DEPLOYED** (#203/#204, prod verified): stuck-'analyzing' chain fixed (lease 4min + interval heartbeat, 503-on-…)

## Current phase
**DEPLOYED TO PRODUCTION** (policy-wallet-omega.vercel.app, Vercel project `policy-wallet`, team moniaros-projects). Insurance-branch product system fully shipped: waves 1–5 + 7 (PRs #67–#69, #71–#73) **deployed to prod 12 Jul** (dpl_GCRMf2Dn…, alias verified); waves 6+8+9 ALL merged + deployed 13 Jul (PR #75 primitives/MASTER radius; PR #76 marketing palette — teal/emerald at ZERO repo-wide, 6 dead landing components deleted; PR #78 extraction citations, flag-gated). EXTRACTION_CITATIONS=1 is LIVE in prod. The approved branch-product-system plan is COMPLETE (waves 1-9). Greece GA go/no-go packet still `HOLD` on human sign-offs.

## Done (recent)
- **B2B CLIENT-DETAIL + TABS PASS (23 Jul, `73eedf0`→`782140a` on `claude/ui-foundation-audit-gtm05i` — NOT merged, NOT deployed):** actually READ the client detail view instead of grep-auditing it (the previous stop-hook's fair criticism), which surfaced three defects a token-scan cannot see. **The tabs were not tabs** — Overview/Policies/Activity/Financials were plain `<button>`s in a `<div>`: a screen reader announced four unrelated buttons, no arrow-key nav, and the selected tab was not tied to the panel it showed. **The tab row overflowed on a phone** — four icon+label tabs in a plain flex with no scroll spilled past ~360px. **Two modals on this screen were inaccessible** — the document-request and proposal dialogs were hand-rolled `fixed inset-0` with no focus trap/Escape/scroll-lock, on the exact screen where I'd claimed the prior commit that "all agent modals use useDialog" (true of `components/agent/*`, false here). All fixed: proper WAI-ARIA tablist, `overflow-x-auto` tab row, both modals → shared `Modal`, plus normalising a raw `bg-[#F8FAFC]` and flat `px-6` to tokens/standard gutter. **Then found the pattern elsewhere:** grepping `activeTab` w/o `role="tab"` caught the agent LANDING page (AgentClient) doing the same — extracted `hooks/useTabs.ts` (the ARIA + roving-tabindex glue) and moved BOTH screens onto it, so there is one implementation. **+14 tests (1432):** client-detail-tabs (5), use-tabs (4), plus the doc-request/proposal modal migration. All guardrails + prod build green. **STILL NOT DONE / caveats:** (1) a third fake-tabs screen exists — `wallet/[id]/PolicyAnalysisTabs.tsx` (B2C, out of this goal's scope) — now trivial to fix with `useTabs`, noted for the B2C follow-up; (2) the B2B screens remain **not rendered/verified visually** (needs dev-Supabase pooler creds) — tab overflow/scroll behaviour is reasoned + unit-tested, not seen at 375px in a browser; (3) branch still unmerged/undeployed.
- **B2B PASS — COMPLETED THE TABLE + AUDIT SWEEP (23 Jul, `b360c4f`→`24d7369` on `claude/ui-foundation-audit-gtm05i` — NOT merged, NOT deployed):** finished what the first B2B pass left partial. **Sorting now on ALL FIVE agent tables** — added opportunities (customer/status/likelihood/next-action) and commissions (LoB/premium/commission/opp-count); my earlier "skip these" reasoning did not hold (both are per-row tables an agent scans). **Fixed a silent bug in the FIRST B2B commit:** team + questionnaire sort accessors were written against GUESSED field names with `??` fallbacks (`r.value ?? r.estimatedValue`, `r.lineOfBusiness ?? r.lob`) — several don't exist on the row types (PipelineItem has `wonPremium`/`estimatedPremium`/`lineOfBusiness`, not `value`/`lob`), so sorting those columns did NOTHING. All accessors now typed to the real row type (`applySort<PipelineItem,…>` etc.), which is what surfaced it — an untyped `any` accessor reads undefined and sorts by nothing silently. **Customer list** (its own hand-rolled sort, predating the shared primitive): fixed bare-localeCompare→`el` collation (Greek names were ordering after z), and made the three sortable `<th onClick>` real keyboard-operable buttons with `aria-sort` + arrow (they were mouse-only and unannounced). Confirmed it does NOT have the dashboard's "0=badly-protected" bug — null score already renders "—". **Audited the rest of the agent surface and found it structurally sound:** 0 raw inputs (the earlier consolidation covered them), 0 raw primary buttons, 0 fixed pixel widths >375px (no overflow risk), and the 10 hand-rolled agent modals already use `useDialog` (focus trap + Escape + scroll-lock) + `role="dialog"` — accessible, just not routed through the `Modal` wrapper, which is a DRYness preference not a defect, so left alone. **New E2E guard:** `agent-viewport-overflow.spec.ts` (37 cases under the `agent-chromium` project) asserts no horizontal overflow at 375–1440px across all 7 agent routes and that tables collapse to stacked cards below lg. **NOT executed** — needs the dev-Supabase pooler creds; committed as a standing guard, verified only via `playwright --list` (37 under agent-chromium, 0 leaking to default). **1423 unit tests, all guardrails + prod build green.**
- **B2B OPERATIONAL PASS (23 Jul, `3b1f865`→`f3e8d1c` on `claude/ui-foundation-audit-gtm05i` — NOT merged, NOT deployed):** the agent workflow screens. **No agent table could be sorted** — renewals, opportunities, commissions, team pipeline, questionnaires each arrived in one fixed server order, so an agent wanting to work the largest premiums first, or find one client in a long list, had no route to it; commissions has no search/filter either. New shared `SortableColumn`/`useTableSort`/`applySort`, wired into renewals (5 cols), team pipeline (5), questionnaires (4). Decisions pinned by tests: missing values sort LAST in both directions (a missing premium is not "cheapest"); the toggle cycles asc→desc→**back to the server default** because that default is itself meaningful (renewals by expiry); string sort uses `el` collation (the default scatters Greek after z). **The first version was invisible on mobile** — `.pw-stacked-table` makes `thead` sr-only below lg, so on the width an agent most needs it the sort was hidden AND still focusable (focus vanished off-screen); added `MobileSortControl` (native select + direction toggle) driving the same state. **Loading states:** every agent route had one except `/dashboard/agent` (the landing page) and `/agent/settings` — both had error boundaries but showed nothing while data resolved; added. **Terminology (vs policywallet.gr/lexiko):** the agent role had **three names** — role switcher and B2C say "Σύμβουλος", the pricing/dual-role screen said "Πράκτορας", so an agent switching roles saw a different job title than on their own billing screen → standardised on Ασφαλιστικός Σύμβουλος; "Απαλλαγή ανά Αξίωση" → "ανά ζημιά". Checked "Πολιτική"-for-policy before sweeping — only hit was "Πολιτική Απορρήτου" (correct for Privacy Policy). **+17 tests (1423).** All guardrails + prod build green. A Latin-lookalike collation edge (names typed with Latin Α/Ο/Ε sort after the Greek alphabet) is **pinned by a test with a note**, not fixed — real Greek-data behaviour worth documenting. **Known and NOT done:** opportunities and commissions tables did NOT get column sorting this pass (opportunities has its own filter UI; commissions is an aggregate view); and the B2B screens were **not rendered/verified visually** (needs dev-Supabase pooler creds), so mobile-sort placement is reasoned from the stacked-table CSS, not seen.
- **B2C TRUST + CLARITY PASS (23 Jul, `64ee6ca`→`abfea5d` on `claude/ui-foundation-audit-gtm05i` — NOT merged, NOT deployed):** reviewed the policyholder journey for statements a customer could act on. **The protection score told people things it could not know.** With no policies uploaded, `healthScore` was hard-coded to 0 and rendered as `0` in a RED ring under "Χρειάζεται προσοχή" — a verdict on someone's protection before the product has seen a document. The identical bug was fixed on /coverage-insights in `65183b7`; **/dashboard kept it**, and /dashboard is where people land. Score is now nullable (em dash, no arc, an invitation). Separately, when the cached engine score is missing the page silently substitutes a **different formula** (flat 25/15/8/3 per-severity penalty vs the engine's weighted profile-relative category average) — materially different numbers for the same portfolio, with nothing saying which you were reading; now labelled a provisional estimate. And the score **never said what it measured**: new `ScoreMethodology` (a `<details>`, so it works pre-hydration) states the method AND the limits — it says nothing about premiums, insurers or wording quality, so a cheap policy with poor terms scores the same as a good one — plus the product's **first not-personalised-advice line** (insurance advice in Greece is regulated: IDD, Law 4583/2018; the app had none anywhere while showing AI-derived scores). **"Not detected" was being told to customers as "does not exist":** the policy page said "Δεν εντοπίστηκαν εξαιρέσεις ή ειδικοί όροι σε αυτό το συμβόλαιο" whenever extraction returned empty — every policy has exclusions (Εξαίρεση, per policywallet.gr/lexiko), and someone believing that sentence finds out at claim time. Auditing the whole copy set rather than the one symptom found **four more**, including a SECOND exclusions surface with the same claim and a gap empty-state reading as a clean bill of health. The extraction disclaimer now states it may be incomplete and that the **policy wording prevails**. **"Ευκαιρίες εξοικονόμησης"** headed a check that only counts branches with >1 active policy (two cars is not evidence of savings) and "no savings opportunities today" asserted a price comparison never performed — renamed to the overlap check it is. **In-product education:** the /lexiko dictionary (12 terms, 40–60 word definitions, both locales) was unreachable from inside the product — a policyholder saw "Εξαίρεση" as a bare label. New `GlossaryHint` reads the **same module** as the public pages (so they cannot drift) and is split presentational + server resolver, because the policy-detail tree is client-side and importing the glossary there would ship all 12 terms to the browser for one sentence; a test asserts neither client file imports it. **+22 tests (1406).** All guardrails + prod build green. **Known and NOT done:** (a) `deductible` / `sumInsured` labels in `CoverageTabView`/`HealthCoverageDetails` still lack hints — they sit several layers deep in the client tree and threading them is a bigger change than could be verified in-session; (b) **277 Greek labels use Title Case** where Greek uses sentence case (a translation artefact that reads unprofessionally) — a blanket sweep would damage defined terms like "Πράσινη Κάρτα", so it needs per-string judgement; (c) the authenticated B2C pages were **not rendered/verified visually** this session (needs the dev-Supabase pooler credentials) — reasoning is from source, not screenshots.
- **SHARED FORMS + DATA DISPLAY — CONSOLIDATED (23 Jul, `cdcc58d`→`c1cf292` on `claude/ui-foundation-audit-gtm05i` — NOT merged, NOT deployed):** the shared layer had the right recipes and almost nobody used them. **Measured drift:** 151 hand-written buttons across **47 distinct appearance signatures** for what is nominally one primary button, against a `button.tsx` with **2 importers**; 107 controls across **48 signatures**, against a form kit with **2 importers**; two `Skeleton` modules (15 + 14 importers) shipping two different shimmers; 31 hand-rolled alert containers across 25 files whose most common styling signature appeared **3 times**, and no shared success treatment at all. **The design system already had the answers** — `.pw-primary-button`/`.pw-secondary-button` (pill radius per `--pw-radius-button`), and MASTER.md had documented the missing input primitive *and specified the recipe to match*; both were simply unadopted. Codemods moved 71 buttons + 105 controls onto them (signatures **47→1** and **48→1**), a further 102 controls came over by rewriting the 11 local `inputBase`/`inputClass` constants they hid behind, and `.arc-btn*` — a second button system at `rounded-xl` that contradicted the pill rule, flagged in MASTER as backlog — is **deleted**. New: `.pw-input`/`.pw-input-sm`, `Alert` (error/success/warning/info, with announcement semantics part of the variant), `ChipToggle`, `StatTile`/`StatGrid`, `Checkbox`/`Radio`/`RowCheckbox`. **Mobile floors** for iOS zoom (16px) and 44px targets went in as unlayered CSS so no hand-written control can opt out. ⚠️ **Two cascade bugs were found only by rendering the pages — the CSS was present and correct, and losing:** the floor sat in `@layer base`, where `font-size: 16px` lost to the `text-sm` utility, so the iOS-zoom fix was **inert** (measured: 14px at 375px, identical to 1440px); and `.pw-input` **outranked per-instance overrides**, so signin's icon-inputs kept `padding-left: 24px` despite `pl-9` and the mail/lock icons sat on top of the placeholder. Fixed by moving the floor unlayered and declaring all four recipes with `:where()`. Both are now encoded as tests. **1385 unit tests** (+31); type-check, lint, i18n, utf8, encoding, `audit:api-auth` and prod build green; verified against the compiled CSS and by measuring the rendered page at 375px and 1440px. **Deliberately not migrated, with reasons:** four red containers that use red as *status colouring for criticality* (ConfirmDialog icon chip, Settings danger zone, notification pill, ActionQueueCard) — folding them into `Alert` would turn a data state into an error state and announce it as one.
- **UI FOUNDATION — PROGRAM COMPLETE to the decision boundary (23 Jul, `f3a0ffd`→`433df77`):** finished the audit-map backlog. **Labels (`f3a0ffd`):** **74 `<label>` elements were siblings of their input with no `htmlFor`** — visually correct, programmatically unlabelled, so every one of those inputs announced as anonymous (worst: admin/partners/[vendorId] 22, AddCustomerModal 11). Paired each with a generated file-scoped id; verified no duplicate ids; confirmed the ones left alone are wrapping labels (valid implicit association). `PolicyTable`'s row menu got Escape + focus return — it already had `role="menu"`, contrary to the map. **375px visual pass (`cf01f68`):** measured in a real browser rather than eyeballed, and found the wallet's add-policy FAB at `bottom-8 z-40` overlapping the 77px bottom nav (also z-40) by ~45px, burying half the primary affordance; lifted above it on mobile only. New `tests/viewport-overflow.spec.ts` — 17 checks: no horizontal overflow on /wallet + /dashboard + /account at 375/390/768/1024/1440, and no two fixed bottom-anchored elements overlapping. Screenshot-confirmed the un-forked wallet renders single-column cards with the four restored actions. **Admin verification (`b2d5850`):** `/admin/*` can't be rendered here (gated on a Supabase JWT role claim; the only DB admin has no `auth.users` row, and minting an admin account for a test run isn't warranted), so the guarantees are asserted directly against `AppShell` instead — one `<main>`, skip-link target, **no** empty bottom bar and no `pb-24` for admin, and the negative case (policyholder still gets both). **Palette (`433df77`):** `.arc-card` was byte-for-byte identical to `.pw-card` → 28 call sites migrated, alias deleted, confirmed gone from the built bundle; `TrustStrip` had 3 hexes and **zero `dark:` variants** (white chip on a dark surface — in the component whose job is looking trustworthy) → tokenized; `EmptyState` took only the exact-match substitutions. **1334 unit tests.**
- **UI FOUNDATION — SHARED LAYERS COMPLETE (23 Jul, 8 commits `6274954`→`4838d62` on `claude/ui-foundation-audit-gtm05i` — NOT merged, NOT deployed):** worked `docs/ui-responsive-audit-map.md` to completion of its shared-layer clusters. **Every claim was re-verified before acting — 5 rows were wrong** (see the map's new §0): MobileAppShell's "broken logout" was unreachable dead code; `wallet/EmptyState` is live, not dead; route states were *inherited*, not missing; **`en-GB` is the declared locale and `en-US` was the drift** (the map says the reverse); and the wallet fork had a second layer the map missed. **Trust (`6274954`):** removed the sign-in **fake Biometric/PIN unlock** — gated on localStorage keys **nothing ever writes**, and its "Biometric" button only pre-filled the saved email, i.e. UI imitating an auth factor the product doesn't implement; kept the honest half (remember-this-device prefill). Replaced **all 4 native `confirm()`s** — account deletion (with GDPR Art. 17 consequences), customer removal, advisor disconnect, app handover — with a new shared `ConfirmDialog` on top of `Modal`, which adds the pending state `confirm()` structurally cannot have. Sign-in's 31 bilingual literals → `t.auth.signInPage`. **a11y (`5e604d9`/`f054024`/`609baed`):** new `AdminDialog` absorbed 6 hand-rolled admin overlays (UsersClient ×4, PoliciesClient ×3, BillingOpsPanel); focus traps on the wallet/destructive dialogs; **menu** semantics + Escape on UserMenu/RoleSwitcher (dropdowns, not dialogs); Escape + `aria-live` on the tour. Verified 3 grep hits were NOT dialogs and left them alone. **Locale (`1cf83ea`):** new `lib/i18n/format.ts` pinning locale **and `Europe/Athens`** — 22 bare `toLocale*` calls were the exact hydration-mismatch class behind Sentry POLICYWALLET-8; 41 `en-US` sites normalized to the declared `en-GB`. **Polling (`448d349`):** new `hooks/usePolling` (backoff + hidden-tab pause); 3 of 4 pollers had been running against a backgrounded phone, the wallet's calling `router.refresh()` (whole server tree) every 2s. **Forms (`20ccffe`):** new `components/ui/form/*` — `Field` owns id/aria wiring so callers can't forget it; AddPolicyClient's toast-only validation is now inline + announced. **Tables (`4838d62`):** new `TableShell` — 36 `overflow-x-auto` wrappers, **none focusable**, so columns past the edge were unreachable by keyboard (WCAG 2.1.1); 6 tables adopted; BulkImportModal's preview had no `overflow-x` at all and was **clipped**. **+100 unit tests (1327).** Three real bugs caught by the repo's own guardrails mid-work and fixed: conditional hooks after an early return, a ref written during render, and an unused eslint-disable masking the latter. **Deliberately not done, with reasons recorded:** table card fallbacks (per-table product decision), full AnalysisCard poller migration (~20-dep effect on an incident-prone flow), blanket route-state files (inherited boundary already works).
- **WALLET UN-FORK + SHELL COMPLETION (23 Jul, `6fa6074` + `399892e` on `claude/ui-foundation-audit-gtm05i` — NOT merged, NOT deployed):** driven by `docs/ui-responsive-audit-map.md` (a second full UI/UX/responsiveness audit, produced independently and merged onto this branch; its findings were re-verified rather than taken on trust — see its new §0 for the reconciliation). **The critical one:** `PolicyWalletClient` did `if (isMobile) return <MobileAppShell/>`, and that early return sat **before** `DeletePolicyDialog`/`BatchUploadModal`/`AiConsentModal`/`PolicyComparison`/`UpgradeModal` — so on a phone the product's core screen **could not delete, batch-upload, consent to AI processing, compare, or upgrade**. My own Batch-2 change (`useIsMobile` 768→1024, right for the shell) had **widened this to tablets**. Fixed by deleting the fork — cheap, because `PolicyWallet.tsx` already rendered both a table and a responsive card grid behind a user `viewMode`; presentation is now CSS-first (cards always below `lg`, toggle `lg:`-only, no hydration branch). **The fix needed a second layer the map missed:** `PolicyCard` accepted `onShare`/`onViewDocuments`/`onRunAnalysis`/`onDelete` and **ignored all four** (it destructured only `{policy,onView,id}`) — those actions existed only in the desktop table's context menu, so un-forking alone would have mounted the modals with nothing able to open them; the card now carries them. Also preserved a monetization surface the deletion would have dropped: the wallet's `UpgradeTriggerCard` existed **only** in the mobile tree, so `/wallet` on desktop had no upgrade trigger at all — now every width. Deleted (all re-verified 0-importer): `MobileAppShell`, `MyPoliciesScreen`, `MyProfileScreen`, `SwipeableCard`, `PullToRefresh`, `UserDashboard`, `AddPolicyForCustomerModal` — but **NOT** `wallet/EmptyState.tsx`, which the map wrongly calls dead (`PolicyWallet` imports it). `useIsMobile` removed entirely once its last consumer went; dropped a dead per-request agent-relationship join on every wallet load; stripped BOMs from 20 files. **Shell completion (`399892e`):** the mobile language toggle called `onNavigate?.('/?lang=el')` and the layout **never passes `onNavigate`** → dead control (now `setLanguage`, which already persists to localStorage + `/api/user/language`); `RoleSwitcher` likewise called a never-passed prop then toasted "Viewing as X" for **a switch that never happened** (now a validated `pw_active_role` cookie + server action); the mobile unread badge was keyed to `id === 'notifications'`, **an id no bottom-nav item has for any role**, so it could never render; `safe-area-inset-bottom` was on the bottom nav but **defined in no stylesheet** (iPhone home-indicator overlap); the drawer had no focus trap/Escape (now the existing `useDialog`); nav was built from raw `roles[0]` — untrimmed, unvalidated, order-dependent — **stranding dual-role users** (the advisor-invite flow creates them) → `parseRoles`+`getPrimaryRole`, and `admin/layout.tsx`'s banned `.includes('admin')` substring check → `hasAnyRole`; `/home` and `/dashboard` were one screen on two URLs → 307 in `proxy.ts` (both redirect chains verified terminating). **New guardrail:** `tests/unit/public-route-allowlist.test.ts` walks `app/(public)` and asserts every route is admitted by the proxy allowlist — the page-level mirror of `audit:api-auth`, for a class that has bitten twice; **verified it fails on an unlisted route**, not just passes vacuously. **1292 unit tests** (+65); all guardrails + prod build green per commit. **Owed:** full Playwright run (see below) and a visual pass on the wallet at 375px — it is the core screen.
- **UI FOUNDATION AUDIT + BATCHES 0–2 (23 Jul, branch `claude/ui-foundation-audit-gtm05i`, 3 commits `1f2a630`/`211872b`/`d0377d6` — NOT merged, NOT deployed):** the marketing surface had been disciplined by Brand Elevation A–D; the **app** surfaces (B2C/agent/admin) and the **shared component layer** never got that pass. Two full sweeps + first-hand verification of every claim → `docs/audits/ui-foundation-audit-2026-07.md`, findings split broken-vs-dislike. Fixed dependency-first (shared layer before pages) so future pages inherit correct behaviour. **Batch 0 — truth:** `MASTER.md`'s canonical-value contract named `components/ui/design-tokens.ts` as the tie-breaker, but that file was **deleted in `834957c`**; its `--space-*` table described variables that don't exist; its shadow table described `--pw-shadow-card`, which **nothing references** (`.pw-card` is flat/border-first); card/input/modal specs contradicted the shipped code. Repointed to `app/globals.css` and corrected each spec; added `font-black`/hand-rolled-overlay/design-tokens-import anti-patterns; fixed the `/* Secondary - Amber */` comment on a dark-green value. **Batch 1 — shared overlay/feedback primitives:** shared `Modal` was **edge-to-edge below 512px** everywhere (the `p-4` gutter sat on a *childless* backdrop while the panel was fixed-centred `w-full`) → `w-[calc(100%-2rem)]`; the **GDPR Art. 9 consent dialog had no accessible name**; `ProcessingHUD` (a full-screen *blocking* overlay) had no `role`/`aria-live`/`aria-busy` and hardcoded "Please Wait"/"Processing action..." shown to Greek users → announced + `t.common.pleaseWait`/`processingAction` (el+en); `AddCustomerModal`'s `aria-labelledby` pointed at an id present in **1 of 4 views** and its success view was English despite the el/en keys already existing; `FloatingActionButton`'s expanded menu had **no Escape** and no `aria-expanded`/`haspopup`; `SwipeableCard`'s un-revealed actions were hidden by opacity alone, leaving **invisible tab stops that fire destructive actions**. **Batch 2 — shell & container:** admin ran a **double shell** — a translated 10-item AppShell sidebar *plus* a nested `AdminSidebar` (14 hardcoded-English, off-brand `stone-*`) *plus a second `<main>` inside AppShell's `<main>`* (~512px of chrome, invalid landmarks), and the lists **disagreed**, leaving Policies/Submissions/AI-Tokens/Activity unreachable from the sidebar an admin actually sees → merged into one translated nav, `admin/layout.tsx` is now just the auth gate, `AdminSidebar` deleted; admins' **empty fixed 76px bottom bar** (+ its `pb-24`) now renders only when it has items; added the app's **first skip link + `<main>` target** and labelled nav landmarks; MainNav/bottom-bar/logos became real `<Link>`s with `aria-current` (were `<button>`+`router.push` — no middle-click, no new-tab, "button" to a screen reader); `useIsMobile` → `useSyncExternalStore`+matchMedia at **1024px** (was `false`-then-effect, so phones painted the *desktop* wallet and swapped after mount; and its 768px cut disagreed with the shell's 1024px chrome, so 768–1023px got mobile chrome around a desktop layout); `MobileAppShell`'s four profile actions all pointed at **non-existent routes** (`/account/edit|payment|settings`, `/auth/signout`) → `/account` + the `signOut` action; dead `AgentMobileNav` deleted; new `PageContainer` primitive adopted by `PageHeader`. **Backlog (in the doc): Batch 3** form/table primitives (111 raw `<input>` in 33 files, one recipe repeated 17×; 24 hand-rolled tables), **Batch 4** consolidation (6 button families, 6 card patterns, 614 raw hexes, and the **~25 hand-rolled `fixed inset-0` overlays that bypass `Modal`** — worst `admin/users/UsersClient.tsx` ×4 with no role/aria/Escape/focus-trap), **Batch 5** page adoption of `PageContainer` + mobile Playwright projects. **Verified live** against a dev server with the E2E session cookies: `/dashboard` and `/dashboard/agent` both 200 with exactly **one** `<main>`, one skip link + `id="main-content"`, both nav landmarks labelled in Greek, sidebar rows now **8/13 real `<a href>` and zero `<button>`** with one `aria-current="page"`, bottom nav 5 real links, no `AdminSidebar` markup; and the modal gutter confirmed at the CSS layer (`.w-\[calc\(100\%-2rem\)\]{width:calc(100% - 2rem)}` — Tailwind normalises the operator, which the fix depended on). **⚠️ Still owed:** a full Playwright run. The dev DB is fine (the `db.*` host is IPv6-only and this Mac has no global v6 → use the IPv4 pooler `aws-1-eu-west-3.pooler.supabase.com` + `--workers=3`); through it, global-setup and BOTH auth setups passed, but the run then stalled on a wedged leftover dev server holding port 3000 + system-Chrome flakiness. Harness flakiness, not product failure — but unfinished. Also unverified by rendering: `/admin/*` (needs the JWT `user_metadata.role` claim; granting it meant mutating a user, which was declined) — check it on the preview deploy.
- **FULL UI/UX/RESPONSIVE AUDIT MAP — `docs/ui-responsive-audit-map.md` (23 Jul, branch `claude/codebase-ui-ux-audit-r0833g`, mapping only — no code changed):** exhaustive pre-feature-work remediation map (~120 files, file-by-file actions, 10 fix clusters, 7-phase plan, verification checklist). **Top criticals (all [BROKEN], not taste):** mobile wallet forks into `MobileAppShell` losing compare/batch/delete/consent/upgrade AND its profile tab links 4 nonexistent routes incl. logout (`/auth/signout`); `AppShell` mobile language toggle + role switcher are dead (`onNavigate`/`onRoleSwitch` never passed), bottom-nav unread badge keyed to a nonexistent id, `safe-area-inset-bottom` class undefined; protected layout builds nav from raw `roles.split(',')[0]` (dual-role users lose their second role); admin renders TWO disagreeing sidebars (`AppShell` nav + English-only `AdminSidebar`) and `admin/layout.tsx` uses the banned `.includes('admin')`; signin ships an unreachable FAKE "Biometric/PIN" block (no WebAuthn; gated on localStorage keys nothing writes). Systemic roots: JS-fork responsiveness (768px hook vs 1024px shell → tablet dead band + hydration flash), 3 token layers + `.pw-*`/`.arc-*`/shadcn button-card triplication, toast-only form validation, 21/29 overlays without focus traps, admin 0/14 loading/error files, 61 residual i18n literals, `en-GB` hardcoded on /home money tiles. Recommended order: tokens+breakpoint+shell dead-controls first, then wallet un-fork, then form/modal/table kits, then B2C→B2B page passes.
- **COVERAGE-INSIGHTS VERDICT SYNC — MERGED + DEPLOYED (23 Jul, `97f908b`, prod `policy-wallet-b1rv7osgp`, aliased www.policywallet.gr; no migration; no new route):** `/coverage-insights` showed several mutually-contradictory headlines/verdicts because it mixed **three unrelated "gap" notions** and never gated on analysis-state or questionnaire completeness: the headline verdict «Η κάλυψή σας χρειάζεται ενίσχυση» (from the profile `protectionScore.overallScore`, low for basic users) glued to «Δεν εντοπίστηκαν κενά» (from `GapInstance` rows); ProtectionScoreCard's «N κενά εντοπίστηκαν» (from `profileGaps+policyGapCount`) colliding with the gap section's "no gaps"; two disagreeing score ladders; and "0 gaps" indistinguishable from "never deep-analyzed" (deep gap detection is Plus-gated → basic tier has 0 rows + no `lastAnalyzedAt`). **Fix — two clearly-separated, consistently-sourced concepts, each gated on its precondition, with a value defined for every state (audited every headline per owner request, not just the reported line):** *Concept B — policy gaps* (headline + top tile + gap section) now has a single source = `GapInstance` (same as `/wallet`), gated on `Policy.lastAnalyzedAt` — states: no policies → "add your first policy"; not-analyzed → "Pending" (+ Plus unlock when locked / refresh hint when pro), never "no gaps"; analyzed+0 → «Επαρκής»/"Adequate — no gaps"; analyzed+gaps → severity-colored "N points". "Policies with points" shows «—» when unknown; "checked & looks good" only when analyzed. *Concept A — coverage completeness* stays in `ProtectionScoreCard` only: badge relabeled «N κατηγορίες χωρίς κάλυψη»/"coverage types missing" (from category data, was "gaps found"), a "provisional — complete your profile" note when the questionnaire is incomplete, single `getScoreTier` ladder. `RecommendationCards` "preparing forever" → three honest empty states (no policies / profile incomplete / well-covered). `page.tsx` reads `lastAnalyzedAt`, threads `hasPolicies`/`hasDeepAnalysis`/`isDeepAnalysisLocked`, drops the misleading engine-fail `healthScore=100` fallback. type-check/lint/i18n/utf8/encoding/unit/build green. **Cross-page `/home` score-ladder alignment noted as a separate follow-up.**
- **ADVISOR-INVITE-BY-EMAIL — MERGED + DEPLOYED (23 Jul, `0f830ae`, prod `policy-wallet-3kl2lq1ji`, aliased www.policywallet.gr; no migration; no new API route):** flipped the "Connect with your advisor" primary action from invite-code to EMAIL, on both surfaces (onboarding step 5 + the `/agent` empty state — the code path is now a secondary "Have an invite code?" reveal). A policyholder types their advisor's email → advisor gets an invitation → clicks → **registers as an agent (auto-connect)** or **logs in (connect)**. Reused the reverse-direction machinery wholesale (`Invite` model, `/invite/[token]` page, `redeemInvite`, `registerUser`'s `?token`/`?role`, Brevo templates, the Track-A JWT-sync helper); the only new logic is a direction-inverted invite (`inviteType:'signup'` + `relationshipType:'client_agent'`) and one `redeemInvite` branch. **New:** `sendAdvisorInviteEmail` (`lib/email/invite-emails.ts`); `inviteAdvisorByEmail` (`app/(protected)/agent/relationship-actions.ts` — validates/normalizes, blocks self-invite, 20/hr rate-limit, already-connected short-circuit, copyable fallback link on delivery failure); `redeemInvite` `client_agent` branch (`app/auth/actions.ts` — **email-bound** so a leaked link can't connect a wrong advisor; makes the redeemer a full agent = adds role + `AgentProfile` + Supabase JWT sync when they lacked it, idempotent when already an agent; upserts the connection `agentUserId=advisor, policyholderUserId=inviter`); `/invite/[token]` page renders a **register/login landing** for advisor invites (register carries `role=agent`; login returns via `callbackUrl`) — agent→client invites unchanged. **+9 unit tests** (`tests/unit/invite-advisor.test.ts`, 1227 total). **Accepted trade-off:** an existing NON-agent account accepting via login gets agent tools only after its next login (session-token constraint); new advisors who register via the link are agents immediately.
- **HOTFIX (ROOT CAUSE): onboarding-upload tx timeout — nested `this.db` inside `PolicyService.create` tx — DEPLOYED (23 Jul, `b7fd5ef`, prod `policy-wallet-wta9fvh3a` target=production, aliased www.policywallet.gr; no migration):** the "Transaction already closed (15000ms)" on onboarding step 2 was NOT the analysis finalize (the earlier `ff85a8c` fix — a valid improvement, but the finalize is the shared path and the dashboard analyzes the same PDF fine). The real cause: `PolicyService.create` (used by onboarding's `uploadAndParse`, but NOT by the dashboard's `createPolicy` action) awaited `this.logActivity()` **inside** its `withTransaction` — and `logActivity` issues its own `this.db` queries (`user.findUnique` + `activityLog.create`). A second pool connection grabbed inside an interactive tx that already holds one **deadlocks under the serverless connection limit** → the tx never completes and expires at exactly 15s (deterministic 15261ms). Raw Prisma message reached the toast via `uploadOnboardingPolicy` returning `error.message`. **Fix — no nested `this.db` inside a `this.db.$transaction`:** `create()` logs AFTER commit; `update()` moved its reads (incl. `getPolicyAccess`, same nested-query deadlock on edit) OUT of the tx and no longer opens an interactive tx for a single write; `delete()` (owner) keeps only the cascade delete in the tx, with storage cleanup + logging post-commit (best-effort). type-check + 1218 unit + build green. **Verify:** upload a policy through onboarding (server-side path — can't repro via anon curl).
- **HOTFIX: analysis-finalize transaction timeout — DEPLOYED (22 Jul, `ff85a8c`, prod `policy-wallet-44ufq1rqu` target=production, aliased www.policywallet.gr; no migration):** onboarding step 2 (upload first policy) threw "Transaction API error: Transaction already closed / expired transaction (15000ms)". The finalize `db.$transaction` held open through a per-gap loop doing `gapDefinition.findUnique` + (maybe) `create` + `gapInstance.create` — ~2–3 sequential pooler round-trips PER gap — so a multi-gap document blew past the 15s ceiling (the earlier raise from 5s→15s only moved the wall). **Fix (less work in the tx):** resolve gap definitions BEFORE the tx via `upsert` (also race-safe on concurrent same-slug creates), build the instance rows in memory, then inside the tx do a single `gapInstance.createMany` instead of N creates — the tx is now a fixed ~4 writes (policy.update + policyDocument.updateMany + gapInstance.deleteMany + createMany) regardless of gap count. Atomicity preserved. type-check + 1218 unit + build green. (Server-side path — the real repro needs an authed upload; code-verified.)
- **B2C UI/UX CONSISTENCY PASS — `/account` + `/wallet` + checkout polished (22 Jul, branch `feat/b2c-uiux-polish`, NOT deployed — ~17 verified per-batch commits, type-check/lint/i18n/utf8/encoding/build green each):** a sequential, page-by-page product-polish pass over the B2C operational surface against the `app/globals.css` design system (the runtime source of truth; MASTER.md is stale). **`/account` (6 batches):** removed a fabricated "Account Shield" card (hardcoded 85 score + fake 2FA/IP/encryption badges); real a11y (roving-tabindex `tablist`, `role="switch"` toggles, `aria-pressed`, labels, focus rings on shared `.pw-*-button`s + `ProcessingHUD`); wired 3 dead Billing buttons → Stripe portal + the no-op role switcher → navigation; success/error toasts on all profile saves (were silent); `TokenUsageCard` palette → tokens; language-aware dates. **`/wallet` (9 batches):** list/detail palette (`#0F172A`/`#94A3B8`/`emerald-*` → tokens) + search `aria-label` + double-empty-state bug; BOTH native `confirm()`s (list delete + collab revoke) → branded dialogs; token-ized the shared `components/ui/Modal` primitive (benefits every modal app-wide); scoped `error.tsx` boundaries; **`PolicyComparison`** (was all-English to EL users → 25 new i18n keys + tokens + locale-aware dates/currency); **`AddPolicyClient`** (tokens, `el?:` ternaries → keys, label a11y); **`BatchUploadModal`** + **`EditPolicyForm`** + edit-page shell (tokens, a11y). **checkout `/upgrade` (4 batches):** **`UpgradeModal`** (the paywall shipping on ~10 surfaces — accessible name, localized close, redirect aria-live, disable-during-redirect, tokens) + **`PlanBadge`**; the `/upgrade` shell (fixed a real loading-state bug where the CTA cleared before the 800ms Stripe redirect + palette); **`PricingComparison`** (Free-card downgrade trap that always errored → non-actionable "Base plan"; hardcoded+mislabeled English table headers → localized `copy.tiers.*`; radio-semantics toggle; table `scope`/`caption`; tokens); success page + `UpgradeTriggerCard`. **support `/help` + `/help/article/[slug]` (2 batches):** all stone-*/slate-* → tokens; ~30 inline `el?:` ternaries → new `t.help.*` keys (both locales); search/clear a11y; `window.alert`("link copied") → toast; dead "was this helpful?" Yes/No buttons made functional (local vote + toast); scoped `error.tsx` on both routes. **history `/notifications` (1 batch):** the data-load-fail card's hardcoded English literal → localized `t.errors.somethingWentWrong`; `NotificationHistory` ~20 stone-*/#166534/#1A2420 → tokens (kept channel brand colors); scoped `error.tsx`. **Shared components improved:** `.pw-*-button` focus rings, `ProcessingHUD`, `Modal`. **The B2C operational surface is now clean.** (`/activity` is agent-only, not B2C; `UserDashboard` linking it is dead code — no standalone B2C history route exists, `/notifications` is the nearest.) **Tracked residual (maintainability / not CI-blocking / renders correctly):** `/wallet` detail status-hex dedup (~10 cards duplicating `TONE_PILL`) + agent-only `PolicyReviewScreen`; `NotificationHistory` is Greek-hardcoded (needs a bilingual i18n pass); checkout VAT labelling (separate Stripe-Tax launch task) + the grid-vs-modal "which plan is recommended" product decision (grid=Starter, modal=Plus). **MERGED + DEPLOYED (22 Jul):** fast-forwarded NEW-UI `2d877c8..ecfa79f` (no divergence — origin was the deployed onboarding tip), `vercel --prod` → `dpl_7Csu2WXHP1Aaq` READY, aliased www.policywallet.gr, smoke-verified. No migration (B2C commits are UI-only). **Next:** the same page-by-page pass can be reused for the B2B/admin surface, and/or take on the tracked residual above.
- **TRACK A — ADMIN SECTION MADE OPERATIONAL: MERGED + DEPLOYED (22 Jul, PR #220 → NEW-UI, prod `dpl_CTAez58Cur…` / `b381239`, aliased www.policywallet.gr; no migration; smoke: public 200 / `/admin/policies`+`/admin/activity`+all `/admin/*` 307→signin):** an audit found admin could *configure* (plans, partner offers) and *stay compliant* (GDPR/DSR) but could NOT *run the business* — no billing ops, no token grant, no policy management, and admin-granting was broken end-to-end. Shipped as 6 per-priority commits. **P1 (foundational):** `changeUserRole` wrote only the DB `users.roles`, never the Supabase JWT `user_metadata.role` that middleware (`proxy.ts`) gates `/admin` on, so a UI-promoted admin was bounced until `auth.users` was fixed by hand; now syncs both (auth user resolved by email — `User.id` is a cuid ≠ auth UUID — metadata merged), effective on the target's **next re-login** → the manual `auth.users` SQL step is no longer needed for UI promotions. Also built the role modal (the button set state but no modal existed and `changeUserRole` was never called). **P2:** billing ops `issueRefund`/`cancelSubscriptionAsAdmin`/`applyCredit` (server-only Stripe, `mapStripeError` friendly messages, audit) on `/admin/billing-reconciliation` — ⚠️ **Stripe is still TEST-mode in prod, so these call the test API (no real money moves) until live keys are set**. **P3:** `grantTokens` (TokenBalance.purchasedTokens increment + €0 TokenPurchase history row + audit; returns new balance). **P4:** `/admin/policies` — `requeuePolicy` (reap scoped to the policy + fresh run + QStash enqueue, honours the GDPR AI-consent gate — top recurring incident), `deletePolicy` (best-effort storage cleanup + cascade — every Policy FK is Cascade/SetNull), `updatePolicyFields`, `mergePolicies` (extracted a shared `mergePolicyRecords` from the consent flow; its 6 tests unchanged). **P5:** `/admin/users/[id]` support view (profile, token balance + inline grant, subscriptions, policies, billing events, sessions, security events; wired the orphaned `getUserDetails`, extended to include tokenBalance + invoices). **P6:** persistent admin **sidebar** (section was URL-only), `/admin/activity` (wired orphaned `getActivityLogs`, fixed the dashboard's broken link), removed fabricated token-dashboard figures (the +12%/+8% trend badges + stale-priced profit margin), deleted dead `AgentVerificationModal` + the unreachable user-detail modal. **+30 unit tests (1218 total)**; type-check/lint/i18n/utf8/audit:api-auth/build all green. **Deferred (noted):** impersonation (needs its own session-minting security design); the JWT-sync effect is on re-login, not instant (the modal surfaces `ROLE_SAVED_JWT_SYNC_FAILED` if sync fails).
- **QStash AI-analysis queue ACTIVATED in prod (22 Jul):** owner added `QSTASH_TOKEN` + `QSTASH_CURRENT/NEXT_SIGNING_KEY` + `QSTASH_URL`; I set `QSTASH_CALLBACK_BASE_URL=https://www.policywallet.gr` (`AI_ANALYSIS_PARALLELISM` already present) and redeployed prod (`policy-wallet-rf2o3ilzl`). `enqueueAnalysisRun` now publishes to QStash (concurrency cap + 5 retries) instead of the inline `after()` fallback — closes the top scale risk (unbounded LLM fan-out on an upload burst). Verified: unsigned `POST /api/v1/jobs/execute-analysis` → **401 "Missing signature"** (signing keys loaded; a 503 would mean not). **Not yet proven end-to-end** — needs one real authed PDF upload to confirm the publish→callback→executeRun round-trip (watch the QStash console for a message + the execute-analysis logs). Separate remaining step if wanted: run `scripts/setup-qstash-reaper-schedule.mjs` for the 15-min reaper cadence (Hobby crons are daily-only).
- **STALE PR SWEEP + Sentry POLICYWALLET-3 cleared (22 Jul):** owner disabled the Brevo authorised-IPs allowlist → -3 resolved (**Sentry now 0 unresolved**). Reviewed all 5 stale open PRs: **#217** revived the two still-valid ones (#84 duplicate analysis-prompt toast — bug was still live: poll `router.refresh()` re-fired the notification-prompt toast every 2–10s, fixed with a `notifyPromptShownRef` guard + stable sonner id; #140 last raw agent palette class `border-l-slate-400`→`neutral-400`) — MERGED + DEPLOYED `policy-wallet-ehzp22sr0`. **Closed** #84/#140 (superseded by #217), #104 (obsolete STATUS snapshot), #44 (QStash scale-hardening code already on NEW-UI; targets dead `product-revision` branch — remaining item is the **prod QStash env config**, ops not code). **#47** (technical deck) left open for owner decision (content stale). All guardrails + 1188 unit + prod build green.
- **SENTRY BOARD CLEARED — 5 code fixes shipped, transient noise triaged (22 Jul):** across the session, fixed + deployed + resolved: **POLICYWALLET-E** (createPolicy limit throw→structured return, #211), **-C** (collab proposals 500 / never-populated body, #212), **-7** (gap-report liability vocab, #212), **-8** (admin/dsr hydration mismatch — bare `toLocale*` UTC-vs-Athens → pinned el-GR + Europe/Athens, #215). Resolved as transient/obsolete with root-cause notes: **-2/-4/-5/-A/-B/-6** (Prisma pooler connection-pool exhaustion `pool_size 15` + a pre-migration preview `tax_id` error — mostly preview, none recurring; auto-reopen on recurrence), **-9** (pre-migration tax_id preview), **-D** (deploy-transition stale server-action, benign), **-F** (single transient /customers event). **Only 1 unresolved remains: POLICYWALLET-3 (Brevo 401 "unrecognised IP") — NEEDS YOUR ACTION: disable the Brevo authorised-IPs allowlist (Vercel egress IPs rotate).**
- **COMPANY DETAILS WITHHELD — «available soon» (22 Jul, #213 MERGED + DEPLOYED `policy-wallet-lrxy57frv`, aliased www.policywallet.gr; prod-verified live):** per owner request, removed the operating company's identifying details (legal name «Insurance Martech Ι.Κ.Ε.», ΓΕΜΗ, ΑΦΜ, registered seat) from every rendered surface — footer (`PublicMegaFooter`), Terms *provider*, Privacy *controller*, Terms *contact* (EL+EN), plus `honest-copy` Athens location — replaced with one `detailsComingSoon` notice from the single source `lib/legal/entity-placeholders.ts`. Court venue genericized Chios→«τα Δικαστήρια της Ελλάδας»/«the courts of Greece» so the seat isn't leaked via jurisdiction. Kept DPO mailbox + liability cap + the brand name «PolicyWallet». Contact-page/JSON-LD address is env-driven (`NEXT_PUBLIC_CONTACT_*`) — confirmed **unset in prod**, so already blank (no change). Restore path documented in the entity file. Live-verified: EN /en/terms + EL /privacy show the notice, no name/ΑΦΜ/ΓΕΜΗ/Χίος. All guardrails + 1188 unit + prod build green.
- **B2C + B2B FULL AUDIT — 8 findings fixed (22 Jul, branch `fix/b2b-b2b-audit-2026-07`, PR #212 — awaiting merge auth):** parallel reviewers (2 completed, 3 interrupted by Fable credit exhaustion → those areas reviewed by hand) + full prod Sentry sweep. **HIGH (outages):** `collaboration/proposals` + `document-requests` routes destructured `body`, which `withApiGuard` only populates when a `validation.body` schema exists — neither had one, so every request 500'd (POLICYWALLET-C). Added Zod schemas. **MED:** (IDOR) `commitScannedPolicy` wrote a customer's ΑΦΜ before `addPolicyForCustomer`'s relationship gate → moved backfill after the gate; (spam/phishing) `createThread`/`addAction` emailed an arbitrary caller-supplied recipient → constrained to relationship/thread participants; `sendQuestionnaire` missing the active-relationship gate + rate limit its siblings enforce → added both; gap-report vocabulary for liability/business/disability slugs unmapped (POLICYWALLET-7, 40 events) → 16-entry block w/ concept-key dedupe. **LOW:** `playbooks` route naive `roles.includes('agent')` → declarative guard role gate. **Verified SOUND (no change):** entire b2c wallet mutation surface ownership-scoped (getPolicyAccess/ownerUserId/access-grant); agent portfolio visibility composes getAgentPolicyVisibilityWhere everywhere; entitlement gates server-side. **Ops (not code):** POLICYWALLET-3 = Brevo IP allowlist blocks rotating Vercel egress IPs → disable it. +5 unit tests; all guardrails + 1188 unit + prod build green. Doc: `docs/audits/b2c-b2b-full-review-2026-07.md`.
- **SENTRY POLICYWALLET-E — POLICY_LIMIT_REACHED escape FIXED (#211 `3eefa53`, 22 Jul, MERGED + DEPLOYED `policy-wallet-6lf2mmzmz`, aliased www.policywallet.gr; no migration; smoke: public 200 / protected 307→signin):** `createPolicy` threw `Error("POLICY_LIMIT_REACHED")` for `AddPolicyClient` to match on `error.message` — but **prod builds redact thrown server-action messages to a digest**, so the mapping only ever worked in dev: a free-tier user at the 1-policy cap on /wallet/add got a generic failure toast instead of the `policy_limit` UpgradeModal (broken paid-aha conversion moment), and every hit landed in Sentry as an unhandled error (4 events since 20 Jul, release `3ebbcf7`). **Fix:** structured `return { error: "POLICY_LIMIT_REACHED" }` (same pattern as `uploadPolicyDocument`) + client branches on `'error' in result` BEFORE the success path — load-bearing pairing, since the old client treated any non-policyId result as success (server-only change would have shown a success toast on error). `limit_hit` conversion event kept. +3 unit tests (`create-policy-limit.test.ts`; 1185 total); all guardrails + prod build green; Sentry issue resolved with fix comment. **Follow-up noted (same defect class, not gating):** `createPolicy` still throws `Unauthorized`/`User record not found`/Zod validation — all reach prod users as redacted generic errors; sweep server-action error returns if these surface.
- **BRAND ELEVATION STAGE A — design-system discipline sweep (19 Jul, branch `feat/brand-elevation`, PR pending — NOT deployed; visual consolidation, review on the Vercel preview):** new opinionated plan `docs/design/BRAND_ELEVATION_2026-07.md` (type ladder 12–56px + weight rules, canonical green family + neutrals with per-file off-palette inventory, ONE spacing rhythm `pt-28/36 · py-20/28 · band py-16`, ONE CTA pair, tone rules banning "AI-powered"/"10x" with before/after voice, motion restraint, verifiable-fact trust tiles). Mechanical half implemented on landing/pricing/LoBPageShell/product-index/solutions-agents + footer: **24 CTAs** → `pw-primary/secondary-button` (+ new `pw-btn-sm/lg` + inverse pair in globals.css), **8 `font-black`** → bold, **60 off-ladder sizes** normalized, off-palette hexes (beige pricing highlight, indigo onboarding pill, `#337D6F`/`#4ADE80`/`#E5E7EB` family strays) → tokens, dead framer-motion components (ValueSection/AISection/Footer/ScrollToTop) deleted, pricing page got a `<main>` landmark + pill toggles. Landing first-load JS measured 448,293→448,176 B gzip (framer-motion was never in that bundle — Stage-B Server-Components refactor is the real perf lever). Copy/tone rewrites, testimonial policy, LoB PageClients + category-pastel decision = Stage B (checklist in the doc). All guardrails + 911 unit + prod build green.
- **AGENT-ONLY ΕΠΙΣΚΟΠΗΣΗ + PAYING-ONLY MANUAL RE-ANALYSIS (19 Jul, branch `fix/agent-review-paid-reanalysis`, PR pending — NOT deployed):** two access-control changes, server-enforced. **(1) Extraction review («Επισκόπηση Ασφαλιστηρίου») is now AGENT-ONLY** — it was the exact opposite (owner-scoped; agents couldn't reach it even for managed policies). Server gates (`/wallet/[id]/review` page + `confirmPolicyReview`/`flagPolicyExtraction`) require agent/admin role + `getPolicyAccess().canWrite`; every b2c trace removed (wallet chip, detail banner → agent-gated `canReviewExtraction` prop, post-upload inline review → analyzing/success state landing on the policy page, gap-engine deep link → policy page); agent entries from the detail banner + a new review button on `/customers/[id]/policy/[policyId]`, with a sanitized `returnTo`. **(2) Manual re-analysis is paying-only for BOTH roles** via ONE shared gate `canAgentTriggerManualAnalysis` (paid agent plan OR b2c pro, + the monthly cap) called by all FOUR manual triggers — the 8-angle review caught that `retryPolicyAnalysis` was an un-gated fourth trigger (free-agent infinite re-runs via the upload-failure retry), that the API routes missed the monthly-cap half (unmetered runs via direct POST), a **backslash open-redirect** in `returnTo` shared by three sanitizer copies (now one hardened `lib/navigation/return-to.ts`), the dual-role-b2c-Pro wrongly-paywalled case, and an admin-vs-agent predicate drift. B2C free/Starter keep the existing pro-tier orchestrator gate + upsell funnel; upload-time auto-analysis for agent_free unchanged (5/mo cap). **Accepted consequences (documented):** agentless b2c policies stay `reviewState='unconfirmed'` permanently (nothing renders it as a nag today) and b2c owners lost the flag-bad-extraction channel (admin triage now only sees agent flags). 800 unit (+18) + all guardrails + prod build green. branch `fix/ai-model-migration-prompts`, PR pending — NOT deployed):** full pass over the document-analysis pipeline. **Models (env defaults):** Gemini 2.5 family retires 2026-10-16 — extraction+gap → `gemini-3-flash-preview` (~60% cheaper than 2.5-pro, newer gen), clarity/QA → `gemini-3.1-flash-lite`, retry fallback → STABLE GA `gemini-3.5-flash`; **the Claude failover chain was silently broken** (`claude-sonnet-4-20250514` deprecated/retiring + `claude-haiku-4-20250414` NEVER EXISTED — both 404 exactly when Gemini is down) → `claude-sonnet-5`/`claude-haiku-4-5`, with `temperature` dropped from all Anthropic calls (Sonnet 5 rejects non-default sampling params with a 400); batch-translator off hardcoded `gemini-2.0-flash` (marked for shutdown) onto new `GEMINI_MODEL_TRANSLATION`. **Prompts:** new shared `lib/services/ai/prompts.ts` replaces three drifted per-provider copies and fixes real contradictions — DD-MM-YYYY vs YYYY-MM-DD date rules, a legacy `{acord...}` output contract competing with the schemas, three conflicting lineOfBusiness enums (incl. invalid "Property"), the bilingual-tags tail that *caused* the `{el,en}`-objects-where-strings-expected pathology `coercedGreekString` patches around, + a premiumAmount-vs-sum-insured guard (the #121 mis-extraction root cause). **8-angle review found + fixed:** a pre-existing **1000x token-metering under-report** (TOKEN_COSTS stored per-1K prices against a per-1M divisor — all historical `cost_eur` rows carry the old scale), three prompt invariants the unification had dropped (clarity acordData destination, single-acord-section rule, citations-off negative instruction), UNKNOWN_MODEL_COSTS now Opus-tier (over-meters, never under). 782 unit (+15) + all guardrails + prod build green (`verify:migrations` skipped — dev DB unreachable; no schema change in diff). **⚠️ Deploy checklist:** (1) audit Vercel prod env — explicitly pinned `GEMINI_MODEL_*`/`CLAUDE_MODEL_*` vars OVERRIDE the new defaults and keep the retiring models; (2) smoke a real policy upload post-deploy (new models are live-unexercised by CI); (3) note cost dashboards will show a 1000x step-up in new `cost_eur` rows (the fix, not a regression). Follow-ups noted: translator still bypasses metering/failover; provider Zod schemas still triplicated; Phase-B drill script + mock service reference old model strings.
- **AGENT PRODUCTION-READINESS: dashboard redesign + empty states + monetization honesty (17 Jul; full multi-lens audit → 7-batch plan; guardrails + 741 unit + build green throughout):** three parallel sweeps found the agent product is **honest** (no dummy/fabricated data survives — the #105/#121 cleanups hold); the real gaps were a hollow first-run, inconsistent empty states, and features sold-but-unbuilt. **Shipped + DEPLOYED (must-fix "30-day" tier):** (1) **#142 monetization honesty** — softened the flagship "branded reports" claim (0% built: no `/agent/reports` route, no report generator) to "coming soon" across the solutions page/SEO/onboarding/mockup; de-listed `apiAccess` (sold on Pro/Agency, zero implementation). (2) **#143 empty-state repair** — the premium `EmptyState` primitive (`components/ui/EmptyState.tsx`) was used on 2 of ~18 empties; routed **14 bare empties** through it (localized copy + one-action CTA + illustrative "Example" previews), added the 4 missing route loading skeletons, gave `AgentInbox` a retry error state (was swallowing fetch failures) + an `activity/error.tsx`. (3) **#144 dashboard first-run** — the purpose-built `AgentGettingStartedChecklist` was **orphaned dead code**; wired it into the dashboard with 5 real server-computed completion signals; killed the falsely-reassuring first-run states (ActionQueue "All caught up" → context-aware, PortfolioHealth "0% gaps" → "no clients yet", suppressed the "+100%" vanity growth, activity-feed empty state); dropped a wasteful double-query (`getDashboardData` ran two queries then discarded both) + removed 4 dead ActionQueue item-types. **PR pending (finishing must-fix):** onboarding — removed the "Professional Title" input (collected then silently discarded, no schema field) + the broken "share link" (pointed at a nonexistent `/agent/[slug]` route on the wrong domain `policywallet.app`; the email invite is the real path); entitlement enforcement — `brandedPortal` now gated server-side on the AGENT's tier in the customer-facing portal (a free agent's custom branding no longer reaches the client; the `AgentPlanGate` blur was cosmetic), `pipelineAnalytics` Revenue-Pulse data withheld server-side for free agents (was shipped in the payload behind a client blur). **[SCALE, not yet built]:** Batch 4 ROI dashboard reframe (revenue-weighted priority + cross-sell/tasks/revenue-at-risk widgets), Batch 6 branded-reports v1 (build the softened claim), Batch 7 CollaborationTimeline i18n. Plan: `~/.claude/plans/you-are-a-multi-agent-crystalline-clarke.md`.
- **AGENT CHECKOUT/BILLING AUDIT + TRUST FIXES (17 Jul, MERGED #141 + DEPLOYED `dpl_CwrqLDf8` / `03449be`):** full multi-lens audit of the B2B pricing/checkout/Stripe/VAT/billing flow, triggered by the **€49.99→€61.99** report. **Root cause (certain):** advertised prices are **VAT-inclusive**, but `lib/billing.ts` treated `plan.price` as net and sent `price × 1.24` as the Stripe `unit_amount` (`:89`) → a **24% overcharge** (€49.99 shown → €61.99 taken; same mechanism the money-path test hit: Starter €19.99→€24.79). Not a Stripe setting, duplicate product, or stale value; prices are otherwise numerically consistent everywhere. **Fixed in code:** (0) **the overcharge** — `unit_amount: Math.round(periodPrice * 100)` charges the advertised inclusive price as-is (€49.99); VAT is the contained portion (€40.31 net + €9.68 VAT), via a new `vatInclusiveBreakdown` in client-safe `lib/billing/vat.ts`; (1) `AgentPricingClient` shows the price labelled "incl. VAT" + a recurring/cancel disclosure; (2) `AccountOverview` upgrade cards labelled "incl. VAT" + localized `perMonth` (was hardcoded `/ mo`); (3) **false "14-DAY FREE TRIAL" on Agent Pro** — `isPro = name.includes('pro')` drove a trial badge/CTA on a plan with no trial (agent charged €61.99 immediately); now gated on real `planHasTrial(plan_id)` via new client-safe `lib/billing/trial-plans.ts`; (4) "Redirecting to Stripe…" → "Redirecting to secure payment…" (both call sites); (5) agent "Modify plan" routed to B2C `/upgrade` → now `/agent/pricing` when `plan_type==='agent'`; (6) public `/for-agents` over-promised "7 templates" (enforced 5) → 5; (7) legacy "Premium" plan-name copy → upgrade-neutral. Consolidated VAT math → client-safe `lib/billing/vat.ts` (server + all UI share `grossWithVat`; `lib/billing.ts` re-exports). **[LAUNCH] compliance tail specced, NOT changed** (needs Stripe-Tax account): itemize the *contained* VAT on the invoice via `automatic_tax`/`tax_behavior:'inclusive'` + `tax_id_collection` + billing-address (the €49.99 line is not yet split into €40.31 net + €9.68 VAT; still GR-24%-for-all, no reverse-charge); local `Invoice` rows never written (empty billing history + reconciliation always alarms); retire the dual live `/api/stripe/webhook`; resolve the Agency self-serve-vs-contact contradiction (a purchasable €99.99 price exists). Full report: [docs/audits/agent-checkout-billing-audit-2026-07.md](audits/agent-checkout-billing-audit-2026-07.md).
- **AGENT DESIGN-SYSTEM: slate/stone → SEMANTIC TOKENS (17 Jul, branch `fix/agent-semantic-tokens` off NEW-UI; guardrails + 741 unit + prod build green; ⚠️ PR pending, NOT deployed — large visual change, review the Vercel preview before prod):** the P1b design-system item, but the audit's framing («agent on slate vs app-wide stone; normalize slate→stone») was **inverted**. Investigation: MASTER.md specifies a **"slate-neutral canvas"** and `globals.css` (runtime token source) maps the neutral scale to slate (`--muted: #f1f5f9`/slate-100, `--border: #e2e8f0`/slate-200, `--muted-foreground: #64748b`/slate-500); slate is also more prevalent (107 files vs 50 stone) and the core b2c wallet uses the semantic tokens (which resolve to slate). So **slate is canonical; stone is the drift** — introduced only via recent agent PRs. Per the user's choice, did the correct fix: migrate **all raw `slate-*`/`stone-*` on 43 agent-scoped files to the design-token system**. Two-tier deterministic transform: (1) exact/near-exact dark-pairs → **role tokens** — `text-{slate,stone}-900 dark:text-white`→`text-foreground`, `text-*-500 dark:text-*-400`→`text-muted-foreground`, `bg-*-100 dark:bg-*-800`→`bg-muted`, `border-*-200 dark:border-*-800`→`border-border` (263 adoptions); (2) everything else → **`neutral-*` scale** (the `@theme --color-neutral-*` = the exact slate hex, so `slate-N`→`neutral-N` is **byte-identical**; `stone-N`→`neutral-N` is the **intended canonical shift** to slate). Added `--color-neutral-950: #020617` to complete the scale (dark page backgrounds). Agent-scope raw palette classes: **147→0**. Verified at the CSS layer (build output confirms `--color-neutral-950 = #020617`, `.dark\:bg-neutral-950 → var(--color-neutral-950)`, role tokens resolve). Re-theming the agent section is now a `globals.css` change. **Risk:** slate portion is provably zero-regression; the ~9 stone-heavy files (e.g. `QuestionnaireSender`, agent modals) shift warm→cool gray by design. **Recommend a visual pass on the preview deploy before prod.** Shared `components/ui/*`, `components/wallet/*`, and other b2c files were left untouched (out of scope; also mostly slate/token already).
- **AGENT i18n EXTERNALIZATION — COMPLETE + DEPLOYED, 2 batches (17 Jul; last P1b item of the agent-B2B production-readiness audit; #137 + #138 merged, prod `dpl_7aKEQQe1` = `d259649` aliased policywallet.gr, smoke green — public 200 / agent 307):** cleared the agent-surface bilingual-literal debt to **zero**. **Enforcement guard:** `scripts/check-i18n-hardcoded.js` was changed-files-only (fast/non-regressive for CI but blind to pre-existing debt); added an **opt-in full-tree scan** (`SCAN_ALL=1` or `--all` → `git ls-files '*.ts' '*.tsx'`) that surfaces the whole backlog without changing the CI gate — it reported 250 app-wide / 147 agent-surface issues. **Batch 1 — MERGED as #137 (`0515eff`)** — 61 literals across 10 files: the flagship `InsightsClient.tsx` (39 → new `insights.practice.*`; hoisted `const lang = language || "en"` to kill a `label:`+`|| "en"` fallback flag; added `p`/`lang` to three `useMemo` deps for React-Compiler's `preserve-manual-memoization`), plus a shared `agentUi.*` (22 atoms) for `AgentCard`, `TrustSignals`, `PortfolioHealth`, `ClientCard`, `ActionQueueCard` (+ `ActionQueueEmpty`, which had a regex-invisible multi-line ternary), `ClientActivityTab`, `AgentPlanGate`, `QuestionnaireForm`; `QuestionnaireSender` got a key on its local `copy` object (its `t` is shadowed by a template `.map` var). **Batch 2 — MERGED as #138 (`d259649`)** — the remaining 85 literals across 7 files: `DocumentRequestFlow`, `ProposalCard`, `AgentInbox` (reuse existing `collaboration.{documentRequests,proposals,inbox}` keys on exact EN+EL match, add new keys where wording differs), `CollaborationTimeline` (new `collaboration.timelineToasts.*` (9); added the missing `useLanguage` import + routed toast literals through `t`), and `ClientOverviewTab`/`ClientFinancialsTab`/`CommissionsLocked` (new `clientOverview.*`/`clientFinancials.*`/`commissionsLocked.*` sections). No behavior change either batch (strings rendered correctly bilingually already). This unblocks the `slate-*`→`stone-*` sweep on the cleared agent files.
- **RETENTION CRONS REVIVED — #123 (`7aea1d2`) MERGED + DEPLOYED (17 Jul, prod `policy-wallet-5enz9zlod`, aliased www.policywallet.gr; no migration; 717 unit + build green):** the 7 scheduled Vercel crons (`renewal-check`, `protection-score-refresh`, `churn-prevention`, `engagement-drip`, `weekly-digest`, `perk-reminders`, `collaboration-reminders`) **never ran in production** — the renewal ladder (the product's core retention promise «η AI σας ειδοποιεί πριν τη λήξη») and every lifecycle email were silently dead. **Confirmed from prod runtime logs:** `GET /api/v1/jobs/protection-score-refresh 307 [serverless-middleware]`, with ZERO hits on the routes' success log lines. Two causes, both silent: (1) `proxy.ts` did not allowlist `/api/v1/jobs`, so each cron request (no Supabase session) was **307-redirected to `/auth/signin`** before the handler — and Vercel counts a 307 as a *successful* cron ping, so nothing ever alerted; (2) the routes only exported **POST**, but Vercel Cron issues **GET**. **Fix:** allowlist `/api/v1/jobs/` in `proxy.ts` (routes self-authenticate via `CRON_SECRET`, same defense-in-depth as the Stripe webhooks) + `export const GET = POST` on all 8 cron routes (no request body read); inventory synced (`audit:api-auth` green). **This corrects the 14 Jul monetization-integrity claim that scheduling the crons made renewals live — they were scheduled but non-functional.** **Verified live:** `GET /api/v1/jobs/renewal-check` now returns **401** (route reached; `CRON_SECRET` confirmed set in prod) instead of a 307 to signin; public 200; `/dashboard/agent` 307. ⚠️ Next scheduled firing is **05:00 UTC** — the true end-to-end proof (200 + "Renewal check job completed" in logs); the first real run may send a **catch-up batch of renewal emails** since the ladder was dead a while (watch Brevo volume). Surfaced by a broader agent-B2B audit whose tenant-isolation findings were already fixed on NEW-UI (the `agent-visibility` model + `isAgentRole` gates); this PR is scoped to the one thing still broken on prod.
- **AGENT DASHBOARD/COMMISSIONS INTEGRITY — #121 (`2f19320`) MERGED + DEPLOYED (16 Jul, prod `dpl_8TxjLp54o1NhcAsMf9UuCoSdx9xE`, aliased www.policywallet.gr; no migration; 717 unit + build green):** live verification (1 customer, 4 policies, 3 exact duplicates, ~€400 book) surfaced wrong agent-facing numbers. **(1) Monthly Revenue «€5,063»** — `dashboard/agent/page.tsx` summed commission over ALL policies (no dedup — the comment there explicitly refused it) so the 3 duplicate rows each counted, and one policy's `premiumAmount` is **mis-extracted** (a sum-insured captured as premium). New `lib/agent/revenue.ts` `computeAgentBookRevenue` dedupes via `selectPremiumBearingPolicies` (same-number re-uploads = one exposure) and drops implausible premiums (`MAX_PLAUSIBLE_ANNUAL_PREMIUM = €100k`), so MRR is the agent's real commission on the in-force book (+4 unit tests). **(2) Dollar signs** on a euro-only product — swapped the `DollarSign` lucide icon for `Euro` on the money cards (`RevenuePulse` — the revenue card, `CommissionsClient`, nav, `TeamClient`, `RenewalsClient`, `ClientDetailView`), fixed the `'€':'$'` invoice ternary (`BillingHistory`), removed the USD/GBP `<option>`s; `RevenuePulse`'s inline bilingual labels moved to existing `agentDashboard.*` keys. **(3) «Πληρότητα Χαρτοφυλακίου 6/100»** was actually the customers' **average protection score** (coverage adequacy, `agent-portal.service.ts computePortfolioCompleteness`), legitimately ~6 for a single-line under-insured customer — relabeled `role-copy.ts` to «Μέσος δείκτης προστασίας» / "Avg. protection score" (no data change). Also explains the €5,063-vs-/commissions-€0 split: dashboard = in-force policy book, /commissions = (empty) opportunity pipeline. **Deferred (pre-existing bilingual-string i18n debt in those files):** the `DollarSign` icon on `/insights` + the dead `agent-rise` module; the extraction root cause (AI reading sum-insured as `premiumAmount`); the 3 existing duplicate policies (user cleans up; #119 prevents new ones).
- **AGENT UPLOAD FOLLOW-UPS (4 issues from live verification) — #119 (`269fb63`) MERGED + DEPLOYED (16 Jul, prod `dpl_5X9rLPw8uNXELoJa4uxHauhJ2D42`, aliased www.policywallet.gr; no migration; 713 unit + build green):** verifying the upload flow as an agent surfaced four issues. **(1) Completion notification arrived "much later."** The `policy_analyzed` row is written promptly at completion; the lag was the `NotificationWatcher`'s 25s poll → lowered to **10s** (matches the b2c wallet cadence). **(2) Same policy added multiple times under one customer** (confirmed live: one customer with 4 policies, several duplicate «liability»/«Η ΕΘΝΙΚΗ» rows from insurer-spelling variance). The only dedup runs post-analysis and is skipped when analysis doesn't run (no consent/quota); added a **pre-add duplicate guard** in `addPolicyForCustomer` keyed on `ownerUserId + policyNumber + lineOfBusiness + startDate` (startDate is the issue-date proxy — there is NO issueDate column; a genuine renewal has a different term so it's not flagged), returning a `duplicate` state the modal surfaces as an **"Add anyway / Cancel"** step; `confirmDuplicate` threads through `commitScannedPolicy`→`addPolicyForCustomer` to override. Prevents NEW duplicates; the pre-existing 4 rows are untouched (deleting prod policies is destructive — left for explicit cleanup). **(3) PWA "install app" popup opened on every reload** — the 7-day snooze was only written by the X button; now snooze-on-show + an `appinstalled` listener + persist on install attempt, so it appears at most once per window (`components/pwa/InstallPrompt.tsx`). **(4) "New Task" modal read as unclosable / filled the screen** — `components/agent/CreateTaskModal.tsx` had X+Cancel but no backdrop-close, no Escape handler, and a header that scrolled out under `max-h-[90vh]`; added backdrop click-to-close (+ inner stopPropagation), an Escape handler, a sticky header (X always reachable), and `max-h` 90vh→85vh. **Verified live in-browser as an agent:** New-Task modal is sane-sized and closes on backdrop click; the duplicate problem confirmed on customer «Policyholder 8110». **Tests:** +2 duplicate-path cases in `upload-policy-orchestration.test.ts`. **Not live-verified (need a real matching-policy upload / 90s wait):** the duplicate warning firing, the 10s notification, and forcing the PWA banner — deployed + unit-tested.
- **AGENT POLICY UPLOAD OUTAGE FIXED — #117 (`dec9cf6`) MERGED + DEPLOYED (16 Jul, prod `dpl_DmBMY4ikYKRdEhvbStNAvf9nakPE`, aliased www.policywallet.gr; no migration; smoke: public 200 / agent 307→signin; 711 unit + build green):** the smart-upload flow returned «Failed to add policy» on every submit. Prod runtime logs showed the real cause (NOT the empty-date red herring, NOT the browser-extension `searchAnalyzer.js`/CSP-worker console noise): `uploadFile` (`lib/storage.ts`) hardcoded the **`'uploads'`** bucket, which exists on **no** environment → Supabase `StorageApiError: Bucket not found (404)` → `addPolicyForCustomer` catch. This was the exact latent bug flagged on 13 Jul («lib/storage.ts uses an 'uploads' bucket that exists on NEITHER env»), surfaced by the new agent-upload feature. **Fix:** route policy documents (`folder === 'policies'`) to the **private `'policies'` bucket at the bucket root** — the same bucket + object shape the b2c client upload (`components/wallet/AddPolicyClient.tsx`) writes — via the **service-role admin client** (`createAdminClient`; ownership-agnostic, since an agent uploads for a customer-owned policy); non-policy folders stay on `'uploads'`. Still returns the `getPublicUrl` string `PolicyDocument.fileUrl` stores and `downloadPolicyDocument` resolves via service role → no read-path change, analysis works. **Also:** `deleteFile` made bucket-agnostic via `resolveSupabaseStorageObject` + the admin client (fixes the already-broken b2c policy-doc deletion, which only parsed `'uploads'` URLs); and `UploadPolicyModal`'s confirm submit (a plain button, not a `<form>`, so `required` was unenforced) now guards insurer/number/start/end so an empty date can't reach the server as `new Date('')`. **Tests:** `storage-upload.test.ts` (4). **Follow-ups (noted):** the other `uploadFile` callers still target `'uploads'` and remain broken on prod (`/api/v1/upload` docs/profile/collaboration; agent onboarding docs); `gap-analysis.service` raw `fetch` 400s on the private bucket; browser preview of private-bucket docs needs signed URLs (pre-existing, b2c too).
- **AGENT BACKGROUND-ANALYSIS NOTIFICATION — MERGED #115 (`2c00262`) + DEPLOYED (16 Jul, prod `dpl_DThZxoKwP2a3sNde7gNUmYY4NA51`, aliased www.policywallet.gr; no migration; smoke: public 200 / agent 307→signin; all guardrails + 707 unit + build green):** after uploading a policy an agent can close the popup, the deep analysis keeps running (already deferred via `after()`), and they get **one** notification when it finishes. Three parts: **(1) exactly one completion notification on every outcome** — success already emitted `policy_analyzed` → the agent and silent-merge `policy_merged`, but the **token-blocked** and **failure-catch** branches of `runBackgroundAnalysis` (`lib/services/policy.service.ts`) emitted nothing, so a failed analysis left the agent waiting; added a `policy_analysis_failed` in-app notification via a `notifyAnalysisFailed` helper (fetches policyNumber+insurer; never throws). Branches are mutually exclusive → one per run. **(2) live in-app toast** — new `components/notifications/NotificationWatcher.tsx` (mounted **for agents only** in `app/(protected)/layout.tsx`; b2c keeps its wallet-page poller) polls a slim new `getRecentNotifications` action (~25s, paused when the tab is hidden), toasts new analysis outcomes even while idle, and `router.refresh()`es so the sidebar unread badge updates; per-user `lastSeen` marker in localStorage so history never re-toasts, capped against a flood; pure `selectNewNotifications` in `lib/notifications/watcher.ts`. **(3) modal copy** — the `UploadPolicyModal` success step now says analysis runs in the background, it is safe to close, and you will be notified when ready (`agentModals.uploadPolicy.analysisStarted`, el+en). **Tests:** `notification-watcher.test.ts` (6, the new-since-lastSeen selector), `analysis-failed-notification.test.ts` (3, the helper). **Follow-ups (noted, deferred):** the broken `NotificationBell` dropdown (never loads its list); email/push delivery (push dormant); b2c parity for the basic-summary path; the `readAt` vs `status` read-state inconsistency across the two mark-read paths.
- **AGENT SMART POLICY UPLOAD — MERGED #113 (`6150fd6`) + DEPLOYED (16 Jul, prod `dpl_JDnTKCj2…`, aliased www.policywallet.gr; migration `20260716150000_add_user_tax_id` applied to prod via `migrate deploy`; smoke: public 200 / agent 307→signin; all guardrails + 698 unit + build green):** an agent uploads a policy document; the AI identifies **both** the customer and the policy, then the flow **creates a new customer**, **attaches to the existing one**, or (on multiple hits) lets the agent **pick from a candidate list disambiguated by ΑΦΜ**. Delivered in 8 stages: **(1) schema** — new `User.taxId @map("tax_id")` (non-unique — same ΑΦΜ can sit on a phantom + a real account, email stays the unique key) + index; hand-authored idempotent migration `20260716150000_add_user_tax_id`; new `lib/identity/tax-id.ts` (`normalizeTaxId` digits-only 8–12, `isValidGreekAfm` mod-11, `maskTaxId`). **(2) extraction** — optional `customerTaxId`/`customerPhone` added to `AIPolicyExtractionResponse` + all 4 provider Zod schemas/returns (gemini/anthropic/openai + mock returns a valid sample ΑΦΜ) + threaded into `acordData.policyholder/insured` via `extraction-enrichment` (normalized; back-compatible). **(3) resolution** — new `lib/services/customer-resolution.service.ts` `resolveCustomerCandidates(agentId,{taxId,email,name,phone})`, **scoped to the agent's own non-inactive `CustomerRelationship`s** (no cross-agent leak); precedence valid-ΑΦΜ > email > phone/name; a valid-ΑΦΜ-vs-email disagreement returns candidates flagged `conflict` (no silent match); masked-VAT + visible-policy-count in the DTO. **(4) orchestration** — two agent actions: `scanPolicyForResolution` (reuses `parsePolicyPdfWithGemini` so the billable scan runs **once** on the 30/hr `agent-scan:` bucket, then resolves) and `commitScannedPolicy` which **converges on the unchanged `addPolicyForCustomer` core** (its relationship gate is a security boundary — left intact): create-new gates `canAgentAddCustomer`, makes the phantom+relationship, then attaches (conflict→attach fallback); attach backfills ΑΦΜ **only-if-null**. **(5) UI** — new `components/agent/UploadPolicyModal.tsx` (upload → parsing → resolution picker → confirm → success with `analysisState`/consent), plus a `presetCustomerId` mode that skips resolution. **(6) consistency** — dashboard "add policy" tile (was wrongly `router.push('/wallet/add')`, the agent's OWN B2C wallet), the customers page (primary "Upload Policy" button), and the per-customer profile (now `UploadPolicyModal presetCustomerId`) all launch the one flow; manual "Add Client" gained an ΑΦΜ field. **(7) i18n** — `agentModals.uploadPolicy.*` + `taxId` labels in el+en. **(8) tests** — `customer-resolution.test.ts` (9), `upload-policy-orchestration.test.ts` (6), extended `extraction-enrichment` (3). Guardrails green: type-check, lint, i18n, utf8, encoding, api-auth (0 — no routes added), **698 unit**, prod build. **Follow-ups (noted, deferred):** `AddPolicyForCustomerModal` is now dead code (unused); ΑΦΜ display/edit on the customer profile header (needs `Customer` type + header plumbing); shared `policy-form/*` subcomponent extraction; phone is a ranking booster only (stored phone isn't normalized). **Prod:** apply the `add_user_tax_id` migration via `migrate deploy` before/with deploy. Manual browser E2E of the scan needs `GEMINI_API_KEY` present.
- **B2B SECURITY + REPORTS + DEAD-BUTTONS PASS — #111 MERGED + DEPLOYED (16 Jul, `bf732e0`, prod `dpl_69UHRZJ4…`, aliased www.policywallet.gr; smoke: public 200 / agent routes 307→signin):** from a 3-lens audit (security / dead action buttons / reports). **Security:** (1) HIGH/GDPR — `addPolicyForCustomer`'s agent-attested-consent "unactivated" gate was `!password && !emailVerified`, but `password` is always null for real users (Supabase holds the credential) → collapsed to "email-unverified", letting an agent attest consent for a real, logged-in account and run AI over their policy without genuine consent; now matches the canonical activation check (also `!lastActiveAt`). (2) `redeemInvite` share/access invites minted an AccessGrant to whoever opened the link (leaked `policy:<id>` token → wrong account); now bound to the addressed email, checked before consuming (+3 tests). (3) `protectionScore` visibility leak in `/api/v1/customers/protection-scores` + `getAgentPortalData` (score/gapCount over the WHOLE portfolio for un-granted customers) → scoped to customers with ≥1 visible policy. (4) per-agent 20/hr rate limits on `createAgentInvite`/`requestAiConsent`/`sendClientInvite` (email-bomb). (5) `sendQuestionnaire` template-ownership check; (6) `getCommissionDashboard` missing `isAgentRole` guard. **Reports:** dashboard coverage-gap %/health scores counted only `open` gaps → `OPEN_GAP_STATUSES` (open/detected/acknowledged), aligned gapsSummary + insights.recentGaps; dashboard renewals/expiry used the placeholder-prone `endDate` column → resolved lifecycle date (matches insights + portal); portal "Pipeline" KPI counted only `open` opps → `notIn (won,lost)`; coverageGap/completeProfile % could exceed 100% → numerators intersected with current client set; commission monthly trend honours stored `estimatedCommission`. **Dead buttons:** bulk Export → real CSV download; bulk Email → BCC real emails (was customer ids); row/grid Phone/Mail → wired to tel:/mailto:; removed the dead dashboard "Request" quick-add tile + deleted unused `components/agent/Dashboard.tsx`. Guardrails green: type-check, lint, i18n, utf8, encoding, api-auth, 680 unit, build. Low/cosmetic left (noted): decorative "Agency Profile" settings label; commission by-line "Opps" count includes lost deals.
- **B2B CONSISTENCY + SCALE — 6 PRs MERGED + DEPLOYED (16 Jul), from a 3-lens audit (dummy-data / UI-UX / scale) of the whole agent section:**
  - **#103 upgrade journey** (deployed `dpl_J3cMxMtv…`): checkout/portal return URLs → canonical `getSiteOrigin()` (were `NEXTAUTH_URL` = the Vercel domain, so test-mode checkout bounced to `*.vercel.app`); Stripe product uses `plan.displayName` not the machine key; agent pricing marks/disables the current plan + server re-subscribe guard; agent-aware success copy.
  - **#105 Waves 1-2** (deployed `dpl_BkUM4Qd…`, migration `20260716130000`): cross-sell fabricated flat €400 premium → customer-derived; profile-completeness 45% fabricated floor → honest; bulk-import re-import-downgrade + limit-bypass; indexes policy_renewals(agent,status), gap_instances(policy,status).
  - **#106 Waves 3-4** (deployed): removed the onboarding demo (fabricated "Allianz €350" analysis) + dead stubs (sendReminder, dead CustomerProfile.tsx, getDashboardStats overview + its wasteful acordData scan, isOnline); insights opportunity metrics → groupBy.
  - **#107 dashboard scale** (deployed): agent landing page quadratic JS joins (O(P·R)/O(R·P)/O(P·G)) → Map/Set lookups, now O(P+R+G+A).
  - **#108 coverageEndDate denormalization** (deployed `dpl_5HeE7weK…`, migration `20260716140000` applied + `scripts/backfill-coverage-end-date.ts` run against prod — only 2 policies, both populated): active-policy counts filter a plain indexed column instead of deserializing acordData per row; mirrors isPolicyCoverageActive (NULL=coverage, 5 parity tests); populated at write points; getCustomers + getAgentPortalData switched off acordData.
  - **#109 i18n** (deployed `dpl_E1tBVB3E…`): localized the 4 fully-English agent pages (OpportunitiesClient, CustomersClient, policy-detail, invite) + enum leakage (branchLabel) + date locales + removed policy-detail dead New Quote/Add Note buttons. Follow-ups noted: page↔getAgentPortalData double book-load, commissions groupBy, ~90 more inline ternaries, design-token convergence.
- **B2B (AGENT SECTION) HARDENING — 4 WAVES (16 Jul, PR #100 MERGED to NEW-UI `6c29314` + DEPLOYED to prod `dpl_HsAG5GJNm5MG1J5pd2Cm6Ai2pcaP`, aliased www.policywallet.gr; migration `20260716120000_b2b_scale_indexes` applied to prod, schema up to date; smoke: public 200 / agent routes 307→signin):** production-readiness sweep of the whole agent section, stacked as 4 reviewable commits. **W1 privacy/security P0:** closed two portfolio leaks where a `CustomerRelationship` (not consent) let the agent dashboard `gapsSummary` + `insights` `recentGaps` read across a customer's whole portfolio (gap titles + policy numbers) — now scoped by `getAgentPolicyVisibilityWhere`; new `lib/auth/require-agent.ts` (`isAgentRole`) guards every agent server action (auth ≠ agent role — a policyholder could invoke agent writes); removed a live-invite-token dev leak on `/customers/invite`. **W2 scale:** killed the opportunity-scoring N+1 (`scoreAgentOpportunities` looped ~8 queries/opp **serially** — 500 opps ≈ 4000 round-trips/render) via `calculateEngagementScoresBatch`/`scoreOpportunitiesBatch` (engagement is per-customer, not per-opp); `getDashboardStats` 3 serial counts → 1 groupBy; **migration `20260716120000_b2b_scale_indexes`** (agent/status + last_interaction; invites had only the token unique; opportunities owner+status). **W3 correctness:** dashboard "Monthly Revenue" was the customer's premium/12 and commission a flat 15% — new `lib/agent/commission.ts` uses the agent's real per-line rates (MRR = commission on in-force book; pipeline agrees with `/commissions`); AI scanner `parsePolicyPdfWithGemini` was unmetered/unlimited → per-agent rate limit (30/hr) + token attribution; `createCustomer` set every manual add to `active` (phantom shows "activated", inflates activation rate) → `active` only for already-activated accounts. **W4 UI/UX:** localized the 4 fully-English agent modals (new `agentModals` namespace, el+en) + dashboard verify-email banner — which surfaced a real bug: `resendVerificationEmail(userEmail, "en")` sent Greek agents English emails (now their language); `alert()` → sonner toasts; agent dashboard made responsive (was fixed `grid-cols-12`); loading skeletons for `/customers|/opportunities|/commissions|/insights`. All 6 guardrails + 665 unit + build green on each wave. A helper agent began the modal i18n then stalled; the rest was completed + verified by hand.
- **MERGED `feat/paid-aha-loop-v1` INTO `NEW-UI` (15 Jul):** the two branches had forked at `d3bdd3f` and prod was flip-flopping between them per session — a P0 privacy fix (#95) even got reverted out of the live deploy when paid-aha-loop was redeployed on top. This merge makes the mainline carry **both** lineages so prod can track one branch again. **Pricing decision (owner):** the **paid-aha-loop tier restructure wins** — it is the deliberate, live, internally-consistent model (Free 1 policy / zero paid AI; Starter €2.99; Plus €7.99/€79 with all AI). Every monetization conflict resolved toward paid-aha-loop; NEW-UI's #90 conversion constants (Free=3 + 3 lifetime AI questions, plus=10, AI at `plus`, €9.99/€99) were **superseded** — `FREE_LIFETIME_QUESTIONS` is now `0` (the account/wallet meters render a 0 allowance; a tiny UI-cleanup follow-up). Folds in the NEW-UI work paid-aha-loop lacked: **#95 P0 privacy** (agent sees only uploaded/granted policies — hand-merged in `insights/actions.ts` to keep BOTH the visibility filter AND paid-aha-loop's premium/lifecycle logic, so the merge does NOT reopen the hole), **#92** expired-coverage (paid-aha-loop's `selectPremiumBearingPolicies` is the surviving helper on /home), **#96** agent upload + `policy_merge_requests` migration, **#93** the reconciled recommendation dedupe. `/home` took paid-aha-loop's denser dashboard wholesale (NEW-UI's #92 value-first ordering superseded — re-apply on top later if wanted). **Real bug caught by NEW-UI's stricter parity test:** `scripts/setup-billing-catalog.ts` still charged the old €9.99/€99 for `ph-pro` on BOTH branches — the catalog was never repriced when Plus dropped to €7.99; fixed to €7.99/€79 so Stripe charges what the UI shows. Both `20260714120000_recommendation_dedupe` (already applied to prod) + the two NEW-UI migrations (`recommendation_unique`, `policy_merge_requests`) coexist; the two recommendation indexes are complementary. 652 unit tests, all 6 guardrails + build green. ⚠️ **Not deployed** — delivered as a PR to NEW-UI for review; prod still runs paid-aha-loop (`00baaf8`) and still LACKS #95 until this lands. The outstanding prod migrations (`recommendation_unique`, `policy_merge_requests`) must be applied before/with the deploy.
- **PAID AHA LOOP v1 — SHIPPED (14 Jul, commit `c7a687f` on `feat/paid-aha-loop-v1`, deployed to prod `dpl_6qiKMTHn5G4LHe27a6UbH9KF6r73`, aliased www.policywallet.gr — /pricing verified serving Starter €2.99 / Plus €7.99):** shortest path from free activation → paid conversion, per the approved plan (`~/.claude/plans/magical-dreaming-toucan.md`). **Tier restructure (relabel-in-place — code keys `free|plus|pro` unchanged, only labels/prices/entitlements/gates move):** Free 3→**1 policy, zero paid AI**; `plus` becomes **"Starter" €2.99** (5 policies, basic reminders, **all AI stripped** — `interactiveQA/aiAnalysis/questions/gaps → 0/false`, `TOKEN_LIMITS.plus 1M→0`); `pro` becomes **"Plus" €7.99** (was €9.99; all AI, unlimited), the recommended tier everywhere. Every deep-AI FEATURE_GATE moved `plus→pro`; 3 new gate keys (`duplicate_coverage_detection`, `claims_preparation_assistant`, `family_portfolio`) + EL/EN copy. Relabelled all display surfaces (public pricing page + comparison rows, `subscription-copy`, `PlanBadge`, `CarriedPlanCard`, nav badges, SEO pricing meta). **Free-AI lockdown (the core correctness work):** removed the free "trial deep-analysis"; deep pipeline (`createRun`) is now **`pro`-only**; free/Starter get a **basic parsed summary only** via a new `orchestrator.extractBasicSummary()` (extraction step + scalar persist, no token gate) wired through `runBackgroundAnalysis` + `triggerOnboardingAnalysis` by tier. **Closed 2 real leaks:** `/api/policies/extract` (was raw Gemini, no auth-guard/rate-limit/cap → `withApiGuard` + rate-limit + policy-cap gate, which also honestly enforces "free = 1 policy"); `parsePolicyPdfWithGemini` (ungated action → agent-role gate). Gap/Q&A gates now block Starter too (`tier !== 'pro'`). **Dual-CTA `UpgradeModal`:** primary "Συνέχεια με Plus — €7.99" + secondary "Ξεκίνα με Starter — €2.99" + "Όχι τώρα". **New `PremiumInsightCards`** 6-card locked grid (reuses per-feature copy) after first parse — onboarding step 3 + policy detail (non-pro). **Per-feature success:** `feature` threaded checkout→success page → uses the (previously unused) `successMessage`/`successCta` + a `UpgradeSuccessTracker`. **Analytics:** 10 new `JourneyEventName`s + 3 server `conv_*` types (`free_ai_call_blocked`/`paid_ai_call_started`/`_completed`) emitted at the block/run sites. **Verification:** 620/620 unit tests (added a paid-aha-loop tier-model test; rewrote the trial-analysis tests for the Plus-only model), `audit:api-auth`/lint/i18n/utf8/encoding/type-check all green, prod **build green** (with dummy Upstash env — local `.env.local` has the placeholder-URL crash). E2E extension + a live Stripe-test smoke are the recommended pre-merge steps (E2E isn't in CI). **Prod cutover COMPLETE:** prod `plans.ph-pro` updated to `price=7.99`, `display_name='PolicyWallet Plus'` (name stays 'pro' → tier resolution intact) via Supabase prod `cquudefwfwrmvpftuhyl` — checkout now charges €7.99 matching the display. Grandfathering of live `ph-plus` AI subscribers ~0 (pre-GA). Branch also carries the unrelated in-tree wallet-density-refactor changes (disjoint files, untouched, uncommitted). Still Stripe TEST-mode billing — live-key cutover is a separate later step. **Follow-ups shipped (14 Jul, 2nd deploy):** batch-upload modal now routes the new `POLICY_LIMIT_REACHED` 403 to the `policy_upload_limit` UpgradeModal (was a generic per-file toast); `tests/money-path.spec.ts` extended for the locked-cards grid + dual-CTA modal, and its helpers/asserts fixed for the new modal + Free-cap-1 (`Συνέχεια με Plus` CTA, `2 / 1` meter, Starter on /upgrade) — 620 unit + all guardrails + build green, spec parses/discovers (E2E not run — needs the local dev stack; a live Stripe-test walk is still the recommended manual pre-GA check).
- **WALLET REFACTOR SHIPPED TO PROD (14 Jul, `38c4502` on `feat/paid-aha-loop-v1`, `dpl_GcgfqnDQD9PV48Pp8RutAQcVBnt4` aliased www.policywallet.gr):** the two commits below (`1815c9b` + `38c4502`) are LIVE. E2E earned its keep here: both wallet specs were **"flaky" (passing on retry luck), not failing**, which hid two real regressions from the density refactor — (1) the rewritten `PolicyTable` dropped its `<h2>`, leaving the policy list with **no accessible name** (the only heading on /wallet was the "welcome back" greeting); restored as a `<section aria-labelledby>` wrapping both grid and list. (2) The status pill read **ΕΝΕΡΓΗ** where the table had read **ΕΝΕΡΓΟ** for months — unifying onto `t.policyStatus` surfaced a **gender inconsistency already in that block** (ΛΗΓΜΕΝΟ/ΕΛΛΙΠΕΣ/ΜΗ ΕΠΑΛΗΘΕΥΜΕΝΟ are neuter, agreeing with «το ασφαλιστήριο»; active was feminine). Set to ΕΝΕΡΓΟ. Both E2E patterns were coupled to prose the refactor deleted: the heading regex knew `συμβόλαια` but not **`ασφαλιστήρια`** — the word the desktop wallet actually uses — and the content probe matched `/ενεργό/i` with an **accented ό**, which JS case-folding does not equate with the pill's uppercase **Ο**, so it had only ever matched the removed line "Ενεργό έως …". Full suite with **retries OFF: 50 passed, 0 failed, 0 flaky** (was 3 flaky). Prod smoke: public routes 200, auth routes 307, pricing serving €2.99/€7.99, **zero runtime errors**. ⚠️ `ΑΚΥΡΩΜΕΝΗ` is still feminine in the same i18n block — a copy call, left alone. ⚠️ The signed-in wallet UI was **not** verified against prod (the local E2E account lives on the dev Supabase, not prod).
- **WALLET DASHBOARD: DENSITY REFACTOR + ONE STATUS PIPELINE (14 Jul):** the wallet was long and repetitive, and the diagnosis found the cause was structural, not cosmetic — **the wallet had three independent status pipelines**: `PolicyCard`'s own badge map (on the canonical lifecycle), `MobilePolicyCard`'s `getStatusConfig` (on the raw stored status, defaulting to **green**), and `document-insights`' tone map. Because the stored status is never recomputed, an expired policy reached the card layer as `action_needed` and rendered as a **red alarm**, contradicting the "expiry is a calendar fact, amber not red" rule that ac0fb69 established. All three now collapse into **`lib/wallet/policy-status-view.ts`** (`getPolicyStatusView` → `{key, tone, label, pillClass, chipClass}`, built on `resolvePolicyLifecycle`) + one **`<StatusPill>`** primitive; callers never pick colours. Tone rules are deliberate and tested: `expired` → amber (already happened), `expiring_soon` → red (still actionable), `unknown_duration` → neutral (we genuinely don't know). The stored status is consulted only for what the lifecycle cannot model — `analyzing`, and a failed extraction (`action_needed`/`incomplete`), which the calendar out-ranks so a stale review flag can never hide a real expiry. **UI:** policy list → compact interactive table (real columns: Policy · Type · Renewal · Premium/yr · Status · Manage; 10 rows/page, was 6, rows ~123px → ~57px); the two full-width banners → one collapsible **Important Notices** box (2 shown, rest behind "+N more"); KPIs promoted from below the filter bar to the top as 3 accent counters (Active/Expiring/Needs attention) + premium, with the completion ring as the Active tile's anchor. **Data honesty fixed in passing:** the grid cards rendered a **fabricated coverage %** (`STATUS_COVERAGE` hardcoded active→88%, expiring→68% — derived from the status alone, describing nothing) — deleted, not restyled; mobile's "ΕΝΕΡΓΑ ΣΥΜΒΟΛΑΙΑ" showed `policies.length` (counting expired ones) while the premium tile beside it correctly excluded them — the two tiles literally disagreed; and mobile printed **raw enum slugs** (`motor`, `legal_expenses`, `cyber`) into the Greek UI — now `normalizeBranch().label[lang]`. Dead code removed: `MobilePolicyCard` (never rendered — the file the red-expired bug lived in), plus `StatusSummary`'s `Sparkline`/`premiumTrend` (always `[]`, could never render), `coverageScore`, `breakdownText`. Verified in-browser at 1440px and 390px against a seeded 10-policy portfolio covering every lifecycle state: desktop 1704px → 1354px **while showing 10 rows instead of 6**; mobile 2186px → 1836px; counts reconcile exactly (6 active + 2 expiring + 1 expired + 1 needs-action = 10). +19 unit tests (619 total), all 6 guardrails + build green. ⚠️ `DesktopPolicyWallet.tsx` is also dead and carries a 4th duplicate status map — left in place, worth deleting.
- **PREMIUM TOTALS COUNTED DEAD POLICIES — FIXED (14 Jul):** the b2c "Συνολικό ετήσιο ασφάλιστρο" widget was summing policies that had long expired. Root cause, one line: **`Policy.status` is an ingestion state that nothing ever recomputes** — no code path in the repo writes `'expired'` or `'cancelled'` onto a policy, and there is no lifecycle cron — so `policies.filter(p => p.status === "active")` on `/home` matched *every policy the user had ever uploaded, forever*. The wallet's own `calculatePremiumFootprint` was closer but read the raw `endDate` column (which can hold the upload-day `+365d` placeholder) and ignored `cancelled`, so `PolicyWallet` computed its *counts* from `resolvePolicyLifecycle` and its *premium* from a weaker predicate — two answers on one screen. Fixed by routing every premium aggregation through one shared predicate, **`isPremiumBearing`** (`lib/wallet/premium-footprint.ts`), built on the canonical `resolvePolicyLifecycle` (renewal-history → extracted envelope → DB column). Counts `active | expiring_soon | action_needed`; excludes `expired | cancelled | unknown_duration | analyzing | deleted`, dedupes re-uploads of the same policy number, and **surfaces** the unknown-duration count in the widget rather than silently dropping it (no-silent-fallback rule). Fixed on both sides: b2c (`/home` premium + LOB breakdown + insurer count + renewals timeline, `/wallet` StatusSummary, MyPoliciesScreen) and agent (Insights `premiumSummary`/`policyBreakdown`/renewal timeline, agent dashboard `totalPremium` → **MRR**, which was inflated by the same rows). Two traps caught while fixing: the `/home` free-tier meter and getting-started checklist now count **all** stored policies, because that is what `checkPolicyLimit` actually blocks on — metering the narrowed set would have promised free users headroom their next upload doesn't have. Verified read-only against real dev data: one wallet showed **€4,030** but only **€1,330** is in force — €2,700 of phantom premium across 10 expired policies, several of them `status='active'` with 2025/2026 end dates. +11 unit tests (609 total green), all 6 guardrails + build green.
- **REPEATED COVERAGE RECOMMENDATIONS FIXED (fix/recommendation-dedupe, 14 Jul):** the app was showing the same suggestion over and over. Sentry (`POLICYWALLET-7`, 6 events on a live `/wallet/[id]`) gave the real prod shape: a **motor** policy whose AI gap slugs were `theft`, `fire`, `glass-breakage`, `own-damage`, `own-vehicle-damage`, `malicious-acts-terrorism` — **none of them in `GAP_CONTENT_MAP`**, which was health-only. All six fell through to `GENERIC_TITLES[mechanic]`, so all six rendered under the one heading «Σημείο προσοχής στην κάλυψη»: six cards, one sentence. Three independent causes, all fixed: **(1) vocabulary** — motor/property entries added (κλοπή, πυρκαγιά, θραύση κρυστάλλων, ίδιες ζημιές, κακόβουλες/τρομοκρατικές, οδική βοήθεια, νομική προστασία, σεισμός/πλημμύρα/χαλάζι…), and the coverage area now follows the **policy's branch** (`fire` on a car is a vehicle cover, not a "property" group). **(2) concept keys** — `GapContent.concept` collapses aliases from different producers: the AI's `theft` ≡ the seeded rule's `motor-theft`, `own-damage` ≡ `own-vehicle-damage`, `maternity-exclusion` ≡ `pregnancy-exclusion` (those two already shared a byte-identical Greek title). `dedupeGaps` keys on concept, not slug. Unmapped vocabulary is now titled with **the AI's own first Greek sentence** instead of a shared generic heading, so two unknown gaps can never read alike (still never the raw English slug — the #86 rule holds). **(3) the engine duplicated rows** — `syncRecommendations` built its "already exists" set once and never added the rows it created in the same loop, so one finding detected twice in a run (two policies, or one policy with two slug spellings) was written twice, and every later run kept them all alive; rule ids are now `policy_gap:<lob>:<concept>` (branch-scoped: missing theft cover on the car ≠ on the house), with in-run dedupe, read-time dedupe (existing prod rows stop rendering on the next page load, before any engine run), P2002-safe creates, and a **partial unique index** on `(user_id, rule_id) WHERE status='active'` (migration `20260714120000` also retires the copies that accumulated). A user's «Μη σχετικό για εμένα» is finally **honoured** — the engine used to recreate the dismissed card on its very next run. Verified end-to-end against a real DB with the exact prod gap set: 7 gap instances → **5 cards / 5 distinct titles** (was 6–7 identical), and a second engine run creates 0 rows (was: grew every run). +22 unit tests (598 total), all 6 guardrails + build green. ⚠️ Prod migration is **not** auto-applied by the build — see Next actions.
- **PUBLIC FORMS FIXED + FORM ALERTS WIRED (fix/public-forms-brevo-alerts, 14 Jul):** both public forms were dead in prod, for *different* reasons. **(1) Contact:** `app/api/contact/route.ts` was the only `nodemailer` user in the repo and built an SMTP transporter from `SMTP_HOST`/`SMTP_USER`/`SMTP_PASSWORD` — vars that exist nowhere → transporter always `null` → **503 on every submission**; in dev the same branch returned a fake `success:true`, which is why it looked healthy locally. Rewritten onto the canonical Brevo transport (`lib/email/email-service.ts`); nodemailer path + dep-only usage deleted. **(2) Newsletter:** the footer POSTed to `/api/v1/landing/waitlist`, whose only sink was HubSpot with `HUBSPOT_ACCESS_TOKEN` unset → 503 even after #89 allowlisted it in the proxy. Replaced by **`/api/v1/newsletter/subscribe`** (Brevo contact-list sink via the existing `createBrevoContact`, `BREVO_LIST_ID_NEWSLETTER`); the HubSpot waitlist route is **deleted** and its proxy/inventory entries retired — this supersedes #89's waitlist allowlist line. **(3) Alerting (new):** every submission now emails `resolveAdminNotificationEmail()` (same inbox as the signup alerts) with **reply-to = the submitter** (`EmailOptions.replyTo` added to the Brevo transport). New `FormSubmission` model + migration persists **before** any network call, so a Brevo outage degrades to `emailSent:false` instead of losing the lead; surfaced at **`/admin/submissions`** (linked from the admin dashboard). Also: **honeypot** on both forms (first bot protection in the repo); the footer's **silent false-success** bug fixed (it only checked `response.ok`, so a proxy redirect rendered "Εγγραφήκατε επιτυχώς" while nothing was stored); `rateLimit()` gained a `bucket` param — route limits previously shared one IP-keyed counter with proxy.ts's global `/api` limit, so merely browsing the site could 429 you out of submitting; `siteConfig.contactEmail` `.com`→`.gr` (it is rendered publicly on /contact and in JSON-LD). Verified live: both endpoints 200 (newsletter with **0 redirects**), rows persisted, `emailSent=true` from real Brevo sends, Brevo contact synced, honeypot stored nothing. 576/576 unit tests, all 6 guardrails + build green.
- **MONETIZATION INTEGRITY WEEK IMPLEMENTED (fix/monetization-integrity, 14 Jul):** the PXA audit's §0/§6 "Now" list, coded + tested. [GATES] closed: `renewal-check` + `protection-score-refresh` crons scheduled (were dormant — no renewal emails ever fired) — **⚠️ scheduling alone did NOT make them fire: the proxy still 307'd every cron to signin and the routes were POST-only vs cron GET; actually fixed 17 Jul in #123 (see top entry)**; **cancel is real** (`cancelSubscription` → Stripe `cancel_at_period_end`, error surfaced instead of silent local flip); `stripeCustomerId` persisted on ALL fulfillment paths (webhook + success page + one-off purchases → billing portal works for new subscribers); `currentPeriodEnd` mirrors Stripe (annual/trial-aware, was hardcoded +30d); **v1 webhook lifecycle sync** (`lib/services/billing/stripe-lifecycle.ts`: subscription.updated/deleted, invoice.paid/payment_failed → dunning via past_due); proxy unblocks (`/invite/` + waitlist — invite loop and lead capture were dead for anonymous visitors); honesty sweep (fake landing stats → true facts, Referrals tab hidden pending real earn/redeem, stale "annual coming soon" FAQ fixed, unbuilt API/white-label/branded-report bullets replaced with shipped features, Agency "Contact Sales" now goes to /contact instead of firing a €99.99 checkout, RC premium→ph-pro + normalizeTier defense, seed.ts agent plans aligned with migration, orphan ph-premium seed removed). `scripts/backfill-stripe-billing.mjs` (dry-run/--apply) repairs existing subscribers. +22 unit tests (562 total green), money-path E2E extended with a cancel block. Audit doc committed: [docs/audits/pxa-monetization-audit-2026-07.md](audits/pxa-monetization-audit-2026-07.md).
- **DATE-PARSING INTEGRITY + EXPIRED STATES (fix/date-parsing-integrity, 13 Jul):** Ethniki #1651622 exposed a data-integrity chain: the AI extracted the dates CORRECTLY as `22-05-2024` (the locked prompts normalize to DD-MM-YYYY) but every `new Date()` downstream failed on the format → the DB columns silently took the upload-day placeholder (+365d) → an expired-since-May-2025 policy showed ΕΝΕΡΓΟ/«ΛΗΓΕΙ ΣΕ 365 ημέρες» under «Υψηλή βεβαιότητα 100%» badges, with the review page rendering the literal "Invalid Date". Fixes: `lib/dates/document-date.ts` (deterministic parser — ISO, dd-MM-yyyy, all 12 Greek months γενική+ονομαστική±διαλυτικά, embedded phrases; failed parse = null, NEVER today; 46 tests); every extracted-date fallback to now()/+365 removed (3 AI services + extract route + orchestrator, which now also persists per-field `extraction.dateParse` state); `resolvePolicyLifecycle` — single status source computed from the extracted envelope date (placeholder columns can never rescue an unparseable value) with new honest states ΛΗΓΜΕΝΟ (amber) and «Άγνωστη διάρκεια» (no badge, no countdown), consumed by portfolio table/grid/mobile/KPIs/hero/dates card; expired banner + renewal-quote promoted to primary CTA; review integrity — parse+quote cross-check invalidates confidence («Απαιτεί επιβεβαίωση»), «Δεν εντοπίστηκε — συμπληρώστε» instead of Invalid Date, confirm blocks on flagged fields (fill or explicit skip); portfolio row title «{Τύπος} • {Ασφαλιστής}» with the insurer exactly once; insurer registry now applied on list+review; amounts el-GR («1.500.000 €», Varies→«Μεταβλητό»); sum-insured sublabel i18n map + Sentry (raw `health.annualLimit` keys gone). Data repair for #1651622 (real 2024–2025 dates into the columns) runs post-deploy.
- **GAP REPORT REDESIGN + €3 UNLOCK (feat/analysis-report-ux, 13 Jul):** the analysis page's ~13 identical red cards with raw English slug titles ("Usa-Copayment") became a professional Greek-first report. Root causes found in Step-0 investigation: titles are minted at persist time by a hyphen-preserving humanizer over UNCONSTRAINED AI clarity slugs (open-ended vocabulary — the same concept shipped twice as `mental_health_exclusion` AND `mental-health-exclusion`); every gap ALSO re-rendered in «Προτάσεις Κάλυψης» via `policy_gap:*` RecommendationInstances. Fixes (engine untouched): `lib/wallet/gap-report.ts` — normalized-slug Greek/English content map (12 prod slugs + seeded 9 + variants) with mechanic+coverage-area dimensions, heuristics + Sentry-once fallback for unknown slugs, slug-level dedupe; summary band + collapsible area groups + 3-line collapsed cards (NO severities — placeholders); cross-section rec filtering; insurer registry («ΕΘΝΙΚΗ Η ΠΡΩΤΗ ΑΣΦΑΛΙΣΤΙΚΗ»→«Εθνική Ασφαλιστική»); exclusions wall neutral + cap 8; danger zone → header kebab. **€3 one-off unlock**: free-tier owners see first 3 gaps, rest = real titles + chips with blurred body, ONE CTA → mode:payment inline price_data checkout (no new Stripe products), fulfillment ×3 idempotent racers (2 webhooks + success page), `Policy.reportUnlockedAt` + `report_unlock_purchases` (migration `20260713150000`); paid tiers auto-unlocked; agent views byte-for-byte unchanged (optional `report` prop). 35 new unit tests incl. the fake-slug→Greek-fallback+Sentry acceptance test.
- **Admin signup alerts SHIPPED (PR #83, deployed 13 Jul):** every completed registration (new account or invited-customer activation) emails agentriseinsurance@gmail.com (override: `ADMIN_NOTIFICATION_EMAIL`) via Brevo — Greek internal template, `after()` + swallowed errors so signup can never block on it; live Brevo smoke verified. 8 new unit tests.
- **THIRD PROD ANALYSIS OUTAGE FIXED — token metering crash (13 Jul):** after the schema fix (#79) and the private-bucket fix (#81), real analyses STILL failed at `metadata_extraction_and_verification` with `UNKNOWN_FAILURE: Cannot read properties of undefined (reading 'input')` — the Gemini call succeeded (~90s), then `trackTokenUsage` crashed: `TOKEN_COSTS[model]` had no entry for `gemini-2.5-pro`/`gemini-2.5-flash` (the lib/env DEFAULTS since the 2.5 upgrade) and `costs.input` threw. Invisible everywhere except prod: local harnesses/E2E pass no `userId`, so metering never ran. Fix: 2.5 price entries + `resolveTokenCosts()` (exact → family-prefix → conservative default, so env-configured model strings can never miss) + `trackTokenUsage` now never throws (metering failure logs instead of killing a finished analysis). Lesson: the analysis pipeline failed at FOUR successive layers (Gemini schema → missing bucket → private-bucket fetch → metering), each only visible after the previous fix — prod money-path needs a true end-to-end smoke with a real user, not just service-layer harnesses.
- **SEO/GEO/AEO FIXES SHIPPED (PR #80, merged + deployed 13 Jul, live-verified):** responds to the 8/7/8 audit. Preview/branch deploys now serve noindex via 3 layers (robots meta + X-Robots-Tag + robots.txt disallow) gated on VERCEL_ENV — prod verified serving INDEX. hreflang (el + x-default, en where real) on every marketing page + guide; **4 new server-rendered English routes** (/en/{product,pricing,company,solutions/agents}) via StaticLanguageProvider; sitemap 31 URLs w/ EN alternates. E-E-A-T plumbing all env/config-gated (team registry→Person schema+/company section, guide author bylines→Article author:Person, footer social icons, sameAs/telephone/address) — **nothing fake ships; fill NEXT_PUBLIC_SOCIAL_*/CONTACT_* + lib/seo/team.ts when real**. **6 new Greek guides** (motor coverage, health απαλλαγή, earthquake cost→ΕΝΦΙΑ cross-link, pet exclusions, ομαδικά, ανασφάλιστα πρόστιμα) with related-links + official sources; HowTo on checklist guide + landing; 63-test SEO guardrail suite fixed 8 title/description outliers; ΕΝΦΙΑ quarterly review TODO (due 2026-10-01). NEXT_PUBLIC_SITE_URL set in Vercel prod (flip to policywallet.gr at apex cutover).
- **PROD UPLOAD OUTAGE FIXED (13 Jul, infra-only, no deploy):** «Η προσθήκη συμβολαίου απέτυχε» on every prod upload — the prod Supabase NEVER had the 'policies' storage bucket (only an unused 'policy-documents') and zero storage RLS policies, so the client-side upload died before createPolicy ran. A launch-cutover gap: the 8 Jul prod stack applied DB migrations but storage buckets/policies are not in migrations. Fixed by replicating dev's exact config (private bucket 'policies' + authenticated INSERT/SELECT RLS), verified via SQL. NOTE: lib/storage.ts (agent upload path) uses an 'uploads' bucket that exists on NEITHER env — the agent upload flow likely falls back/fails; provision it when touching that flow.
- **AI ANALYSIS OUTAGE FIXED (PR #79, merged + deployed 13 Jul):** Gemini started rejecting every call embedding AcordDataSchema (~150 props) with "schema produces a constraint that has too many states for serving" — ALL models, extraction + gap analysis + clarity all dead in prod (invisible to CI/E2E: extraction is route-mocked there). Root-caused with a real Ethniki motor PDF via schema bisection (every section serves alone; the cumulative schema busts the budget — likely a Google-side tightening; same schema verified 11 Jul). Fix: same Zod schemas sent IN THE PROMPT with output:no-schema JSON mode + local validation (lib/services/ai/json-mode-schema.ts — null-cleansing, bilingual-string coercion, lenient fallback w/ logging). Verified end-to-end on the real PDF: extraction (canonical motor id, 9/9 field confidence, 8 coverages), citations (8 verbatim Greek snippets + page refs), gap analysis (theft gap detected, νομική προστασία €15k correctly present), clarity (Greek summary naming the actual FIAT 500X). EXTRACTION_CITATIONS=1 restored after the fix. ⚠️ Watch the first real uploads: validateJsonModeObject logs strict-parse fallbacks — recurring warns mean the schema-in-prompt needs tightening.
- **PROFESSIONAL QUESTIONNAIRES SHIPPED (PR #77, merged + deployed 13 Jul; prod DB migrated + registered):** the questionnaire manager's thin seeded content replaced by 7 professional Greek-market system templates (87 bilingual EL/EN questions, only the 4 renderable types): Motor/Home/Health/Life/Pet upgraded to v2 (claims history, anti-seismic construction period, ΕΟΠΥΥ/group-policy overlap, cover bands, hospital class…) + new Travel Intake + new **Annual Insurance Needs Review** (mirrors gap-engine ProfileFields for future profile feeding). English-only seed placeholders retired (FK-safe) + seed block removed. Fixed audited send-picker defect: `getQuestionnaireTemplates` now auth-guarded with system-or-own visibility (was listing every agent's custom templates). New agent-journey E2E test (6/6). Prod verified via MCP: 7 system templates live, existing user's custom template untouched.
- **UI PRIMITIVES + PROD CUTOVER (PR #75, merged + deployed 13 Jul):** ConfidenceBadge / ConsentStatusBadge / TrustStrip (landing emoji pills → lucide icons) / UploadDropzone extracted to components/ui (E2E hooks preserved); ui/button → pill + ui/card → rounded-2xl per MASTER (only 2 shadcn consumers — verified tiny blast radius); product catalog extracted to `prisma/product-catalog.ts` + `scripts/gen-lookup-seed-sql.ts` emits idempotent PROD lookup SQL (types+products only, never demo data). Verified: money-path 9/9 + batch-upload E2E, visual diff vs NEW-UI baselines (landing pills only; wallet/account visual tests time out on NEW-UI too — pre-existing RUN_VISUAL flakiness). Prod lookup tables SEEDED 13 Jul via `scripts/gen-lookup-seed-sql.ts` output (20 types / 17 products, MCP-verified) — the prod add-policy dropdown now offers all 20 branches. Local-dev note: when the machine has no IPv6, use pooler `aws-1-eu-west-3.pooler.supabase.com` (user `postgres.<ref>`, port 5432) for dev DB/E2E.
- **BRANCH PRODUCT SYSTEM SHIPPED (PRs #67–#69, #71–#73, merged + deployed 12 Jul):** canonical insurance taxonomy (`lib/insurance/taxonomy.ts`, 36 branches, replaced 7 divergent LOB definitions; `normalizeBranch` read-adapter, write enum now 20 types incl. cyber/pension/boat); 9 rich Greek content bundles (hedged compliance tone, banned-phrase test) + generic fallback; **/branches** coverage grid + per-branch product pages (policies, engine-badged common gaps, perks, renewals, Q&A deep-links via `?q=` prefill); branch-aware PolicyQA questions; home page componentized w/ real i18n (the inline `t(el,en)` closure + fabricated 12% savings figure are gone — honest overlap copy) + 3 new widgets (BranchCoverageMap, gaps summary, recommended actions); coverage-insights render now READ-ONLY (`getGapEngineSnapshot` — full evidence parity, writes behind an explicit refresh button); real `requestRenewalQuote` action (agent notified); /upgrade fake insurer logos removed; seeds idempotent (20 types/17 products/3 DEMO-* policies); 2 new gap rules (`home_no_earthquake`, `motor_no_roadside`). 327 unit tests, new branches E2E spec (7/7), money-path 9/9 throughout. 5 dead files deleted (incl. `design-tokens.ts`, `MobileWalletView`). Remaining waves: 6 (primitives + MASTER radius fix, needs RUN_VISUAL), 8 (marketing palette, optional), 9 (extraction citations, deferred).
- **15-area product review WRITTEN (12 Jul):** [docs/audits/product-review-15-areas-2026-07.md](audits/product-review-15-areas-2026-07.md) — grades every mandate area against shipped PRs #46–#66, separates GATES vs POLISH, and sets the top-5 next investments: real mobile Tasks/Alerts tabs (stubs today), onboarding analysis-wait teaching moment, self-serve GDPR data export, renewal-reminder preferences, portfolio health score on /home (the /home score + coverage map shipped 12 Jul in #72).
- **Money-path E2E + mobile trigger fix (PR #66, merged 12 Jul, deployed with #65):** new `tests/money-path.spec.ts` (9/9 green) pins the revenue-integrity contract (bogus `session_id` on `/upgrade/success` must render *pending*, never *activated*; server still reports `tier: free`), the Pro savings-report gate, the PDF-preview lock click-through, the Q&A pre-empt, the two-policy home triggers (self-provisioning fixture), and the mobile upgrade surface. The spec caught a real bug: the P3 `mobile_wallet_tile` trigger lived in dead `MobileWalletView` — phone users had NO upgrade surface on the wallet tab; now wired into `MyPoliciesScreen` (tier plumbed through PolicyWalletClient→MobileAppShell). Also: global-setup seeds a fixture PDF doc; eslint ignores playwright-report/test-results (local E2E used to poison lint with ~3.3k artifact errors). Dev Supabase restored → full local suite green again (79 passed / 48 opt-in skips).
- **CONVERSION OVERHAUL COMPLETE (PRs #59, #60, #61, #62, #63 — deployed 11–12 Jul):** audit-first, 4 phases per the approved plan.
  - **P1 revenue integrity (#59):** closed the free-Pro loophole (`upgradeSubscription` now always goes through Stripe; existing free-granted subs grandfathered until `currentPeriodEnd` via `isSubscriptionLive`); batch upload now enforces the 3-policy cap; context-preserving checkout loop (`returnTo` → `/upgrade/success` verifies sessions directly with Stripe, webhook-independent; cancel no longer 404s); ph-pro 14-day trial wired into checkout; GA unblocked in CSP; token meter numbers = enforcement numbers; EL policy-limit toast fixed → LimitReachedModal.
  - **P2 gate system (#60):** `lib/monetization/` (FEATURE_GATES ×10, PLAN_PRICING, EL/EN trigger copy — parity-tested vs entitlements + pricing page, forbidden-urgency-phrase tests) + `components/monetization/` (UpgradeModal in-place checkout w/ monthly/annual toggle, LockedInsightPreview real-data blur, UsageMeter, PlanBadge, BillingTrustBox); 14 typed funnel events.
  - **P3 triggers (#61, #62):** home usage banner (2/3 meter) + multi-insurer card + renewal teaser + carried-plan card (signup-selected plan finally activatable — straight to checkout); PolicyQA free-tier pre-empt; recommendation evidence soft-lock; onboarding trial-exhausted CTA; PDF-lock click-through; mobile Upgrade tile wired; savings-report export card (Pro); **token top-up actually charges now** (hosted Checkout mode:payment + idempotent fulfillment — was a dead PaymentIntent); billing portal button; annual-savings nudge; TokenUsageCard mounted.
  - **P4 analytics (#63):** server-side `conv_*` mirror (NotificationEvent) on checkout started/completed + limit hits with trigger-source attribution; 30-day conversion funnel card on admin dashboard (signups→activated→trial→limit hits→checkouts) + per-source checkout chips. 281 unit tests green.
- **Policy detail page REDESIGNED (PR #57, deployed 11 Jul):** `/wallet/[id]` rebuilt as a 60-second scannable flow — anchored section nav; plain-language AI summary with per-policy health donut + at-a-glance chips; key dates & renewal (period progress, extracted renewal date/frequency, auto-renewal warning, cron reminder trail); **first-ever rendering of extracted `notableConditions`/`finePrintClauses`/`perksAndBenefits`** (risk-badged fine print, claim deadlines, perk hotlines); data-aware claims guidance; related recommendations (persisted gap-engine output, same-LOB); agent timeline promoted from tab. Monolith decomposed into `components/wallet/policy-detail/*` + shared read model `lib/wallet/policy-detail.ts` (24 new unit tests, 257 total). Verified live on prod alias, zero runtime errors.
- **E2E suite REPAIRED (11 Jul):** Playwright green end-to-end for the first time — `--project=chromium --project=agent-chromium --project=sentry`. Root causes fixed: config waited on :5000 while dev binds :3000 (AirPlay's 403 masked it); test users now auto-provisioned by `tests/global-setup.ts` (Supabase auth via SQL + Prisma rows + fixture policy; prod-guarded) with login-only setups; `.env.local` had a typo'd anon key (broke all local auth) and a placeholder DIRECT_URL (broke every dev Prisma call); old dev DB was 3 migrations behind (now current). Audit suites opt-in (`RUN_UX_AUDIT`/`RUN_VISUAL`); a11y gate = no critical violations (known serious color-contrast findings from the repaint logged for the UX backlog). Docs updated in CLAUDE.md/AGENTS.md.
- **Prod env keys SET (11 Jul):** Brevo SMTP key, Stripe test keys, `GEMINI_API_KEY` configured in Vercel — activated by redeploy; AI pipeline now runs on Gemini (real per-field confidence on the upload review screen), magic-link emails live, money path testable.
- **Smart recommendation cards SHIPPED (PR #53, deployed 10 Jul):** every coverage recommendation now shows risk + severity, plain-language explanation, a "From your policy data" evidence block quoting the user's own facts, a suggested next step, and Review-this / Ask-my-agent / Not-relevant CTAs. Five new portfolio rules (`lib/services/gap-engine/portfolio-rules.ts`, no DB migration): expiring motor (≤30d), low health limit (<€100k extracted), duplicate coverage per LOB, unclear exclusions (links to the extraction review), no agent connected. Evidence recomputed live per engine run; portfolio cards excluded from product matching (anti-salesy). 17 new unit tests (221 total).
- **Upload review screen SHIPPED (PR #51, deployed 10 Jul):** post-upload AI-extraction review with per-field confidence chips (surfaced for the first time — a persist-time bug was silently erasing them), inline editing of all key fields (incl. newly-extracted issue date / payment frequency / renewal date), Confirm & save, Flag-incorrect-extraction (notificationEvent + activityLog trail), "AI can make mistakes" microcopy. Soft gate: `acordData.extraction.reviewState` drives an amber "Review extracted data" chip across the wallet + detail-page banner until confirmed; review-later at `/wallet/[id]/review`. 31 new unit tests (204 total).
- **Premium empty states SHIPPED (PR #50, deployed 10 Jul):** shared `EmptyState` primitive (public-site design language) wired across wallet (desktop+mobile), My Agent (now with a working inline invite-code form — was a dead end), coverage-insights recommendations (no longer renders nothing), agent customers (zero-book + invited-tab), agent renewals, home renewals block. Claims intentionally excluded (no claims domain exists).
- **Agent-managed policies SHIPPED (PR #48, deployed 10 Jul):** agents upload policies for customers (scanned PDF now persisted), edit them per tier (previously-dead `maxPoliciesPerCustomer` + agent `monthlyTokenBudget` now enforced), and hand customers access via invites. Ownership stays with the customer; agent capability = auto-minted revocable `manage` AccessGrant. New central `lib/policy-access.ts` authorization (all policy read/write/delete paths routed through it; agent portfolio read tightened); analysis attribution unified to the initiating agent; AI consent via **agent attestation** for phantom customers (activity-log evidence, cleared on activation — ⚠️ pending counsel sign-off). Backfill migration applied+registered on prod (0 rows). 173/173 tests, all guardrails, zero runtime errors post-deploy.
- **UI repaint SHIPPED (PR #46, deployed 10 Jul):** logged-in app aligned to the marketing design language (deep green #29685B / mint dark mode / Inter), ~150 files, verified live.
- **Production launch ops (8–10 Jul):** PR #45 merged + deployed. PolicyWallet-Prod Supabase (`cquudefwfwrmvpftuhyl`) is the end-to-end prod stack: full 29-migration Prisma chain applied via MCP + registered in `_prisma_migrations` (59 tables, seed plans/templates); auth + DB env cut over (pooler URLs — direct `db.*` hosts are IPv6-only, unreachable from Vercel); CSP now derives Supabase origin from env. `NEXTAUTH_URL`/`NEXT_PUBLIC_APP_URL` set (custom auth links no longer localhost). GA active (`G-G9ZFY7QQX6`).
- **Auth email flow fixed:** Supabase Site URL → live site + 3 redirect allowlist URLs; duplicate built-in "Confirm email" disabled (app runs its own token flow via Brevo); **Brevo Authorised-IPs blocking deactivated** — it was rejecting all API sends from Vercel's dynamic IPs (root cause of missing branded emails). `info@policywallet.gr` is a verified sender; `BREVO_API_KEY`/`SENDER_EMAIL`/`SENDER_NAME` set. Supabase custom SMTP form prefilled (Brevo relay :587) — awaiting SMTP key (≠ API key) for magic-link emails.
- **SEO/GEO/AEO overhaul live** (responds to 7 Jul audit 4/4/5): robots/sitemap 200 on prod, unique Greek titles/canonicals, server-rendered JSON-LD, 1200×630 OG, `/guides`, proxy.ts allowlist fix. Post-deploy checklist in `docs/operations/SEO_STRATEGY.md` (Search Console submission pending).
- **Hobby-plan cron fix:** synthetic-launch-check cron now daily (sub-daily rejected at deploy).
- Prior passes: production hardening (Sentry, rate-limit, pooled DB), 3 endpoint scope-gap fixes, pricing-cutover copy, nodemailer 9, design-sync, monetization stack. All guardrails + 145/145 unit tests green.

## Money path — VERIFIED END-TO-END WITH A REAL CARD (12 Jul)
sk_test key received → billing catalog applied to Stripe TEST (5 plans monthly+annual + 4 token packs → `lib/billing-catalog.json`); prod `plans` rows updated with real price IDs (placeholders gone); TEST webhook registered + `STRIPE_WEBHOOK_SECRET` set in Vercel; **PR #65** fixed the proxy 307-redirecting Stripe's anonymous webhook POSTs to signin (would have silently killed delivery — endpoint now answers 400 missing-signature). **4242 purchase completed** (Agent Starter, €24.79 incl. 24% VAT): Stripe session paid → `subscriptions` row active with `stripeSubscriptionId` (period 12 Jul→11 Aug) → success page won the fulfillment race; webhook event delivered, signature-verified and no-oped idempotently (`processed_webhook_events`) → `conv_checkout_completed` mirrored for the admin funnel. PR #66 (parallel session) shipped the money-path E2E spec + the MyPoliciesScreen mobile upgrade trigger; deployed together with #65. Token packs share the (unit-tested) payment-mode path but haven't had a card test yet.

## Blocked / user-gated
- **Brevo IP allowlist (POLICYWALLET-3) — user action:** Brevo rejects transactional sends with 401 "unrecognised IP" because an IP allowlist is enabled and Vercel egress IPs rotate. Disable the authorised-IPs restriction at https://app.brevo.com/security/authorised_ips or signup/lifecycle emails fail intermittently. Cannot be fixed in code.
- **Housekeeping:** Supabase "outstanding invoices" banner (pay to avoid disruption); **rotate Brevo API key, prod DB password AND the sk_test key** (all transited chat); old Supabase project (`lzqvtvjggylcujenlelh`, 31 demo accounts) idle — decide keep/pause; grandfathered free-granted subs may deserve a courtesy in-app note before their period lapses. QStash keys still unset → AI analysis inline.

## Top risks (ranked)
1. **Medium — email deliverability fresh**: Brevo free tier caps 300/day; sender domain DKIM state unverified via API.
2. **Medium — scale**: QStash AI-analysis queue now ACTIVE in prod (22 Jul) — upload burst no longer fans out unbounded LLM calls; still needs a real-upload round-trip proof + a k6 run. Pooled DB `connection_limit=1` workaround in place; the `pool_size 15` pooler limit remains the next scale ceiling (source of the resolved -2/-4/-5/-A/-B Sentry cluster).
3. **Medium — governance HOLD**: legal/DPO/product + UAT + SRE-restore sign-offs pending; email-verify hard gate off (flip `ENFORCE_EMAIL_VERIFICATION=1` before real traffic).
4. **Low — TEST-mode billing**: catalog/webhook/keys are Stripe TEST; live cutover (live keys + live webhook/secret + catalog re-run) required before charging real cards.
5. **Low — Vercel git auto-deploy off**: deploys via `vercel deploy --prod` CLI only.
6. **Low — `Policy.endDate` is not trustworthy, so lifecycle filters cannot live in SQL**: the real end date is in `acordData`, so `isPremiumBearing` is an in-memory filter over full rows — on the agent side that means pulling the JSON blob for every customer policy (Insights/agent dashboard now select `acordData`). Durable fix: **backfill `endDate` from the extracted envelope**, then a `where: { endDate: { gte: now } }` becomes safe and the aggregates can go back to being DB-level. Related: `Policy.status` has no enum and ~7 values in the wild (`active`, `analyzing`, `expiring_soon`, `action_needed`, `incomplete`, `deleted`, `cancelled`) — worth constraining.

## Next 3 actions
0. **UI foundation branch is unmerged, and E2E is blocked by a dead dev DB.** `tests/global-setup.ts` provisions against `db.lzqvtvjggylcujenlelh.supabase.co`, which **no longer resolves in DNS** (the idle project under Housekeeping — paused or deleted), so *every* Playwright project aborts in global setup. This blocks E2E for all future work, not just this branch: restore or repoint the dev database in `.env.local` first. Then run `npx playwright test --project=chromium --project=agent-chromium` and `RUN_UX_AUDIT=1` — priority checks: admin renders one sidebar and no empty bottom bar, nav links still navigate + close the mobile drawer, `/wallet` on a phone doesn't flash the desktop layout, and the mobile profile actions land on `/account`. Then merge + deploy, or continue with Batch 3 (form/table primitives).
0. **B2B hardening is LIVE (done 16 Jul).** Remaining verification needs an agent login (couldn't do headless): confirm an agent with only a `pending_activation` relationship (no grant/upload) sees **nothing** of the customer's gaps; the dashboard "Revenue Pulse" matches `/commissions`; the 4 agent modals + verify banner render in Greek. Follow-up (own PR): denormalize `coverageEndDate` (+ backfill) so active-policy counts stop deserializing `acordData` per policy.
1. Ship the recommendation dedupe: deploy the code, then **apply migration `20260714120000_recommendation_dedupe` to prod** (`next build` does NOT run `prisma migrate deploy` — prod migrations are applied by hand). Order is safe either way: the app dedupes on its own and catches P2002, so the code can land before the index. Then re-open the motor policy from Sentry `POLICYWALLET-7` and confirm the gap report reads 5 distinct Greek titles, and watch for new `gap-report: unknown gap slug` events — each one is a word the map still owes (pet/boat/cyber vocabulary is the likely next gap).
1. Merge fix/monetization-integrity (after review) → deploy → run `scripts/backfill-stripe-billing.mjs` against prod (dry-run first) → confirm in the Stripe dashboard that the registered webhook endpoint is `/api/v1/billing/webhook` and add the 4 lifecycle events to its subscribed-events list → verify the renewal-check cron fires in prod logs.
2. Token-pack card test (Account → billing → buy a pack): now also verifies the fixed money path end-to-end (`stripeCustomerId` persisted → billing portal opens). Then citations live-check (real PDF upload; rollback = unset EXTRACTION_CITATIONS).
3. Credential rotation pass: BOTH Supabase DB passwords (dev + prod — the shared password was pasted in chat again 13 Jul) + the Brevo API key.
